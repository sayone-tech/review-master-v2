---
phase: 17-tag-rework-reviewtag-model-and-filter
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - apps/reviews/services/enrichment.py
  - apps/reviews/serializers.py
  - apps/reviews/selectors/reviews.py
  - apps/reviews/tests/test_enrichment_service.py
autonomous: true
requirements:
  - TAG-02

must_haves:
  truths:
    - "enrich_review() writes ReviewTag rows instead of updating Review.tags JSONField"
    - "Re-enriching a review deletes old ReviewTag rows before creating new ones (idempotent)"
    - "Comment-less reviews go through _persist_success_no_comment() which also clears stale ReviewTag rows"
    - "ReviewReadSerializer returns tags as [{label, polarity}] using ReviewTagSerializer"
    - "Review list queryset prefetches tags — no N+1 queries on review list pages"
  artifacts:
    - path: "apps/reviews/services/enrichment.py"
      provides: "tags= removed from both .update() calls; ReviewTag.objects.filter/bulk_create inside transaction.atomic()"
      contains: "ReviewTag.objects.bulk_create"
    - path: "apps/reviews/serializers.py"
      provides: "ReviewTagSerializer class; ReviewReadSerializer.tags declared explicitly as ReviewTagSerializer(many=True)"
      contains: "class ReviewTagSerializer"
    - path: "apps/reviews/selectors/reviews.py"
      provides: "base_reviews_queryset() includes .prefetch_related('tags')"
      contains: "prefetch_related.*tags"
  key_links:
    - from: "apps/reviews/services/enrichment.py"
      to: "apps/reviews/models.py"
      via: "ReviewTag imported and used for bulk_create"
      pattern: "from apps.reviews.models import.*ReviewTag"
    - from: "apps/reviews/serializers.py"
      to: "apps/reviews/models.py"
      via: "ReviewTagSerializer nested on ReviewReadSerializer; related_name=tags is the source"
      pattern: "ReviewTagSerializer.*many=True"
    - from: "apps/reviews/selectors/reviews.py"
      to: "apps/reviews/serializers.py"
      via: "prefetch_related('tags') prevents N+1 when ReviewTagSerializer iterates review.tags.all()"
      pattern: "prefetch_related.*tags"
---

<objective>
Update the enrichment service write path (both _persist_success and _persist_success_no_comment), swap the serializer from JSONField auto-discovery to explicit ReviewTagSerializer nesting, add prefetch_related("tags") to the base reviews queryset, and update the enrichment tests to assert ReviewTag rows are created correctly.

Purpose: This plan completes TAG-02 — the read/write pipeline for tags is fully migrated to the relational model while maintaining the same [{label, polarity}] JSON shape the frontend already consumes.
Output: Updated enrichment.py (two write sites), serializers.py (ReviewTagSerializer + explicit tags field), selectors/reviews.py (prefetch added), enrichment tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-tag-rework-reviewtag-model-and-filter/17-CONTEXT.md
@.planning/phases/17-tag-rework-reviewtag-model-and-filter/17-RESEARCH.md
@.planning/phases/17-tag-rework-reviewtag-model-and-filter/17-PATTERNS.md
@.planning/phases/17-tag-rework-reviewtag-model-and-filter/17-01-SUMMARY.md

<interfaces>
<!-- Key signatures the executor needs. Do not re-read these files cold — extract what matters. -->

From apps/reviews/services/enrichment.py (two write sites to modify):
Site 1 — _persist_success() inside transaction.atomic() block: Review.objects.filter(pk=review.pk).update(enrichment_status=SUCCESS, sentiment=..., tags=[...], extracted_action_items=[...], enrichment_version=F(...)+1); AiUsageLog.objects.create(...)
  ACTION: Remove tags=[...] from .update(); add ReviewTag.objects.filter(review_id=review.pk).delete() + ReviewTag.objects.bulk_create([ReviewTag(review_id=review.pk, label=tag.label.title(), polarity=tag.polarity) for tag in result.tags]) INSIDE the transaction.atomic() block, before AiUsageLog.objects.create().

Site 2 — _persist_success_no_comment() inside transaction.atomic() block: Review.objects.filter(pk=review.pk).update(enrichment_status=SUCCESS, sentiment=..., tags=[], extracted_action_items=[], enrichment_version=F(...)+1)
  ACTION: Remove tags=[] from .update(); add ReviewTag.objects.filter(review_id=review.pk).delete() INSIDE the transaction (no bulk_create needed — empty list).

From apps/reviews/serializers.py:
- ReviewReplySerializer is a plain serializers.Serializer (not ModelSerializer) — use same pattern for ReviewTagSerializer
- ReviewReadSerializer is ModelSerializer; currently auto-discovers tags from JSONField — after JSONField removal, must explicitly declare: tags = ReviewTagSerializer(many=True, read_only=True)
- "tags" already appears in ReviewReadSerializer.Meta.fields list — no change needed there

From apps/reviews/selectors/reviews.py:
- base_reviews_queryset() currently ends with: .select_related("shop", "shop__region", "replied_by")
- Add: .prefetch_related("tags") as the final chain call
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update enrichment service — remove tags from both .update() calls; write ReviewTag rows</name>
  <files>apps/reviews/services/enrichment.py, apps/reviews/tests/test_enrichment_service.py</files>
  <read_first>
    apps/reviews/services/enrichment.py — read the full file; locate _persist_success() and _persist_success_no_comment(); note the exact transaction.atomic() block boundaries; identify both `tags=` kwargs in the .update() calls; note the existing `from apps.reviews.models import Review` import line.
    .planning/phases/17-tag-rework-reviewtag-model-and-filter/17-PATTERNS.md — Pattern 2 (both write sites, verbatim code for each).
    .planning/phases/17-tag-rework-reviewtag-model-and-filter/17-RESEARCH.md — Pitfall 2 (two write sites warning), Common Pitfalls section.
    apps/reviews/tests/test_enrichment_service.py — read existing tests to understand fixture/mock patterns and where to add new tests.
  </read_first>
  <behavior>
    - Test 1: enrich_review() for a review with a comment creates ReviewTag rows: assert ReviewTag.objects.filter(review=review).count() == len(mocked_result.tags)
    - Test 2: Labels stored title-cased: ReviewTag objects have label == tag.label.title() not the raw GPT label
    - Test 3: Re-enriching a review deletes old ReviewTag rows before creating new ones — call enrich_review() twice with different tags; assert final ReviewTag count matches the second enrichment's tags only
    - Test 4: enrich_review() for a comment-less review (routes to _persist_success_no_comment) creates no ReviewTag rows; if stale tags existed from a previous enrichment, they are deleted
    - Test 5: tags= kwarg no longer appears in any Review.objects.filter(...).update() call in enrichment.py (source assertion)
    - Test 6: ReviewTag.objects.bulk_create() call is inside the transaction.atomic() block (verify by reading the source — documented in acceptance criteria via grep)
  </behavior>
  <action>
    In apps/reviews/services/enrichment.py:

    1. Extend the `from apps.reviews.models import Review` import to `from apps.reviews.models import Review, ReviewTag`.

    2. In _persist_success(): inside the existing `with transaction.atomic():` block, remove the `tags=[...]` kwarg from `Review.objects.filter(pk=review.pk).update(...)`. After the .update() call and BEFORE `AiUsageLog.objects.create(...)`, add:
       - `ReviewTag.objects.filter(review_id=review.pk).delete()`  # D-09: idempotency
       - `ReviewTag.objects.bulk_create([ReviewTag(review_id=review.pk, label=tag.label.title(), polarity=tag.polarity) for tag in result.tags])`

    3. In _persist_success_no_comment(): inside the existing `with transaction.atomic():` block, remove the `tags=[]` kwarg from `Review.objects.filter(pk=review.pk).update(...)`. After the .update() call, add:
       - `ReviewTag.objects.filter(review_id=review.pk).delete()`  # idempotency for re-enrichment
       (No bulk_create — comment-less reviews have no tags.)

    Critical constraint: Both delete + bulk_create calls MUST remain inside the `with transaction.atomic():` block. The `_emit_enrichment_progress()` call (if any) remains OUTSIDE the transaction (existing pattern).

    In apps/reviews/tests/test_enrichment_service.py:
    Add tests for behaviors 1–4. Import ReviewTagFactory from apps.reviews.tests.factories. Use `@pytest.mark.django_db`. Mock the OpenAI client as existing tests do; provide a mocked EnrichmentResult with known tags for assertion.
  </action>
  <verify>
    <automated>cd /Users/renjith/Documents/Accounts/review-master && python -m pytest apps/reviews/tests/test_enrichment_service.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pytest apps/reviews/tests/test_enrichment_service.py -x -q` exits 0 with all tests passing
    - `grep -n "tags=" apps/reviews/services/enrichment.py | grep "\.update("` returns no match (no tags= kwarg in any .update() call)
    - `grep -n "ReviewTag.objects.bulk_create" apps/reviews/services/enrichment.py` returns a match
    - `grep -n "ReviewTag.objects.filter(review_id" apps/reviews/services/enrichment.py | wc -l | tr -d ' '` outputs 2 (one delete call per write site)
    - `grep -n "from apps.reviews.models import" apps/reviews/services/enrichment.py` shows ReviewTag in the import
  </acceptance_criteria>
  <done>Both _persist_success() and _persist_success_no_comment() no longer write to Review.tags. Both delete ReviewTag rows then bulk_create (or just delete for comment-less). All enrichment tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ReviewTagSerializer, swap tags field in ReviewReadSerializer, add prefetch_related in selector</name>
  <files>apps/reviews/serializers.py, apps/reviews/selectors/reviews.py</files>
  <read_first>
    apps/reviews/serializers.py — read the full file; locate ReviewReplySerializer (the plain serializers.Serializer analog, lines ~67–70); locate ReviewReadSerializer and its Meta.fields list; confirm there is NO explicit tags field declaration (it was auto-discovered from JSONField); note where to insert ReviewTagSerializer (before ReviewReadSerializer).
    apps/reviews/selectors/reviews.py — read the full file; locate base_reviews_queryset() and its chain ending in .select_related("shop", "shop__region", "replied_by"); note any existing .prefetch_related() calls.
    .planning/phases/17-tag-rework-reviewtag-model-and-filter/17-PATTERNS.md — Pattern 3 (ReviewTagSerializer + ReviewReadSerializer swap) and the selector section.
    .planning/phases/17-tag-rework-reviewtag-model-and-filter/17-RESEARCH.md — Pitfall 1 (N+1 on tags), Open Question 1 (query count ceiling).
  </read_first>
  <behavior>
    - Test 1: ReviewTagSerializer serializes a ReviewTag instance to {"label": "Cleanliness", "polarity": "positive"}
    - Test 2: ReviewReadSerializer with a Review that has two ReviewTag rows returns tags as [{"label": ..., "polarity": ...}, {...}] (same JSON shape as old JSONField)
    - Test 3: base_reviews_queryset() queryset has "tags" in _prefetch_related_lookups (or evaluating the queryset issues exactly one IN query for tags — confirmed by query count test)
    - Test 4 (regression): The existing REVW-14 query count test (test_reviews_list_query_count_org_admin) passes — if it fails at the old ceiling, update the ceiling to 6 with a comment explaining the extra prefetch query
  </behavior>
  <action>
    In apps/reviews/serializers.py:
    - Add ReviewTagSerializer BEFORE ReviewReadSerializer as a plain serializers.Serializer subclass (not ModelSerializer) — following the ReviewReplySerializer pattern. Fields: label = serializers.CharField(read_only=True), polarity = serializers.CharField(read_only=True). Add the # type: ignore[type-arg] comment on the class line as the existing pattern uses.
    - In ReviewReadSerializer class body, add an EXPLICIT tags field declaration: `tags = ReviewTagSerializer(many=True, read_only=True)`. This replaces the auto-discovered JSONField. The "tags" key in Meta.fields does not need to change — just the explicit field declaration is new.

    In apps/reviews/selectors/reviews.py:
    - In base_reviews_queryset(), add `.prefetch_related("tags")` as the final chain call after `.select_related("shop", "shop__region", "replied_by")`. This is a one-line addition. The "tags" here refers to the RelatedManager name (related_name="tags" on ReviewTag.review FK).

    No other selector changes needed. list_reviews() and other selectors that call base_reviews_queryset() inherit the prefetch automatically.

    In apps/reviews/tests/test_views.py:
    - Add test_review_list_tags_shape: create a Review with two ReviewTag rows (e.g. via ReviewTagFactory twice); call GET /api/v1/reviews/ (authenticated as org admin); assert the first review in the response has a "tags" key whose value is a list of dicts each with "label" and "polarity" keys. This is the Wave 0 shape-regression contract — it proves ReviewTagSerializer returns [{label, polarity}] objects, not the old raw JSON blob from JSONField.
  </action>
  <verify>
    <automated>cd /Users/renjith/Documents/Accounts/review-master && python -m pytest apps/reviews/tests/test_views.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `python manage.py check` exits 0
    - `grep -n "class ReviewTagSerializer" apps/reviews/serializers.py` returns a match
    - `grep -n "tags = ReviewTagSerializer" apps/reviews/serializers.py` returns a match with "many=True"
    - `grep -n "prefetch_related.*tags" apps/reviews/selectors/reviews.py` returns a match in base_reviews_queryset()
    - The existing query count test for review list passes (or its ceiling is updated to 6 with a comment if the prefetch adds exactly 1 query)
    - A review list API call returns tags as an array of objects with label and polarity keys (not a raw JSON blob)
  </acceptance_criteria>
  <done>ReviewTagSerializer declared; ReviewReadSerializer.tags is the explicit ReviewTagSerializer(many=True) field; base_reviews_queryset() prefetches tags; no N+1 on review list; query count test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| enrichment service → DB | Tag delete + bulk_create must be atomic; a failed bulk_create must roll back the review status update |
| API response | tags JSON shape must remain [{label, polarity}] for frontend compatibility — no shape regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Tampering | enrichment.py atomic block | mitigate | delete + bulk_create inside transaction.atomic() — verified by grep in acceptance criteria |
| T-17-04 | Information Disclosure | ReviewTagSerializer | accept | labels are AI-generated topic strings; no PII; read_only=True prevents write-through |
| T-17-05 | Denial of Service | N+1 on review list | mitigate | prefetch_related("tags") collapses all tag fetches into one IN query; REVW-14 query count test enforces ceiling |
</threat_model>

<verification>
After all tasks:
- `python manage.py check` exits 0
- `pytest apps/reviews/tests/test_enrichment_service.py -x -q` exits 0
- `pytest apps/reviews/tests/test_views.py -x -q` exits 0
- `grep -n "tags=" apps/reviews/services/enrichment.py | grep "\.update("` returns no matches
- `grep -n "class ReviewTagSerializer" apps/reviews/serializers.py` returns a match
- `grep -n "prefetch_related.*tags" apps/reviews/selectors/reviews.py` returns a match
</verification>

<success_criteria>
The enrichment pipeline writes ReviewTag rows (not JSONField), re-enrichment is idempotent (delete before bulk_create), ReviewReadSerializer returns the same [{label, polarity}] shape via ReviewTagSerializer, and the review list query count remains within the REVW-14 ceiling thanks to prefetch_related("tags").
</success_criteria>

<output>
Create `.planning/phases/17-tag-rework-reviewtag-model-and-filter/17-02-SUMMARY.md` when done.
</output>
