---
phase: 17-tag-rework-reviewtag-model-and-filter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/reviews/models.py
  - apps/reviews/migrations/0008_reviewtag.py
  - apps/reviews/tests/factories.py
  - apps/reviews/tests/test_models.py
autonomous: true
requirements:
  - TAG-01

must_haves:
  truths:
    - "ReviewTag model exists in the database with id, review_id, label, polarity columns"
    - "Review.tags JSONField column is gone from the database"
    - "ReviewTag rows are cascade-deleted when the parent Review is deleted"
    - "ReviewTagFactory can be used in tests without errors"
    - "ReviewFactory no longer passes a tags kwarg to Review.objects.create()"
  artifacts:
    - path: "apps/reviews/migrations/0008_reviewtag.py"
      provides: "CreateModel ReviewTag, AddIndex reviewtag_review_label_idx, RemoveField review.tags"
    - path: "apps/reviews/models.py"
      provides: "ReviewTag class with Polarity TextChoices, FK to Review (related_name=tags), label, polarity, Meta indexes"
      contains: "class ReviewTag"
    - path: "apps/reviews/tests/factories.py"
      provides: "ReviewTagFactory; ReviewFactory without tags field"
      contains: "ReviewTagFactory"
  key_links:
    - from: "apps/reviews/models.py"
      to: "apps/reviews/migrations/0008_reviewtag.py"
      via: "django makemigrations output matches hand-written migration"
      pattern: "reviewtag_review_label_idx"
    - from: "apps/reviews/tests/factories.py"
      to: "apps/reviews/models.py"
      via: "ReviewTagFactory references ReviewTag model"
      pattern: "model.*ReviewTag"
---

<objective>
Add the ReviewTag relational model, generate its migration (creating the table + composite index and dropping the Review.tags JSONField), and update the test factory layer to remove the stale tags attribute from ReviewFactory and add ReviewTagFactory.

Purpose: Every subsequent plan in this phase depends on the ReviewTag table existing. This plan is the sole foundation — nothing else can proceed until the model and migration are committed.
Output: ReviewTag model in models.py, migration 0008_reviewtag.py, updated factories.py, and model-level tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-tag-rework-reviewtag-model-and-filter/17-CONTEXT.md
@.planning/phases/17-tag-rework-reviewtag-model-and-filter/17-RESEARCH.md
@.planning/phases/17-tag-rework-reviewtag-model-and-filter/17-PATTERNS.md

<interfaces>
<!-- Key types from the file being modified. Read these before touching anything. -->

From apps/reviews/models.py (excerpt — Review model FK pattern and Meta indexes pattern):
- Review has: organisation FK, shop FK, tags = models.JSONField(default=list, blank=True) [LINE 78 — REMOVE THIS]
- Meta indexes use: ClassVar[list[models.Index]] = [models.Index(fields=[...], name="...")]
- TextChoices inner class pattern: class SomeChoice(models.TextChoices): A = "a", "A Label"

From apps/reviews/tests/factories.py (excerpt):
- ReviewFactory at lines 15–36 has: tags: ClassVar[list] = [] [LINE 35 — REMOVE THIS]
- Factory pattern: class XFactory(DjangoModelFactory): class Meta: model = "app.ModelName"
- SubFactory pattern: field = factory.SubFactory(OtherFactory)
- Faker pattern: field = factory.Faker("word")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ReviewTag model to models.py and remove Review.tags JSONField</name>
  <files>apps/reviews/models.py</files>
  <read_first>
    apps/reviews/models.py — read the full file; note the exact line where tags = models.JSONField(...) appears (line ~78); note the Polarity/EnrichmentStatus TextChoices patterns; note the Meta.indexes ClassVar pattern.
    apps/shops/models.py — lines 13–27 for the TextChoices inner class style used in this codebase.
    .planning/phases/17-tag-rework-reviewtag-model-and-filter/17-PATTERNS.md — Pattern 1 (ReviewTag model verbatim).
  </read_first>
  <behavior>
    - Test 1: ReviewTag model has Polarity TextChoices with POSITIVE/NEUTRAL/NEGATIVE values matching "positive"/"neutral"/"negative"
    - Test 2: ReviewTag.review is a ForeignKey to Review with on_delete=CASCADE and related_name="tags"
    - Test 3: ReviewTag.label has max_length=100, db_index=True
    - Test 4: ReviewTag.polarity has max_length=10, choices=Polarity.choices
    - Test 5: ReviewTag.Meta.db_table == "reviews_reviewtag"
    - Test 6: ReviewTag.Meta.indexes contains an Index on ["review", "label"] named "reviewtag_review_label_idx"
    - Test 7: Review model no longer has a "tags" attribute that is a JSONField (hasattr(Review, 'tags') still True but it is now a RelatedManager, not a JSONField)
    - Test 8: str(ReviewTag instance) returns "ReviewTag({review_id}, {label}, {polarity})"
    - Test 9: Deleting a Review cascades and deletes associated ReviewTag rows
  </behavior>
  <action>
    Add the ReviewTag class to apps/reviews/models.py after the Review class definition. Use the exact class definition from PATTERNS.md Pattern 1. The class has: an inner Polarity TextChoices class with POSITIVE="positive", NEUTRAL="neutral", NEGATIVE="negative"; a review ForeignKey to "reviews.Review" with on_delete=CASCADE, related_name="tags"; label CharField(max_length=100, db_index=True); polarity CharField(max_length=10, choices=Polarity.choices); Meta with db_table="reviews_reviewtag" and indexes=[models.Index(fields=["review", "label"], name="reviewtag_review_label_idx")] as a ClassVar; and a __str__ returning f"ReviewTag({self.review_id}, {self.label}, {self.polarity})".

    Also remove the line `tags = models.JSONField(default=list, blank=True)` from the Review class (per D-06). Do not remove any other fields. Add `from typing import ClassVar` import if not already present (check existing imports first).

    The related_name="tags" on ReviewTag.review means review_instance.tags is now a RelatedManager — the same attribute name the old JSONField used. This is intentional for serializer transparency (D-10).
  </action>
  <verify>
    <automated>cd /Users/renjith/Documents/Accounts/review-master && python manage.py check apps.reviews 2>&1 | grep -E "error|Error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `python manage.py check apps.reviews` exits with no errors
    - `grep -n "class ReviewTag" apps/reviews/models.py` returns a match
    - `grep -n "tags = models.JSONField" apps/reviews/models.py` returns no match
    - `grep -n "reviewtag_review_label_idx" apps/reviews/models.py` returns a match
  </acceptance_criteria>
  <done>ReviewTag model defined with all fields, FK, Meta, indexes, and __str__; Review.tags JSONField line removed; no Django system check errors.</done>
</task>

<task type="auto">
  <name>Task 2: Write migration 0008_reviewtag.py</name>
  <files>apps/reviews/migrations/0008_reviewtag.py</files>
  <read_first>
    apps/reviews/migrations/0007_replied_by_fk.py — for the exact header format, import list, and dependency declaration pattern.
    .planning/phases/17-tag-rework-reviewtag-model-and-filter/17-PATTERNS.md — Pattern 2 (migration verbatim).
    apps/reviews/migrations/ — list directory to confirm 0007_replied_by_fk.py is still the latest; do not create 0008 if a newer file exists.
  </read_first>
  <action>
    Create apps/reviews/migrations/0008_reviewtag.py with:
    - dependencies = [("reviews", "0007_replied_by_fk")]
    - operations in this order (ORDER MATTERS — CreateModel before RemoveField):
      1. migrations.CreateModel("ReviewTag", fields=[id BigAutoField PK, review ForeignKey(CASCADE, related_name="tags", to="reviews.review"), label CharField(db_index=True, max_length=100), polarity CharField(max_length=10)], options={"db_table": "reviews_reviewtag"})
      2. migrations.AddIndex(model_name="reviewtag", index=models.Index(fields=["review", "label"], name="reviewtag_review_label_idx"))
      3. migrations.RemoveField(model_name="review", name="tags")

    Use the exact migration structure from PATTERNS.md Pattern 2. Import django.db.models.deletion and import models from django.db. Do NOT include a polarity choices declaration in the migration (choices are not stored in migrations by default, and this keeps the migration lean).

    This migration must be hand-written (not auto-generated) to ensure the correct operation order. Do not run makemigrations.
  </action>
  <verify>
    <automated>cd /Users/renjith/Documents/Accounts/review-master && python manage.py migrate --check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File apps/reviews/migrations/0008_reviewtag.py exists
    - `python manage.py migrate --check` exits 0 after applying the migration (or reports "No migrations to apply" if already applied)
    - `python manage.py sqlmigrate reviews 0008_reviewtag 2>&1 | grep -i "CREATE TABLE"` shows CREATE TABLE for reviews_reviewtag
    - `python manage.py sqlmigrate reviews 0008_reviewtag 2>&1 | grep -i "DROP COLUMN\|ALTER TABLE.*DROP"` shows removal of the tags column
    - `python manage.py migrate reviews 2>&1 | grep -v "^$"` exits without error
  </acceptance_criteria>
  <done>Migration 0008_reviewtag.py applied successfully: ReviewTag table created, composite index added, Review.tags column dropped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update factories.py and add model-level tests</name>
  <files>apps/reviews/tests/factories.py, apps/reviews/tests/test_models.py</files>
  <read_first>
    apps/reviews/tests/factories.py — read the full file; note line 35 (tags: ClassVar[list] = []) to remove; note the existing factory import pattern and DjangoModelFactory usage.
    apps/reviews/tests/test_models.py — read existing tests to understand the file structure; add new ReviewTag tests in a new class ReviewTagModelTests.
    .planning/phases/17-tag-rework-reviewtag-model-and-filter/17-PATTERNS.md — factories section (ReviewTagFactory pattern).
  </read_first>
  <behavior>
    Factory tests:
    - Test 1: ReviewFactory() creates a Review with no tags attribute that is a JSONField (tags is now a RelatedManager; Review.objects.create() accepts no tags= kwarg)
    - Test 2: ReviewTagFactory() creates a ReviewTag with a valid review FK, a non-empty label, and polarity in ["positive", "neutral", "negative"]

    Model tests (add to test_models.py):
    - Test 3: ReviewTag __str__ returns "ReviewTag({review_id}, {label}, {polarity})"
    - Test 4: ReviewTag.Meta.db_table == "reviews_reviewtag"
    - Test 5: ReviewTag has a composite index named "reviewtag_review_label_idx" on (review, label)
    - Test 6: Deleting a Review cascades and deletes associated ReviewTag rows (create review + tag, delete review, assert ReviewTag.objects.count() == 0)
    - Test 7: ReviewTag.label has db_index=True (check field.db_index attribute)
  </behavior>
  <action>
    In apps/reviews/tests/factories.py:
    - Remove the line `tags: ClassVar[list] = []` from ReviewFactory (per Pitfall 3 in RESEARCH.md — leaving this causes FieldDoesNotExist at test time).
    - Add `ReviewTag` to the model import at the top of the file alongside `Review`.
    - Add ReviewTagFactory after ReviewFactory using DjangoModelFactory with Meta.model = "reviews.ReviewTag" (string label avoids circular import), review = factory.SubFactory(ReviewFactory), label = factory.Faker("word"), polarity = "positive".

    In apps/reviews/tests/test_models.py:
    - Add a ReviewTagModelTests class with the test methods described above. Import ReviewTagFactory from factories. Use `@pytest.mark.django_db` on each test.

    Verify the factory file has no `ClassVar` import error after removing the only ClassVar usage if that import is not used elsewhere — check if ClassVar appears anywhere else in factories.py before removing.
  </action>
  <verify>
    <automated>cd /Users/renjith/Documents/Accounts/review-master && python -m pytest apps/reviews/tests/test_models.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pytest apps/reviews/tests/test_models.py -x -q` exits 0 with all tests passing
    - `grep -n "tags" apps/reviews/tests/factories.py | grep -v "ReviewTag"` returns no match (no stale tags field on ReviewFactory)
    - `grep -n "ReviewTagFactory" apps/reviews/tests/factories.py` returns a match
    - `grep -n "reviewtag_review_label_idx" apps/reviews/tests/test_models.py` returns a match (index existence test)
  </acceptance_criteria>
  <done>ReviewTagFactory exists in factories.py; ReviewFactory has no tags attribute; model-level tests for ReviewTag pass including cascade delete and index verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration | DDL runs with DB superuser rights; incorrect operation order (RemoveField before CreateModel) could corrupt schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | Migration operation order | mitigate | CreateModel runs before RemoveField in operations list; verified by sqlmigrate output in acceptance criteria |
| T-17-02 | Information Disclosure | ReviewTag.review FK cascade | accept | Cascade delete is correct behaviour — tags belong to reviews; no sensitive data exposure |
</threat_model>

<verification>
After all three tasks:
- `python manage.py check` exits 0
- `python manage.py migrate reviews` exits 0
- `pytest apps/reviews/tests/test_models.py -x -q` exits 0
- `grep -n "tags = models.JSONField" apps/reviews/models.py` returns no match
- `grep -n "class ReviewTag" apps/reviews/models.py` returns a match
- `grep -n "ReviewTagFactory" apps/reviews/tests/factories.py` returns a match
</verification>

<success_criteria>
ReviewTag table exists in the database with id, review_id (FK, CASCADE), label (varchar 100, indexed), polarity (varchar 10) columns and a composite index on (review_id, label). Review table no longer has a tags column. ReviewTagFactory is available for all subsequent plans' tests. ReviewFactory creates Reviews without passing a tags kwarg.
</success_criteria>

<output>
Create `.planning/phases/17-tag-rework-reviewtag-model-and-filter/17-01-SUMMARY.md` when done.
</output>
