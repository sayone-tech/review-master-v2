---
phase: 05-profile-and-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - "05-02"
files_modified:
  - templates/accounts/profile.html
  - apps/accounts/tests/test_views.py
autonomous: false
requirements:
  - PROF-01
  - PROF-02

must_haves:
  truths:
    - "GET /admin/profile/ renders two cards: 'Your profile' (name/email/role with Edit button) and 'Change password' (three password fields with strength indicator)"
    - "Clicking Edit next to the name reveals an input with Save/Cancel buttons (Alpine.js x-data)"
    - "Cancel button reverts the input to the original name without reload"
    - "Save button submits POST /admin/profile/update-name/ and on success triggers a success toast on redirect"
    - "New password field shows a 4-bar strength indicator that updates as the user types"
    - "Show/hide toggles work independently on current_password, new_password, confirm_password"
    - "Invalid form submissions re-render the profile page with field error messages visible (b'at least 2', b'Current password is incorrect', b'Passwords do not match')"
    - "Submitting password change with valid inputs shows a 'Password updated.' toast and keeps the user logged in"
  artifacts:
    - path: "templates/accounts/profile.html"
      provides: "Two-card profile page with Alpine.js edit-in-place + password change form"
      contains: "Change password"
    - path: "apps/accounts/tests/test_views.py"
      provides: "xfail markers from Plan 05-02 removed"
      contains: "test_update_name_post_invalid"
  key_links:
    - from: "templates/accounts/profile.html"
      to: "/admin/profile/update-name/"
      via: "form action attribute on name-edit form"
      pattern: "action=.{% url 'profile_update_name' %}"
    - from: "templates/accounts/profile.html"
      to: "/admin/profile/change-password/"
      via: "form action attribute on password change form"
      pattern: "action=.{% url 'profile_change_password' %}"
    - from: "templates/accounts/profile.html"
      to: "templates/accounts/invite_accept.html"
      via: "verbatim reuse of Alpine.js strength indicator + show/hide patterns"
      pattern: "get score.*pw.length >= 10"
---

<objective>
Ship the profile page UI: two cards stacked under `max-w-[640px]`, with Alpine.js edit-in-place for the name and a password change form that reuses the strength-indicator + show/hide patterns from `templates/accounts/invite_accept.html` verbatim. Also render form errors when the views (Plan 05-02) re-render on validation failure, which turns the 3 xfail tests GREEN.

Purpose: User-facing completion of PROF-01 and PROF-02. This plan has a human-verify checkpoint because Alpine.js reactivity (toggle, strength bars, show/hide) cannot be meaningfully asserted by pytest alone.
Output: `/admin/profile/` is a fully functional two-card page; xfail markers removed; all 8 view tests GREEN; visual + interaction checkpoint signed off.
</objective>

<execution_context>
@/Users/renjith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/renjith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-profile-and-hardening/05-CONTEXT.md
@.planning/phases/05-profile-and-hardening/05-RESEARCH.md
@.planning/phases/05-profile-and-hardening/05-02-SUMMARY.md

@templates/accounts/profile.html
@templates/accounts/invite_accept.html
@templates/components/toasts.html
@apps/accounts/forms.py
@apps/accounts/views.py
@apps/accounts/tests/test_views.py

<interfaces>
<!-- Context the executor must match; do NOT invent. -->

From Plan 05-02 `views.py`, the views pass these context keys on validation failure:
```python
return render(request, "accounts/profile.html", {
    "name_form": form_or_unbound,
    "pw_form":  form_or_unbound,
})
```

Form field names used in POSTs:
- `/admin/profile/update-name/` — POST field: `full_name`
- `/admin/profile/change-password/` — POST fields: `current_password`, `new_password`, `confirm_password`

URL names:
- `{% url 'profile' %}` → `/admin/profile/`
- `{% url 'profile_update_name' %}` → `/admin/profile/update-name/`
- `{% url 'profile_change_password' %}` → `/admin/profile/change-password/`

Alpine.js patterns to copy verbatim from invite_accept.html (see file for exact DOM):
- Show/hide toggle: `x-data="{ show: false }"` wrapper with `:type="show ? 'text' : 'password'"` and eye/eye-off button
- Strength indicator: `x-data="{ pw: '', get score() {...}, get label() {...}, get barClass() {...} }"` on the form; 4-bar display via `<template x-for="i in 4">`

Toast delivery: `messages.success(request, "...")` in views is auto-rendered by `templates/components/toasts.html` seed script — no extra template work needed here.

Django messages tags:
- `messages.success()` → tag string `"success"` → green toast icon
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite profile.html as two-card page with Alpine.js forms</name>
  <files>templates/accounts/profile.html, apps/accounts/tests/test_views.py</files>
  <read_first>
    - templates/accounts/profile.html (current 24-line stub — replace wholesale)
    - templates/accounts/invite_accept.html (lines 14-140 — Alpine.js patterns to COPY, not reinvent)
    - templates/components/toasts.html (confirm seed script already handles messages.success)
    - apps/accounts/forms.py (ProfileNameForm + ProfilePasswordChangeForm field names)
    - apps/accounts/views.py (context keys `name_form` and `pw_form` are optional — may be None on GET)
    - apps/accounts/tests/test_views.py (3 tests currently marked xfail; must remove markers and see them pass)
    - templates/partials/head.html (Alpine.js is already loaded globally)
  </read_first>
  <action>
    Replace the entire contents of `templates/accounts/profile.html` with the two-card layout below. Then remove the 3 xfail markers in `test_views.py` that Plan 05-02 added.

    1. Overwrite `templates/accounts/profile.html` with EXACTLY this structure:

       ```html
       {% extends "base.html" %}
       {% block title %}Profile — Review Master{% endblock %}
       {% block page_title %}Profile{% endblock %}

       {% block content %}
         <div class="max-w-[640px] space-y-4">

           {# =============== Card 1: Your profile =============== #}
           <section class="bg-white border border-line rounded-card p-6" data-testid="profile-card">
             <h1 class="text-[18px] font-semibold text-ink mb-1">Your profile</h1>
             <p class="text-[13.5px] text-muted mb-5">Manage your account details and preferences.</p>

             <dl class="space-y-3 text-[13.5px]">
               {# Name row — Alpine.js edit-in-place #}
               <div class="flex gap-3" data-testid="field-name">
                 <dt class="w-32 text-subtle shrink-0 pt-1">Name</dt>
                 <dd class="text-ink flex-1"
                     x-data="{
                             editing: {% if name_form.errors %}true{% else %}false{% endif %},
                             orig: '{{ user.full_name|escapejs }}',
                             val: '{{ name_form.full_name.value|default:user.full_name|escapejs }}'
                             }">
                   {# Static display mode #}
                   <div x-show="!editing" class="flex items-center gap-2">
                     <span class="font-medium" x-text="val || '—'"></span>
                     <button type="button" @click="editing = true"
                             class="text-[12px] text-subtle hover:text-ink underline"
                             data-testid="btn-edit-name">Edit</button>
                   </div>

                   {# Edit mode form #}
                   <form x-show="editing" method="post"
                         action="{% url 'profile_update_name' %}"
                         class="flex items-center gap-2 flex-wrap">
                     {% csrf_token %}
                     <input type="text" name="full_name" x-model="val"
                            minlength="2" maxlength="100" required
                            class="px-2 py-1 text-[13.5px] border rounded-md focus:outline-none focus:ring focus:ring-black/[0.06] focus:border-ink {% if name_form.full_name.errors %}border-red{% else %}border-line{% endif %}"
                            data-testid="input-name">
                     <button type="submit"
                             class="text-[12px] text-yellow hover:text-yellow-hover font-medium"
                             data-testid="btn-save-name">Save</button>
                     <button type="button" @click="editing = false; val = orig"
                             class="text-[12px] text-subtle hover:text-ink"
                             data-testid="btn-cancel-name">Cancel</button>
                   </form>

                   {# Error messages for name form #}
                   {% if name_form.full_name.errors %}
                     <p class="mt-2 text-[12px] text-red flex items-center gap-1" role="alert" data-testid="error-name">
                       <span class="w-3 h-3" aria-hidden="true" data-lucide="alert-circle"></span>
                       {{ name_form.full_name.errors.0 }}
                     </p>
                   {% endif %}
                 </dd>
               </div>

               {# Email row — read-only #}
               <div class="flex gap-3">
                 <dt class="w-32 text-subtle shrink-0">Email</dt>
                 <dd class="text-ink">{{ user.email }}</dd>
               </div>

               {# Role row — read-only #}
               <div class="flex gap-3">
                 <dt class="w-32 text-subtle shrink-0">Role</dt>
                 <dd class="text-ink">{{ user.get_role_display }}</dd>
               </div>
             </dl>
           </section>

           {# =============== Card 2: Change password =============== #}
           <section class="bg-white border border-line rounded-card p-6" data-testid="password-card">
             <h2 class="text-[18px] font-semibold text-ink mb-1">Change password</h2>
             <p class="text-[13.5px] text-muted mb-5">Update the password used to sign in to Review Master.</p>

             <form method="post" action="{% url 'profile_change_password' %}" novalidate
                   x-data="{
                           pw: '',
                           get score() {
                           let s = 0;
                           if (this.pw.length >= 10) s++;
                           if (/[A-Z]/.test(this.pw)) s++;
                           if (/[0-9]/.test(this.pw)) s++;
                           if (/[^A-Za-z0-9]/.test(this.pw)) s++;
                           return s;
                           },
                           get label() { return ['Too short','Weak','Fair','Good','Strong'][this.score]; },
                           get barClass() { return ['bg-line','bg-red','bg-amber','bg-yellow','bg-green'][this.score]; }
                           }">
               {% csrf_token %}

               {# Current password #}
               <div class="mb-4" data-testid="field-current-password">
                 <label for="id_current_password" class="block text-[13px] font-medium text-ink mb-1.5">
                   Current password<span class="text-red ml-0.5" aria-hidden="true">*</span><span class="sr-only"> required</span>
                 </label>
                 <div x-data="{ show: false }" class="relative">
                   <input id="id_current_password" name="current_password"
                          :type="show ? 'text' : 'password'" required autocomplete="current-password"
                          class="w-full pl-3 pr-10 py-[9px] text-[13.5px] bg-white border rounded-md text-text focus:outline-none focus:ring focus:ring-black/[0.06] focus:border-ink {% if pw_form.current_password.errors %}border-red{% else %}border-line{% endif %}">
                   <button type="button" tabindex="-1" @click="show = !show"
                           class="absolute right-2 top-1/2 -translate-y-1/2 w-[30px] h-[30px] rounded-sm text-subtle hover:bg-line-soft flex items-center justify-center"
                           :aria-label="show ? 'Hide password' : 'Show password'" aria-label="Show password">
                     <span class="w-4 h-4" aria-hidden="true" :data-lucide="show ? 'eye-off' : 'eye'" data-lucide="eye"></span>
                   </button>
                 </div>
                 {% if pw_form.current_password.errors %}
                   <p class="mt-2 text-[12px] text-red flex items-center gap-1" role="alert" data-testid="error-current-password">
                     <span class="w-3 h-3" aria-hidden="true" data-lucide="alert-circle"></span>
                     {{ pw_form.current_password.errors.0 }}
                   </p>
                 {% endif %}
               </div>

               {# New password + strength indicator #}
               <div class="mb-2" data-testid="field-new-password">
                 <label for="id_new_password" class="block text-[13px] font-medium text-ink mb-1.5">
                   New password<span class="text-red ml-0.5" aria-hidden="true">*</span><span class="sr-only"> required</span>
                 </label>
                 <div x-data="{ show: false }" class="relative">
                   <input id="id_new_password" name="new_password"
                          :type="show ? 'text' : 'password'" required autocomplete="new-password" x-model="pw"
                          class="w-full pl-3 pr-10 py-[9px] text-[13.5px] bg-white border rounded-md text-text focus:outline-none focus:ring focus:ring-black/[0.06] focus:border-ink {% if pw_form.new_password.errors %}border-red{% else %}border-line{% endif %}">
                   <button type="button" tabindex="-1" @click="show = !show"
                           class="absolute right-2 top-1/2 -translate-y-1/2 w-[30px] h-[30px] rounded-sm text-subtle hover:bg-line-soft flex items-center justify-center"
                           :aria-label="show ? 'Hide password' : 'Show password'" aria-label="Show password">
                     <span class="w-4 h-4" aria-hidden="true" :data-lucide="show ? 'eye-off' : 'eye'" data-lucide="eye"></span>
                   </button>
                 </div>
                 {% if pw_form.new_password.errors %}
                   <p class="mt-2 text-[12px] text-red flex items-center gap-1" role="alert" data-testid="error-new-password">
                     <span class="w-3 h-3" aria-hidden="true" data-lucide="alert-circle"></span>
                     {{ pw_form.new_password.errors.0 }}
                   </p>
                 {% endif %}
               </div>

               {# Strength indicator — 4-bar, verbatim from invite_accept.html #}
               <div class="mb-4" aria-live="polite" data-testid="strength-indicator">
                 <div class="flex gap-1">
                   <template x-for="i in 4" :key="i">
                     <div class="h-1 rounded-full flex-1" :class="i <= score ? barClass : 'bg-line'"></div>
                   </template>
                 </div>
                 <div class="mt-1 text-[12px] text-subtle text-right" x-text="label">&nbsp;</div>
               </div>

               {# Confirm password #}
               <div class="mb-4" data-testid="field-confirm-password">
                 <label for="id_confirm_password" class="block text-[13px] font-medium text-ink mb-1.5">
                   Confirm new password<span class="text-red ml-0.5" aria-hidden="true">*</span><span class="sr-only"> required</span>
                 </label>
                 <div x-data="{ show: false }" class="relative">
                   <input id="id_confirm_password" name="confirm_password"
                          :type="show ? 'text' : 'password'" required autocomplete="new-password"
                          class="w-full pl-3 pr-10 py-[9px] text-[13.5px] bg-white border rounded-md text-text focus:outline-none focus:ring focus:ring-black/[0.06] focus:border-ink {% if pw_form.confirm_password.errors %}border-red{% else %}border-line{% endif %}">
                   <button type="button" tabindex="-1" @click="show = !show"
                           class="absolute right-2 top-1/2 -translate-y-1/2 w-[30px] h-[30px] rounded-sm text-subtle hover:bg-line-soft flex items-center justify-center"
                           :aria-label="show ? 'Hide password' : 'Show password'" aria-label="Show password">
                     <span class="w-4 h-4" aria-hidden="true" :data-lucide="show ? 'eye-off' : 'eye'" data-lucide="eye"></span>
                   </button>
                 </div>
                 {% if pw_form.confirm_password.errors %}
                   <p class="mt-2 text-[12px] text-red flex items-center gap-1" role="alert" data-testid="error-confirm-password">
                     <span class="w-3 h-3" aria-hidden="true" data-lucide="alert-circle"></span>
                     {{ pw_form.confirm_password.errors.0 }}
                   </p>
                 {% endif %}
               </div>

               <button type="submit"
                       class="inline-flex items-center justify-center gap-1.5 font-sans font-medium border transition-[background,border-color,color] duration-[120ms] focus:outline-none focus-visible:ring-2 focus-visible:ring-yellow focus-visible:ring-offset-2 px-3.5 py-2 text-[13.5px] rounded-md bg-yellow text-black border-yellow-hover hover:bg-yellow-hover"
                       data-testid="btn-submit-password">
                 Change password
               </button>
             </form>
           </section>

         </div>
       {% endblock %}
       ```

    2. In `apps/accounts/tests/test_views.py`, REMOVE the `@pytest.mark.xfail(...)` decorator lines that Plan 05-02 added above:
       - `TestProfileNameUpdate.test_update_name_post_invalid`
       - `TestPasswordChangeView.test_change_password_wrong_current`
       - `TestPasswordChangeView.test_change_password_mismatch`

       All 3 should now be plain test methods. Run pytest after to confirm they pass (they assert that error text appears in the rendered HTML — which the template above now includes via `{{ form.field.errors.0 }}`).

    Anti-patterns to avoid:
    - Do NOT hardcode `/admin/profile/update-name/` — use `{% url 'profile_update_name' %}`
    - Do NOT add hover/focus-trap logic that isn't in invite_accept.html — keep parity
    - Do NOT inline `<style>` blocks — use Tailwind utility classes exclusively
    - Do NOT use `messages.info` anywhere in response messages (stays in views, not template)
    - Do NOT reference form fields that don't exist in the context (view passes `name_form` / `pw_form` only on validation failure — use `{% if name_form.full_name.errors %}` guards, never dereference `name_form.full_name` directly without the `.errors` check)

    Note on Alpine initial state for editing: `x-data="{ editing: {% if name_form.errors %}true{% else %}false{% endif %}, ... }"` — this auto-opens the edit form when the name form had errors, so the user sees their invalid input AND the error message.
  </action>
  <verify>
    <automated>uv run pytest apps/accounts/tests/test_views.py apps/accounts/tests/test_services.py -x -q && grep -c 'data-testid="strength-indicator"' templates/accounts/profile.html</automated>
  </verify>
  <acceptance_criteria>
    - `templates/accounts/profile.html` contains the string `data-testid="profile-card"`
    - `templates/accounts/profile.html` contains the string `data-testid="password-card"`
    - `templates/accounts/profile.html` contains the string `data-testid="strength-indicator"`
    - `templates/accounts/profile.html` contains `{% url 'profile_update_name' %}`
    - `templates/accounts/profile.html` contains `{% url 'profile_change_password' %}`
    - `templates/accounts/profile.html` contains `x-model="pw"` (strength binding on new_password input)
    - `templates/accounts/profile.html` contains `pw.length >= 10` (strength scoring — verbatim from invite_accept.html)
    - `templates/accounts/profile.html` contains three `x-data="{ show: false }"` occurrences (one per password field)
    - `templates/accounts/profile.html` contains `@click="editing = false; val = orig"` (Cancel revert)
    - `grep -c '@pytest.mark.xfail' apps/accounts/tests/test_views.py` returns 0 (no xfails remain in the file at all)
    - `uv run pytest apps/accounts/tests/test_views.py::TestProfileNameUpdate -x -q` exits 0 (4/4 pass)
    - `uv run pytest apps/accounts/tests/test_views.py::TestPasswordChangeView -x -q` exits 0 (4/4 pass)
    - `uv run pytest apps/accounts/tests/test_services.py -x -q` exits 0 (6/6 pass)
  </acceptance_criteria>
  <done>
    Profile page fully rendered; every error path shows its error text; all 14 PROF-01/PROF-02 tests GREEN.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify profile page interactive behaviour</name>
  <files>templates/accounts/profile.html</files>
  <read_first>
    - templates/accounts/profile.html (just rewritten in Task 1)
  </read_first>
  <what-built>
    Two-card profile page at `/admin/profile/`:
    - Card 1 "Your profile": name display with Edit button, edit mode shows input + Save/Cancel, email + role read-only
    - Card 2 "Change password": three password fields, all with show/hide eye toggle, new password shows 4-bar strength indicator
    - Django messages success toast wiring
  </what-built>
  <action>
    PAUSE execution and prompt the user to perform the manual verification steps below. Do not proceed to any subsequent plan until the user replies "approved".

    Pre-step (tell the user): start local dev (`make up`, Vite dev server `npm run dev` in frontend/, then `python manage.py runserver` or equivalent).

    Verification steps to walk the user through:

    1. Log in as a superadmin at `/login/`, then navigate to `/admin/profile/`.
    2. Verify BOTH cards render with the correct titles and separation.
    3. Name edit:
       - Click "Edit" → verify input appears with current name pre-filled and Save / Cancel buttons visible.
       - Type a new name, click Cancel → verify the displayed name reverts to the original (NO page reload).
       - Click Edit, type "A" (too short), click Save → verify page reloads, edit form stays open (auto-opened by `x-data` init), and error message "Name must be at least 2 characters." appears in red.
       - Click Edit, type a valid new name, click Save → verify page reloads, name displays as the new value, and a green "Name updated." toast appears top-right.
    4. Password change:
       - Click the eye icon on EACH of the three password fields — each toggles its field independently between masked and plaintext.
       - Type in New password field and watch the 4-bar indicator: 1 bar for short, then more bars fill as length/uppercase/digit/symbol appear. Label changes "Too short" → "Weak" → "Fair" → "Good" → "Strong".
       - Submit with wrong current password → expect error "Current password is incorrect." displayed under Current password field in red.
       - Submit with mismatched new vs confirm → expect "Passwords do not match." under Confirm password.
       - Submit with correct current + valid matching new → expect a green "Password updated." toast, user stays logged in (can still navigate to /admin/organisations/).
    5. Accessibility spot-check: tab through the password form — focus ring is visible on inputs and submit button; eye toggle buttons have screen-reader aria-label ("Show password" / "Hide password").
    6. Mobile (DevTools responsive mode, <768px): both cards stack; edit form controls wrap nicely; no horizontal scroll.

    Wait for user reply of "approved" (or a list of issues). Do NOT proceed automatically.
  </action>
  <verify>
    <automated>echo "Manual checkpoint — resume only after user replies 'approved'. No automated verification possible for Alpine.js reactivity."</automated>
  </verify>
  <done>
    User replies with "approved" after walking through all 6 verification steps. Any issues raised by the user must be fixed and re-verified before the checkpoint is cleared.
  </done>
  <resume-signal>Type "approved" to finalise the plan, or describe issues found (specific card, specific interaction).</resume-signal>
</task>

</tasks>

<verification>
- [ ] `uv run pytest apps/accounts/tests/test_services.py apps/accounts/tests/test_views.py -x -q` — 6 services + all view tests pass (8 new + pre-existing)
- [ ] `grep -c '@pytest.mark.xfail' apps/accounts/tests/test_views.py` returns 0
- [ ] `uv run python manage.py check` — 0 errors
- [ ] Human-verify checkpoint signed off
</verification>

<success_criteria>
1. Two cards render with correct layout under max-w-[640px]
2. Edit-in-place works (Edit / Save / Cancel, Cancel reverts)
3. 4-bar strength indicator responds to typing in New password
4. Show/hide toggle works independently on all three password fields
5. Validation errors display inline
6. Success toasts appear after save via Django messages
7. Session stays authenticated after password change
</success_criteria>

<output>
After completion, create `.planning/phases/05-profile-and-hardening/05-03-SUMMARY.md` with:
- Final template line count + key structural decisions
- Tests now fully green (14/14 PROF-01/PROF-02)
- Human-verify approval timestamp + any notes
- Commit SHAs
</output>
