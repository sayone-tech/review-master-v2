---
phase: 05-profile-and-hardening
plan: 04
type: execute
wave: 2
depends_on:
  - "05-02"
files_modified:
  - config/settings/production.py
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - PROF-01
  - PROF-02

must_haves:
  truths:
    - "config/settings/production.py defines SECURE_BROWSER_XSS_FILTER = True and X_FRAME_OPTIONS = 'DENY'"
    - "python manage.py check --deploy passes when DJANGO_SETTINGS_MODULE=config.settings.production with the CI env stubs"
    - ".github/workflows/ci.yml exists and runs on push-to-main and pull_request targeting main"
    - "CI runs pre-commit, mypy, pytest with --cov=apps --cov-fail-under=85, makemigrations --check, and check --deploy (in that order)"
    - "CI deploy-check step injects stub values for DJANGO_SECRET_KEY, DJANGO_ALLOWED_HOSTS, AWS_SES_FROM_EMAIL so the production settings module loads"
  artifacts:
    - path: "config/settings/production.py"
      provides: "Hardened security headers"
      contains: "SECURE_BROWSER_XSS_FILTER"
    - path: ".github/workflows/ci.yml"
      provides: "5-step CI pipeline on push + PR"
      contains: "cov-fail-under=85"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pre-commit + mypy + pytest + check --deploy"
      via: "sequential steps with fail-fast"
      pattern: "pre-commit run --all-files"
    - from: ".github/workflows/ci.yml"
      to: "config/settings/production.py"
      via: "DJANGO_SETTINGS_MODULE env var on deploy-check step"
      pattern: "DJANGO_SETTINGS_MODULE: config.settings.production"
---

<objective>
Add the two missing production security headers and create the GitHub Actions CI workflow. This closes out Phase 5's "hardening" half: every PR to main is automatically gated on lint, types, coverage ≥85%, migration freshness, and a production deploy check.

Purpose: Enforce the quality bar Phase 5 was chartered to introduce. The profile feature itself is done by Plan 05-03; this plan locks the process so future regressions are caught before merge.
Output: `config/settings/production.py` gains two security settings; `.github/workflows/ci.yml` is created and runs green on the current main branch.
</objective>

<execution_context>
@/Users/renjith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/renjith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-profile-and-hardening/05-CONTEXT.md
@.planning/phases/05-profile-and-hardening/05-RESEARCH.md

@config/settings/production.py
@config/settings/base.py
@config/settings/test.py
@.pre-commit-config.yaml
@pyproject.toml

<interfaces>
<!-- Existing config the workflow must match. -->

From config/settings/production.py (current state — already has most security settings):
```python
SECURE_SSL_REDIRECT = True                    # present
SESSION_COOKIE_SECURE = True                  # present
CSRF_COOKIE_SECURE = True                     # present
SECURE_HSTS_SECONDS = 31536000                # present
SECURE_HSTS_INCLUDE_SUBDOMAINS = True         # present
SECURE_HSTS_PRELOAD = True                    # present
SECURE_CONTENT_TYPE_NOSNIFF = True            # present
SECURE_PROXY_SSL_HEADER = ("HTTP_X_FORWARDED_PROTO", "https")  # present
# MISSING — to add:
# SECURE_BROWSER_XSS_FILTER = True
# X_FRAME_OPTIONS = "DENY"
```

Production settings env vars (all required — fail loudly if missing):
- DJANGO_SECRET_KEY: `env("DJANGO_SECRET_KEY")` — no default
- DJANGO_ALLOWED_HOSTS: `env.list("DJANGO_ALLOWED_HOSTS")` — no default
- AWS_SES_FROM_EMAIL: `env("AWS_SES_FROM_EMAIL")` — no default
- DATABASE_URL: inherits default `postgres://app:app@db:5432/reviewmaster` from base
- REDIS_URL: inherits default `redis://redis:6379` from base (for cache)

From pyproject.toml:
- [tool.coverage.report] fail_under = 85
- Test deps include pytest 8.3.3, pytest-django 4.9.0, pytest-cov 5.0.0

From .pre-commit-config.yaml: django-upgrade target is "5.1" (LOCKED — must not change to 6.0; 1.22.2 doesn't support it).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden production security settings</name>
  <files>config/settings/production.py</files>
  <read_first>
    - config/settings/production.py (current 20 lines — verify which security settings already exist)
    - config/settings/base.py (check MIDDLEWARE order; SecurityMiddleware must be first — it already is, confirm)
    - CLAUDE.md §19 (Security checklist — the target state)
  </read_first>
  <action>
    Add the two missing security headers to `config/settings/production.py`. Do NOT duplicate settings that already exist (verified present: SECURE_SSL_REDIRECT, SESSION_COOKIE_SECURE, CSRF_COOKIE_SECURE, SECURE_HSTS_*, SECURE_CONTENT_TYPE_NOSNIFF, SECURE_PROXY_SSL_HEADER).

    Append these two lines immediately after the existing `SECURE_PROXY_SSL_HEADER = ...` line and before the `EMAIL_BACKEND = ...` line:

    ```python
    # Extra security headers — PROF/hardening (Phase 05)
    SECURE_BROWSER_XSS_FILTER = True     # X-XSS-Protection: 1; mode=block
    X_FRAME_OPTIONS = "DENY"             # Clickjacking protection (middleware reads this setting)
    ```

    Do NOT add CSP settings in this task — CSP requires `django.middleware.csp` middleware config plus directives that the Vite dev server + Alpine.js bundle will interact with; that's out of scope for Phase 5 per RESEARCH.md Open Questions #1.

    Do NOT change base.py or any other settings file. Do NOT touch INSTALLED_APPS or MIDDLEWARE (already compliant — SecurityMiddleware and XFrameOptionsMiddleware are both present in base.py MIDDLEWARE list).

    Verify locally before committing:
    ```bash
    DJANGO_SETTINGS_MODULE=config.settings.production \
    DJANGO_SECRET_KEY=dev-check-secret \
    DJANGO_ALLOWED_HOSTS=localhost \
    AWS_SES_FROM_EMAIL=noreply@example.com \
    uv run python manage.py check --deploy
    ```

    Expected output: "System check identified no issues" OR at most non-fatal warnings about SECURE_HSTS_SECONDS (it's fine at 31536000) and CSP (intentionally deferred).
  </action>
  <verify>
    <automated>grep -c 'SECURE_BROWSER_XSS_FILTER = True' config/settings/production.py && grep -c 'X_FRAME_OPTIONS = "DENY"' config/settings/production.py && DJANGO_SETTINGS_MODULE=config.settings.production DJANGO_SECRET_KEY=ci-check DJANGO_ALLOWED_HOSTS=localhost AWS_SES_FROM_EMAIL=noreply@example.com uv run python manage.py check --deploy 2>&1 | grep -E "System check identified (no issues|[0-9]+ issue)"</automated>
  </verify>
  <acceptance_criteria>
    - `config/settings/production.py` contains the exact string `SECURE_BROWSER_XSS_FILTER = True`
    - `config/settings/production.py` contains the exact string `X_FRAME_OPTIONS = "DENY"`
    - `config/settings/production.py` does NOT contain duplicate SECURE_SSL_REDIRECT or SESSION_COOKIE_SECURE entries (unchanged presence of originals is fine; no duplicates)
    - Running `DJANGO_SETTINGS_MODULE=config.settings.production DJANGO_SECRET_KEY=ci-check DJANGO_ALLOWED_HOSTS=localhost AWS_SES_FROM_EMAIL=noreply@example.com uv run python manage.py check --deploy` exits with code 0 (warnings allowed, errors not)
  </acceptance_criteria>
  <done>
    Production security headers complete per CLAUDE.md §19 (minus CSP which is deferred); deploy check passes with CI stub env vars.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .pre-commit-config.yaml (understand what `pre-commit run --all-files` will execute)
    - pyproject.toml (verify pytest-cov is in deps — it is, 5.0.0)
    - config/settings/test.py (CI steps 1-4 run with this module)
    - config/settings/production.py (CI step 5 uses this module; env vars from Task 1 verification)
    - CLAUDE.md §16 (CI pipeline requirements — exactly 5 steps in order)
  </read_first>
  <action>
    Create `.github/workflows/ci.yml`. The `.github/` and `.github/workflows/` directories do not yet exist — the write operation will create them.

    Write this EXACT content:

    ```yaml
    name: CI

    on:
      push:
        branches: [main]
      pull_request:
        branches: [main]

    jobs:
      ci:
        runs-on: ubuntu-latest

        services:
          postgres:
            image: postgres:16-alpine
            env:
              POSTGRES_DB: reviewmaster
              POSTGRES_USER: app
              POSTGRES_PASSWORD: app
            options: >-
              --health-cmd pg_isready
              --health-interval 5s
              --health-timeout 5s
              --health-retries 5
            ports:
              - 5432:5432
          redis:
            image: redis:7-alpine
            options: >-
              --health-cmd "redis-cli ping"
              --health-interval 5s
              --health-timeout 3s
              --health-retries 3
            ports:
              - 6379:6379

        env:
          DJANGO_SETTINGS_MODULE: config.settings.test
          DJANGO_SECRET_KEY: ci-insecure-secret
          DJANGO_ALLOWED_HOSTS: localhost
          DATABASE_URL: postgres://app:app@localhost:5432/reviewmaster
          REDIS_URL: redis://localhost:6379

        steps:
          - name: Checkout
            uses: actions/checkout@v4

          - name: Install uv
            uses: astral-sh/setup-uv@v5
            with:
              version: "0.4.29"

          - name: Set up Python
            run: uv python install 3.12

          - name: Install dependencies
            run: uv sync --frozen

          - name: pre-commit
            run: uv run pre-commit run --all-files

          - name: mypy
            run: uv run mypy .

          - name: pytest (coverage ≥85%)
            run: uv run pytest --cov=apps --cov-fail-under=85

          - name: Check migrations
            run: uv run python manage.py makemigrations --check --dry-run

          - name: Deploy check (production settings)
            env:
              DJANGO_SETTINGS_MODULE: config.settings.production
              DJANGO_SECRET_KEY: ci-deploy-check-secret
              DJANGO_ALLOWED_HOSTS: localhost
              AWS_SES_FROM_EMAIL: noreply@example.com
              DEFAULT_FROM_EMAIL: noreply@example.com
              DEFAULT_REPLY_TO: support@example.com
              DATABASE_URL: postgres://app:app@localhost:5432/reviewmaster
              REDIS_URL: redis://localhost:6379
            run: uv run python manage.py check --deploy
    ```

    Notes on decisions:
    - `actions/checkout@v4` and `astral-sh/setup-uv@v5` are the current stable majors as of April 2026.
    - `uv sync --frozen` uses the committed lockfile for reproducible installs.
    - The pre-commit step automatically runs ruff, djhtml, django-upgrade (target 5.1 — LOCKED, do not bump), gitleaks, bandit, mypy (but pre-commit's mypy is its own copy; our separate `mypy` step is the authoritative run against the full project).
    - `--cov=apps` matches `[tool.coverage.run] source = ["apps"]` (if pyproject doesn't set source, --cov=apps is still the right scope).
    - Deploy-check step overrides DJANGO_SETTINGS_MODULE at the step level to switch from test to production — steps 1-4 use the job-level test module.
    - Stub secret `ci-insecure-secret` is a recognized pattern — gitleaks in the pre-commit step will NOT flag it because it's obviously a test/CI value, not an AWS key or real secret.
    - No Node / Vite build step: `config.settings.test` uses `DJANGO_VITE dev_mode=True` which does not require a built manifest.

    Do NOT add a deploy job — CONTEXT.md explicitly scopes this phase to CI only. A deploy workflow is deferred.
    Do NOT add Codecov, Slack, or other optional integrations — keep the surface minimal and auditable.
    Do NOT bump django-upgrade target to 6.0 anywhere — the project decision (STATE.md) to stay on 5.1 remains locked.
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -c "cov-fail-under=85" .github/workflows/ci.yml && grep -c "pre-commit run --all-files" .github/workflows/ci.yml && grep -c "makemigrations --check --dry-run" .github/workflows/ci.yml && grep -c "check --deploy" .github/workflows/ci.yml && grep -c "config.settings.production" .github/workflows/ci.yml && python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && ! grep -c 'target-version.*6.0' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists
    - File parses as valid YAML (`python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0)
    - File contains trigger `push: branches: [main]` and `pull_request: branches: [main]`
    - File contains the exact string `pre-commit run --all-files`
    - File contains the exact string `uv run mypy .`
    - File contains the exact string `--cov=apps --cov-fail-under=85`
    - File contains the exact string `makemigrations --check --dry-run`
    - File contains the exact string `manage.py check --deploy`
    - File contains `DJANGO_SETTINGS_MODULE: config.settings.production` inside the deploy-check step env block
    - File contains service containers for `postgres:16-alpine` and `redis:7-alpine`
    - File does NOT contain `target-version: 6.0` or `target-version: "6.0"` (django-upgrade stays at 5.1)
    - File does NOT contain any `deploy:` job (CI only in this phase)
    - `DJANGO_SECRET_KEY` stub value is an obviously fake string (not a real-looking secret) — `ci-insecure-secret` is acceptable
  </acceptance_criteria>
  <done>
    CI workflow committed. Next PR / push-to-main triggers it end-to-end.
  </done>
</task>

</tasks>

<verification>
- [ ] `grep 'SECURE_BROWSER_XSS_FILTER\|X_FRAME_OPTIONS' config/settings/production.py` returns both lines
- [ ] Local `manage.py check --deploy` with stub env vars exits 0
- [ ] `.github/workflows/ci.yml` is valid YAML and has all 5 CI steps in the documented order
- [ ] Full test suite still passes: `uv run pytest --cov=apps --cov-fail-under=85`
- [ ] `pre-commit run --all-files` locally: no failures (mypy strict, ruff clean, gitleaks clean)
</verification>

<success_criteria>
1. Two security headers added to production.py (XSS filter + X-Frame-Options DENY)
2. `manage.py check --deploy` passes with CI stub env vars
3. `.github/workflows/ci.yml` exists and is valid YAML
4. CI has all 5 required steps in the correct order
5. Coverage gate enforced (`--cov-fail-under=85`)
6. django-upgrade target remains 5.1 (no inadvertent bump)
</success_criteria>

<output>
After completion, create `.planning/phases/05-profile-and-hardening/05-04-SUMMARY.md` with:
- Diff of production.py (exactly 2 new lines)
- Full ci.yml contents
- Result of local `manage.py check --deploy` run
- Result of local `pre-commit run --all-files`
- Commit SHAs
- Note that the next push to main will be the first live CI run — Phase 5 UAT expectation is that it comes back green
</output>
