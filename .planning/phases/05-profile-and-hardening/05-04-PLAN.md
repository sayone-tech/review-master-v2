---
phase: 05-profile-and-hardening
plan: 04
type: execute
wave: 2
depends_on:
  - "05-02"
files_modified:
  - config/settings/production.py
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - HRDG-01
  - HRDG-02

must_haves:
  truths:
    - "config/settings/production.py defines SECURE_BROWSER_XSS_FILTER = True and X_FRAME_OPTIONS = 'DENY'"
    - "config/settings/production.py defines SECURE_CSP (Django 6 built-in) with default-src 'self', script-src 'self' + 'unsafe-inline', style-src 'self' + 'unsafe-inline', img-src 'self' + data:, font-src 'self'"
    - "django.middleware.csp.ContentSecurityPolicyMiddleware is added to MIDDLEWARE in production (or base) without breaking base stack"
    - "python manage.py check --deploy passes when DJANGO_SETTINGS_MODULE=config.settings.production with the CI env stubs"
    - ".github/workflows/ci.yml exists and runs on push-to-main and pull_request targeting main"
    - "CI runs pre-commit, mypy, pytest with --cov=apps --cov-fail-under=85, makemigrations --check, and check --deploy (in that order)"
    - "CI deploy-check step injects stub values for DJANGO_SECRET_KEY, DJANGO_ALLOWED_HOSTS, AWS_SES_FROM_EMAIL so the production settings module loads"
  artifacts:
    - path: "config/settings/production.py"
      provides: "Hardened security headers + minimal permissive CSP"
      contains: "SECURE_BROWSER_XSS_FILTER"
    - path: ".github/workflows/ci.yml"
      provides: "5-step CI pipeline on push + PR"
      contains: "cov-fail-under=85"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pre-commit + mypy + pytest + check --deploy"
      via: "sequential steps with fail-fast"
      pattern: "pre-commit run --all-files"
    - from: ".github/workflows/ci.yml"
      to: "config/settings/production.py"
      via: "DJANGO_SETTINGS_MODULE env var on deploy-check step"
      pattern: "DJANGO_SETTINGS_MODULE: config.settings.production"
    - from: "config/settings/production.py"
      to: "django.middleware.csp.ContentSecurityPolicyMiddleware"
      via: "MIDDLEWARE list entry + SECURE_CSP dict read by middleware"
      pattern: "ContentSecurityPolicyMiddleware"
---

<objective>
Add the missing production security headers (XSS filter, X-Frame-Options, and a minimal permissive Content Security Policy), and create the GitHub Actions CI workflow. This closes out Phase 5's "hardening" half: every PR to main is automatically gated on lint, types, coverage ≥85%, migration freshness, and a production deploy check.

Purpose: Enforce the quality bar Phase 5 was chartered to introduce (HRDG-01, HRDG-02). The profile feature itself is done by Plans 05-01..05-03; this plan locks the process so future regressions are caught before merge.
Output: `config/settings/production.py` gains three security settings (XSS, X-Frame-Options, CSP); `.github/workflows/ci.yml` is created and runs green on the current main branch.
</objective>

<execution_context>
@/Users/renjith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/renjith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-profile-and-hardening/05-CONTEXT.md
@.planning/phases/05-profile-and-hardening/05-RESEARCH.md

@config/settings/production.py
@config/settings/base.py
@config/settings/test.py
@.pre-commit-config.yaml
@pyproject.toml

<interfaces>
<!-- Existing config the workflow must match. -->

From config/settings/production.py (current state — already has most security settings):
```python
SECURE_SSL_REDIRECT = True                    # present
SESSION_COOKIE_SECURE = True                  # present
CSRF_COOKIE_SECURE = True                     # present
SECURE_HSTS_SECONDS = 31536000                # present
SECURE_HSTS_INCLUDE_SUBDOMAINS = True         # present
SECURE_HSTS_PRELOAD = True                    # present
SECURE_CONTENT_TYPE_NOSNIFF = True            # present
SECURE_PROXY_SSL_HEADER = ("HTTP_X_FORWARDED_PROTO", "https")  # present
# MISSING — to add:
# SECURE_BROWSER_XSS_FILTER = True
# X_FRAME_OPTIONS = "DENY"
# SECURE_CSP = { ...minimal permissive policy... }
```

Production settings env vars (all required — fail loudly if missing):
- DJANGO_SECRET_KEY: `env("DJANGO_SECRET_KEY")` — no default
- DJANGO_ALLOWED_HOSTS: `env.list("DJANGO_ALLOWED_HOSTS")` — no default
- AWS_SES_FROM_EMAIL: `env("AWS_SES_FROM_EMAIL")` — no default
- DATABASE_URL: inherits default `postgres://app:app@db:5432/reviewmaster` from base
- REDIS_URL: inherits default `redis://redis:6379` from base (for cache)

From Django 6.0 docs (CONFIRMED — Django==6.0.2 is installed per pyproject.toml):
- `django.middleware.csp.ContentSecurityPolicyMiddleware` is bundled in Django 6.0 (NEW in 6.0)
- Setting name is `SECURE_CSP` (NOT `CSP_*` individual settings — those belong to the third-party `django-csp` package, which we are NOT using)
- `SECURE_CSP` is a flat dict: directive-name → list of sources
- Middleware recommended placement: near the bottom of MIDDLEWARE
- Zero new dependencies required (use the built-in middleware)

From pyproject.toml:
- django==6.0.2 (Django 6 CSP available — DO NOT add django-csp to dependencies)
- [tool.coverage.report] fail_under = 85
- Test deps include pytest 8.3.3, pytest-django 4.9.0, pytest-cov 5.0.0

From .pre-commit-config.yaml: django-upgrade target is "5.1" (LOCKED — must not change to 6.0; 1.22.2 doesn't support it).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden production security settings (XSS filter, X-Frame-Options, CSP)</name>
  <files>config/settings/production.py</files>
  <read_first>
    - config/settings/production.py (current 23 lines — confirm which security settings already exist: SECURE_SSL_REDIRECT, SESSION_COOKIE_SECURE, CSRF_COOKIE_SECURE, SECURE_HSTS_SECONDS, SECURE_HSTS_INCLUDE_SUBDOMAINS, SECURE_HSTS_PRELOAD, SECURE_CONTENT_TYPE_NOSNIFF, SECURE_PROXY_SSL_HEADER — already present; do NOT duplicate)
    - config/settings/base.py (check MIDDLEWARE list — SecurityMiddleware must remain first; ContentSecurityPolicyMiddleware should be appended near the bottom)
    - pyproject.toml (confirm django==6.0.2 — required for built-in CSP middleware)
    - CLAUDE.md §19 (Security checklist — the target state includes CSP per updated CONTEXT.md)
  </read_first>
  <action>
    Add the three missing security settings to `config/settings/production.py`. Do NOT duplicate settings that already exist (verified present: SECURE_SSL_REDIRECT, SESSION_COOKIE_SECURE, CSRF_COOKIE_SECURE, SECURE_HSTS_*, SECURE_CONTENT_TYPE_NOSNIFF, SECURE_PROXY_SSL_HEADER).

    **Decision on CSP implementation: use Django 6 built-in CSP middleware — zero new dependencies.**
    - Django 6.0.2 is already installed (verified in pyproject.toml).
    - Django 6 ships `django.middleware.csp.ContentSecurityPolicyMiddleware` with a `SECURE_CSP` dict setting.
    - Per CONTEXT.md (updated): "add `django-csp` (or Django 6 built-in SecurityMiddleware CSP config) with `CSP_DEFAULT_SRC = ("'self'",)` and `CSP_SCRIPT_SRC = ("'self'", "'unsafe-inline'")`". Since Django 6 built-in is available, prefer it (zero new deps per RESEARCH.md guidance). The directive names in the built-in are `default-src` / `script-src` (hyphenated dict keys), not the `CSP_DEFAULT_SRC` constant names (those are for the django-csp package).
    - Do NOT add `django-csp` to pyproject.toml. Do NOT use the `csp.middleware.CSPMiddleware` class path.

    1. Append these three blocks immediately after the existing `SECURE_PROXY_SSL_HEADER = ...` line and before the `EMAIL_BACKEND = ...` line in `config/settings/production.py`:

       ```python
       # Extra security headers — HRDG-02 (Phase 05)
       SECURE_BROWSER_XSS_FILTER = True     # X-XSS-Protection: 1; mode=block
       X_FRAME_OPTIONS = "DENY"             # Clickjacking protection (XFrameOptionsMiddleware reads this setting)

       # Content Security Policy — Django 6 built-in (HRDG-02)
       # Minimal permissive policy: 'unsafe-inline' on script/style is required for
       # Alpine.js inline directives and Tailwind/Vite bundled CSS. Refine later once
       # all inline handlers are migrated to external modules with nonces.
       SECURE_CSP = {
           "default-src": ["'self'"],
           "script-src": ["'self'", "'unsafe-inline'"],
           "style-src": ["'self'", "'unsafe-inline'"],
           "img-src": ["'self'", "data:"],
           "font-src": ["'self'"],
       }

       # Append CSP middleware to the base MIDDLEWARE list (Django 6 built-in).
       # Placed near the bottom so any middleware that reads csp_nonce can run before it.
       MIDDLEWARE = MIDDLEWARE + [  # noqa: F405
           "django.middleware.csp.ContentSecurityPolicyMiddleware",
       ]
       ```

    2. Do NOT change `base.py`, `test.py`, or `local.py`. CSP is enabled in production only — local dev with Vite's HMR websocket would require additional directives we don't want to maintain.

    3. Do NOT touch INSTALLED_APPS. The CSP middleware is registered via MIDDLEWARE only.

    4. Do NOT add `django-csp` to pyproject.toml. Django 6's built-in is sufficient and already available.

    Verify locally before committing:
    ```bash
    DJANGO_SETTINGS_MODULE=config.settings.production \
    DJANGO_SECRET_KEY=dev-check-secret \
    DJANGO_ALLOWED_HOSTS=localhost \
    AWS_SES_FROM_EMAIL=noreply@example.com \
    uv run python manage.py check --deploy
    ```

    Expected output: "System check identified no issues" OR at most non-fatal warnings. The check MUST exit 0. If Django raises an ImportError for `django.middleware.csp.ContentSecurityPolicyMiddleware`, that means Django 6.0 is not installed — stop and escalate (this would contradict pyproject.toml).

    Also sanity-check the CSP header is actually emitted:
    ```bash
    DJANGO_SETTINGS_MODULE=config.settings.production \
    DJANGO_SECRET_KEY=dev-check-secret \
    DJANGO_ALLOWED_HOSTS=testserver \
    AWS_SES_FROM_EMAIL=noreply@example.com \
    uv run python -c "
    import django; django.setup()
    from django.test import Client
    c = Client()
    r = c.get('/login/')
    print('CSP header:', r.headers.get('Content-Security-Policy'))
    "
    ```

    Expected: CSP header contains `default-src 'self'; script-src 'self' 'unsafe-inline'` etc.
  </action>
  <verify>
    <automated>grep -c 'SECURE_BROWSER_XSS_FILTER = True' config/settings/production.py && grep -c 'X_FRAME_OPTIONS = "DENY"' config/settings/production.py && grep -c 'SECURE_CSP = {' config/settings/production.py && grep -c 'ContentSecurityPolicyMiddleware' config/settings/production.py && ! grep -c 'django-csp' pyproject.toml && DJANGO_SETTINGS_MODULE=config.settings.production DJANGO_SECRET_KEY=ci-check DJANGO_ALLOWED_HOSTS=localhost AWS_SES_FROM_EMAIL=noreply@example.com uv run python manage.py check --deploy 2>&1 | grep -E "System check identified (no issues|[0-9]+ issue)"</automated>
  </verify>
  <acceptance_criteria>
    - `config/settings/production.py` contains the exact string `SECURE_BROWSER_XSS_FILTER = True`
    - `config/settings/production.py` contains the exact string `X_FRAME_OPTIONS = "DENY"`
    - `config/settings/production.py` contains the exact string `SECURE_CSP = {`
    - `config/settings/production.py` contains the exact string `"default-src": ["'self'"]`
    - `config/settings/production.py` contains `"script-src"` with both `"'self'"` and `"'unsafe-inline'"` entries
    - `config/settings/production.py` contains the exact string `django.middleware.csp.ContentSecurityPolicyMiddleware`
    - `config/settings/production.py` does NOT contain duplicate SECURE_SSL_REDIRECT or SESSION_COOKIE_SECURE entries
    - `pyproject.toml` does NOT contain the string `django-csp` (we use Django 6 built-in, not the third-party package)
    - Running `DJANGO_SETTINGS_MODULE=config.settings.production DJANGO_SECRET_KEY=ci-check DJANGO_ALLOWED_HOSTS=localhost AWS_SES_FROM_EMAIL=noreply@example.com uv run python manage.py check --deploy` exits with code 0 (warnings allowed, errors not)
  </acceptance_criteria>
  <done>
    Production security headers complete per CLAUDE.md §19 (including minimal permissive CSP via Django 6 built-in middleware); deploy check passes with CI stub env vars; no new dependencies added.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .pre-commit-config.yaml (understand what `pre-commit run --all-files` will execute)
    - pyproject.toml (verify pytest-cov is in deps — it is, 5.0.0)
    - config/settings/test.py (CI steps 1-4 run with this module)
    - config/settings/production.py (CI step 5 uses this module; env vars from Task 1 verification)
    - CLAUDE.md §16 (CI pipeline requirements — exactly 5 steps in order)
  </read_first>
  <action>
    Create `.github/workflows/ci.yml`. The `.github/` and `.github/workflows/` directories do not yet exist — the write operation will create them.

    Write this EXACT content:

    ```yaml
    name: CI

    on:
      push:
        branches: [main]
      pull_request:
        branches: [main]

    jobs:
      ci:
        runs-on: ubuntu-latest

        services:
          postgres:
            image: postgres:16-alpine
            env:
              POSTGRES_DB: reviewmaster
              POSTGRES_USER: app
              POSTGRES_PASSWORD: app
            options: >-
              --health-cmd pg_isready
              --health-interval 5s
              --health-timeout 5s
              --health-retries 5
            ports:
              - 5432:5432
          redis:
            image: redis:7-alpine
            options: >-
              --health-cmd "redis-cli ping"
              --health-interval 5s
              --health-timeout 3s
              --health-retries 3
            ports:
              - 6379:6379

        env:
          DJANGO_SETTINGS_MODULE: config.settings.test
          DJANGO_SECRET_KEY: ci-insecure-secret
          DJANGO_ALLOWED_HOSTS: localhost
          DATABASE_URL: postgres://app:app@localhost:5432/reviewmaster
          REDIS_URL: redis://localhost:6379

        steps:
          - name: Checkout
            uses: actions/checkout@v4

          - name: Install uv
            uses: astral-sh/setup-uv@v5
            with:
              version: "0.4.29"

          - name: Set up Python
            run: uv python install 3.12

          - name: Install dependencies
            run: uv sync --frozen

          - name: pre-commit
            run: uv run pre-commit run --all-files

          - name: mypy
            run: uv run mypy .

          - name: pytest (coverage ≥85%)
            run: uv run pytest --cov=apps --cov-fail-under=85

          - name: Check migrations
            run: uv run python manage.py makemigrations --check --dry-run

          - name: Deploy check (production settings)
            env:
              DJANGO_SETTINGS_MODULE: config.settings.production
              DJANGO_SECRET_KEY: ci-deploy-check-secret
              DJANGO_ALLOWED_HOSTS: localhost
              AWS_SES_FROM_EMAIL: noreply@example.com
              DEFAULT_FROM_EMAIL: noreply@example.com
              DEFAULT_REPLY_TO: support@example.com
              DATABASE_URL: postgres://app:app@localhost:5432/reviewmaster
              REDIS_URL: redis://localhost:6379
            run: uv run python manage.py check --deploy
    ```

    Notes on decisions:
    - `actions/checkout@v4` and `astral-sh/setup-uv@v5` are the current stable majors as of April 2026.
    - `uv sync --frozen` uses the committed lockfile for reproducible installs.
    - The pre-commit step automatically runs ruff, djhtml, django-upgrade (target 5.1 — LOCKED, do not bump), gitleaks, bandit, mypy (but pre-commit's mypy is its own copy; our separate `mypy` step is the authoritative run against the full project).
    - `--cov=apps` matches `[tool.coverage.run] source = ["apps"]` (if pyproject doesn't set source, --cov=apps is still the right scope).
    - Deploy-check step overrides DJANGO_SETTINGS_MODULE at the step level to switch from test to production — steps 1-4 use the job-level test module.
    - Stub secret `ci-insecure-secret` is a recognized pattern — gitleaks in the pre-commit step will NOT flag it because it's obviously a test/CI value, not an AWS key or real secret.
    - No Node / Vite build step: `config.settings.test` uses `DJANGO_VITE dev_mode=True` which does not require a built manifest.

    Do NOT add a deploy job — CONTEXT.md explicitly scopes this phase to CI only. A deploy workflow is deferred.
    Do NOT add Codecov, Slack, or other optional integrations — keep the surface minimal and auditable.
    Do NOT bump django-upgrade target to 6.0 anywhere — the project decision (STATE.md) to stay on 5.1 remains locked.
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -c "cov-fail-under=85" .github/workflows/ci.yml && grep -c "pre-commit run --all-files" .github/workflows/ci.yml && grep -c "makemigrations --check --dry-run" .github/workflows/ci.yml && grep -c "check --deploy" .github/workflows/ci.yml && grep -c "config.settings.production" .github/workflows/ci.yml && python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && ! grep -c 'target-version.*6.0' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists
    - File parses as valid YAML (`python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0)
    - File contains trigger `push: branches: [main]` and `pull_request: branches: [main]`
    - File contains the exact string `pre-commit run --all-files`
    - File contains the exact string `uv run mypy .`
    - File contains the exact string `--cov=apps --cov-fail-under=85`
    - File contains the exact string `makemigrations --check --dry-run`
    - File contains the exact string `manage.py check --deploy`
    - File contains `DJANGO_SETTINGS_MODULE: config.settings.production` inside the deploy-check step env block
    - File contains service containers for `postgres:16-alpine` and `redis:7-alpine`
    - File does NOT contain `target-version: 6.0` or `target-version: "6.0"` (django-upgrade stays at 5.1)
    - File does NOT contain any `deploy:` job (CI only in this phase)
    - `DJANGO_SECRET_KEY` stub value is an obviously fake string (not a real-looking secret) — `ci-insecure-secret` is acceptable
  </acceptance_criteria>
  <done>
    CI workflow committed. Next PR / push-to-main triggers it end-to-end.
  </done>
</task>

</tasks>

<verification>
- [ ] `grep 'SECURE_BROWSER_XSS_FILTER\|X_FRAME_OPTIONS\|SECURE_CSP' config/settings/production.py` returns all three lines
- [ ] `grep 'ContentSecurityPolicyMiddleware' config/settings/production.py` returns the middleware registration line
- [ ] `grep 'django-csp' pyproject.toml` returns NO matches (we use Django 6 built-in)
- [ ] Local `manage.py check --deploy` with stub env vars exits 0
- [ ] `.github/workflows/ci.yml` is valid YAML and has all 5 CI steps in the documented order
- [ ] Full test suite still passes: `uv run pytest --cov=apps --cov-fail-under=85`
- [ ] `pre-commit run --all-files` locally: no failures (mypy strict, ruff clean, gitleaks clean)
</verification>

<success_criteria>
1. Three security headers added to production.py (XSS filter + X-Frame-Options DENY + SECURE_CSP)
2. `django.middleware.csp.ContentSecurityPolicyMiddleware` registered via MIDDLEWARE in production
3. No new dependencies added (Django 6 built-in CSP used; `django-csp` package NOT added)
4. `manage.py check --deploy` passes with CI stub env vars
5. `.github/workflows/ci.yml` exists and is valid YAML
6. CI has all 5 required steps in the correct order
7. Coverage gate enforced (`--cov-fail-under=85`)
8. django-upgrade target remains 5.1 (no inadvertent bump)
</success_criteria>

<output>
After completion, create `.planning/phases/05-profile-and-hardening/05-04-SUMMARY.md` with:
- Diff of production.py (new lines: XSS filter, X-Frame-Options, SECURE_CSP dict, MIDDLEWARE append of CSP middleware)
- Full ci.yml contents
- Result of local `manage.py check --deploy` run
- Result of local `pre-commit run --all-files`
- Commit SHAs
- Note that the next push to main will be the first live CI run — Phase 5 UAT expectation is that it comes back green
- Confirmation that `pyproject.toml` dependencies were NOT modified (Django 6 built-in CSP, no `django-csp` added)
</output>
