---
phase: 24-polarity-auto-reclassification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/settings/base.py
  - apps/reviews/models.py
  - apps/reviews/migrations/0012_orgcanonicaltag_polarity_reclassified_at.py
  - apps/reviews/tests/test_polarity_reclassify.py
autonomous: true
requirements: [POL-01, POL-02]
nyquist_compliant: true

must_haves:
  truths:
    - "Three operational knobs POLARITY_RECLASSIFY_THRESHOLD (0.15), POLARITY_RECLASSIFY_WINDOW_DAYS (30), POLARITY_RECLASSIFY_MIN_REVIEWS (10) are readable from Django settings (D-04)"
    - "OrgCanonicalTag carries a nullable polarity_reclassified_at column the weekly job can stamp (Claude's Discretion in D-05 research; helps Phase 25 D-07 display)"
    - "A reclassify_polarity_task route entry points the weekly task at the default queue (D-05; not ai-enrichment-*)"
    - "A failing test module exists asserting the reclassification behaviours (Wave 0 scaffold for POL-02 / POL-03)"
    - "POL-01 is re-confirmed: a regression test asserts a newly created OrgCanonicalTag carries a non-empty polarity_type (D-01; Phase 22 owns the prompt/parser — not touched)"
  artifacts:
    - path: "config/settings/base.py"
      provides: "POLARITY_RECLASSIFY_* settings + CELERY_TASK_ROUTES entry"
      contains: "POLARITY_RECLASSIFY_THRESHOLD"
    - path: "apps/reviews/migrations/0012_orgcanonicaltag_polarity_reclassified_at.py"
      provides: "Nullable polarity_reclassified_at DateTimeField on OrgCanonicalTag"
      contains: "polarity_reclassified_at"
    - path: "apps/reviews/tests/test_polarity_reclassify.py"
      provides: "Wave 0 failing test scaffold + POL-01 regression test"
      contains: "polarity_reclassified"
  key_links:
    - from: "apps/reviews/services/reclassify.py (Plan 02)"
      to: "settings.POLARITY_RECLASSIFY_THRESHOLD"
      via: "settings read"
      pattern: "POLARITY_RECLASSIFY_THRESHOLD"
    - from: "config/settings/base.py CELERY_TASK_ROUTES"
      to: "apps.reviews.tasks.reclassify_polarity_task"
      via: "queue route"
      pattern: "reclassify_polarity_task.*default"
---

<objective>
Lay the foundation for the weekly polarity auto-reclassification job: the three
configurable settings, the route entry, a nullable `polarity_reclassified_at`
denormalised column on `OrgCanonicalTag`, and a Wave-0 failing test module that
encodes every POL-02 / POL-03 behaviour plus a POL-01 re-confirmation regression
test.

This plan delivers config + schema + test scaffolding only. The service, task,
and Beat seed land in Plan 02 (Wave 2), which makes the tests pass.

Purpose: Establish the contracts (settings names + the new field) so the Plan-02
service has stable identifiers to read, and front-load the test expectations so
Plan 02 is implemented test-first (Nyquist).
Output: Modified `config/settings/base.py` and `apps/reviews/models.py`, new
schema migration `0012`, new test module `test_polarity_reclassify.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-polarity-auto-reclassification/24-CONTEXT.md
@.planning/phases/24-polarity-auto-reclassification/24-RESEARCH.md
@.planning/phases/24-polarity-auto-reclassification/24-PATTERNS.md
@.planning/phases/24-polarity-auto-reclassification/24-VALIDATION.md

<interfaces>
<!-- Verified from codebase — executor uses these directly, no exploration needed. -->

apps/reviews/models.py — OrgCanonicalTag.PolarityType (TextChoices):
  ALWAYS_POSITIVE = "always_positive"
  ALWAYS_NEGATIVE = "always_negative"
  MIXED           = "mixed"
OrgCanonicalTag fields: organisation (FK, related_name="canonical_tags"),
  label (CharField max_length=100), polarity_type (CharField max_length=20,
  choices=PolarityType.choices), review_count (PositiveIntegerField default=0),
  inherits TimeStampedModel (created_at/updated_at). Latest migration: 0011.

apps/reviews/models.py — ReviewTag.Polarity (TextChoices): positive/neutral/negative;
  fields: review (FK), label, polarity (CharField max_length=10),
  canonical_tag (nullable FK).
apps/reviews/models.py — Review: review_create_time (DateTimeField db_index=True),
  deleted_at (DateTimeField null=True db_index=True); composite index
  (organisation, review_create_time).

config/settings/base.py existing knob block (lines ~194-199):
  SEED_PHASE_SIZE = env.int("SEED_PHASE_SIZE", default=50)
  OPENAI_GLOBAL_RATE_LIMIT = env.int("OPENAI_GLOBAL_RATE_LIMIT", default=500)
config/settings/base.py CELERY_TASK_ROUTES (lines ~120-130) existing tail:
  "apps.reviews.tasks.finalize_canonical_tags_task": {"queue": "tag-merge"},
  "apps.common.tasks.publish_celery_queue_depths_task": {"queue": "default"},

apps/reviews/tests/factories.py — OrgCanonicalTagFactory(organisation, label,
  polarity_type default MIXED, review_count default 0); ReviewFactory;
  ReviewTagFactory(review, label, polarity default "positive", canonical_tag None);
  AuditLogFactory(organisation, actor=None, entity_type, entity_id, action,
  before_data, after_data). OrganisationFactory in apps/organisations/tests/factories.py.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add POLARITY_RECLASSIFY_* settings + reclassify route</name>
  <files>config/settings/base.py</files>
  <read_first>
    - config/settings/base.py lines ~120-130 (CELERY_TASK_ROUTES dict) and ~187-199 (SEED_PHASE_SIZE / OPENAI_GLOBAL_RATE_LIMIT operational-knob block) — the analog block per 24-PATTERNS.md "config/settings/base.py" section.
  </read_first>
  <action>
    Append a "Phase 24 — polarity auto-reclassification (POL-02)" block immediately after the existing OPENAI_GLOBAL_RATE_LIMIT line, defining three settings per D-04: POLARITY_RECLASSIFY_THRESHOLD via env.float default 0.15, POLARITY_RECLASSIFY_WINDOW_DAYS via env.int default 30, POLARITY_RECLASSIFY_MIN_REVIEWS via env.int default 10. Mirror the inline-comment style of the SEED_PHASE_SIZE block. If env.float is unavailable in the installed django-environ (RESEARCH A2), fall back to float(env("POLARITY_RECLASSIFY_THRESHOLD", default="0.15")). Then add one entry to CELERY_TASK_ROUTES after the finalize_canonical_tags_task line routing "apps.reviews.tasks.reclassify_polarity_task" to the default queue per D-05 (weekly, low-frequency, low-concurrency — never ai-enrichment-*, google-sync, or tag-merge).
  </action>
  <verify>
    <automated>DJANGO_SETTINGS_MODULE=config.settings.test python -c "import django; django.setup(); from django.conf import settings; assert settings.POLARITY_RECLASSIFY_THRESHOLD == 0.15; assert settings.POLARITY_RECLASSIFY_WINDOW_DAYS == 30; assert settings.POLARITY_RECLASSIFY_MIN_REVIEWS == 10; assert settings.CELERY_TASK_ROUTES['apps.reviews.tasks.reclassify_polarity_task']['queue'] == 'default'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - settings.POLARITY_RECLASSIFY_THRESHOLD == 0.15 (float), WINDOW_DAYS == 30, MIN_REVIEWS == 10.
    - CELERY_TASK_ROUTES routes reclassify_polarity_task to the "default" queue.
    - No existing route entries removed or reordered destructively.
  </acceptance_criteria>
  <done>The three POLARITY_RECLASSIFY_* settings resolve to their documented defaults and the reclassify task routes to the default queue.</done>
</task>

<task type="auto">
  <name>Task 2: Add nullable polarity_reclassified_at field + migration 0012</name>
  <files>apps/reviews/models.py, apps/reviews/migrations/0012_orgcanonicaltag_polarity_reclassified_at.py</files>
  <read_first>
    - apps/reviews/models.py lines 128-170 (OrgCanonicalTag definition — PolarityType, polarity_type, review_count, Meta indexes) — where the new field is added.
    - apps/reviews/migrations/0011_orgcanonicaltag_reviewtag_canonical_tag.py — the latest migration and the AddField analog per 24-PATTERNS.md "0012_orgcanonicaltag_polarity_reclassified_at.py" section.
  </read_first>
  <action>
    Add a nullable denormalised column polarity_reclassified_at = models.DateTimeField(null=True, blank=True) to OrgCanonicalTag (research "Optional: polarity_reclassified_at Timestamp" — recommended ADD; gives the Phase 25 D-07 tag list a cheap last-reclassified value without an AuditLog JOIN; AuditLog remains authoritative). Place it near polarity_type with a one-line comment noting it is stamped by the weekly Phase 24 job and is a denormalised cache. Generate the migration with manage.py makemigrations reviews and rename the generated file to 0012_orgcanonicaltag_polarity_reclassified_at.py; it must be a single AddField on orgcanonicaltag depending only on reviews 0011, with no data backfill (null means "never auto-reclassified"). Do NOT alter review_count or any other field — D-03 (Phase 22) keeps review_count untouched in this phase.
  </action>
  <verify>
    <automated>DJANGO_SETTINGS_MODULE=config.settings.test python manage.py makemigrations --check --dry-run reviews && DJANGO_SETTINGS_MODULE=config.settings.test python -c "import django; django.setup(); from apps.reviews.models import OrgCanonicalTag; f=OrgCanonicalTag._meta.get_field('polarity_reclassified_at'); assert f.null and f.blank; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - OrgCanonicalTag.polarity_reclassified_at exists, is nullable and blank-able.
    - makemigrations --check --dry-run reports no missing migrations (the 0012 file captures the field).
    - Migration 0012 is a single AddField depending on reviews 0011; no other field changed; review_count untouched.
  </acceptance_criteria>
  <done>The model has a nullable polarity_reclassified_at column and migration 0012 applies it cleanly with no other schema drift.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-0 failing test module + POL-01 re-confirmation test</name>
  <files>apps/reviews/tests/test_polarity_reclassify.py</files>
  <read_first>
    - apps/reviews/tests/test_enrichment_service.py (query-count test shape with CaptureQueriesContext) and apps/reviews/tests/test_finalise.py (bare-function + @pytest.mark.django_db service-test style) — analogs per 24-PATTERNS.md "test_reclassify_service.py" section.
    - apps/reviews/tests/factories.py (OrgCanonicalTagFactory, ReviewFactory, ReviewTagFactory, AuditLogFactory) and apps/organisations/tests/factories.py (OrganisationFactory).
    - 24-VALIDATION.md Per-Task Verification Map (the 12 coverage areas) and 24-RESEARCH.md "Phase Requirements -> Test Map" (test names).
  </read_first>
  <action>
    Create the new module apps/reviews/tests/test_polarity_reclassify.py importing run_polarity_reclassification from apps.reviews.services.reclassify (does not yet exist — import is expected to fail at collection until Plan 02; that failing state is the Wave-0 RED). Write one @pytest.mark.django_db test per 24-VALIDATION.md coverage area, each constructing data via factories and asserting against settings defaults (threshold 0.15, window 30 days, min 10): (a) flip always_positive -> mixed when opposite negative ratio > 0.15 over min-sample; (b) no flip below MIN_REVIEWS (e.g. 4 reviews, 1 opposite); (c) boundary — exactly 0.15 ratio does NOT flip (strict > per D-02); (d) already-mixed tags skipped, no AuditLog row written (one-way sticky, D-01); (e) neutral reviews count in denominator only, never numerator (D-02); (f) soft-deleted reviews (Review.deleted_at set) excluded from denominator (D-02); (g) window measured by Review.review_create_time — a review older than 30 days excluded (D-03); (h) multi-tenant isolation — an org-A flip never reads or writes org-B tags (§9/§22); (i) idempotency — a second run in the same week flips nothing more and writes no extra AuditLog rows (D-05); (j) no-N+1 query count is fixed across N=2 vs N=5 always_positive tags via CaptureQueriesContext, asserting equality and a small ceiling; (k) POL-03 AuditLog row written per flip with organisation=tag.org, actor None, entity_type "canonical_tag", action "polarity_reclassified", before_data {"polarity_type": old}, after_data containing polarity_type "mixed", opposite_ratio, window_days, reviews_in_window (D-06). Plus a separate POL-01 re-confirmation test asserting a freshly created OrgCanonicalTag (always_positive) has a non-empty polarity_type in PolarityType.values — framed as "polarity assigned at creation, not re-implemented here" (D-01); do NOT import or touch apps/integrations/openai. Use only assertions + factories; no GPT, Redis, or WebSocket mocking is needed in this phase.
  </action>
  <verify>
    <automated>DJANGO_SETTINGS_MODULE=config.settings.test python -m pytest apps/reviews/tests/test_polarity_reclassify.py --collect-only -q 2>&1 | grep -q "polarity" && echo "tests-defined"; DJANGO_SETTINGS_MODULE=config.settings.test python -m pytest apps/reviews/tests/test_polarity_reclassify.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - The module defines at least 12 distinct test functions covering every 24-VALIDATION.md area plus the POL-01 re-confirmation test.
    - Running the module currently FAILS or errors at collection/run (RED) because apps.reviews.services.reclassify does not exist yet — this is the expected Wave-0 state.
    - No import of apps/integrations/openai anywhere in the file; no GPT/Redis/WebSocket mocks.
    - Test bodies use factory-boy factories and assert against documented settings defaults (0.15 / 30 / 10).
  </acceptance_criteria>
  <done>test_polarity_reclassify.py exists with the full failing behaviour suite + POL-01 re-confirmation test, ready to turn GREEN in Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings/env → process | Operational knobs read from env; typed casts (env.float/env.int) constrain input |
| (no user→API boundary) | This plan adds no endpoint, no request handling, no user input path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | POLARITY_RECLASSIFY_* env values | accept | Typed env.float/env.int casts reject non-numeric input at boot; values are operator-controlled config, not user input |
| T-24-02 | Information Disclosure | polarity_reclassified_at column | accept | Nullable timestamp carries no PII; org-scoped via the parent OrgCanonicalTag.organisation FK |
| T-24-03 | Denial of Service | schema migration 0012 | mitigate | Single nullable AddField, no data backfill, no table rewrite of existing rows — safe online add |
| T-24-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced this phase (RESEARCH Package Legitimacy Audit: N/A); nothing to install |
</threat_model>

<verification>
- `DJANGO_SETTINGS_MODULE=config.settings.test python manage.py makemigrations --check --dry-run reviews` reports no missing migrations.
- Settings assertion one-liner (Task 1 verify) passes.
- `test_polarity_reclassify.py` collects and runs RED (service not yet implemented) — confirming the Wave-0 scaffold is in place.
</verification>

<success_criteria>
- Three POLARITY_RECLASSIFY_* settings resolve to 0.15 / 30 / 10.
- reclassify_polarity_task routes to the default queue.
- OrgCanonicalTag.polarity_reclassified_at exists (nullable) with migration 0012.
- The behaviour + POL-01 re-confirmation test suite exists and is RED, pending Plan 02.
</success_criteria>

<output>
Create `.planning/phases/24-polarity-auto-reclassification/24-01-SUMMARY.md` when done
</output>
