---
phase: 22-canonical-tag-foundation-mapping-pipeline
plan: 05
type: execute
wave: 3
depends_on: ["22-01", "22-03", "22-04"]
files_modified:
  - apps/reviews/services/enrichment.py
  - apps/reviews/tests/test_enrichment_service.py
autonomous: true
requirements: [CTAG-06, CTAG-07]
must_haves:
  truths:
    - "D-01: a newly proposed canonical tag is created here with GPT's polarity_type. D-03: review_count is NOT incremented in this hot path (derive-on-read)."
    - "After enrichment, each ReviewTag row has canonical_tag populated with the matched/created OrgCanonicalTag (resolving org via review.organisation_id)"
    - "A new canonical label proposed by GPT creates exactly one OrgCanonicalTag row with its polarity_type; an existing label is reused"
    - "Canonical lookup/insert happens inside the existing _persist_success transaction.atomic() — a failure rolls back the whole enrichment write"
    - "Re-enriching the same review creates no duplicate OrgCanonicalTag rows and does not miscount (review_count untouched in the hot path)"
    - "Exactly one AiUsageLog row is written per enrichment call"
    - "The canonical lookup/insert adds a fixed, bounded number of queries regardless of tag count (no N+1)"
  artifacts:
    - path: "apps/reviews/services/enrichment.py"
      provides: "Canonical FK resolution folded into _persist_success atomic block"
      contains: "OrgCanonicalTag"
    - path: "apps/reviews/tests/test_enrichment_service.py"
      provides: "FK populate, atomic rollback, idempotency, query-count, one-AiUsageLog tests"
  key_links:
    - from: "apps/reviews/services/enrichment.py _persist_success"
      to: "apps/reviews/models.py OrgCanonicalTag"
      via: "filter(organisation_id, label__in) + bulk_create(ignore_conflicts=True) + re-SELECT"
      pattern: "OrgCanonicalTag.objects.filter"
    - from: "apps/reviews/services/enrichment.py ReviewTag.bulk_create"
      to: "apps/reviews/models.py ReviewTag.canonical_tag"
      via: "canonical_tag=fk in the bulk_create list comp"
      pattern: "canonical_tag="
---

<objective>
Fold canonical lookup/insert + FK population into the existing `_persist_success` `transaction.atomic()` block, so every enriched review's `ReviewTag` rows get their `canonical_tag` FK set and new canonical labels are created — all in the single existing write, with exactly one `AiUsageLog` row.

Purpose: CTAG-06 (post-enrichment lookup/insert inside the existing atomic block; matched → FK, new → insert), CTAG-07 (still exactly one AiUsageLog row, no extra OpenAI call). D-03: `review_count` is NOT touched here (derive-on-read). Landmine (Pitfall 4): use a batch SELECT + `bulk_create(ignore_conflicts=True)` + re-SELECT — NOT per-tag `get_or_create`, which would poison the outer transaction on the (org,label) race. Landmine (Pitfall 3): batch-resolve to keep the hot path query count fixed regardless of tag count.
Output: extended `_persist_success`, enrichment service tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-CONTEXT.md
@.planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-RESEARCH.md
@.planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-PATTERNS.md

<interfaces>
<!-- _persist_success existing atomic block (enrichment.py:91-128) — the seam to extend -->
with transaction.atomic():
    Review.objects.filter(pk=review.pk).update(... enrichment_status=SUCCESS ...)
    ReviewTag.objects.filter(review_id=review.pk).delete()
    ReviewTag.objects.bulk_create([
        ReviewTag(review_id=review.pk, label=tag.label.title(), polarity=tag.polarity)
        for tag in result.tags
    ])
    AiUsageLog.objects.create(...)   # EXACTLY ONE row — keep exactly one (CTAG-07)
# AFTER commit: _emit_enrichment_progress(review=review); _schedule_action_item_promotion(review=review)

<!-- result.tags items now carry: .label, .polarity, .canonical (normalized Title Case <=3 words, from 22-03), .polarity_type (literal|None) -->
<!-- review.organisation_id already loaded via select_related("shop__organisation") in enrich_review:428 -->
<!-- OrgCanonicalTag (22-01): organisation FK, label, polarity_type, review_count (cache only, DO NOT touch) -->
<!-- _persist_success_no_comment (enrichment.py:210-234) has NO tags → no canonical work needed -->
<!-- Test seam to patch: apps.reviews.services.enrichment.call_openai_enrichment (NOT the SDK) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve canonical FKs inside the _persist_success atomic block</name>
  <read_first>
    - apps/reviews/services/enrichment.py (the file being modified — _persist_success 74-139, the atomic block 91-128; _persist_success_no_comment 210-234; enrich_review select_related at 428)
    - apps/reviews/models.py (OrgCanonicalTag + ReviewTag.canonical_tag from 22-01)
    - .planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-RESEARCH.md (Pattern 5; Pitfall 3 batch-resolve; Pitfall 4 ignore_conflicts vs transaction poisoning; Pitfall 5 review_count derive-on-read)
    - .planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-PATTERNS.md (enrichment.py Pattern Assignment)
  </read_first>
  <behavior>
    - Test (CTAG-06 / CTAG-02): after enrich_review, each ReviewTag has canonical_tag set to the OrgCanonicalTag whose label == the tag's normalized canonical
    - Test (CTAG-01 via this path): a tag with a never-seen canonical label creates exactly one OrgCanonicalTag (org-scoped) carrying the GPT polarity_type; an existing label is reused (no new row)
    - Test (CTAG-06): if the ReviewTag bulk_create raises, the whole transaction rolls back (Review stays not-SUCCESS, no OrgCanonicalTag committed for this review's new labels) — atomic
    - Test (CTAG-07): exactly one AiUsageLog row is written per successful enrich_review
    - Test (idempotency / D-03): calling enrich_review twice for the same review leaves OrgCanonicalTag count stable for the org and review_count unchanged (0) — no double count
    - Test (no-N+1): query count for enrich_review is bounded by a fixed ceiling independent of tag count
  </behavior>
  <action>
    In apps/reviews/services/enrichment.py extend the existing `_persist_success` `transaction.atomic()` block (lines 91-128). After `ReviewTag.objects.filter(review_id=review.pk).delete()` and before/within the `ReviewTag.objects.bulk_create(...)`: set `org_id = review.organisation_id` (already loaded — no extra query). Collect the distinct normalized canonical labels from `result.tags` (each `tag.canonical` is already normalized by the 22-03 validator). Resolve FKs batch-style to avoid N+1 AND avoid poisoning the outer atomic block (Pitfall 3 + 4): (1) one SELECT `existing = OrgCanonicalTag.objects.filter(organisation_id=org_id, label__in=labels)` mapped label→instance; (2) for labels not in `existing`, build `OrgCanonicalTag(organisation_id=org_id, label=label, polarity_type=<the polarity_type from the tag that proposed it, falling back to MIXED when None>)` and `OrgCanonicalTag.objects.bulk_create(missing, ignore_conflicts=True)` — `ignore_conflicts=True` is race-safe inside the outer transaction (does NOT abort it, unlike get_or_create); (3) re-SELECT `OrgCanonicalTag.objects.filter(organisation_id=org_id, label__in=labels)` to get FKs for all labels (covers rows another worker inserted concurrently). Build a label→OrgCanonicalTag map. Then set `canonical_tag=canonical_map[tag.canonical]` on each `ReviewTag` in the existing bulk_create list comp (keep `label=tag.label.title()`, `polarity=tag.polarity`). Do NOT touch `review_count` anywhere (D-03 — derive-on-read; touching it would double-count on re-enrichment). Keep `AiUsageLog.objects.create(...)` exactly once (CTAG-07). Keep `_emit_enrichment_progress` and `_schedule_action_item_promotion` AFTER the commit (unchanged). Do NOT add canonical work to `_persist_success_no_comment` (no tags). Add no second OpenAI call.

    Derived per-query budget for the hot path (justifies the `<= 12` ceiling in Task 2 — a FIXED count independent of tag count): (1) `select_for_update` row lock on Review in the enrich_review wrapper; (2) the vocab/status SELECT(s) already present pre-canonical; (3) Review `.update(... enrichment_status=SUCCESS ...)`; (4) `ReviewTag` delete; (5) canonical existing-label SELECT `filter(organisation_id, label__in)`; (6) canonical `bulk_create(ignore_conflicts=True)`; (7) canonical re-SELECT `filter(organisation_id, label__in)`; (8) `ReviewTag.bulk_create`; (9) `AiUsageLog.objects.create`. That is 9 canonical-path-relevant queries plus existing pre-canonical reads → the canonical fold-in adds exactly 3 fixed queries (one SELECT, one bulk_create, one re-SELECT) regardless of how many tags or distinct canonical labels appear. The ceiling is set at 12 to leave a small margin for the pre-existing select_for_update + vocab reads, NOT to absorb per-tag growth; if the executor confirms the actual count is lower at GREEN, tighten the ceiling in Task 2 to the observed number.
  </action>
  <verify>
    <automated>pytest apps/reviews/tests/test_enrichment_service.py -k "canonical_fk or atomic or usage_log or idempot or query_count" -q</automated>
  </verify>
  <acceptance_criteria>
    - apps/reviews/services/enrichment.py _persist_success uses `OrgCanonicalTag.objects.filter(organisation_id=..., label__in=...)` and `bulk_create(..., ignore_conflicts=True)` (not per-tag get_or_create)
    - The ReviewTag bulk_create list comp sets `canonical_tag=` from the resolved map
    - `grep -n "review_count" apps/reviews/services/enrichment.py` returns no match (review_count never written in the hot path)
    - Exactly one `AiUsageLog.objects.create` call remains in _persist_success
    - `pytest apps/reviews/tests/test_enrichment_service.py -k "canonical_fk or atomic or usage_log or idempot or query_count"` exits 0
  </acceptance_criteria>
  <done>Canonical FKs resolved batch-style inside the existing atomic block; new labels inserted race-safely with polarity_type; review_count untouched; one AiUsageLog row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add enrichment-service tests (FK populate, atomic rollback, idempotency, query-count, one-AiUsageLog)</name>
  <read_first>
    - apps/reviews/tests/test_enrichment_service.py (existing tests — the one-AiUsageLog-row assertion and idempotency test to extend; the call_openai_enrichment patch pattern)
    - .planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-RESEARCH.md (Code Examples — _build_result_with_canonical, query-count ceiling test, idempotency test shapes)
    - .planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-VALIDATION.md (CTAG-02/06/07 + query-count + idempotency test rows)
  </read_first>
  <behavior>
    - See Task 1 behavior block — these tests implement those assertions.
  </behavior>
  <action>
    In apps/reviews/tests/test_enrichment_service.py add tests, patching the service seam `apps.reviews.services.enrichment.call_openai_enrichment` to return an `EnrichmentResult` with two tags (one with a concrete polarity_type, one with polarity_type=None mapping to an existing canonical) plus a `_usage()` dict — NOT the SDK. Pre-create an OrgCanonicalTag via OrgCanonicalTagFactory for the existing-label match case. Add:

    (a) `-k canonical_fk` — assert each resulting ReviewTag.canonical_tag resolves to an OrgCanonicalTag with the matching normalized label and the new one carries the proposed polarity_type.

    (b) `-k atomic` — inject the failure deterministically: `patch("apps.reviews.services.enrichment.ReviewTag.objects.bulk_create", side_effect=IntegrityError(...))` (the ReviewTag insert that runs LAST inside the block, AFTER the canonical SELECT/bulk_create/re-SELECT — so the rollback must unwind the canonical inserts too). Wrap the `enrich_review(...)` call in `pytest.raises(IntegrityError)` (or the wrapper's re-raise type — confirm what `_persist_success` lets propagate; do NOT swallow it). Then assert AFTER the atomic block has exited, using FRESH querysets (re-fetch from the DB — do NOT reuse in-memory objects, which would not reflect the rollback): `Review.objects.get(pk=review.pk).enrichment_status != Review.EnrichmentStatus.SUCCESS` AND `OrgCanonicalTag.objects.filter(organisation_id=<org>, label=<the new canonical label>).count() == 0`. Do NOT bypass or re-open `_persist_success`'s atomic block — patch only the inner `bulk_create` so the real `transaction.atomic()` semantics under pytest-django (each test wrapped in its own transaction) are what's exercised; the assertion runs after the with-block raises and unwinds.

    (c) extend the existing `-k usage_log` assertion to confirm exactly one AiUsageLog row after canonical changes.

    (d) `-k idempot` — enrich twice, assert OrgCanonicalTag count for the org is stable and review_count stays 0.

    (e) `-k query_count` — wrap enrich_review in `CaptureQueriesContext` (patch `_emit_enrichment_progress` to no-op) and assert `len(ctx.captured_queries) <= 12` (fixed ceiling, NOT proportional to tag count, per §6.9 — see the derived per-query breakdown in Task 1's action). If the observed count at GREEN is below 12, tighten the literal to the observed value so the ceiling is justified, not padded.
  </action>
  <verify>
    <automated>pytest apps/reviews/tests/test_enrichment_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    - apps/reviews/tests/test_enrichment_service.py contains tests selectable by `-k canonical_fk`, `-k atomic`, `-k idempot`, and `-k query_count`
    - The `-k atomic` test injects the failure via `patch(... ReviewTag.objects.bulk_create, side_effect=IntegrityError)` (or the canonical bulk_create) and asserts AFTER the atomic block exits using fresh querysets: `Review....enrichment_status != SUCCESS` AND `OrgCanonicalTag.objects.filter(organisation_id=<org>, label=<new_label>).count() == 0`
    - The query-count test uses CaptureQueriesContext with a fixed integer ceiling (`<= 12`, or tighter if the observed count is lower) justified by the per-query breakdown in Task 1
    - `pytest apps/reviews/tests/test_enrichment_service.py` exits 0
  </acceptance_criteria>
  <done>FK-populate, atomic-rollback (deterministic injection + post-block fresh-queryset assertions), idempotency, query-count ceiling, and one-AiUsageLog tests pass against mocked OpenAI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GPT canonical output → DB write | Canonical labels (validated in 22-03) persisted org-scoped inside a transaction |
| concurrent workers → (org,label) row | Two workers proposing the same new label must converge without aborting either transaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-12 | Information Disclosure | Canonical FK resolution | mitigate | Lookups filter strictly by `organisation_id` from `review.organisation_id`; OrgCanonicalTag rows created with the review's org — no cross-tenant write or read. |
| T-22-13 | Tampering | (org,label) insert race | mitigate | `bulk_create(ignore_conflicts=True)` + re-SELECT converges concurrent inserts without aborting the outer atomic transaction (Pitfall 4); the DB unique constraint is authoritative. |
| T-22-14 | Denial of Service (cost) | Duplicate OpenAI calls | mitigate | Canonicalisation is pure DB inside the existing block; exactly one AiUsageLog row and zero extra OpenAI calls (CTAG-07) — asserted by test. |
| T-22-15 | Denial of Service (perf) | N+1 in hot path | mitigate | Batch SELECT + bulk_create + re-SELECT keeps query count fixed regardless of tag count; CaptureQueriesContext ceiling test enforces it (§6.9). |
| T-22-SC | Tampering | npm/pip installs | mitigate | No package installs — no new dependencies. |
</threat_model>

<verification>
- `pytest apps/reviews/tests/test_enrichment_service.py` passes (FK populate, atomic rollback, idempotency, query-count, one-AiUsageLog)
- `pytest apps/reviews apps/integrations/openai` green (wave merge)
- `mypy apps/reviews/services/enrichment.py` clean
- `grep -n review_count apps/reviews/services/enrichment.py` returns nothing (D-03 invariant)
</verification>

<success_criteria>
- Each ReviewTag gets canonical_tag populated post-enrichment; new canonical labels created with polarity_type, existing reused — all inside the existing transaction.atomic() (CTAG-06)
- Exactly one AiUsageLog row per enrichment; no extra OpenAI call (CTAG-07)
- review_count untouched in the hot path (D-03); re-enrichment creates no duplicate canonical rows; query count bounded (no N+1)
</success_criteria>

<output>
Create `.planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-05-SUMMARY.md` when done
</output>
