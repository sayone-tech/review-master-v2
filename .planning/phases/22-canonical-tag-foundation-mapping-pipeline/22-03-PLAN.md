---
phase: 22-canonical-tag-foundation-mapping-pipeline
plan: 03
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - apps/integrations/openai/parser.py
  - apps/integrations/openai/tests/fixtures/enrichment_success.json
  - apps/integrations/openai/tests/test_parser.py
  - apps/integrations/openai/tests/test_client.py
autonomous: true
requirements: [CTAG-04, CTAG-05]
must_haves:
  truths:
    - "D-01: Phase 22 captures GPT's polarity_type for new canonical tags (the parser/schema change lands here). D-05: canonical label format (Title Case, <=3 words) is enforced server-side via a mutating field_validator, never a JSON-schema pattern/constr."
    - "Tag parses a 'canonical' string and a 'polarity_type' that is one of the three literals OR null"
    - "An existing-tag mapping (polarity_type omitted/null) parses successfully — the field is a nullable union with NO default"
    - "The canonical label is normalized server-side to Title Case, <=3 words"
    - "The existing enrichment_success.json fixture parses under the extended schema (consumed by BOTH test_parser.py and test_client.py)"
  artifacts:
    - path: "apps/integrations/openai/parser.py"
      provides: "Tag.canonical + Tag.polarity_type + normalize_canonical validator"
      contains: "canonical"
    - path: "apps/integrations/openai/tests/fixtures/enrichment_success.json"
      provides: "Fixture tags carrying canonical + polarity_type"
    - path: "apps/integrations/openai/tests/test_parser.py"
      provides: "canonical normalization + nullable polarity_type parse cases"
  key_links:
    - from: "apps/integrations/openai/parser.py Tag"
      to: "apps/reviews/models.py OrgCanonicalTag.PolarityType"
      via: "matching literal values always_positive/always_negative/mixed"
      pattern: "polarity_type"
---

<objective>
Extend the structured-output `Tag` schema so GPT can return a `canonical` label and a `polarity_type` for newly proposed tags, with server-side Title-Case normalization. This is the contract the prompt (22-04) and the persistence fold-in (22-05) build against.

Purpose: CTAG-04 (GPT maps each tag to a canonical label or proposes a new one with polarity_type, in one call), D-01 (capture polarity_type at creation), D-05 (enforce label format server-side), CTAG-05 (canonical labels in English — enforced jointly with the prompt). Landmine: Structured Outputs strict mode requires a nullable union with NO default and NO schema-level pattern/constr (Pitfall 1 & 7).
Output: extended `Tag` model, updated fixture, parser tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-CONTEXT.md
@.planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-RESEARCH.md
@.planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-PATTERNS.md

<interfaces>
<!-- Current parser.py:16-37 — the contract to extend -->
class Tag(BaseModel):
    label: str
    polarity: Literal["positive", "negative", "neutral"]

class EnrichmentResult(BaseModel):
    sentiment: Literal["positive", "neutral", "negative"]
    tags: list[Tag]
    action_items: list[ActionItem]
    @field_validator("tags")
    @classmethod
    def max_five_tags(cls, v: list[Tag]) -> list[Tag]:
        return v[:5]   # <-- the mutating-validator idiom to mirror for normalize_canonical

<!-- OrgCanonicalTag.PolarityType literal values (from 22-01): always_positive | always_negative | mixed -->

<!-- enrichment_success.json has TWO consumers — both validate the SAME fixture via EnrichmentResult: -->
<!--   - test_parser.py:test_parses_known_good_fixture (loads fixture, validates) -->
<!--   - test_client.py:_load_fixture (line 54-56: EnrichmentResult.model_validate(payload), used by ~6 tests) -->
<!-- Making Tag.canonical a required field WILL break test_client.py too unless the fixture carries the new keys. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Tag with canonical + polarity_type and a normalize_canonical validator</name>
  <read_first>
    - apps/integrations/openai/parser.py (the file being modified — Tag at 16-18, max_five_tags validator at 33-37 is the analog)
    - .planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-RESEARCH.md (Pitfall 1 nullable-union rule; Pitfall 7 mutating-validator-not-constraint; Pattern 3)
    - .planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-PATTERNS.md (parser.py Pattern Assignment)
  </read_first>
  <behavior>
    - Test: Tag(label="great coffee", polarity="positive", canonical="food quality", polarity_type="always_positive") yields canonical=="Food Quality" (Title Case)
    - Test: a canonical with >3 words ("very good food quality here") is truncated to <=3 words after normalization
    - Test: Tag(..., polarity_type=None) parses (existing-tag mapping case) — the field is a nullable union
    - Test: EnrichmentResult.model_validate of a payload with two tags (one polarity_type set, one null) parses without error
  </behavior>
  <action>
    In apps/integrations/openai/parser.py extend `class Tag` with two new fields: `canonical: str` (plain required string) and `polarity_type: Literal["always_positive", "always_negative", "mixed"] | None` written as a nullable union with NO `= None` default (Structured Outputs strict mode requires every field present + nullable-union for optional — Pitfall 1). The three literal values MUST match OrgCanonicalTag.PolarityType exactly. Add a `@field_validator("canonical")` classmethod `normalize_canonical(cls, v: str) -> str` mirroring the `max_five_tags` mutating-validator style: strip, split, keep at most the first 3 words, Title-Case each word, rejoin; fall back to `v.strip().title()` if empty. Do NOT use a Pydantic `pattern`/`constr`/`Field(...)` constraint to enforce the format — the SDK strips/rejects schema-level constraints (Pitfall 7). Keep `from typing import Literal` and `field_validator` imports.
  </action>
  <verify>
    <automated>pytest apps/integrations/openai/tests/test_parser.py -k "canonical or polarity_type" -q</automated>
  </verify>
  <acceptance_criteria>
    - apps/integrations/openai/parser.py Tag has fields `canonical: str` and `polarity_type: Literal["always_positive", "always_negative", "mixed"] | None`
    - There is NO `= None` default on polarity_type (`grep -n "polarity_type" apps/integrations/openai/parser.py` shows no `= None`)
    - apps/integrations/openai/parser.py contains `def normalize_canonical` decorated with `@field_validator("canonical")`
    - No `pattern=` or `constr` appears on the canonical field
  </acceptance_criteria>
  <done>Tag parses canonical + nullable polarity_type; canonical normalized to Title Case <=3 words via a mutating field_validator.</done>
</task>

<task type="auto">
  <name>Task 2: Update the enrichment_success fixture and extend parser tests (keep both fixture consumers green)</name>
  <read_first>
    - apps/integrations/openai/tests/fixtures/enrichment_success.json (the fixture that test_parses_known_good_fixture loads — will break without the new fields)
    - apps/integrations/openai/tests/test_parser.py (test_parses_known_good_fixture ~line 20, test_truncates_tags_over_five ~lines 30-37 — analogs)
    - apps/integrations/openai/tests/test_client.py (SECOND consumer of the same fixture — `_load_fixture` at lines 54-56 does `EnrichmentResult.model_validate(payload)`, exercised by test_success_returns_parsed_and_usage_data, test_cached_tokens_default_zero_when_details_missing, test_langsmith_best_effort, test_trace_id_captured_from_run_tree; adding the required canonical field breaks ALL of these unless the fixture carries the new keys)
    - .planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-RESEARCH.md (Code Examples — mocked response shape with canonical + polarity_type)
  </read_first>
  <action>
    Update apps/integrations/openai/tests/fixtures/enrichment_success.json so every tag object carries a `canonical` string and a `polarity_type` (use a concrete literal for at least one tag and `null` for at least one tag to exercise both branches). This single fixture is validated by BOTH test_parser.py and test_client.py, so the fixture update is what keeps test_client.py green — do NOT edit test_client.py's assertions, only confirm it still passes against the updated fixture. In apps/integrations/openai/tests/test_parser.py add cases (named so `-k canonical` and `-k polarity_type` select them): canonical normalization (lowercase/many-words input → Title Case ≤3 words), a nullable-polarity_type parse case (omit/null → parses, value is None), and a both-set/one-null EnrichmentResult parse case. Ensure the existing `test_parses_known_good_fixture` (test_parser.py) AND the fixture-loading tests in test_client.py still pass against the updated fixture.
  </action>
  <verify>
    <automated>pytest apps/integrations/openai/tests/test_parser.py apps/integrations/openai/tests/test_client.py -q</automated>
  </verify>
  <acceptance_criteria>
    - apps/integrations/openai/tests/fixtures/enrichment_success.json tags each contain a `canonical` key and a `polarity_type` key
    - `pytest apps/integrations/openai/tests/test_parser.py apps/integrations/openai/tests/test_client.py` exits 0 (both fixture consumers — parser tests AND the client tests that call `EnrichmentResult.model_validate` on the fixture)
    - `pytest apps/integrations/openai/tests/test_parser.py -k canonical` selects and passes at least one test
  </acceptance_criteria>
  <done>Fixture reflects the extended schema; full test_parser.py AND test_client.py pass (the fixture is consumed by both), including canonical + nullable polarity_type cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GPT output → parser | Untrusted model output validated by Pydantic before any DB write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | Tampering | Canonical label from model output | mitigate | Server-side `normalize_canonical` field_validator bounds the label to Title Case ≤3 words (D-05); never trust the prompt alone. |
| T-22-06 | Input Validation | polarity_type field | mitigate | `Literal[...] | None` rejects any value outside the three allowed types at parse time. |
| T-22-SC | Tampering | npm/pip installs | mitigate | No package installs — pydantic/openai already pinned (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `pytest apps/integrations/openai/tests/test_parser.py apps/integrations/openai/tests/test_client.py` passes (both fixture consumers)
- `mypy apps/integrations/openai/parser.py` clean (nullable union typed correctly under strict)
</verification>

<success_criteria>
- Tag schema accepts canonical (normalized Title Case ≤3 words) + nullable polarity_type with no default (CTAG-04, D-01, D-05)
- Canonical labels constrained to English Title Case via server-side validator (CTAG-05 enforced jointly with the prompt in 22-04)
- Existing structured-output parsing remains green under the extended schema in BOTH test_parser.py and test_client.py
</success_criteria>

<output>
Create `.planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-03-SUMMARY.md` when done
</output>
