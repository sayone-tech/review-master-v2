---
phase: 22-canonical-tag-foundation-mapping-pipeline
plan: 06
type: execute
wave: 2
depends_on: ["22-02"]
files_modified:
  - apps/reviews/tasks.py
  - apps/reviews/tests/test_tasks.py
autonomous: true
requirements: [QUEUE-02]
must_haves:
  truths:
    - "enrich_review_task carries a rate_limit sourced from settings.ENRICHMENT_RATE_LIMIT"
    - "The task docstring states the rate limit is PER-WORKER (not global) and that true-global is deferred to Phase 23"
    - "No code, comment, or test claims the rate limit is global"
  artifacts:
    - path: "apps/reviews/tasks.py"
      provides: "rate_limit on enrich_review_task with per-worker caveat"
      contains: "rate_limit=settings.ENRICHMENT_RATE_LIMIT"
    - path: "apps/reviews/tests/test_tasks.py"
      provides: "test asserting the task carries rate_limit from the setting"
  key_links:
    - from: "apps/reviews/tasks.py enrich_review_task"
      to: "config/settings/base.py ENRICHMENT_RATE_LIMIT"
      via: "settings import in the decorator"
      pattern: "rate_limit=settings.ENRICHMENT_RATE_LIMIT"
---

<objective>
Apply a configurable Celery `rate_limit` to `enrich_review_task` (QUEUE-02), sourced from the setting added in 22-02, and document the per-worker caveat (D-06).

Purpose: QUEUE-02 (configurable enrichment rate limit). Landmine (D-06, Pitfall 6): Celery `rate_limit` is PER-WORKER, NOT global. The success criterion's "global ~500/min" wording is reconciled here — P22 ships the per-worker limit (target ÷ expected workers) and documents that true cross-worker global throttling (Redis token bucket) is deferred to Phase 23. No code/comment/test may claim global semantics.
Output: rate_limit on the task decorator, docstring caveat, task test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-CONTEXT.md
@.planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-RESEARCH.md
@.planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-PATTERNS.md

<interfaces>
<!-- tasks.py:168-176 — the decorator to extend -->
@shared_task(  # type: ignore[misc]
    bind=True,
    max_retries=3,
    autoretry_for=(OpenAITransientError, EnrichmentParseError),
    retry_backoff=30,
    retry_backoff_max=600,
    retry_jitter=True,
)
def enrich_review_task(self: Any, review_id: int) -> None:

<!-- settings.ENRICHMENT_RATE_LIMIT (default "125/m") added in 22-02. Format: "<n>/s"|"<n>/m"|"<n>/h" -->
<!-- Test analogs: test_enrich_review_task_calls_service (~line 77), test_initial_backfill_dispatched_to_google_sync_queue (~line 63) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rate_limit to enrich_review_task with the per-worker caveat</name>
  <read_first>
    - apps/reviews/tasks.py (the file being modified — enrich_review_task decorator at 168-176, imports at top)
    - .planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-RESEARCH.md (Pitfall 6 — rate_limit is per-worker; Assumption A1)
    - .planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-PATTERNS.md (tasks.py Pattern Assignment — D-06 caveat wording)
  </read_first>
  <action>
    In apps/reviews/tasks.py add `rate_limit=settings.ENRICHMENT_RATE_LIMIT` to the `@shared_task(...)` decorator of `enrich_review_task`. Add `from django.conf import settings` if not already imported. Extend the task docstring with a paragraph stating: Celery `rate_limit` is enforced PER WORKER INSTANCE, not globally across all workers (D-06 / verified vs Celery docs); the configured value (`settings.ENRICHMENT_RATE_LIMIT`, default "125/m") is the platform target divided by expected worker count; a true cross-worker global throttle (Redis token bucket) is deferred to Phase 23 (QUEUE-01). Do NOT write any comment or docstring text claiming "global". Leave the task body and routing (ai-enrichment queue via CELERY_TASK_ROUTES) unchanged.
  </action>
  <verify>
    <automated>grep -q "rate_limit=settings.ENRICHMENT_RATE_LIMIT" apps/reviews/tasks.py && ! grep -in "global.*rate" apps/reviews/tasks.py</automated>
  </verify>
  <acceptance_criteria>
    - apps/reviews/tasks.py decorator for enrich_review_task contains `rate_limit=settings.ENRICHMENT_RATE_LIMIT`
    - The enrich_review_task docstring contains the words "per worker" (caveat present)
    - No occurrence of a claim that the rate limit is "global" in apps/reviews/tasks.py
  </acceptance_criteria>
  <done>enrich_review_task carries the configurable per-worker rate_limit with a documented caveat; no global claim.</done>
</task>

<task type="auto">
  <name>Task 2: Add a test asserting the task carries the rate_limit from settings</name>
  <read_first>
    - apps/reviews/tests/test_tasks.py (test_enrich_review_task_calls_service ~line 77, test_initial_backfill_dispatched_to_google_sync_queue ~line 63 — analogs for inspecting task config)
    - .planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-VALIDATION.md (QUEUE-02 test row)
  </read_first>
  <action>
    In apps/reviews/tests/test_tasks.py add `test_enrich_review_task_has_rate_limit` (named so `-k rate_limit` selects it) asserting `enrich_review_task.rate_limit == settings.ENRICHMENT_RATE_LIMIT` (inspect the task object's `rate_limit` attribute, as the existing tests inspect task config). Import `enrich_review_task` and `settings` as the surrounding tests do. The test MUST NOT assert any global/cross-worker semantics — it asserts only that the configured per-worker value is wired from settings.
  </action>
  <verify>
    <automated>pytest apps/reviews/tests/test_tasks.py -k rate_limit -q</automated>
  </verify>
  <acceptance_criteria>
    - apps/reviews/tests/test_tasks.py contains `def test_enrich_review_task_has_rate_limit`
    - `pytest apps/reviews/tests/test_tasks.py -k rate_limit` exits 0
    - The test does not contain the word "global"
  </acceptance_criteria>
  <done>Test confirms enrich_review_task.rate_limit equals the configured setting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Celery worker → OpenAI | Rate limiting bounds outbound call volume per worker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-10 | Denial of Service (cost / TPM) | enrich_review_task | mitigate | Per-worker rate_limit caps enrichment dispatch rate as a TPM safety net; true-global cap deferred to Phase 23 (documented, A1). |
| T-22-11 | Repudiation | Rate-limit semantics in docs | mitigate | Docstring explicitly states per-worker (not global) to prevent a future contributor relying on a false global guarantee. |
| T-22-SC | Tampering | npm/pip installs | mitigate | No package installs — celery already pinned. |
</threat_model>

<verification>
- `grep "rate_limit=settings.ENRICHMENT_RATE_LIMIT" apps/reviews/tasks.py` matches
- `pytest apps/reviews/tests/test_tasks.py` green
- No "global" rate-limit claim anywhere in tasks.py or test_tasks.py
</verification>

<success_criteria>
- enrich_review_task enforces a configurable per-worker rate limit from settings.ENRICHMENT_RATE_LIMIT (QUEUE-02)
- Per-worker caveat documented; true-global throttle explicitly deferred to Phase 23 (reconciles the criterion-4 "global" wording with D-06)
</success_criteria>

<output>
Create `.planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-06-SUMMARY.md` when done
</output>
