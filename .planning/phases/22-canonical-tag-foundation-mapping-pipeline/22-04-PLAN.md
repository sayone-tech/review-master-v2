---
phase: 22-canonical-tag-foundation-mapping-pipeline
plan: 04
type: execute
wave: 2
depends_on: ["22-01", "22-02"]
files_modified:
  - apps/integrations/openai/prompts.py
  - apps/integrations/openai/client.py
  - apps/reviews/selectors/canonical_tags.py
  - apps/reviews/services/enrichment.py
  - apps/integrations/openai/tests/test_prompts.py
autonomous: true
requirements: [CTAG-03, CTAG-05]
must_haves:
  truths:
    - "build_enrichment_messages accepts a canonical_vocab kwarg and appends a map-or-propose instruction block listing the vocabulary"
    - "When vocab is empty, the prompt instructs GPT to propose canonical labels (Title Case, <=3 words, English) for every tag"
    - "The canonical instruction requires English output for canonical labels"
    - "ENRICHMENT_PROMPT_VERSION is 4 (bumped from 3); REPLY_GENERATION_PROMPT_VERSION untouched"
    - "get_org_vocabulary returns the org's top-N canonical labels ordered by -review_count, limit from settings"
    - "canonical_vocab threads through enrich_review -> call_openai_enrichment -> build_enrichment_messages"
  artifacts:
    - path: "apps/integrations/openai/prompts.py"
      provides: "Vocab injection + version bump"
      contains: "canonical_vocab"
    - path: "apps/integrations/openai/client.py"
      provides: "call_openai_enrichment threads canonical_vocab to the builder"
      contains: "canonical_vocab"
    - path: "apps/reviews/selectors/canonical_tags.py"
      provides: "get_org_vocabulary selector"
      contains: "def get_org_vocabulary"
    - path: "apps/integrations/openai/tests/test_prompts.py"
      provides: "vocab-injection + version==4 tests"
  key_links:
    - from: "apps/reviews/selectors/canonical_tags.py get_org_vocabulary"
      to: "apps/reviews/models.py OrgCanonicalTag"
      via: "filter(organisation_id=...).order_by('-review_count')[:limit]"
      pattern: "order_by.*review_count"
    - from: "apps/integrations/openai/client.py call_openai_enrichment"
      to: "apps/integrations/openai/prompts.py build_enrichment_messages"
      via: "canonical_vocab kwarg pass-through"
      pattern: "build_enrichment_messages.*canonical_vocab"
---

<objective>
Inject the org's current canonical vocabulary into the single enrichment prompt and add the map-or-propose + English-canonical instruction; thread the vocabulary through the three call signatures; add the vocab selector; bump the prompt version.

Purpose: CTAG-03 (org vocabulary injected into the single prompt), CTAG-05 (canonical labels in English), supporting CTAG-04. D-02 caps the vocabulary to top-N by review_count. Landmine: vocab must thread through ALL THREE signatures (enrich_review → call_openai_enrichment → build_enrichment_messages); the builder is called at client.py:200 with no vocab today.
Output: extended prompt builder (version 4), client pass-through, new selector, prompt tests.

Note: this plan edits `apps/reviews/services/enrichment.py` (Task 3, threading only). Plan 22-05 (wave 3) further edits the same file's `_persist_success`; 22-05 `depends_on` 22-04 so the edits are serialized — never run them in the same wave.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-CONTEXT.md
@.planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-RESEARCH.md
@.planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-PATTERNS.md

<interfaces>
<!-- prompts.py:19 -->  ENRICHMENT_PROMPT_VERSION = 3
<!-- prompts.py:56-74 — builder to extend (English-only already in SYSTEM_PROMPT lines 27-28) -->
def build_enrichment_messages(*, review: Any) -> list[dict[str, str]]:
    brand = review.shop.organisation.name
    shop_name = review.shop.name
    user_payload = f"Brand: {brand}\nShop: {shop_name}\nStar rating: {review.star_rating} of 5\nReview text:\n{review.comment or '(no comment provided)'}"
    return [{"role":"system","content":SYSTEM_PROMPT}, {"role":"user","content":user_payload}]

<!-- client.py:188-200 — the threading seam -->
def call_openai_enrichment(*, review: Any, model: str | None = None) -> tuple[EnrichmentResult, dict[str, Any]]:
    use_model = model or settings.OPENAI_MODEL
    messages = build_enrichment_messages(review=review)   # <-- line 200, add canonical_vocab pass-through

<!-- enrich_review (enrichment.py:497) currently calls: call_openai_enrichment(review=review_for_prompt) -->
<!-- Selector convention: CLAUDE.md §5 read-only, *-kwargs. settings.CANONICAL_VOCAB_INJECT_LIMIT from 22-02. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add get_org_vocabulary selector</name>
  <read_first>
    - apps/reviews/selectors/ (existing selectors — CLAUDE.md §5 read-only *-kwargs convention)
    - apps/reviews/models.py (OrgCanonicalTag from 22-01 — organisation FK, review_count, orgcanon_org_count_idx index)
    - .planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-PATTERNS.md (selectors/canonical_tags.py Pattern Assignment)
  </read_first>
  <action>
    Create apps/reviews/selectors/canonical_tags.py with `def get_org_vocabulary(*, organisation_id: int, limit: int) -> list[str]` that returns `list(OrgCanonicalTag.objects.filter(organisation_id=organisation_id).order_by("-review_count").values_list("label", flat=True)[:limit])`. Read-only, fully type-annotated, no mutations (CLAUDE.md §5). The `[:limit]` slice and the `(organisation, -review_count)` index keep this a single bounded query. Caller passes `limit=settings.CANONICAL_VOCAB_INJECT_LIMIT` (do NOT read settings inside the selector — keep it pure). This query exploits the `orgcanon_org_count_idx` index.
  </action>
  <verify>
    <automated>python -c "import django,os;os.environ.setdefault('DJANGO_SETTINGS_MODULE','config.settings.test');django.setup();from apps.reviews.selectors.canonical_tags import get_org_vocabulary;import inspect;assert 'organisation_id' in inspect.signature(get_org_vocabulary).parameters"</automated>
  </verify>
  <acceptance_criteria>
    - apps/reviews/selectors/canonical_tags.py contains `def get_org_vocabulary(*, organisation_id: int, limit: int) -> list[str]`
    - The selector orders by `-review_count` and slices to `limit` (single query, org-filtered)
    - The selector contains no write operations and does not import django.conf.settings
  </acceptance_criteria>
  <done>get_org_vocabulary returns the org's top-N canonical labels ordered by -review_count.</done>
</task>

<task type="auto">
  <name>Task 2: Inject vocabulary into build_enrichment_messages and bump the version</name>
  <read_first>
    - apps/integrations/openai/prompts.py (the file being modified — SYSTEM_PROMPT 21-53, build_enrichment_messages 56-74, ENRICHMENT_PROMPT_VERSION line 19, REPLY_GENERATION_PROMPT_VERSION line 85)
    - .planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-RESEARCH.md (Pattern 4 vocab injection)
    - .planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-PATTERNS.md (prompts.py Pattern Assignment — threading-seam warning)
  </read_first>
  <action>
    In apps/integrations/openai/prompts.py change `ENRICHMENT_PROMPT_VERSION = 3` to `= 4` (line 19). Do NOT touch `REPLY_GENERATION_PROMPT_VERSION`; do NOT trigger any bulk re-enrichment (deferred). Add to the canonical instruction (either by extending SYSTEM_PROMPT or appending to the user payload) wording requiring that each tag include a `canonical` label and, WHEN proposing a NEW canonical label, a `polarity_type` of always_positive|always_negative|mixed; the canonical label must be Title Case, ≤3 words, and in English. Add a `canonical_vocab: list[str] | None = None` kwarg to `build_enrichment_messages`. When `canonical_vocab` is non-empty, append a block listing the labels (comma-joined) instructing GPT to map each tag to ONE existing label when it fits, otherwise propose a new canonical label. When empty/None, append a block instructing GPT to propose canonical labels for every tag (no existing vocabulary yet). Keep the prompt context to brand + shop + rating + text only (Phase 12 lock — NO address, NO reviewer name). The English-only rule is already present (lines 27-28) — extend it to cover canonical labels explicitly (CTAG-05).
  </action>
  <verify>
    <automated>pytest apps/integrations/openai/tests/test_prompts.py -k "vocab or version" -q</automated>
  </verify>
  <acceptance_criteria>
    - apps/integrations/openai/prompts.py contains `ENRICHMENT_PROMPT_VERSION = 4`
    - `grep -n REPLY_GENERATION_PROMPT_VERSION apps/integrations/openai/prompts.py` still shows `= 1` (untouched)
    - build_enrichment_messages signature includes `canonical_vocab: list[str] | None = None`
    - When a non-empty vocab list is passed, the returned messages contain the joined labels; when empty, a "propose canonical labels" instruction is present
  </acceptance_criteria>
  <done>Prompt injects capped vocab with a map-or-propose + English-canonical instruction; version is 4; reply version untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Thread canonical_vocab through call_openai_enrichment and enrich_review</name>
  <read_first>
    - apps/integrations/openai/client.py (call_openai_enrichment at 188-200 — add the kwarg and pass it to build_enrichment_messages at line 200)
    - apps/reviews/services/enrichment.py (enrich_review at 407-505 — the call at line 497; org id available via review.organisation_id from select_related at 428)
    - .planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-PATTERNS.md (prompts.py threading-seam warning — all three signatures)
  </read_first>
  <action>
    In apps/integrations/openai/client.py add a `canonical_vocab: list[str] | None = None` kwarg to `call_openai_enrichment` and pass it through to `build_enrichment_messages(review=review, canonical_vocab=canonical_vocab)` at line 200. In apps/reviews/services/enrichment.py, inside `enrich_review`, BEFORE the OpenAI call (line ~497) and AFTER moderation, fetch the vocab via `get_org_vocabulary(organisation_id=review.organisation_id, limit=settings.CANONICAL_VOCAB_INJECT_LIMIT)` (import the selector; settings is already imported) using the org id already loaded by `select_related("shop__organisation")` — no extra org query. Pass the result as `call_openai_enrichment(review=review_for_prompt, canonical_vocab=vocab)`. Do NOT change the no-comment skip path (it never calls OpenAI). Do NOT modify `_persist_success` here (that is plan 22-05's exclusive change to this file). This is the third of the three threaded signatures.
  </action>
  <verify>
    <automated>python -c "import inspect,django,os;os.environ.setdefault('DJANGO_SETTINGS_MODULE','config.settings.test');django.setup();from apps.integrations.openai.client import call_openai_enrichment;assert 'canonical_vocab' in inspect.signature(call_openai_enrichment).parameters" && grep -q "canonical_vocab" apps/reviews/services/enrichment.py</automated>
  </verify>
  <acceptance_criteria>
    - apps/integrations/openai/client.py call_openai_enrichment signature includes `canonical_vocab`
    - apps/integrations/openai/client.py passes canonical_vocab to build_enrichment_messages
    - apps/reviews/services/enrichment.py calls get_org_vocabulary with review.organisation_id and limit=settings.CANONICAL_VOCAB_INJECT_LIMIT and passes the result to call_openai_enrichment
    - `pytest apps/reviews/tests/test_enrichment_service.py` still passes (no regression from the new kwarg)
  </acceptance_criteria>
  <done>Vocabulary threads end-to-end from enrich_review through the client to the prompt builder; org resolved without an extra query.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| org vocabulary → prompt | Only the calling review's own org vocabulary is injected; cross-tenant leakage must be impossible |
| review text → prompt | Already moderated; canonical work adds no new logging of review content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-07 | Information Disclosure | Vocabulary injection | mitigate | get_org_vocabulary filters strictly by `organisation_id` resolved from `review.organisation_id`; no cross-org vocabulary can reach the prompt. |
| T-22-08 | Denial of Service (cost) | Prompt token growth | mitigate | limit=settings.CANONICAL_VOCAB_INJECT_LIMIT caps the injected list (D-02); ~150-300 extra tokens, bounded and tracked via AiUsageLog. |
| T-22-09 | Information Disclosure | Review PII in logs | accept | No new logging of review content added; existing pipeline complies with §22 (review text not logged at INFO+). |
| T-22-SC | Tampering | npm/pip installs | mitigate | No package installs — no new dependencies. |
</threat_model>

<verification>
- `pytest apps/integrations/openai/tests/test_prompts.py` passes (vocab injection + version==4)
- `pytest apps/reviews/tests/test_enrichment_service.py apps/integrations/openai` green (no regression from threaded kwarg)
- `mypy apps/integrations/openai/prompts.py apps/integrations/openai/client.py apps/reviews/selectors/canonical_tags.py` clean
</verification>

<success_criteria>
- Org's capped canonical vocabulary is injected into the single enrichment prompt with a map-or-propose instruction (CTAG-03)
- Canonical labels (and existing tags/action items) constrained to English by the prompt + the 22-03 validator (CTAG-05)
- Vocabulary threads through all three signatures; ENRICHMENT_PROMPT_VERSION bumped to 4 with no bulk re-enrichment
</success_criteria>

<output>
Create `.planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-04-SUMMARY.md` when done
</output>
