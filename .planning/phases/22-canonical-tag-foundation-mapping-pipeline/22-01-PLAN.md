---
phase: 22-canonical-tag-foundation-mapping-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/reviews/models.py
  - apps/reviews/migrations/0011_orgcanonicaltag_reviewtag_canonical_tag.py
  - apps/reviews/tests/factories.py
  - apps/reviews/tests/test_models.py
autonomous: true
requirements: [CTAG-01, CTAG-02, CTAG-08]
must_haves:
  truths:
    - "D-04: ReviewTag stores the canonical FK only (no denormalized label). D-03: review_count is a cache column, never incremented in the enrichment hot path."
    - "An OrgCanonicalTag row can be created for an organisation with a Title-Case label, polarity_type, review_count, and timestamps"
    - "Two OrgCanonicalTag rows with the same (organisation, label) are rejected by a DB unique constraint"
    - "A ReviewTag row can exist with canonical_tag=NULL and remains valid (pre-phase rows untouched, no backfill)"
    - "The migration adds the model + nullable FK without modifying the existing uniq_reviewtag_review_label_polarity constraint"
  artifacts:
    - path: "apps/reviews/models.py"
      provides: "OrgCanonicalTag model + nullable ReviewTag.canonical_tag FK"
      contains: "class OrgCanonicalTag"
    - path: "apps/reviews/migrations/0011_orgcanonicaltag_reviewtag_canonical_tag.py"
      provides: "Single migration creating the model + FK + index, no backfill"
      contains: "OrgCanonicalTag"
    - path: "apps/reviews/tests/factories.py"
      provides: "OrgCanonicalTagFactory"
      contains: "class OrgCanonicalTagFactory"
    - path: "apps/reviews/tests/test_models.py"
      provides: "Model creation, unique (org,label), null canonical_tag validity tests"
  key_links:
    - from: "apps/reviews/models.py ReviewTag.canonical_tag"
      to: "apps/reviews/models.py OrgCanonicalTag"
      via: "ForeignKey on_delete=SET_NULL related_name=review_tags"
      pattern: "canonical_tag = models.ForeignKey"
---

<objective>
Create the per-organisation canonical tag data layer: a new `OrgCanonicalTag` model and a nullable `canonical_tag` FK on `ReviewTag`, plus a single non-backfilling migration. This is the structural foundation every downstream plan depends on.

Purpose: CTAG-01 (per-org canonical vocabulary), CTAG-02 (ReviewTag carries a nullable canonical FK), CTAG-08 (pre-phase rows stay valid with a null mapping). FK-only storage (D-04) keeps the future Phase 25 rename O(1); `review_count` is a cache column never written inline (D-03).
Output: `OrgCanonicalTag` model, `ReviewTag.canonical_tag` FK, one migration, `OrgCanonicalTagFactory`, model tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-CONTEXT.md
@.planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-PATTERNS.md

<interfaces>
<!-- Existing ReviewTag to mirror (apps/reviews/models.py:128-162). canonical_tag FK is NEW. -->
class ReviewTag(models.Model):
    class Polarity(models.TextChoices): POSITIVE/NEUTRAL/NEGATIVE
    review = FK("reviews.Review", on_delete=CASCADE, related_name="tags")
    label = CharField(max_length=100, db_index=True)
    polarity = CharField(max_length=10, choices=Polarity.choices)
    Meta.db_table = "reviews_reviewtag"
    Meta.constraints = [UniqueConstraint(["review","label","polarity"], name="uniq_reviewtag_review_label_polarity")]
    Meta.indexes = [Index(["review","label"], name="reviewtag_review_label_idx")]

<!-- Nullable SET_NULL FK idiom to mirror: Review.replied_by (apps/reviews/models.py:60-67) -->
<!-- TimeStampedModel base: apps/common/models.py (gives created_at/updated_at) -->
<!-- Tenant-scoping FK idiom: Review.organisation (apps/reviews/models.py:34-39) -->
<!-- Factory analog: ReviewTagFactory (apps/reviews/tests/factories.py:38-44), OrganisationFactory SubFactory usage -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add OrgCanonicalTag model + nullable ReviewTag.canonical_tag FK</name>
  <read_first>
    - apps/reviews/models.py (the file being modified — ReviewTag at lines 128-162, Review.replied_by at lines 60-67, Review.organisation at lines 34-39)
    - apps/common/models.py (TimeStampedModel base — the analog for created_at/updated_at)
    - apps/integrations/openai/models.py (AiPricing — analog for TextChoices + UniqueConstraint + ClassVar-typed Meta)
    - .planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-PATTERNS.md (Pattern Assignments → OrgCanonicalTag section)
  </read_first>
  <behavior>
    - Test: OrgCanonicalTag(organisation=org, label="Food Quality", polarity_type="mixed") saves with created_at/updated_at populated and review_count defaulting to 0
    - Test: creating a second OrgCanonicalTag with the same (organisation, label) raises IntegrityError
    - Test: two organisations may each have an OrgCanonicalTag with label "Food Quality" (constraint is per-org)
    - Test: a ReviewTag created without canonical_tag has canonical_tag is None and is valid
  </behavior>
  <action>
    In apps/reviews/models.py add a new `OrgCanonicalTag(TimeStampedModel)` model (import `TimeStampedModel` from `apps.common.models` — do NOT redeclare timestamp fields). Define nested `class PolarityType(models.TextChoices)` with members `ALWAYS_POSITIVE = "always_positive", "Always Positive"`, `ALWAYS_NEGATIVE = "always_negative", "Always Negative"`, `MIXED = "mixed", "Mixed"`. Fields: `organisation = models.ForeignKey("organisations.Organisation", on_delete=models.CASCADE, related_name="canonical_tags", db_index=True)`; `label = models.CharField(max_length=100)`; `polarity_type = models.CharField(max_length=20, choices=PolarityType.choices)`; `review_count = models.PositiveIntegerField(default=0)` with a comment stating it is a DENORMALIZED CACHE only, populated 0, NEVER incremented in the enrichment hot path (D-03 derive-on-read; refreshed only by the Phase 24 weekly job / Phase 25 merge). Inner `class Meta` with `db_table = "reviews_orgcanonicaltag"`, `constraints: ClassVar[list[models.BaseConstraint]]` containing `models.UniqueConstraint(fields=["organisation", "label"], name="uniq_orgcanonicaltag_org_label")`, and `indexes: ClassVar[list[models.Index]]` containing `models.Index(fields=["organisation", "-review_count"], name="orgcanon_org_count_idx")`. Add a `__str__` returning label + org id. Use the same `ClassVar`-typed Meta style as ReviewTag (mypy strict, §17). Then ADD to the existing `ReviewTag` model a field `canonical_tag = models.ForeignKey("reviews.OrgCanonicalTag", null=True, blank=True, on_delete=models.SET_NULL, related_name="review_tags", db_index=True)`. Do NOT add `canonical_tag` to the existing `uniq_reviewtag_review_label_polarity` constraint (leaving it out preserves the delete-then-bulk_create race guard).
  </action>
  <verify>
    <automated>python manage.py makemigrations --check --dry-run --settings=config.settings.test 2>&1 | grep -qv '^#' && python -c "import django,os;os.environ.setdefault('DJANGO_SETTINGS_MODULE','config.settings.test');django.setup();from apps.reviews.models import OrgCanonicalTag,ReviewTag;assert OrgCanonicalTag.PolarityType.MIXED=='mixed';assert ReviewTag._meta.get_field('canonical_tag').null"</automated>
  </verify>
  <acceptance_criteria>
    - apps/reviews/models.py contains `class OrgCanonicalTag(TimeStampedModel)`
    - apps/reviews/models.py OrgCanonicalTag.Meta defines constraint name `uniq_orgcanonicaltag_org_label` and index name `orgcanon_org_count_idx`
    - apps/reviews/models.py ReviewTag has a `canonical_tag` field that is `null=True`, `on_delete=models.SET_NULL`, `related_name="review_tags"`
    - The existing constraint `uniq_reviewtag_review_label_polarity` still lists exactly `["review", "label", "polarity"]` (canonical_tag NOT added)
  </acceptance_criteria>
  <done>OrgCanonicalTag model and nullable ReviewTag.canonical_tag FK exist; existing ReviewTag constraint unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the single non-backfilling migration</name>
  <read_first>
    - apps/reviews/migrations/ (most recent migration file — for the numbering + dependency convention)
    - .planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-RESEARCH.md (Runtime State Inventory — migration safety note A2; no backfill per CTAG-08)
  </read_first>
  <action>
    Run `python manage.py makemigrations reviews --settings=config.settings.test` to generate ONE migration creating `OrgCanonicalTag` and adding the nullable `ReviewTag.canonical_tag` FK + the `orgcanon_org_count_idx` index. Rename the generated file descriptively to `0011_orgcanonicaltag_reviewtag_canonical_tag.py` (match the actual next sequence number if it differs from 0011 — per §18 descriptive name, one migration per PR). The migration MUST contain NO data migration / RunPython backfill (CTAG-08 — existing rows keep canonical_tag NULL). Confirm the migration's `dependencies` point at the current latest reviews migration. Do NOT use AddIndex CONCURRENTLY (default Django add is fine at current scale, A2).
  </action>
  <verify>
    <automated>python manage.py makemigrations --check --dry-run --settings=config.settings.test && python manage.py migrate reviews --settings=config.settings.test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - A single new file exists under apps/reviews/migrations/ named `0011_orgcanonicaltag_reviewtag_canonical_tag.py` (or the correct next sequence number) that creates OrgCanonicalTag and adds field canonical_tag
    - `grep -L RunPython` on the migration confirms no RunPython backfill block
    - `python manage.py makemigrations --check --dry-run` exits 0 (no pending model changes)
  </acceptance_criteria>
  <done>One migration created and applies cleanly; no backfill; makemigrations --check is clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add OrgCanonicalTagFactory and model tests</name>
  <read_first>
    - apps/reviews/tests/factories.py (ReviewTagFactory at lines 38-44, ReviewFactory SubFactory usage at lines 15-35 — analogs)
    - apps/reviews/tests/test_models.py (existing structure to extend)
    - .planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-VALIDATION.md (CTAG-01 / CTAG-08 test rows)
  </read_first>
  <behavior>
    - Test (CTAG-01): OrgCanonicalTagFactory() creates a row with a non-empty label, a valid polarity_type, review_count==0, and populated created_at
    - Test (CTAG-01): saving two OrgCanonicalTag rows with the same organisation and label raises IntegrityError
    - Test (CTAG-01): the same label under two different organisations both save successfully
    - Test (CTAG-08): a ReviewTag with canonical_tag left unset has canonical_tag is None and saves; a ReviewTag pointed at an OrgCanonicalTag resolves the FK
  </behavior>
  <action>
    In apps/reviews/tests/factories.py add `OrgCanonicalTagFactory(DjangoModelFactory)` mirroring `ReviewTagFactory`: `class Meta: model = OrgCanonicalTag`; `organisation = factory.SubFactory(OrganisationFactory)`; `label = factory.Sequence(lambda n: f"Canonical {n}")`; `polarity_type = OrgCanonicalTag.PolarityType.MIXED`; `review_count = 0`. Import `OrgCanonicalTag` at the top. In apps/reviews/tests/test_models.py add tests keyed so `-k canonical` and `-k null_canonical` select them: a creation/timestamps test, a `(org, label)` uniqueness test using `pytest.raises(IntegrityError)` (and within `transaction.atomic()` so the test DB is not poisoned), a cross-org same-label test, and a null-canonical_tag validity test. Use the factory; never hardcode model construction that bypasses the factory pattern.
  </action>
  <verify>
    <automated>pytest apps/reviews/tests/test_models.py -k "canonical or null_canonical" -q</automated>
  </verify>
  <acceptance_criteria>
    - apps/reviews/tests/factories.py contains `class OrgCanonicalTagFactory`
    - `pytest apps/reviews/tests/test_models.py -k canonical` exits 0
    - `pytest apps/reviews/tests/test_models.py -k null_canonical` exits 0
  </acceptance_criteria>
  <done>OrgCanonicalTagFactory exists; model tests for creation, uniqueness, cross-org, and null FK validity pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GPT output → DB | Canonical labels originate from model output; format enforced downstream (22-03), but the model schema must be org-scoped from creation |
| org A data ↔ org B data | OrgCanonicalTag is per-tenant; isolation must hold at the schema level |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Information Disclosure | OrgCanonicalTag rows | mitigate | Direct `organisation` FK with db_index; unique scoped to `(organisation, label)`; all future reads filter by organisation_id (CLAUDE.md §9). No user-facing endpoint in this plan. |
| T-22-02 | Tampering | ReviewTag.canonical_tag FK | mitigate | `on_delete=SET_NULL` so deleting a canonical tag cannot cascade-destroy review tags; FK excluded from the existing uniqueness constraint to preserve the race guard. |
| T-22-SC | Tampering | npm/pip installs | mitigate | No package installs in this plan — no new dependencies (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `python manage.py makemigrations --check --dry-run --settings=config.settings.test` exits 0
- `pytest apps/reviews/tests/test_models.py -k "canonical or null_canonical"` passes
- `pytest apps/reviews/tests/test_tasks.py apps/reviews/tests/test_enrichment_service.py` still green (no regression from the new FK column)
</verification>

<success_criteria>
- OrgCanonicalTag model exists with per-org unique label, polarity_type, review_count cache column (default 0, never incremented inline), and timestamps (CTAG-01)
- ReviewTag has a nullable canonical_tag FK (CTAG-02 storage; population happens in 22-05)
- Pre-phase ReviewTag rows remain valid with canonical_tag NULL; migration performs no backfill (CTAG-08)
</success_criteria>

<output>
Create `.planning/phases/22-canonical-tag-foundation-mapping-pipeline/22-01-SUMMARY.md` when done
</output>
