---
phase: 21-audit-log-viewer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/common/selectors/__init__.py
  - apps/common/selectors/audit_logs.py
  - apps/common/serializers.py
  - apps/common/filters.py
  - apps/common/pagination.py
  - apps/common/tests/test_audit_log_selectors.py
autonomous: true
requirements:
  - REQ-01
  - REQ-02
  - REQ-03
  - REQ-04

must_haves:
  truths:
    - "list_audit_logs_for_org() returns only review+reply and action_item entries for the given org (D-01, D-02)"
    - "list_audit_logs_for_staff() filters to entity_ids that map to accessible shops and SHOP-scope action items only (D-03)"
    - "AuditLogReadSerializer omits before_data and includes actor_name (D-10, D-11)"
    - "AuditLogCursorPagination uses ordering=('-created_at', 'id') tiebreaker (D-08)"
    - "AuditLogFilterSet exposes entity_type, actor, date_from, date_to, shop params (D-07)"
  artifacts:
    - path: apps/common/selectors/__init__.py
      provides: "Package init for selectors sub-package"
    - path: apps/common/selectors/audit_logs.py
      provides: "list_audit_logs_for_org, list_audit_logs_for_staff"
      exports: ["list_audit_logs_for_org", "list_audit_logs_for_staff", "AUDIT_ENTITY_FILTER"]
    - path: apps/common/serializers.py
      provides: "AuditLogReadSerializer"
      exports: ["AuditLogReadSerializer"]
    - path: apps/common/filters.py
      provides: "AuditLogFilterSet"
      exports: ["AuditLogFilterSet"]
    - path: apps/common/pagination.py
      provides: "AuditLogCursorPagination alongside DefaultPageNumberPagination"
      contains: "class AuditLogCursorPagination"
  key_links:
    - from: apps/common/selectors/audit_logs.py
      to: apps/reviews/selectors/reviews.py
      via: "get_accessible_shop_ids import"
      pattern: "from apps.reviews.selectors.reviews import get_accessible_shop_ids"
    - from: apps/common/selectors/audit_logs.py
      to: apps/common/models.py
      via: "AuditLog.objects.filter"
      pattern: "AUDIT_ENTITY_FILTER"
---

<objective>
Build the backend data layer for the Audit Log viewer: selector functions with org+staff scoping, the cursor pagination class (first in codebase), the read serializer (no before_data), and the FilterSet for query param handling.

Purpose: Wave 2 viewset and tests depend on these contracts. Getting the selector right first ensures Staff scoping (D-03) is correct before wiring to HTTP.
Output: apps/common/selectors/audit_logs.py, apps/common/serializers.py, apps/common/filters.py, apps/common/pagination.py (appended), selector unit tests.
</objective>

<execution_context>
@/Users/renjith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/renjith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-audit-log-viewer/21-CONTEXT.md
@.planning/phases/21-audit-log-viewer/21-RESEARCH.md
</context>

<interfaces>
From apps/common/models.py — AuditLog fields:
  organisation = ForeignKey("organisations.Organisation", ...)
  actor = ForeignKey(settings.AUTH_USER_MODEL, null=True, ...)
  entity_type = CharField(max_length=50)
  entity_id = CharField(max_length=200)
  action = CharField(max_length=100)
  before_data = JSONField(null=True)
  after_data = JSONField(null=True)
  created_at = DateTimeField(auto_now_add=True)
  Meta.ordering = ["-created_at"]
  Meta.indexes includes: audit_org_entity_date_idx on (organisation, entity_type, created_at)

From apps/common/pagination.py:
  class DefaultPageNumberPagination(PageNumberPagination):
      page_size = 25
      page_size_query_param = "page_size"
      max_page_size = 100

From apps/action_items/selectors/items.py — Staff scoping pattern:
  from apps.reviews.selectors.reviews import get_accessible_shop_ids
  accessible = get_accessible_shop_ids(user_id=user.pk)
  qs = qs.filter(scope=ActionItem.Scope.SHOP, shop_id__in=accessible)

From apps/action_items/filters.py — FilterSet pattern:
  class ActionItemFilterSet(django_filters.FilterSet):
      class Meta:
          model = ActionItem
          fields: ClassVar[list[str]] = []

From apps/reviews/tests/factories.py — AuditLogFactory already exists at lines 47-57:
  class AuditLogFactory(DjangoModelFactory):
      class Meta:
          model = AuditLog
      organisation = factory.SubFactory(OrganisationFactory)
      actor = None
      entity_type = "review"
      entity_id = factory.Sequence(lambda n: str(n))
      action = "reply_posted"
      before_data = None
      after_data = factory.LazyFunction(lambda: {"reply_text": "ok"})

From apps/accounts/models.py — User.Role:
  User.Role.STAFF_ADMIN  (string value "STAFF_ADMIN")
  User.Role.ORG_ADMIN    (string value "ORG_ADMIN")
  user.full_name attribute exists (confirmed line 26)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create selectors sub-package and selector functions</name>
  <files>apps/common/selectors/__init__.py, apps/common/selectors/audit_logs.py</files>
  <read_first>
    - apps/common/models.py (AuditLog model fields and Meta)
    - apps/action_items/selectors/items.py (Staff scoping pattern with get_accessible_shop_ids)
    - apps/reviews/selectors/reviews.py (get_accessible_shop_ids signature)
    - apps/action_items/models.py (ActionItem.Scope.SHOP for two-step query)
    - apps/reviews/models.py (Review model fields: organisation_id, shop_id, deleted_at)
  </read_first>
  <behavior>
    - list_audit_logs_for_org(organisation_id=N) returns AuditLog queryset filtered to that org, entity_type__in=["review","action_item"], with Q(entity_type="action_item") | Q(entity_type="review", action__in=["reply_posted","reply_deleted","reply_failed"]), with select_related("actor")
    - list_audit_logs_for_staff(organisation_id=N, user=u) first fetches Review PKs whose shop_id is in get_accessible_shop_ids(user_id=u.pk) and deleted_at__isnull=True, then fetches ActionItem PKs with scope=SHOP and shop_id in accessible shops, concatenates them as str, then applies entity_id__in=[...] on top of the org filter + AUDIT_ENTITY_FILTER
    - Both functions apply .select_related("actor")
    - AUDIT_ENTITY_FILTER constant exported from module for reuse
    - apps/common/selectors/__init__.py is empty (just the package marker)
  </behavior>
  <action>
    Create apps/common/selectors/__init__.py as an empty file.

    Create apps/common/selectors/audit_logs.py with:
    - Module docstring referencing D-01, D-02, D-03 and the two-step Staff scoping approach from RESEARCH.md Pattern 1
    - from __future__ import annotations at top
    - Import Q, QuerySet from django.db.models
    - Import AuditLog from apps.common.models
    - AUDIT_ENTITY_FILTER constant: Q(entity_type="action_item") | Q(entity_type="review", action__in=["reply_posted", "reply_deleted", "reply_failed"])
    - list_audit_logs_for_org(*, organisation_id: int) -> QuerySet[AuditLog]: filters by organisation_id, applies AUDIT_ENTITY_FILTER, returns with .select_related("actor")
    - list_audit_logs_for_staff(*, organisation_id: int, user) -> QuerySet[AuditLog]: imports get_accessible_shop_ids from apps.reviews.selectors.reviews, Review from apps.reviews.models, ActionItem from apps.action_items.models inside the function body (lazy imports to avoid circular). Fetches accessible_shop_ids, then review_ids as list of str PKs (filter: organisation_id, shop_id__in=accessible_shop_ids, deleted_at__isnull=True), then action_item_ids as list of str PKs (filter: organisation_id, scope=ActionItem.Scope.SHOP, shop_id__in=accessible_shop_ids). Concatenates both lists, applies .filter(entity_id__in=accessible_entity_ids) on top of org+AUDIT_ENTITY_FILTER query with select_related("actor")

    CRITICAL: Both functions must use keyword-only arguments (per D-03 pattern). Do not expose before_data in any queryset annotation.
  </action>
  <verify>
    <automated>pytest apps/common/tests/test_audit_log_selectors.py -x -q</automated>
  </verify>
  <done>
    - apps/common/selectors/__init__.py exists (empty)
    - apps/common/selectors/audit_logs.py exports AUDIT_ENTITY_FILTER, list_audit_logs_for_org, list_audit_logs_for_staff
    - test_audit_log_selectors.py passes: org selector returns only review+action_item events, Staff selector excludes brand-scope items and inaccessible-shop review entries
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AuditLogReadSerializer + AuditLogCursorPagination + AuditLogFilterSet</name>
  <files>apps/common/serializers.py, apps/common/pagination.py, apps/common/filters.py</files>
  <read_first>
    - apps/common/models.py (AuditLog field names, actor FK)
    - apps/common/pagination.py (existing DefaultPageNumberPagination — must append, not replace)
    - apps/action_items/filters.py (FilterSet pattern with Meta.fields = [])
    - apps/reviews/models.py (Review fields: shop_id, deleted_at)
    - apps/accounts/models.py (User.full_name attribute)
  </read_first>
  <behavior>
    - AuditLogReadSerializer.Meta.fields = ["id", "created_at", "entity_type", "entity_id", "action", "actor_id", "actor_name", "after_data"] — no before_data
    - get_actor_id returns obj.actor_id (the FK integer, or None)
    - get_actor_name returns actor.full_name if actor is not None and full_name is truthy, else None
    - AuditLogCursorPagination.page_size = 50, page_size_query_param = "page_size", max_page_size = 100, ordering = ("-created_at", "id") — tuple tiebreaker for cursor stability (RESEARCH.md Pitfall 3)
    - AuditLogFilterSet: entity_type = CharFilter(field_name="entity_type"), actor = CharFilter(method="filter_actor"), date_from = DateFilter(field_name="created_at", lookup_expr="gte"), date_to = DateFilter(field_name="created_at", lookup_expr="lte"), shop = NumberFilter(method="filter_shop")
    - filter_actor: value=="system" -> filter(actor__isnull=True); else int(value) -> filter(actor_id=int(value)); invalid -> return qs unchanged
    - filter_shop: fetches Review PKs with shop_id=value, deleted_at__isnull=True, then filters qs with entity_type="review", entity_id__in=[str(pk) for pk in review_ids]
  </behavior>
  <action>
    Create apps/common/serializers.py — new file (does not yet exist, confirmed in RESEARCH.md). Include:
    - from __future__ import annotations
    - from rest_framework import serializers
    - from apps.common.models import AuditLog
    - AuditLogReadSerializer(serializers.ModelSerializer) with SerializerMethodField for actor_id and actor_name
    - Meta.model = AuditLog, Meta.fields = ["id", "created_at", "entity_type", "entity_id", "action", "actor_id", "actor_name", "after_data"]
    - get_actor_id returns obj.actor_id
    - get_actor_name: actor = obj.actor; if actor is None return None; full_name = getattr(actor, "full_name", "") or ""; return str(full_name) if full_name else None

    Append to apps/common/pagination.py (file already exists — read it first):
    - from rest_framework.pagination import CursorPagination
    - class AuditLogCursorPagination(CursorPagination): page_size = 50, page_size_query_param = "page_size", max_page_size = 100, ordering = ("-created_at", "id")

    Create apps/common/filters.py — new file. Include:
    - from __future__ import annotations
    - from typing import ClassVar
    - import django_filters
    - from apps.common.models import AuditLog
    - class AuditLogFilterSet(django_filters.FilterSet) with fields: entity_type (CharFilter), actor (CharFilter, method="filter_actor"), date_from (DateFilter, field_name="created_at", lookup_expr="gte"), date_to (DateFilter, field_name="created_at", lookup_expr="lte"), shop (NumberFilter, method="filter_shop")
    - Meta: model = AuditLog, fields: ClassVar[list[str]] = []
    - filter_actor method: system->actor__isnull=True, int->actor_id=int(value), else return qs
    - filter_shop method: lazy import Review from apps.reviews.models; fetch review_ids; filter qs by entity_type="review" and entity_id__in

    All files use ruff-compatible style: from __future__ import annotations, type annotations on all methods, ClassVar on class-level list fields.
  </action>
  <verify>
    <automated>pytest apps/common/tests/test_audit_log_selectors.py -x -q</automated>
  </verify>
  <done>
    - apps/common/serializers.py exists with AuditLogReadSerializer; serialized output has actor_name and no before_data
    - apps/common/pagination.py has AuditLogCursorPagination with ordering = ("-created_at", "id")
    - apps/common/filters.py has AuditLogFilterSet with all five filter fields
    - Selector tests pass (both selectors, serializer field assertions)
  </done>
</task>

<task type="auto">
  <name>Task 3: Selector unit tests</name>
  <files>apps/common/tests/test_audit_log_selectors.py</files>
  <read_first>
    - apps/common/tests/conftest.py (existing conftest fixtures)
    - apps/reviews/tests/factories.py (AuditLogFactory at lines 47-57, OrganisationFactory, UserFactory, ShopFactory)
    - apps/common/selectors/audit_logs.py (selector functions just created)
    - apps/common/serializers.py (AuditLogReadSerializer just created)
    - apps/action_items/models.py (ActionItem.Scope.SHOP / BRAND values)
  </read_first>
  <action>
    Create apps/common/tests/test_audit_log_selectors.py covering:

    Import AuditLogFactory from apps.reviews.tests.factories (do NOT create a duplicate factory).

    Test cases to implement:
    1. test_list_audit_logs_for_org_includes_reply_actions — creates AuditLog rows with entity_type="review" and action in ["reply_posted","reply_deleted","reply_failed"]; org selector returns all three; a "sync_triggered" row with entity_type="shop_sync" is excluded.
    2. test_list_audit_logs_for_org_includes_action_item_events — creates AuditLog with entity_type="action_item", action="action_item.status_changed"; org selector returns it.
    3. test_list_audit_logs_for_org_excludes_other_orgs — creates AuditLog for org_A and org_B; selector for org_A returns only org_A rows.
    4. test_list_audit_logs_for_staff_excludes_brand_scope_items — creates ActionItem with scope=BRAND in accessible shop; AuditLog entry for that item; Staff selector excludes it.
    5. test_list_audit_logs_for_staff_excludes_inaccessible_shop_reviews — creates two shops; Staff user accessible to shop_A only; AuditLog entry for review in shop_B is excluded.
    6. test_serializer_includes_actor_name_excludes_before_data — creates AuditLog with actor; serializes it; asserts "actor_name" in result, "before_data" not in result.
    7. test_serializer_system_actor_name_is_none — creates AuditLog with actor=None; serializes; asserts actor_name is None.

    Use pytest fixtures and factory_boy. Use @pytest.mark.django_db on each test.
    Use CaptureQueriesContext for at least one test to confirm select_related("actor") prevents N+1 (create 5 AuditLog rows with distinct actors; assert query count <= 3 for list_audit_logs_for_org).
  </action>
  <verify>
    <automated>pytest apps/common/tests/test_audit_log_selectors.py -x -q</automated>
  </verify>
  <done>
    All 7+ test cases pass. Query count assertion for N+1 prevention passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Org Admin → Selector | Org ID comes from authenticated user; selector enforces org isolation |
| Staff Admin → Selector | Staff two-step scope enforces shop and Scope=SHOP constraints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Information Disclosure | list_audit_logs_for_org | mitigate | Always filter by organisation_id from authenticated user, never from request param |
| T-21-02 | Information Disclosure | list_audit_logs_for_staff | mitigate | Two-step scope: materialise only entity_ids from accessible shops and SHOP-scope items before querying AuditLog |
| T-21-SC | Tampering | No package installs in this plan | accept | No new packages — all dependencies already present |
</threat_model>

<verification>
pytest apps/common/tests/test_audit_log_selectors.py -x -q
python manage.py makemigrations --check --dry-run
</verification>

<success_criteria>
- apps/common/selectors/__init__.py and apps/common/selectors/audit_logs.py exist and are importable
- list_audit_logs_for_org returns only review+action_item events (excludes shop_sync)
- list_audit_logs_for_staff applies the two-step entity_ids scope
- AuditLogReadSerializer excludes before_data; includes actor_name (str or None)
- AuditLogCursorPagination.ordering = ("-created_at", "id")
- AuditLogFilterSet has entity_type, actor, date_from, date_to, shop filters
- All selector tests pass; query count test confirms no N+1
- No new migrations required (no model changes)
</success_criteria>

<output>
Create .planning/phases/21-audit-log-viewer/21-01-SUMMARY.md when done
</output>
