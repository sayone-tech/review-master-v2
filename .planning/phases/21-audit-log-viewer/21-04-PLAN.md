---
phase: 21-audit-log-viewer
plan: 04
type: execute
wave: 2
depends_on:
  - "21-01"
  - "21-02"
files_modified:
  - frontend/src/entrypoints/audit-log.tsx
  - frontend/src/widgets/audit-log/types.ts
  - frontend/src/widgets/audit-log/utils.ts
  - frontend/src/widgets/audit-log/api.ts
  - frontend/src/widgets/audit-log/useAuditLog.ts
  - frontend/src/widgets/audit-log/TypePill.tsx
  - frontend/src/widgets/audit-log/AuditLogFilters.tsx
  - frontend/src/widgets/audit-log/AuditLogTable.tsx
  - frontend/src/widgets/audit-log/AuditLogWidget.tsx
  - frontend/vite.config.ts
autonomous: true
requirements:
  - REQ-07
  - REQ-08

must_haves:
  truths:
    - "Audit log table renders with 5 columns: Date/Time, Actor, Type, Action, Details (D-15)"
    - "Filter bar has Type, Date Range (with 7d/30d/90d/Custom presets), and Actor dropdowns (D-16)"
    - "Default state on load: 30d preset active, all other filters at All (UI-SPEC)"
    - "Expanding a row shows after_data JSON; only one row expanded at a time (D-15)"
    - "TypePill renders Reply (blue) for review entries, Action Item (amber) for action_item (UI-SPEC §7)"
    - "Empty state and error state match UI-SPEC copy (D-17, D-18)"
    - "Cursor pagination Next/Previous navigation works; page-size dropdown resets cursor (UI-SPEC §9)"
    - "Filter Apply writes URL params; widget reads URL params on mount (D-16)"
    - "action_item.merged expanded panel renders merged_ids array (not count) from after_data (RESEARCH.md Pitfall 5)"
    - "Vite entrypoint audit-log is registered in vite.config.ts (D-14)"
  artifacts:
    - path: frontend/src/entrypoints/audit-log.tsx
      provides: "React mount for #audit-log-root"
    - path: frontend/src/widgets/audit-log/types.ts
      provides: "AuditLogRow interface, ACTION_LABEL map, FilterParams type"
      contains: "ACTION_LABEL"
    - path: frontend/src/widgets/audit-log/utils.ts
      provides: "formatRelativeDate function (copied from ReviewTable)"
    - path: frontend/src/widgets/audit-log/api.ts
      provides: "fetchAuditLogs(params, cursor?) -> Promise"
    - path: frontend/src/widgets/audit-log/useAuditLog.ts
      provides: "useAuditLog hook with cursor state, filters, loading, error"
    - path: frontend/src/widgets/audit-log/TypePill.tsx
      provides: "TypePill component for entity_type display"
    - path: frontend/src/widgets/audit-log/AuditLogFilters.tsx
      provides: "Filter bar with draft-then-apply pattern"
    - path: frontend/src/widgets/audit-log/AuditLogTable.tsx
      provides: "DataTable wrapper with expandable rows"
    - path: frontend/src/widgets/audit-log/AuditLogWidget.tsx
      provides: "Widget root composing all sub-components"
    - path: frontend/vite.config.ts
      provides: "audit-log entrypoint registered"
      contains: '"audit-log"'
  key_links:
    - from: frontend/src/entrypoints/audit-log.tsx
      to: frontend/src/widgets/audit-log/AuditLogWidget.tsx
      via: "import { AuditLogWidget }"
      pattern: "AuditLogWidget"
    - from: frontend/src/widgets/audit-log/AuditLogWidget.tsx
      to: frontend/src/widgets/audit-log/useAuditLog.ts
      via: "useAuditLog() hook"
      pattern: "useAuditLog"
    - from: frontend/src/widgets/audit-log/api.ts
      to: "/api/v1/audit-logs/"
      via: "fetch GET"
      pattern: "api/v1/audit-logs"
---

<objective>
Build the complete React widget for the Activity Log page: types, API client, cursor-state hook, filter bar, table with expandable rows, TypePill, and the Vite entrypoint. The widget reads the API built in Plans 21-01/02 and mounts into the template from Plan 21-03.

Purpose: Delivers the user-facing Activity Log page — the phase's primary user-visible deliverable.
Output: 9 new files in frontend/src/widgets/audit-log/ + frontend/src/entrypoints/audit-log.tsx + vite.config.ts update.
</objective>

<execution_context>
@/Users/renjith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/renjith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-audit-log-viewer/21-CONTEXT.md
@.planning/phases/21-audit-log-viewer/21-RESEARCH.md
@.planning/phases/21-audit-log-viewer/21-UI-SPEC.md
@.planning/phases/21-audit-log-viewer/21-01-SUMMARY.md
@.planning/phases/21-audit-log-viewer/21-02-SUMMARY.md
@.planning/phases/21-audit-log-viewer/21-03-SUMMARY.md
</context>

<interfaces>
API response shape (from AuditLogReadSerializer):
  {
    next: string | null,      // cursor URL or null
    previous: string | null,  // cursor URL or null
    results: AuditLogRow[]
  }
  AuditLogRow {
    id: string,
    created_at: string,       // ISO datetime
    entity_type: "review" | "action_item",
    entity_id: string,
    action: string,           // "reply_posted", "action_item.merged", etc.
    actor_id: number | null,
    actor_name: string | null,
    after_data: Record<string, unknown> | null
  }

NOTE on action_item.merged: after_data = {"merged_ids": [...]} — NO "count" key.
Read after_data.merged_ids.length in expanded panel if count is needed (RESEARCH.md Pitfall 5).

From frontend/src/widgets/data-table/DataTable.tsx (confirmed):
  interface Column<T> { key: string; header: string; skeletonWidth?: string; render: (row: T) => ReactNode }
  interface DataTableProps<T> {
    columns: Column<T>[];
    rows: T[];
    loading: boolean;
    skeletonRows?: number;   // default: 5
    renderExpanded?: (row: T) => ReactNode;
    getRowKey: (row: T) => string;
    renderRowActions?: (row: T) => ReactNode;   // omit for read-only table
    emptyState?: ReactNode;
  }
  renderExpanded colSpan = columns.length (no renderRowActions, no selection -> colSpan = 5)

From frontend/src/entrypoints/action-items-management.tsx — entrypoint pattern:
  function mount() {
    const root = document.getElementById("action-items-management-root");
    if (!root || root.dataset.mounted) return;
    root.dataset.mounted = "1";
    // read data-* and json_script
    createRoot(root).render(<StrictMode>...</StrictMode>);
  }
  mount();
  document.addEventListener("turbo:load", mount);

From frontend/vite.config.ts — input object:
  rollupOptions.input = {
    ...existing entries...,
    // ADD:
    "audit-log": resolve(__dirname, "src/entrypoints/audit-log.tsx"),
  }

From frontend/src/widgets/review-management/ReviewTable.tsx lines 37-49 — formatRelativeDate:
  function formatRelativeDate(iso: string): string {
    const d = new Date(iso);
    const diffMs = Date.now() - d.getTime();
    const diffDays = Math.floor(diffMs / (1000 * 60 * 60 * 24));
    if (diffDays < 1) return "today";
    if (diffDays === 1) return "1 day ago";
    if (diffDays < 30) return `${diffDays} days ago`;
    const diffMonths = Math.floor(diffDays / 30);
    if (diffMonths === 1) return "1 month ago";
    if (diffMonths < 12) return `${diffMonths} months ago`;
    const diffYears = Math.floor(diffMonths / 12);
    return diffYears === 1 ? "1 year ago" : `${diffYears} years ago`;
  }
  // Copy verbatim to audit-log/utils.ts — DO NOT import cross-widget

Tailwind tokens (confirmed in tailwind.config.js):
  text-ink, text-text, text-muted, text-faint, text-subtle
  bg-line-soft, border-line, border-line-soft
  bg-blue-tint, text-blue, bg-amber-tint, text-amber
  rounded-card (CSS var), yellow, yellow-hover
  font-mono (Geist Mono)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Types, utils, API client, and Vite config</name>
  <files>
    frontend/src/widgets/audit-log/types.ts,
    frontend/src/widgets/audit-log/utils.ts,
    frontend/src/widgets/audit-log/api.ts,
    frontend/vite.config.ts
  </files>
  <read_first>
    - frontend/vite.config.ts (full file — must append to rollupOptions.input, not replace)
    - frontend/src/widgets/review-management/ReviewTable.tsx lines 37-49 (formatRelativeDate to copy)
    - frontend/src/widgets/action-items/api.ts (fetch pattern to replicate — base URL, error handling)
    - frontend/src/widgets/action-items/types.ts (interface pattern for filter params)
  </read_first>
  <action>
    Create frontend/src/widgets/audit-log/types.ts:
    - AuditLogRow interface with fields: id (string), created_at (string), entity_type ("review" | "action_item"), entity_id (string), action (string), actor_id (number | null), actor_name (string | null), after_data (Record<string, unknown> | null)
    - ACTION_LABEL: Record<string, string> = {"reply_posted": "Reply posted", "reply_deleted": "Reply deleted", "reply_failed": "Reply failed", "action_item.created": "Action item created", "action_item.status_changed": "Status changed", "action_item.assigned": "Assigned", "action_item.note_added": "Note added", "action_item.merged": "Merged as duplicate"} — per D-20 and UI-SPEC Copywriting Contract
    - FilterParams interface: {entity_type?: string; actor?: string; date_from?: string; date_to?: string}
    - ActorOption interface: {id: number; full_name: string}
    - AuditLogResponse interface: {next: string | null; previous: string | null; results: AuditLogRow[]}

    Create frontend/src/widgets/audit-log/utils.ts:
    - Copy formatRelativeDate verbatim from ReviewTable.tsx lines 37-49
    - Export it as named export
    - Do NOT import from ReviewTable — copy the full implementation

    Create frontend/src/widgets/audit-log/api.ts:
    - fetchAuditLogs(params: FilterParams, cursor?: string): Promise<AuditLogResponse>
    - Builds URL from /api/v1/audit-logs/ with URLSearchParams for params; if cursor is provided, it is a full URL (from next/previous) — use cursor directly as the URL in that case
    - fetch() with credentials: "same-origin"
    - Throws on non-ok response (throw new Error(`HTTP ${resp.status}`))
    - Returns resp.json() as AuditLogResponse

    Update frontend/vite.config.ts:
    - Add "audit-log": resolve(__dirname, "src/entrypoints/audit-log.tsx") to rollupOptions.input object
    - All other existing entries must remain unchanged
  </action>
  <verify>
    <automated>cd /Users/renjith/Documents/Accounts/review-master/frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>
    - types.ts exports AuditLogRow, ACTION_LABEL, FilterParams, ActorOption, AuditLogResponse
    - utils.ts exports formatRelativeDate (copied, not imported)
    - api.ts exports fetchAuditLogs; handles cursor URL vs params-based URL
    - vite.config.ts includes "audit-log" entry in rollupOptions.input
    - TypeScript compilation passes for these files (no type errors)
  </done>
</task>

<task type="auto">
  <name>Task 2: Hook, TypePill, and entrypoint</name>
  <files>
    frontend/src/widgets/audit-log/useAuditLog.ts,
    frontend/src/widgets/audit-log/TypePill.tsx,
    frontend/src/entrypoints/audit-log.tsx
  </files>
  <read_first>
    - frontend/src/widgets/audit-log/types.ts (AuditLogRow, FilterParams, AuditLogResponse — just created)
    - frontend/src/widgets/audit-log/api.ts (fetchAuditLogs signature — just created)
    - frontend/src/entrypoints/action-items-management.tsx (mount pattern to replicate exactly)
    - frontend/src/widgets/action-items/ActionItemManagementWidget.tsx (hook usage pattern)
    - .planning/phases/21-audit-log-viewer/21-UI-SPEC.md (Section 5 cursor pagination state, Section 7 TypePill)
  </read_first>
  <action>
    Create frontend/src/widgets/audit-log/useAuditLog.ts:
    - Manages state: filters (FilterParams), cursor (string | null for next), prevCursors (string[] stack for back-navigation), rows (AuditLogRow[]), loading (boolean), error (string | null), pageSize (number, default 50)
    - On mount and when filters/cursor/pageSize change: call fetchAuditLogs(filters, cursor || undefined) with page_size param appended; set loading, update rows, next cursor, store prevCursors stack
    - URL param sync: on mount, read window.location.search params (entity_type, date_from, date_to, actor) as initial filters; if no date params, set default date_from = today − 30 days, date_to = today (ISO date strings, "YYYY-MM-DD")
    - goNext(): sets cursor = response.next (the full URL value); pushes current cursor to prevCursors stack
    - goPrev(): pops from prevCursors stack; sets cursor to popped value (or null for first page)
    - applyFilters(newFilters): updates filters state, resets cursor to null, clears prevCursors stack, writes params to window.history.pushState
    - resetFilters(): clears all filters, sets default 30d date range, resets cursor, calls pushState with empty params
    - changePageSize(n): sets pageSize = n, resets cursor to null
    - Returns: {rows, loading, error, filters, pageSize, hasNext: bool, hasPrev: bool, goNext, goPrev, applyFilters, resetFilters, changePageSize, expandedId, setExpandedId}
    - expandedId (string | null) state for tracking which row is expanded (only one at a time per UI-SPEC §8)

    Create frontend/src/widgets/audit-log/TypePill.tsx:
    - Props: {entityType: "review" | "action_item"}
    - TYPE_PILL map: {review: {label: "Reply", bg: "bg-blue-tint", text: "text-blue"}, action_item: {label: "Action Item", bg: "bg-amber-tint", text: "text-amber"}}
    - Renders: <span className={`inline-flex items-center whitespace-nowrap text-[12px] font-semibold rounded-full px-2 py-1 ${pill.bg} ${pill.text}`}>{pill.label}</span>
    - If entityType not in map, render raw entityType string in text-muted

    Create frontend/src/entrypoints/audit-log.tsx:
    - Follows action-items-management.tsx pattern exactly
    - root = document.getElementById("audit-log-root")
    - if (!root || root.dataset.mounted) return; root.dataset.mounted = "1"
    - userRole: string = root.dataset.userRole ?? "STAFF_ADMIN"
    - actors: ActorOption[] = JSON.parse(document.getElementById("audit-log-actors-data")?.textContent ?? "[]")
    - createRoot(root).render(<StrictMode><AuditLogWidget userRole={userRole} actors={actors} /></StrictMode>)
    - mount(); document.addEventListener("turbo:load", mount)
  </action>
  <verify>
    <automated>cd /Users/renjith/Documents/Accounts/review-master/frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>
    - useAuditLog.ts hook compiles without TypeScript errors
    - TypePill.tsx renders correct pill for "review" (blue) and "action_item" (amber)
    - audit-log.tsx entrypoint reads #audit-log-root, data-user-role, and audit-log-actors-data json_script
    - TypeScript compilation passes for all three files
  </done>
</task>

<task type="auto">
  <name>Task 3: AuditLogFilters, AuditLogTable, AuditLogWidget</name>
  <files>
    frontend/src/widgets/audit-log/AuditLogFilters.tsx,
    frontend/src/widgets/audit-log/AuditLogTable.tsx,
    frontend/src/widgets/audit-log/AuditLogWidget.tsx
  </files>
  <read_first>
    - frontend/src/widgets/audit-log/useAuditLog.ts (hook return shape — just created)
    - frontend/src/widgets/audit-log/types.ts (AuditLogRow, ACTION_LABEL, ActorOption — just created)
    - frontend/src/widgets/audit-log/utils.ts (formatRelativeDate — just created)
    - frontend/src/widgets/audit-log/TypePill.tsx (just created)
    - frontend/src/widgets/data-table/DataTable.tsx (Column interface, DataTableProps, renderExpanded prop)
    - frontend/src/widgets/action-items/ActionItemFilters.tsx (filter bar pattern — selectCls, label style, Apply/Reset buttons)
    - frontend/src/widgets/action-items/ActionItemManagementWidget.tsx (widget root composition pattern)
    - .planning/phases/21-audit-log-viewer/21-UI-SPEC.md (Sections 5-12, Copywriting Contract, Interaction Contract)
  </read_first>
  <action>
    Create frontend/src/widgets/audit-log/AuditLogFilters.tsx:
    - Props: {filters: FilterParams; actors: ActorOption[]; onApply: (f: FilterParams) => void; onReset: () => void; hasActiveFilters: boolean}
    - Draft state: local useState<FilterParams> initialized from props.filters
    - Date preset state: "7d" | "30d" | "90d" | "Custom" — default "30d" on first render (if no URL params, the hook defaults to 30d so filters.date_from will match)
    - Layout: bg-white border border-line rounded-xl p-4 mb-4; 4-column grid: minmax(0,1fr) minmax(0,1.4fr) minmax(0,1fr) auto; gap-4; items-end
    - Type filter: <select> with label "Type", Tag lucide icon (15px), options: "All types" (value=""), "Replies" (value="review"), "Action Items" (value="action_item")
    - Date Range filter: preset row (7d/30d/90d/Custom buttons) + dual date inputs (Calendar lucide icon, em dash separator). Presets 7d/30d/90d immediately apply (call onApply); Custom preset enables manual date input + Apply button
    - Preset active style: bg-black text-white rounded-sm; inactive: bg-white text-muted border border-line rounded-sm; size: px-2 py-1 text-[12px] font-semibold
    - Actor filter: <select> with label "Actor", Users lucide icon (15px), options: "All actors" (value=""), "System" (value="system"), then actors sorted by full_name (value=actor.id.toString())
    - Apply button: bg-yellow text-black border border-yellow-hover text-[14px] font-semibold px-4 py-2 rounded-[10px] hover:bg-yellow-hover — calls onApply(draft)
    - Reset button (conditional on hasActiveFilters): dashed border text-[14px] px-4 py-2 rounded-[10px] — calls onReset()
    - Copywriting exactly per UI-SPEC Copywriting Contract

    Create frontend/src/widgets/audit-log/AuditLogTable.tsx:
    - Props: {rows: AuditLogRow[]; loading: boolean; error: string | null; onRetry: () => void; hasActiveFilters: boolean; onClearFilters: () => void; expandedId: string | null; onExpand: (id: string | null) => void}
    - Uses DataTable<AuditLogRow> from ../data-table/DataTable
    - 5 columns per UI-SPEC Section 6: created_at (160px skeleton), actor_name (100px), entity_type (60px), action (140px), details (24px)
    - created_at cell: <time dateTime={row.created_at} title={new Date(row.created_at).toLocaleString()}>{formatRelativeDate(row.created_at)}</time>
    - actor_name cell: row.actor_name or <span className="text-muted italic">System</span>
    - entity_type cell: <TypePill entityType={row.entity_type} />
    - action cell: ACTION_LABEL[row.action] ?? <span className="text-muted">{row.action}</span>
    - details cell: caret button (ChevronRight collapsed / ChevronDown expanded), aria-expanded, aria-label="Show details" / "Hide details", toggles expandedId via onExpand
    - renderExpanded: returns expanded panel — <pre className="text-[12px] font-mono text-text whitespace-pre-wrap break-all leading-relaxed max-h-48 overflow-y-auto">{JSON.stringify(row.after_data, null, 2)}</pre> — renders raw JSON, does NOT extract count (per RESEARCH.md Pitfall 5)
    - Expanded panel: <tr className="bg-[#FAFAFA] border-b border-line"><td colSpan={5} className="px-4 py-3">...</td></tr>
    - Empty state (no rows, no active filters): ClipboardList icon + "No activity logged yet" copy per UI-SPEC Section 11
    - Empty state (filters active): Search icon + "No activity matches your filters" + "Clear filters" button per UI-SPEC Section 11
    - Error state: AlertCircle icon + "Could not load activity log" + Retry button per UI-SPEC Section 12
    - skeletonRows={8} passed to DataTable
    - <caption className="sr-only">Activity log</caption> on the inner table
    - Table accessibility: th scope="col", aria attributes on expand buttons per UI-SPEC Accessibility Contract

    Create frontend/src/widgets/audit-log/AuditLogWidget.tsx:
    - Props: {userRole: string; actors: ActorOption[]}
    - Uses useAuditLog() hook
    - Layout: <div className="space-y-4">:
        1. <h1 className="text-[20px] font-semibold text-ink">Activity Log</h1>
        2. <AuditLogFilters /> wired to hook's applyFilters, resetFilters, filters
        3. <div className="border border-line rounded-card overflow-hidden">:
           - <AuditLogTable /> wired to hook's rows, loading, error
           - Pagination footer: <nav aria-label="Pagination" className="flex items-center justify-between px-4 py-3 border-t border-line bg-[#FBFBFB] text-[12px] text-muted">
             Left: "Rows:" label + <select> (options 10, 25, 50, value=pageSize, onChange=changePageSize, aria-label="Rows per page")
             Right: Previous button (disabled if !hasPrev, aria-label="Previous page"), Next button (disabled if !hasNext, aria-label="Next page")
             Button style: w-8 h-8 rounded-sm bg-white border border-line flex items-center justify-center disabled:opacity-40 disabled:cursor-not-allowed enabled:hover:bg-line-soft

    hasActiveFilters: boolean = computed from whether any filter differs from the 30d default state (entity_type != "", actor != "", or custom date range not matching today-30d to today)
  </action>
  <verify>
    <automated>cd /Users/renjith/Documents/Accounts/review-master/frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | head -40</automated>
  </verify>
  <done>
    - AuditLogFilters.tsx renders Type, Date Range (with presets), Actor filters and Apply/Reset buttons
    - AuditLogTable.tsx renders 5-column DataTable with expandable JSON detail panel; empty and error states per spec
    - AuditLogWidget.tsx composes all sub-components with pagination footer; TypeScript compiles without errors
    - Expanded action_item.merged rows show {"merged_ids": [...]} JSON (no undefined count)
    - TypeScript compilation passes for all three files
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/v1/audit-logs/ | credentials: "same-origin"; CSRF not required for GET |
| json_script → React | Django escapes output; React reads as parsed JSON (no innerHTML eval) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-09 | Information Disclosure | api.ts fetchAuditLogs | mitigate | credentials: "same-origin" ensures only authenticated browser sessions can call the endpoint |
| T-21-10 | Tampering | useAuditLog URL params | mitigate | Filter params written to URL via pushState only; API validates all params via AuditLogFilterSet server-side |
| T-21-11 | Information Disclosure | Expanded after_data panel | accept | after_data is org-scoped data the logged-in user is authorized to see; JSON.stringify renders raw payload without server logic |
| T-21-SC | Tampering | No package installs in this plan | accept | No new packages — all frontend dependencies already present |
</threat_model>

<verification>
cd /Users/renjith/Documents/Accounts/review-master/frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | head -40
grep "audit-log" /Users/renjith/Documents/Accounts/review-master/frontend/vite.config.ts
</verification>

<success_criteria>
- TypeScript compilation passes for all 9 new files with no type errors
- ACTION_LABEL map in types.ts has all 8 action-to-label entries from D-20
- formatRelativeDate in utils.ts is a copy (not an import from ReviewTable)
- api.ts calls GET /api/v1/audit-logs/ with credentials: "same-origin"
- useAuditLog.ts defaults to 30d date range on mount when no URL params present
- AuditLogWidget.tsx composes filters, table, and cursor pagination footer
- Expanded row renders JSON.stringify(row.after_data) — not after_data.count
- vite.config.ts has "audit-log" entry in rollupOptions.input
</success_criteria>

<output>
Create .planning/phases/21-audit-log-viewer/21-04-SUMMARY.md when done
</output>
