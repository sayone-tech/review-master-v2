---
phase: 21-audit-log-viewer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/common/views.py
  - apps/common/urls.py
  - config/urls.py
  - config/settings/base.py
  - apps/common/tests/test_audit_log_api.py
autonomous: true
requirements:
  - REQ-01
  - REQ-02
  - REQ-03
  - REQ-04
  - REQ-05
  - REQ-09

must_haves:
  truths:
    - "GET /api/v1/audit-logs/ returns 200 for ORG_ADMIN with cursor-paginated results (D-04, D-05, D-08)"
    - "Superadmin receives 403; unauthenticated receives 401 (D-06)"
    - "Staff Admin receives only entries from accessible shops and SHOP-scope items (D-03)"
    - "Throttle scope audit_log_list enforced at 120/minute (D-09)"
    - "entity_type, actor, date_from, date_to, shop filters work correctly (D-07)"
    - "Response has next/previous cursor URLs, no before_data in results (D-11)"
    - "Query count is fixed regardless of result size (CLAUDE.md §6.9)"
  artifacts:
    - path: apps/common/views.py
      provides: "AuditLogViewSet appended after existing views"
      contains: "class AuditLogViewSet"
    - path: apps/common/urls.py
      provides: "audit_log_view Django template view URL at /admin/org/activity-log/"
      contains: "audit_log_view"
    - path: config/urls.py
      provides: "router.register for AuditLogViewSet at api/v1/audit-logs"
      contains: "audit-log"
    - path: config/settings/base.py
      provides: "audit_log_list throttle rate"
      contains: '"audit_log_list": "120/minute"'
  key_links:
    - from: apps/common/views.py
      to: apps/common/selectors/audit_logs.py
      via: "get_queryset() delegates to selector"
      pattern: "list_audit_logs_for_org|list_audit_logs_for_staff"
    - from: config/urls.py
      to: apps/common/views.py
      via: "router.register"
      pattern: "AuditLogViewSet"
---

<objective>
Wire the AuditLogViewSet into the URL routing, add the throttle rate setting, and write the full API test suite. This plan runs in Wave 1 alongside Plan 21-01 (no file overlap), using the contracts defined there.

Purpose: Delivers a working GET /api/v1/audit-logs/ endpoint with all scoping, filtering, pagination, and throttle enforcement. Tests prove REQ-01 through REQ-05 before the frontend is built.
Output: AuditLogViewSet in apps/common/views.py, URL registration, settings update, API test file.
</objective>

<execution_context>
@/Users/renjith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/renjith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-audit-log-viewer/21-CONTEXT.md
@.planning/phases/21-audit-log-viewer/21-RESEARCH.md
</context>

<interfaces>
From apps/common/views.py — existing content (append only, do not replace):
  - ScalarDocsView, healthz, readyz, home, showcase, page_not_found, server_error functions
  - Imports at top: from django.core.cache import cache, from django.db import connection, etc.

From apps/common/permissions.py — IsOrgScoped:
  class IsOrgScoped(BasePermission):
      def has_permission(self, request, view) -> bool:
          # Allows ORG_ADMIN or STAFF_ADMIN with organisation_id set
      def has_object_permission(...) -> bool:
          # Checks obj.organisation_id == user.organisation_id

From config/urls.py — existing router registrations pattern:
  router = SimpleRouter()
  router.register(r"api/v1/reviews", ReviewViewSet, basename="review")
  # Add: router.register(r"api/v1/audit-logs", AuditLogViewSet, basename="audit-log")

From config/settings/base.py — existing DEFAULT_THROTTLE_RATES:
  "DEFAULT_THROTTLE_RATES": {
      "user": "1000/hour",
      "anon": "100/hour",
      "login": "10/15min",
      "review_reply": "30/minute",
      "generate_reply": "10/minute",
  }
  # Add: "audit_log_list": "120/minute"

From apps/common/selectors/audit_logs.py (Plan 21-01 output):
  list_audit_logs_for_org(*, organisation_id: int) -> QuerySet[AuditLog]
  list_audit_logs_for_staff(*, organisation_id: int, user) -> QuerySet[AuditLog]

From apps/common/serializers.py (Plan 21-01 output):
  class AuditLogReadSerializer(ModelSerializer)
    Meta.fields = ["id", "created_at", "entity_type", "entity_id", "action",
                   "actor_id", "actor_name", "after_data"]

From apps/common/pagination.py (Plan 21-01 output):
  class AuditLogCursorPagination(CursorPagination):
      page_size = 50; ordering = ("-created_at", "id")

From apps/common/filters.py (Plan 21-01 output):
  class AuditLogFilterSet(django_filters.FilterSet)
      fields: entity_type, actor, date_from, date_to, shop

From apps/accounts/models.py:
  User.Role.STAFF_ADMIN, User.Role.ORG_ADMIN, User.Role.SUPERADMIN
  user.organisation_id (FK to Organisation)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Append AuditLogViewSet + audit_log_view to apps/common/views.py</name>
  <files>apps/common/views.py</files>
  <read_first>
    - apps/common/views.py (full file — read all existing content before appending)
    - apps/common/selectors/audit_logs.py (selector function signatures)
    - apps/common/serializers.py (AuditLogReadSerializer)
    - apps/common/pagination.py (AuditLogCursorPagination)
    - apps/common/filters.py (AuditLogFilterSet)
    - apps/common/permissions.py (IsOrgScoped)
    - apps/accounts/models.py (User.Role values)
    - templates/reviews/review_list.html (template view pattern — review_list function)
    - apps/reviews/views.py (review_list Django view for actors query pattern)
  </read_first>
  <action>
    Append to apps/common/views.py (do NOT replace existing content). Add the following after the last existing function/class:

    New imports to add at the top of the file (alongside existing imports):
    - from rest_framework import mixins, viewsets
    - from rest_framework.throttling import ScopedRateThrottle
    - from django_filters.rest_framework import DjangoFilterBackend
    - from apps.common.filters import AuditLogFilterSet
    - from apps.common.models import AuditLog
    - from apps.common.pagination import AuditLogCursorPagination
    - from apps.common.permissions import IsOrgScoped
    - from apps.common.selectors.audit_logs import list_audit_logs_for_org, list_audit_logs_for_staff
    - from apps.common.serializers import AuditLogReadSerializer
    - from apps.accounts.models import User

    AuditLogViewSet class (per D-04, RESEARCH.md Pattern 3):
    - Inherits from mixins.ListModelMixin, viewsets.GenericViewSet
    - permission_classes = [IsOrgScoped]  # noqa: RUF012
    - serializer_class = AuditLogReadSerializer
    - pagination_class = AuditLogCursorPagination
    - filter_backends = [DjangoFilterBackend]  # noqa: RUF012
    - filterset_class = AuditLogFilterSet
    - throttle_scope = "audit_log_list"
    - throttle_classes = [ScopedRateThrottle]  # noqa: RUF012
    - queryset = AuditLog.objects.none()  (required by DRF router introspection)
    - get_queryset(self): reads user = self.request.user, org_id = getattr(user, "organisation_id", None); if org_id is None return AuditLog.objects.none(); if getattr(user, "role", None) == User.Role.STAFF_ADMIN return list_audit_logs_for_staff(organisation_id=org_id, user=user); else return list_audit_logs_for_org(organisation_id=org_id)

    audit_log_view Django template view function:
    - @login_required decorator (import from django.contrib.auth.decorators)
    - Signature: def audit_log_view(request: HttpRequest) -> HttpResponse
    - Queries distinct actors for the org: AuditLog.objects.filter(organisation_id=request.user.organisation_id).filter(actor__isnull=False).select_related("actor").values("actor_id", "actor__full_name").distinct().order_by("actor__full_name") — converts to list of {"id": actor_id, "full_name": actor__full_name}
    - Renders "org-admin/audit-log.html" with context: {"actors_json": actors_list, "user_role": request.user.role, "page_title": "Activity Log"}

    CRITICAL: Use json.dumps for actors_json in context if the template uses json_script tag; otherwise pass the raw Python list and let the template json_script handle serialization. Check how review_list view passes shops_json (it passes the Python object, not json.dumps).
  </action>
  <verify>
    <automated>python manage.py check --deploy 2>&1 | head -20</automated>
  </verify>
  <done>
    - apps/common/views.py contains AuditLogViewSet class and audit_log_view function
    - python manage.py check passes without errors related to common.views
    - Imports resolve: no ImportError on startup
  </done>
</task>

<task type="auto">
  <name>Task 2: URL registration + settings update</name>
  <files>apps/common/urls.py, config/urls.py, config/settings/base.py</files>
  <read_first>
    - apps/common/urls.py (existing urlpatterns — must append, not replace)
    - config/urls.py (existing router.register calls and import block)
    - config/settings/base.py (DEFAULT_THROTTLE_RATES dict — exact keys and format)
  </read_first>
  <action>
    Update apps/common/urls.py: add to the existing urlpatterns list:
    - from apps.common.views import audit_log_view (add to existing import line)
    - path("admin/org/activity-log/", audit_log_view, name="audit_log_list") — added to urlpatterns list

    Update config/urls.py:
    - Add import: from apps.common.views import AuditLogViewSet (alongside existing imports at top)
    - Add: router.register(r"api/v1/audit-logs", AuditLogViewSet, basename="audit-log") — add after the existing router.register(r"api/v1/reviews", ...) line

    Update config/settings/base.py:
    - In DEFAULT_THROTTLE_RATES dict, add: "audit_log_list": "120/minute" — placed after "generate_reply": "10/minute" entry (per D-09)

    All edits must preserve existing content exactly. Do not reorder, remove, or reformat existing entries.
  </action>
  <verify>
    <automated>python manage.py check && python manage.py show_urls 2>/dev/null | grep audit-log || python -c "import django; django.setup(); from django.urls import reverse; print(reverse('audit-log-list'))"</automated>
  </verify>
  <done>
    - GET /api/v1/audit-logs/ is a registered route (basename "audit-log", action "list")
    - /admin/org/activity-log/ is a registered Django template view route (name "audit_log_list")
    - DEFAULT_THROTTLE_RATES["audit_log_list"] == "120/minute" in base.py
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: API test suite</name>
  <files>apps/common/tests/test_audit_log_api.py</files>
  <read_first>
    - apps/common/tests/conftest.py (existing conftest fixtures — api_client, org_admin_client, etc.)
    - apps/common/tests/test_views.py (existing API test patterns for this app)
    - apps/reviews/tests/factories.py (AuditLogFactory, UserFactory, OrganisationFactory, ShopFactory)
    - apps/action_items/tests/factories.py (ActionItemFactory — for Staff scoping tests)
  </read_first>
  <behavior>
    - test_list_audit_logs_org_admin: GET /api/v1/audit-logs/ as ORG_ADMIN returns 200 with results list
    - test_cursor_pagination: response has "next" and "previous" keys (cursor pagination)
    - test_list_query_count: 50 AuditLog rows with distinct actors; CaptureQueriesContext; assert len(captured_queries) <= 5
    - test_staff_scope: Staff user can only see entries from their accessible shop; entry from inaccessible shop is absent
    - test_staff_cannot_see_brand_items: AuditLog for brand-scope action item absent from Staff response
    - test_filter_entity_type: ?entity_type=review returns only review-type rows; action_item rows absent
    - test_filter_date_range: ?date_from=YYYY-MM-DD&date_to=YYYY-MM-DD returns only rows in range
    - test_filter_actor_system: ?actor=system returns only entries where actor is null
    - test_serializer_fields: response result has actor_name, no before_data key
    - test_throttle_scope: viewset.throttle_scope == "audit_log_list"
    - test_superadmin_forbidden: GET /api/v1/audit-logs/ as SUPERADMIN returns 403
    - test_unauthenticated_returns_401: GET /api/v1/audit-logs/ without auth returns 401
  </behavior>
  <action>
    Create apps/common/tests/test_audit_log_api.py. Use pytest + pytest-django.

    Import AuditLogFactory from apps.reviews.tests.factories (do NOT create a duplicate).

    Each test uses @pytest.mark.django_db. Use DRF's APIClient or the existing conftest api_client fixture. Authenticate users with force_login or force_authenticate.

    For test_list_query_count: use django.test.utils.CaptureQueriesContext with django.db.connection. Create 50 AuditLog rows with distinct actor users via AuditLogFactory. Assert len(ctx.captured_queries) <= 5.

    For test_staff_scope: create two shops, a StaffAccessScope linking the user to shop_A only (import from apps.accounts.models or apps.shops.models as appropriate — check existing Staff tests for the right model), create AuditLog for review in shop_A and shop_B, verify only shop_A entry appears.

    For test_throttle_scope: import AuditLogViewSet from apps.common.views; assert AuditLogViewSet.throttle_scope == "audit_log_list".

    Use pytest-style assertions (assert x == y). No unittest.TestCase.
  </action>
  <verify>
    <automated>pytest apps/common/tests/test_audit_log_api.py -x -q</automated>
  </verify>
  <done>
    All 12 test cases pass. Query count test confirms fixed overhead regardless of result size.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → AuditLogViewSet | Untrusted query params; IsOrgScoped enforces auth + role; django-filter validates param values |
| AuditLogViewSet → Selectors | org_id sourced from authenticated user, never from request params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Elevation of Privilege | AuditLogViewSet | mitigate | IsOrgScoped denies Superadmin (role not in ORG_ADMIN, STAFF_ADMIN) |
| T-21-04 | Information Disclosure | AuditLogViewSet.get_queryset | mitigate | org_id from request.user.organisation_id; not from query params; selector enforces isolation |
| T-21-05 | Denial of Service | GET /api/v1/audit-logs/ | mitigate | ScopedRateThrottle at 120/minute; CursorPagination.max_page_size=100 caps page |
| T-21-06 | Information Disclosure | audit_log_view actors query | mitigate | actors query filtered by organisation_id from authenticated user |
| T-21-SC | Tampering | No package installs in this plan | accept | No new packages — all dependencies already present |
</threat_model>

<verification>
pytest apps/common/tests/test_audit_log_api.py -x -q
python manage.py check
python manage.py makemigrations --check --dry-run
</verification>

<success_criteria>
- GET /api/v1/audit-logs/ returns 200 for ORG_ADMIN
- GET /api/v1/audit-logs/ returns 403 for SUPERADMIN
- Response body has "next", "previous", "results" keys (cursor pagination shape)
- Results do not contain "before_data" field
- Staff Admin sees only accessible-shop entries
- "audit_log_list": "120/minute" present in DEFAULT_THROTTLE_RATES
- All 12 API tests pass
- No new migrations
</success_criteria>

<output>
Create .planning/phases/21-audit-log-viewer/21-02-SUMMARY.md when done
</output>
