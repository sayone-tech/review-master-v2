---
phase: 21-audit-log-viewer
plan: 03
type: execute
wave: 2
depends_on:
  - "21-01"
  - "21-02"
files_modified:
  - templates/org-admin/audit-log.html
  - templates/partials/sidebar_org.html
autonomous: true
requirements:
  - REQ-06
  - REQ-07
  - REQ-08

must_haves:
  truths:
    - "Navigating to /admin/org/activity-log/ renders audit-log.html with #audit-log-root mount point (D-13)"
    - "Sidebar shows Activity Log nav item for both ORG_ADMIN and STAFF_ADMIN, with clock icon (D-12)"
    - "Template passes actors_json and user_role to the React widget via json_script (D-13)"
    - "Vite entrypoint audit-log.tsx is referenced correctly in the template (D-14)"
  artifacts:
    - path: templates/org-admin/audit-log.html
      provides: "Django template extending base_org.html; mounts #audit-log-root"
      contains: "audit-log-root"
    - path: templates/partials/sidebar_org.html
      provides: "Activity Log nav item appended after the last {% endif %} in the ul"
      contains: "activity-log"
  key_links:
    - from: templates/org-admin/audit-log.html
      to: frontend/src/entrypoints/audit-log.tsx
      via: "{% vite_asset %} tag"
      pattern: "audit-log.tsx"
    - from: templates/partials/sidebar_org.html
      to: templates/partials/_nav_item.html
      via: "{% include %} with href=/admin/org/activity-log/"
      pattern: "activity-log"
---

<objective>
Create the Django template for the Activity Log page and add the sidebar nav item. These are server-side rendering changes that the React widget (Plan 21-04) depends on for its mount point and bootstrap data.

Purpose: Provides the HTML scaffold the React widget mounts into. The sidebar nav item makes the page discoverable for both ORG_ADMIN and STAFF_ADMIN users.
Output: templates/org-admin/audit-log.html (new directory + file), sidebar_org.html modified.
</objective>

<execution_context>
@/Users/renjith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/renjith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-audit-log-viewer/21-CONTEXT.md
@.planning/phases/21-audit-log-viewer/21-RESEARCH.md
@.planning/phases/21-audit-log-viewer/21-UI-SPEC.md
@.planning/phases/21-audit-log-viewer/21-01-SUMMARY.md
@.planning/phases/21-audit-log-viewer/21-02-SUMMARY.md
</context>

<interfaces>
From templates/reviews/review_list.html — template pattern to replicate:
  {% extends "base_org.html" %}
  {% load static django_vite %}
  {% block content %}
    <div id="review-management-root" data-current-user-role="{{ current_user_role }}"></div>
    {{ shops_json|json_script:"review-shops-data" }}
  {% endblock %}
  {% block extra_js %}
    {% vite_asset 'src/entrypoints/review-management.tsx' %}
  {% endblock %}

From templates/partials/sidebar_org.html — current last items in the <ul role="list">:
  (Inside the {% if user.role != "STAFF_ADMIN" %} block, the last item is Reports)
  After the closing {% endif %} of that block, the </ul> closes
  The Activity Log nav item is appended INSIDE the <ul> AFTER the closing {% endif %}
  of the Org-Admin-only block, before </ul>

From templates/partials/_nav_item.html — include syntax (confirmed in RESEARCH.md Pattern 8):
  {% include "partials/_nav_item.html" with href="..." icon="..." label="..." %}

From apps/common/views.py (Plan 21-02 output) — audit_log_view context:
  context = {"actors_json": actors_list, "user_role": request.user.role, "page_title": "Activity Log"}
  Template receives: actors_json (Python list), user_role (string), page_title (string)

From RESEARCH.md Pattern 7 — audit-log.html structure:
  {{ actors_json|json_script:"audit-log-actors-data" }}
  <div id="audit-log-root" data-user-role="{{ user_role }}"></div>
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create templates/org-admin/ directory and audit-log.html</name>
  <files>templates/org-admin/audit-log.html</files>
  <read_first>
    - templates/reviews/review_list.html (pattern to replicate — extends, load, blocks)
    - templates/base_org.html (check block names: content, extra_js, page_title if used)
    - templates/partials/_nav_item.html (to understand how active route detection works)
    - apps/common/views.py (audit_log_view context dict — actors_json, user_role, page_title)
  </read_first>
  <action>
    Create the directory templates/org-admin/ (it does not exist).
    Create templates/org-admin/audit-log.html with:

    - {% extends "base_org.html" %}
    - {% load static django_vite %}
    - {% block page_title %}Activity Log{% endblock %} — only if base_org.html has a page_title block; otherwise omit
    - {% block content %} containing:
        1. <div id="audit-log-root" data-user-role="{{ user_role }}"></div>
        2. {{ actors_json|json_script:"audit-log-actors-data" }}
    - {% block extra_js %} containing:
        {% vite_asset 'src/entrypoints/audit-log.tsx' %}

    CRITICAL: Check base_org.html to confirm the exact block names used (content, extra_js). Do not invent block names that don't exist in the base template.

    CRITICAL: The mount point div id must be exactly "audit-log-root" — this is what the entrypoint reads with document.getElementById("audit-log-root").

    The actors_json context variable is a Python list (not pre-serialized JSON string) passed from audit_log_view. The json_script tag handles serialization. The element id "audit-log-actors-data" matches what the entrypoint reads.
  </action>
  <verify>
    <automated>python -c "import django; django.setup(); from django.template.loader import get_template; t = get_template('org-admin/audit-log.html'); print('OK')"</automated>
  </verify>
  <done>
    - templates/org-admin/audit-log.html exists and is loadable by Django's template engine
    - Template contains id="audit-log-root", data-user-role="{{ user_role }}", json_script tag with id "audit-log-actors-data"
    - Template loads django_vite and references src/entrypoints/audit-log.tsx
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Activity Log nav item to sidebar_org.html</name>
  <files>templates/partials/sidebar_org.html</files>
  <read_first>
    - templates/partials/sidebar_org.html (full file — understand existing ul structure, the {% if user.role != "STAFF_ADMIN" %} block, where {% endif %} closes it, where </ul> appears)
    - templates/partials/_nav_item.html (confirm the include syntax and required params)
    - .planning/phases/21-audit-log-viewer/21-UI-SPEC.md (Section 1: Sidebar Nav Item spec)
  </read_first>
  <action>
    Modify templates/partials/sidebar_org.html to add the Activity Log nav item.

    Placement (per D-12 and UI-SPEC Section 1): Inside the <ul role="list">, AFTER the closing {% endif %} of the Org-Admin-only conditional block (the block containing Reports, Team, Templates, Regions), and BEFORE the closing </ul> tag.

    The current sidebar_org.html structure ends the Org-Admin block with:
        {% include "partials/_nav_item.html" with href="/admin/org/reports/" icon="bar-chart-2" label="Reports" %}
      {% endif %}
    </ul>

    Insert after the {% endif %} and before </ul>:
        {% include "partials/_nav_item.html" with href="/admin/org/activity-log/" icon="clock" label="Activity Log" %}

    NO role guard on this nav item — both ORG_ADMIN and STAFF_ADMIN can see and access the Activity Log. The API handles data scoping.

    Do not change any other part of the sidebar template.
  </action>
  <verify>
    <automated>grep -c "activity-log" /Users/renjith/Documents/Accounts/review-master/templates/partials/sidebar_org.html</automated>
  </verify>
  <done>
    - sidebar_org.html contains the Activity Log nav item include with href="/admin/org/activity-log/" and icon="clock"
    - The nav item appears after the closing {% endif %} of the Org-Admin-only block
    - No role guard wraps the new nav item
    - grep count for "activity-log" in sidebar_org.html == 1
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Template | audit_log_view is @login_required; no unauthenticated access to template |
| Template → Browser | actors_json uses Django's json_script which escapes for safe embedding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-07 | Information Disclosure | audit-log-actors-data json_script | mitigate | Django json_script tag HTML-escapes all values; actors list is sourced from organisation_id-filtered query |
| T-21-08 | Information Disclosure | audit_log_view @login_required | mitigate | @login_required redirects unauthenticated users to /accounts/login/ |
| T-21-SC | Tampering | No package installs in this plan | accept | No new packages — templates only |
</threat_model>

<verification>
python -c "import django; django.setup(); from django.template.loader import get_template; t = get_template('org-admin/audit-log.html'); print('Template loads OK')"
grep "activity-log" /Users/renjith/Documents/Accounts/review-master/templates/partials/sidebar_org.html
python manage.py check
</verification>

<success_criteria>
- templates/org-admin/audit-log.html exists and Django can load it without TemplateDoesNotExist error
- Template has id="audit-log-root" div with data-user-role attribute
- Template has json_script tag producing element with id="audit-log-actors-data"
- Template references {% vite_asset 'src/entrypoints/audit-log.tsx' %}
- sidebar_org.html contains Activity Log nav item with /admin/org/activity-log/ href and clock icon
- Nav item is outside the {% if user.role != "STAFF_ADMIN" %} conditional block
</success_criteria>

<output>
Create .planning/phases/21-audit-log-viewer/21-03-SUMMARY.md when done
</output>
