---
phase: 01-foundation
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/partials/sidebar.html
  - templates/partials/head.html
  - templates/base.html
  - templates/pages/showcase.html
autonomous: true
gap_closure: true
requirements:
  - DSYS-04
  - DSYS-05

must_haves:
  truths:
    - "Clicking the hamburger button on mobile causes the sidebar to slide in as a drawer overlay"
    - "The /__ui__/ showcase page mounts the React entrypoint and renders Modal, ConfirmModal, DataTable, and Toast demo buttons"
  artifacts:
    - path: "templates/partials/sidebar.html"
      provides: "Alpine sidebar that reacts to $store.nav.mobileOpen live store value"
      contains: "$store.nav.mobileOpen"
    - path: "templates/partials/head.html"
      provides: "Vite React Refresh preamble required for @vitejs/plugin-react in dev mode"
      contains: "vite_react_refresh"
    - path: "templates/base.html"
      provides: "extra_js block rendered after </main> for script injection without DOM ordering issues"
      contains: "block extra_js"
    - path: "templates/pages/showcase.html"
      provides: "vite_asset tag placed in extra_js block (not inside content block)"
      contains: "block extra_js"
  key_links:
    - from: "templates/partials/head.html"
      to: "src/entrypoints/showcase.tsx"
      via: "{% vite_react_refresh %} preamble enables React HMR module execution"
      pattern: "vite_react_refresh"
    - from: "hamburger button in topbar"
      to: "templates/partials/sidebar.html <aside>"
      via: "$store.nav.mobileOpen read directly (not via stale local copy)"
      pattern: "\\$store\\.nav\\.mobileOpen"
---

<objective>
Close two UAT gaps from 01-UAT.md:

**Gap A** — Mobile sidebar hamburger is non-functional. The `<aside>` in sidebar.html copies `$store.nav.mobileOpen` into a stale local Alpine variable at mount time, so store mutations from the hamburger button are never observed.

**Gap B** — React widgets (Modal, ConfirmModal, DataTable, Toast) never mount on `/__ui__/` because `{% vite_react_refresh %}` is absent from head.html. Without the React Refresh preamble, `@vitejs/plugin-react` aborts in dev mode before showcase.tsx executes. A secondary issue places the `{% vite_asset %}` tag inside `{% block content %}` instead of after `</main>`.

Purpose: Restore UAT tests 5, 9, 10, 11, and 12 to passing status.
Output: Four modified template files. No new files, no migration, no Python changes.
</objective>

<execution_context>
@/Users/renjith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/renjith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- Alpine store initialisation: $store.nav registered with { mobileOpen: false, ... }
     in src/entrypoints/app-shell.ts (or equivalent). The hamburger in topbar.html sets
     $store.nav.mobileOpen = true. Both are already correct — only sidebar.html reads
     the wrong value. -->
</context>

<interfaces>
<!-- Existing sidebar.html x-data (the broken line): -->
<!--   x-data="{ mobileOpen: $store.nav?.mobileOpen ?? false, collapsed: false }" -->
<!-- Existing :class binding (reads stale local): -->
<!--   :class="mobileOpen ? '!flex !fixed !w-[260px]' : ''" -->

<!-- Existing head.html Vite tags (missing react refresh): -->
<!--   {% vite_hmr_client %}                             present -->
<!--   {% vite_react_refresh %}                          MISSING — must be added here -->

<!-- base.html current structure (no extra_js slot): -->
<!--   <main>{% block content %}{% endblock %}</main>    must add block extra_js after </main> -->

<!-- showcase.html vite_asset current position: -->
<!--   {% vite_asset 'src/entrypoints/showcase.tsx' %}  inside {% block content %}  (wrong) -->
<!--   must move to: {% block extra_js %}...{% endblock %}                           (correct) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix Alpine mobile sidebar — read live store instead of stale local copy</name>
  <files>templates/partials/sidebar.html</files>
  <action>
In `templates/partials/sidebar.html`, on the opening `<aside>` tag:

1. Remove `mobileOpen` from the `x-data` object. Keep `collapsed: false` if present (or any other local state). The result should be:
   `x-data="{ collapsed: false }"`
   If no other local state exists, simplify to removing the attribute entirely and relying only on the store — but keep x-data if anything else uses it.

2. Change the `:class` binding from `"mobileOpen ? '...' : ''"` to read the live store:
   `:class="$store.nav?.mobileOpen ? '!flex !fixed !w-[260px]' : ''"`

Do NOT change any other part of the file (backdrop div already reads `$store.nav?.mobileOpen` correctly and must not be touched).

After the edit, the `<aside>` opening tag should look like:
```html
<aside
  x-data="{ collapsed: false }"
  x-cloak
  class="..."
  :class="$store.nav?.mobileOpen ? '!flex !fixed !w-[260px]' : ''"
  data-testid="sidebar"
  aria-label="Main navigation"
>
```
  </action>
  <verify>
    <automated>grep -n 'mobileOpen' templates/partials/sidebar.html</automated>
  </verify>
  <done>
The string `mobileOpen: $store.nav` no longer appears in sidebar.html (stale copy removed). Every remaining `mobileOpen` reference reads `$store.nav?.mobileOpen` directly. The backdrop div is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add vite_react_refresh to head.html, add extra_js block to base.html, move vite_asset in showcase.html</name>
  <files>
    templates/partials/head.html,
    templates/base.html,
    templates/pages/showcase.html
  </files>
  <action>
**Step 1 — templates/partials/head.html**

Add `{% vite_react_refresh %}` on the line immediately after `{% vite_hmr_client %}`. The tag order must be:
```django
{% vite_hmr_client %}
{% vite_react_refresh %}
```
No other changes to head.html.

**Step 2 — templates/base.html**

Add `{% block extra_js %}{% endblock %}` on a new line after the closing `</main>` tag and before `{% include "partials/shell_close.html" %}`. The relevant section should become:
```html
    </main>
    {% block extra_js %}{% endblock %}
    {% include "partials/shell_close.html" %}
```
No other changes to base.html.

**Step 3 — templates/pages/showcase.html**

Remove `{% vite_asset 'src/entrypoints/showcase.tsx' %}` from its current position inside `{% block content %}` (it is on the last line before `{% endblock %}`).

Add it inside a new `{% block extra_js %}` block after the `{% endblock %}` that closes `{% block content %}`:
```django
{% endblock %}

{% block extra_js %}
  {% vite_asset 'src/entrypoints/showcase.tsx' %}
{% endblock %}
```

Verify `{% load django_vite %}` is still present at the top of showcase.html (it already is — do not duplicate it).
  </action>
  <verify>
    <automated>python -c "
import re, sys

head = open('templates/partials/head.html').read()
base = open('templates/base.html').read()
showcase = open('templates/pages/showcase.html').read()

errors = []

# head.html: vite_react_refresh must appear after vite_hmr_client
hmr_pos = head.find('vite_hmr_client')
refresh_pos = head.find('vite_react_refresh')
if refresh_pos == -1:
    errors.append('head.html: missing vite_react_refresh')
elif refresh_pos < hmr_pos:
    errors.append('head.html: vite_react_refresh appears before vite_hmr_client')

# base.html: block extra_js must exist and appear after </main>
main_close = base.find('</main>')
extra_js_pos = base.find('block extra_js')
if extra_js_pos == -1:
    errors.append('base.html: missing block extra_js')
elif extra_js_pos < main_close:
    errors.append('base.html: block extra_js appears before </main>')

# showcase.html: vite_asset must NOT be inside block content
content_start = showcase.find('{% block content %}')
content_end = showcase.find('{% endblock %}', content_start)
asset_pos = showcase.find(\"vite_asset 'src/entrypoints/showcase.tsx'\")
if asset_pos == -1:
    errors.append('showcase.html: vite_asset for showcase.tsx is missing')
elif content_start < asset_pos < content_end:
    errors.append('showcase.html: vite_asset is still inside block content')

# showcase.html: vite_asset must be inside block extra_js
extra_js_start = showcase.rfind('{% block extra_js %}')
extra_js_end = showcase.rfind('{% endblock %}')
if extra_js_start == -1 or not (extra_js_start < asset_pos < extra_js_end):
    errors.append('showcase.html: vite_asset not found inside block extra_js')

if errors:
    print('FAIL')
    for e in errors: print(' -', e)
    sys.exit(1)
else:
    print('PASS')
"
</automated>
  </verify>
  <done>
head.html contains `{% vite_react_refresh %}` immediately after `{% vite_hmr_client %}`. base.html has `{% block extra_js %}{% endblock %}` after `</main>`. showcase.html's `{% vite_asset %}` call is inside `{% block extra_js %}` and NOT inside `{% block content %}`. The Python verification script exits 0.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, perform a final cross-file consistency check:

1. `grep -n 'mobileOpen' templates/partials/sidebar.html` — output must show only lines containing `$store.nav` (no bare `mobileOpen` variable initialisation).
2. `grep -n 'vite_react_refresh' templates/partials/head.html` — must return exactly one match.
3. `grep -n 'block extra_js' templates/base.html` — must return at least one match.
4. `grep -n 'vite_asset' templates/pages/showcase.html` — must return exactly one match, and it must be below the `block extra_js` marker in the file.

No Python, no migrations, no new files required. These are pure template edits.
</verification>

<success_criteria>
- UAT Test 5 (Mobile Sidebar Drawer): Hamburger button click causes sidebar to appear as overlay drawer on mobile viewport.
- UAT Tests 9–12 (React widgets): `/__ui__/` page renders Modal trigger button, ConfirmModal trigger, DataTable demo, and Toast trigger — all via the mounted showcase.tsx React entrypoint.
- No regression on the 7 previously passing UAT tests (verified by the unchanged Alpine store architecture and preserved block structure in base.html).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-06-SUMMARY.md` using the standard summary template.
</output>
