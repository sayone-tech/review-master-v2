---
phase: 25-org-admin-tag-management-dashboard-polarity
plan: 03
type: execute
wave: 3
depends_on: ["25-02"]
files_modified:
  - frontend/vite.config.ts
  - frontend/src/entrypoints/tag-management.tsx
  - frontend/src/widgets/tag-management/types.ts
  - frontend/src/widgets/tag-management/api.ts
  - frontend/src/widgets/tag-management/PolarityBadge.tsx
  - frontend/src/widgets/tag-management/RenameInput.tsx
  - frontend/src/widgets/tag-management/TagActionsMenu.tsx
  - frontend/src/widgets/tag-management/TagTable.tsx
  - frontend/src/widgets/tag-management/useTagList.ts
  - frontend/src/widgets/tag-management/TagMergeModal.tsx
  - frontend/src/widgets/tag-management/useMergeProgress.ts
  - frontend/src/widgets/tag-management/MergeProgressBanner.tsx
  - frontend/src/widgets/tag-management/TagManagementWidget.tsx
autonomous: true
requirements: [TMGT-02, TMGT-03, TMGT-04, TMGT-06]
must_haves:
  truths:
    - "The Tags page renders a sortable, paginated data table of canonical tags with Label, Polarity badge, Review Count, First Seen, and an actions menu"
    - "A tag can be renamed inline with validation and a duplicate-label error surfaced in the cell"
    - "A tag can be merged via a two-step modal with a searchable target picker and an irreversibility warning"
    - "Merge progress is shown via a 2s HTTP-polled banner that survives reload, dismisses, completes with a toast, and shows a rollback failure state — no WebSocket"
    - "Decisions covered: D-02 (React data-table widget — sortable, paginated), D-03/D-04 (inline rename UI with validation + duplicate-label error), D-05 (merge modal with searchable target picker + 're-maps N reviews, cannot be undone' warning), D-08 (durable TagMergeJob progress over ~2s HTTP poll, dismiss + reload-survival + toast + rollback, no new WebSocket consumer per §13.2)"
  artifacts:
    - path: "frontend/src/widgets/tag-management/TagManagementWidget.tsx"
      provides: "root widget composing table + progress banner"
      min_lines: 30
    - path: "frontend/src/widgets/tag-management/useMergeProgress.ts"
      provides: "2s poll hook auto-stopping on terminal status"
      contains: "setInterval"
    - path: "frontend/src/entrypoints/tag-management.tsx"
      provides: "Vite entrypoint mounting #tag-management-root"
      contains: "tag-management-root"
  key_links:
    - from: "frontend/src/widgets/tag-management/api.ts"
      to: "/api/v1/reviews/canonical-tags/ + tag-merge-jobs/"
      via: "fetch/rename/merge/poll/dismiss calls"
      pattern: "canonical-tags|tag-merge-jobs"
    - from: "frontend/vite.config.ts"
      to: "tag-management entrypoint"
      via: "rollupOptions.input entry"
      pattern: "tag-management"
    - from: "frontend/src/widgets/tag-management/useMergeProgress.ts"
      to: "tag-merge-jobs/active/"
      via: "2s poll, stop on SUCCESS/FAILED"
      pattern: "2_?000"
---

<objective>
Build the `TagManagementWidget` React surface mounted at `/admin/org/tags/`: the sortable/paginated data table, inline rename, the two-step searchable merge modal, the 2s HTTP-polled merge-progress banner (with dismiss, reload-survival, completion toast, and rollback failure state), the polarity badge, and the Vite entrypoint. All against the 25-02 endpoints. No new WebSocket consumer (§13.2).

Purpose: Deliver the user-facing tag-management UI for TMGT-02 (table), TMGT-03 (inline rename), TMGT-04 (merge modal), and TMGT-06 (HTTP-polled progress). Depends on 25-02 for the canonical-tags + tag-merge-jobs endpoints and the `tags.html` mount point.
Output: 11 new `tag-management/` files + the Vite entrypoint + the vite.config.ts entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-org-admin-tag-management-dashboard-polarity/25-CONTEXT.md
@.planning/phases/25-org-admin-tag-management-dashboard-polarity/25-PATTERNS.md
@.planning/phases/25-org-admin-tag-management-dashboard-polarity/25-UI-SPEC.md
@.planning/phases/25-org-admin-tag-management-dashboard-polarity/25-02-SUMMARY.md

<interfaces>
<!-- 25-02 endpoints (this plan's dependency) + verified frontend analogs. -->

API (built in 25-02):
  GET   /api/v1/reviews/canonical-tags/?ordering=&page=&page_size=   → paginated {results:[{id,label,polarity_type,review_count,first_seen}], count, ...}
  PATCH /api/v1/reviews/canonical-tags/{id}/rename/  {label}          → 200 {tag} | 400 {label: "...already exists"}
  POST  /api/v1/reviews/canonical-tags/{id}/merge/   {target_id}      → 201 {job_id} | 409 (active job)
  GET   /api/v1/reviews/tag-merge-jobs/active/                        → {id,status,processed,total,source_label,target_label,error_message} | null
  PATCH /api/v1/reviews/tag-merge-jobs/{id}/dismiss/                  → 200

Frontend analogs (VERIFIED — clone, do not modify):
  frontend/src/entrypoints/audit-log.tsx     — mount() + dataset.mounted guard + turbo:load
  frontend/src/widgets/audit-log/AuditLogWidget.tsx — space-y-4 + h1 + border card + pagination footer layout
  frontend/src/widgets/data-table/DataTable.tsx     — DataTable<Row> + DataTableColumn + renderRowActions (USE AS-IS)
  frontend/src/widgets/action-items/MergeModal.tsx  — two-step "pick"|"confirm" state machine, useEffect([open]) reset, emitToast
  frontend/src/widgets/modal/Modal.tsx              — size="lg", dismissible, role=dialog (USE AS-IS)
  frontend/src/widgets/notif-bell/useNotifications.ts — setInterval/clearInterval poll hook (clone at 2s, auto-stop on terminal)
  frontend/src/lib/toast.ts                          — emitToast({kind,title,body}) (USE AS-IS)
  frontend/vite.config.ts                            — rollupOptions.input "audit-log" entry pattern
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: types + api + PolarityBadge + entrypoint + vite entry</name>
  <files>frontend/src/widgets/tag-management/types.ts, frontend/src/widgets/tag-management/api.ts, frontend/src/widgets/tag-management/PolarityBadge.tsx, frontend/src/entrypoints/tag-management.tsx, frontend/vite.config.ts</files>
  <read_first>
    - frontend/src/entrypoints/audit-log.tsx (analog — full mount pattern)
    - frontend/vite.config.ts (modifying — "audit-log" rollupOptions.input entry near line 35)
    - frontend/src/widgets/notif-bell/api.ts (analog — fetch helper + CSRF/credentials convention for the reviews API)
    - .planning/phases/25-org-admin-tag-management-dashboard-polarity/25-UI-SPEC.md (types.ts shape, PolarityBadge BADGE map + classes, copy)
    - .planning/phases/25-org-admin-tag-management-dashboard-polarity/25-PATTERNS.md (entrypoint + vite entry)
  </read_first>
  <action>
    Create types.ts with PolarityType union (always_positive|always_negative|mixed), OrgCanonicalTagRow (id, label, polarity_type, review_count, first_seen) and TagMergeJobRow (id, status PENDING|IN_PROGRESS|SUCCESS|FAILED, processed, total, source_label, target_label, error_message) per UI-SPEC Surface 4/5. Create api.ts with fetchTags({ordering,page,page_size}), renameTag(id,label), startMerge(sourceId,targetId), fetchActiveJob(), dismissMergeJob(id) hitting the 25-02 endpoints, following the existing reviews/notif-bell fetch convention (credentials same-origin, CSRF header from cookie for PATCH/POST). renameTag and startMerge MUST surface non-2xx response bodies/status so callers can detect the 400-duplicate and 409-conflict cases. Create PolarityBadge.tsx per UI-SPEC Surface 1: a BADGE record mapping each PolarityType to {label, cls} (always_positive -> bg-green-tint text-[#166534] "Always Positive"; always_negative -> bg-red-tint text-[#991B1B] "Always Negative"; mixed -> bg-amber-tint text-[#92400E] "Mixed"), rendered as a span with inline-flex items-center px-2 py-1 rounded-sm text-[12px] font-medium plus cls and the text label (text label present, not color alone, per the accessibility contract). Create the tag-management.tsx entrypoint cloning audit-log.tsx: mount() on #tag-management-root with the dataset.mounted guard, render TagManagementWidget in StrictMode, register on both mount() and the turbo:load event. Add the entry "tag-management": resolve(__dirname, "src/entrypoints/tag-management.tsx") to vite.config.ts rollupOptions.input after the "audit-log" entry.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -iE "tag-management/(types|api|PolarityBadge)" || echo TS_OK</automated>
  </verify>
  <acceptance_criteria>
    - Files types.ts, api.ts, PolarityBadge.tsx, frontend/src/entrypoints/tag-management.tsx exist.
    - `grep -n "tag-management" frontend/vite.config.ts` returns a match.
    - `grep -n "canonical-tags" frontend/src/widgets/tag-management/api.ts` and `grep -n "tag-merge-jobs" frontend/src/widgets/tag-management/api.ts` both match.
    - `grep -n "tag-management-root" frontend/src/entrypoints/tag-management.tsx` returns a match.
    - `grep -c "Always Positive\|Always Negative\|Mixed" frontend/src/widgets/tag-management/PolarityBadge.tsx` is >= 3.
  </acceptance_criteria>
  <done>types/api/PolarityBadge/entrypoint/vite-entry exist; api targets the 25-02 endpoints; entrypoint mounts the widget root.</done>
</task>

<task type="auto">
  <name>Task 2: TagTable + useTagList + RenameInput + TagActionsMenu (table, sort, pagination, inline rename)</name>
  <files>frontend/src/widgets/tag-management/useTagList.ts, frontend/src/widgets/tag-management/TagTable.tsx, frontend/src/widgets/tag-management/RenameInput.tsx, frontend/src/widgets/tag-management/TagActionsMenu.tsx</files>
  <read_first>
    - frontend/src/widgets/audit-log/AuditLogWidget.tsx (analog — table card + pagination footer layout, sort header toggling)
    - frontend/src/widgets/data-table/DataTable.tsx (DataTable<Row>, DataTableColumn, renderRowActions API — use as-is)
    - frontend/src/widgets/tag-management/api.ts (fetchTags/renameTag from Task 1)
    - .planning/phases/25-org-admin-tag-management-dashboard-polarity/25-UI-SPEC.md (Surface 1 columns + sort + pagination + skeleton; Surface 2 inline rename input/validation/keyboard/states/copy)
  </read_first>
  <action>
    Create useTagList.ts: holds ordering (default -review_count), page, page_size (10/25/50), fetches via fetchTags, exposes rows/count/loading/error and setOrdering/setPage/refetch. Sort toggles direction when an active column header is re-clicked (ordering values label|-label|review_count|-review_count|created_at|-created_at). Create RenameInput.tsx per UI-SPEC Surface 2: inline input maxLength 100 with Save Label/Cancel buttons, Enter submits / Escape cancels, client empty-after-trim validation, in-flight "Saving…" disabled state, server-error mapping (400 containing "already exists"/"duplicate" -> "A tag with that name already exists. Use Merge to combine tags."; other 400 -> "Could not rename. Please try again.") shown in a p with role="alert" text-[12px] text-red mt-1; on 200 revert to read mode (no toast) and signal the parent to update the row label. Create TagActionsMenu.tsx: icon-only MoreHorizontal trigger with aria-label="Tag actions" opening a dropdown (existing platform dropdown classes) with "Rename" (puts the row into rename mode) and "Merge into…" (opens TagMergeModal with this row as source). Create TagTable.tsx: DataTable<OrgCanonicalTagRow> with 5 columns (label — swaps to RenameInput when this row is in rename mode; polarity_type -> PolarityBadge; review_count right-aligned tabular-nums; first_seen formatted date; actions -> TagActionsMenu via renderRowActions), sort headers carrying aria-sort, a 6-row skeleton, and the empty/filtered-empty/error states + copy from the UI-SPEC Copywriting Contract. At most one row in rename mode at a time (single renamingId state lifted to the widget).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -iE "tag-management/(TagTable|useTagList|RenameInput|TagActionsMenu)" || echo TS_OK</automated>
  </verify>
  <acceptance_criteria>
    - The 4 files exist.
    - `grep -n "DataTable" frontend/src/widgets/tag-management/TagTable.tsx` returns a match (reuses the shared component).
    - `grep -n "aria-sort" frontend/src/widgets/tag-management/TagTable.tsx` returns a match.
    - `grep -n 'role="alert"' frontend/src/widgets/tag-management/RenameInput.tsx` returns a match.
    - `grep -n 'aria-label="Tag actions"' frontend/src/widgets/tag-management/TagActionsMenu.tsx` returns a match.
    - `cd frontend && npx tsc --noEmit` reports no errors in these four files.
  </acceptance_criteria>
  <done>Sortable/paginated table renders canonical tags with polarity badges, actions menu, and working inline rename with duplicate-error surfacing; TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 3: TagMergeModal + useMergeProgress + MergeProgressBanner + TagManagementWidget (merge + 2s poll)</name>
  <files>frontend/src/widgets/tag-management/TagMergeModal.tsx, frontend/src/widgets/tag-management/useMergeProgress.ts, frontend/src/widgets/tag-management/MergeProgressBanner.tsx, frontend/src/widgets/tag-management/TagManagementWidget.tsx</files>
  <read_first>
    - frontend/src/widgets/action-items/MergeModal.tsx (analog — two-step pick/confirm machine, error banner, footer buttons, emitToast)
    - frontend/src/widgets/modal/Modal.tsx (size="lg", dismissible — use as-is)
    - frontend/src/widgets/notif-bell/useNotifications.ts (analog — setInterval/clearInterval poll cleanup)
    - frontend/src/widgets/tag-management/api.ts (startMerge/fetchActiveJob/dismissMergeJob from Task 1)
    - frontend/src/lib/toast.ts (emitToast — use as-is)
    - frontend/src/widgets/audit-log/AuditLogWidget.tsx (widget shell layout)
    - .planning/phases/25-org-admin-tag-management-dashboard-polarity/25-UI-SPEC.md (Surface 3 merge modal full contract; Surface 4 progress banner/poll/states/copy; state transition diagrams)
  </read_first>
  <action>
    Create TagMergeModal.tsx per UI-SPEC Surface 3, cloning MergeModal.tsx's useState "pick"|"confirm" machine with a useEffect([open]) reset: Step 1 fetches the org tag list once via fetchTags({page_size:200}) excluding the source, applies a client-side search filter, renders a radio target picker (selected highlight border-yellow bg-yellow-tint), shows the "All N reviews tagged…" body, and disables the proceed button until a target is chosen; Step 2 confirm shows the red AlertTriangle irreversibility warning; on confirm POST via startMerge(sourceId, targetId) -> on 201 close the modal and signal the parent that a job started; map 409 to "A merge is already in progress. Wait for it to complete before starting another." and other errors to the generic copy in a role="alert" banner. Use Modal size="lg" and dismissible={!submitting}. Create useMergeProgress.ts cloning useNotifications: fetchJob via fetchActiveJob; a useEffect that runs fetchJob immediately then sets setInterval(fetchJob, 2_000) ONLY while status is PENDING/IN_PROGRESS, clearing on cleanup and stopping on SUCCESS/FAILED; expose job, a dismiss() (local-session hide for in-progress; dismissMergeJob(id) for FAILED so it does not reappear on reload), and a refetch/kickoff to begin polling right after a merge launches. Create MergeProgressBanner.tsx per UI-SPEC Surface 4: in-progress banner role="status" aria-live="polite" (Loader2 spinner, "Merging tags…", "Re-mapping {source} -> {target} · {processed}/{total} reviews", a role="progressbar" with pct or an indeterminate fill, dismiss X with aria-label="Dismiss merge progress"); PENDING "Queued — waiting to start"; on SUCCESS emit emitToast({kind:"success", title:"Merge complete", body:'"{source}" merged into "{target}".'}) and trigger a table refetch; FAILED role="alert" red rollback banner with error_message + dismiss X aria-label="Dismiss error". Create TagManagementWidget.tsx (root) cloning the AuditLogWidget shell: space-y-4 + an h1 text-[20px] font-semibold text-ink "Tags" + MergeProgressBanner above the table card wrapping TagTable; it owns renamingId and the merge-modal source state, and wires useMergeProgress so a successful merge refetches the table (per Surface 4 + the state diagrams). Reload survival is automatic because the banner reads the active job from the durable endpoint on mount.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -iE "tag-management/(TagMergeModal|useMergeProgress|MergeProgressBanner|TagManagementWidget)" || (cd frontend && npm run build 2>&1 | tail -3)</automated>
  </verify>
  <acceptance_criteria>
    - The 4 files exist.
    - `grep -n "2_000\|2000" frontend/src/widgets/tag-management/useMergeProgress.ts` returns a match (2s poll).
    - `grep -n 'role="progressbar"' frontend/src/widgets/tag-management/MergeProgressBanner.tsx` returns a match.
    - `grep -n "emitToast" frontend/src/widgets/tag-management/MergeProgressBanner.tsx` returns a match.
    - `grep -rn "channels\|WebSocket\|new WebSocket" frontend/src/widgets/tag-management/` returns nothing (no WebSocket — §13.2).
    - `cd frontend && npm run build` succeeds (the tag-management entry bundles).
  </acceptance_criteria>
  <done>Merge modal, 2s poll hook, progress banner (in-progress/pending/success-toast/failure), and the root widget are implemented and bundle; no WebSocket used.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → reviews API | client widget calls org-scoped tag/merge/poll endpoints (auth enforced server-side in 25-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-AC1 | Elevation of Privilege | client API calls | mitigate | Authoritative defense is server-side (IsOrgAdmin + org-scoped querysets in 25-02). The client never sends organisation_id; the widget is only reachable from the ORG_ADMIN-only page. |
| T-25-13 | Tampering | progress transport | mitigate | Merge progress uses 2s HTTP polling of tag-merge-jobs/active/ only — NO new WebSocket consumer (§13.2). Grep gate asserts zero WebSocket usage in the widget. |
| T-25-V2 | Input Validation | rename/merge input | mitigate | Client validation is UX-only; server (25-02) re-validates label 1–100 + iexact duplicate + active-job 409. Client surfaces those errors but never bypasses them. |
| T-25-SC | Tampering | npm installs | accept | No new npm packages (recharts/lucide/react already installed — RESEARCH Package Legitimacy Audit: none added). |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean for all tag-management files.
- `cd frontend && npm run build` succeeds (tag-management entry bundles).
- `grep -rn "WebSocket" frontend/src/widgets/tag-management/` returns nothing (§13.2).
- Manual (VALIDATION Manual-Only): trigger a merge -> in-progress bar polls; reload mid-merge -> bar re-appears from the durable job; on completion -> toast; Staff login -> Tags item absent (sidebar belt-and-braces from 25-02).
</verification>

<success_criteria>
- Sortable, paginated tag table with polarity badges and actions menu.
- Inline rename with duplicate-error surfacing.
- Two-step merge modal with searchable target picker + irreversibility warning + 409 handling.
- 2s HTTP-polled progress banner: in-progress/pending/success-toast/failure-rollback, dismiss, reload survival.
- No WebSocket consumer added.
</success_criteria>

<output>
Create `.planning/phases/25-org-admin-tag-management-dashboard-polarity/25-03-SUMMARY.md` when done.
</output>
