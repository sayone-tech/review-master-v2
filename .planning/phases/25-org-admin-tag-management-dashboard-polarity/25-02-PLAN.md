---
phase: 25-org-admin-tag-management-dashboard-polarity
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - apps/reviews/serializers.py
  - apps/reviews/views.py
  - apps/reviews/urls.py
  - templates/org_admin/tags.html
  - templates/partials/sidebar_org.html
  - apps/reviews/tests/test_views.py
autonomous: true
requirements: [TMGT-01, TMGT-02, TMGT-04, TMGT-05, TMGT-06]
must_haves:
  truths:
    - "ORG_ADMIN reaches /admin/org/tags/ (200); STAFF_ADMIN is redirected/403 at both the template view and the API"
    - "The canonical-tags list endpoint is paginated, column-sortable, and query-count-bounded (≤3 queries), org-scoped"
    - "Rename and merge are exposed as endpoints that call the 25-01 services; merge returns 201 with a job_id, or 409 when an active job exists"
    - "The tag-merge-job active/ poll endpoint returns the caller's org's active job; dismiss/ marks it dismissed; both are org-scoped"
    - "The Tags nav item appears in the org sidebar only for non-Staff users"
  artifacts:
    - path: "apps/reviews/views.py"
      provides: "OrgCanonicalTagViewSet, TagMergeJobViewSet, tags_page_view"
      contains: "class OrgCanonicalTagViewSet"
    - path: "templates/org_admin/tags.html"
      provides: "Django template mounting #tag-management-root"
      contains: "tag-management-root"
    - path: "apps/reviews/urls.py"
      provides: "canonical-tags + tag-merge-jobs routes + tags page URL"
      contains: "tags"
  key_links:
    - from: "apps/reviews/views.py"
      to: "apps/reviews/services/tag_management.py"
      via: "rename/merge actions call services"
      pattern: "rename_canonical_tag|create_merge_job"
    - from: "apps/reviews/views.py"
      to: "apps/reviews/selectors/canonical_tags.py"
      via: "list viewset get_queryset"
      pattern: "list_canonical_tags_for_org"
    - from: "templates/partials/sidebar_org.html"
      to: "/admin/org/tags/"
      via: "Staff-gated nav item"
      pattern: "/admin/org/tags/"
---

<objective>
Expose the Phase 25 tag-management backend over the API and the Django template layer: two serializers, `OrgCanonicalTagViewSet` (list + rename + merge actions), `TagMergeJobViewSet` (active poll + dismiss), URL registration, the ORG_ADMIN-only `/admin/org/tags/` template view + `tags.html`, the Staff-gated sidebar nav item, and the Wave 0 view-test scaffold turned green.

Purpose: Deliver TMGT-01 (access control at view + sidebar), TMGT-02 (sortable, paginated, query-bounded list), and the API surfaces for rename (TMGT-03), merge (TMGT-04/05), and merge-progress polling (TMGT-06). Depends on 25-01 for the model, services, and selector.
Output: serializers, viewsets, URLs, `tags.html`, sidebar nav item, and green view tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-org-admin-tag-management-dashboard-polarity/25-CONTEXT.md
@.planning/phases/25-org-admin-tag-management-dashboard-polarity/25-RESEARCH.md
@.planning/phases/25-org-admin-tag-management-dashboard-polarity/25-PATTERNS.md
@.planning/phases/25-org-admin-tag-management-dashboard-polarity/25-UI-SPEC.md
@.planning/phases/25-org-admin-tag-management-dashboard-polarity/25-VALIDATION.md
@.planning/phases/25-org-admin-tag-management-dashboard-polarity/25-01-SUMMARY.md

<interfaces>
<!-- From 25-01 (this plan's dependency) + verified analogs. -->

From apps/reviews/services/tag_management.py (built in 25-01):
  rename_canonical_tag(*, tag, new_label, organisation_id) -> OrgCanonicalTag   # raises ValidationError on dup/length
  create_merge_job(*, source_tag, target_id, organisation_id) -> TagMergeJob     # raises ValidationError on active-job/cross-org/self
From apps/reviews/selectors/canonical_tags.py (built in 25-01):
  list_canonical_tags_for_org(*, organisation_id) -> QuerySet[OrgCanonicalTag]
From apps/reviews/models.py (built in 25-01):
  TagMergeJob: organisation FK, source_label, target_label, status (PENDING/IN_PROGRESS/SUCCESS/FAILED),
    processed, total, error_message, dismissed

From apps/accounts/permissions.py (VERIFIED):
  class IsOrgAdmin(BasePermission)   # 403 for Staff/unauth; ORG_ADMIN + non-null organisation_id
  @org_admin_required                # template-view decorator; non-ORG_ADMIN redirected (Staff cannot reach)
  # DO NOT use @login_required / IsOrgScoped here — those let Staff in (D-01).

From apps/common/views.py (VERIFIED analog — AuditLogViewSet):
  mixins.ListModelMixin + viewsets.GenericViewSet; pagination; get_queryset() calls selector with org_id; queryset = Model.objects.none()
From apps/common/pagination.py (VERIFIED):
  DefaultPageNumberPagination  # page_size_query_param="page_size", max_page_size=100
From rest_framework.filters:
  OrderingFilter  # ordering_fields = ["label","review_count","created_at"]; default ordering ["-review_count"]

From templates/partials/sidebar_org.html (VERIFIED):
  {% if user.role != "STAFF_ADMIN" %} ... {% include "partials/_nav_item.html" with href=... icon=... label=... %} {% endif %}
  # Add Tags nav item after the Activity Log item, inside this guard.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 view tests (TMGT-01/02/04/06) then serializers + viewsets + URLs</name>
  <files>apps/reviews/tests/test_views.py, apps/reviews/serializers.py, apps/reviews/views.py, apps/reviews/urls.py</files>
  <read_first>
    - apps/reviews/tests/test_views.py (modifying — existing view-test conventions, force_login, CaptureQueriesContext usage)
    - apps/common/views.py (analog — AuditLogViewSet list+pagination+selector pattern, ~lines 221-296)
    - apps/reviews/views.py (modifying — existing viewset registrations, imports)
    - apps/reviews/serializers.py (modifying — existing read/create serializer conventions)
    - apps/reviews/urls.py (modifying — existing router/url registration)
    - apps/accounts/permissions.py (IsOrgAdmin ~line 27)
    - apps/common/pagination.py (DefaultPageNumberPagination)
    - .planning/phases/25-org-admin-tag-management-dashboard-polarity/25-PATTERNS.md (OrgCanonicalTagViewSet + TagMergeJobViewSet bodies)
    - .planning/phases/25-org-admin-tag-management-dashboard-polarity/25-VALIDATION.md (exact test names)
  </read_first>
  <behavior>
    - test_canonical_tags_list_query_count: 20 tags, list with page_size=20 → ≤3 queries (session + COUNT + SELECT).
    - test_canonical_tags_ordering: `?ordering=label` and `?ordering=-review_count` reorder results.
    - test_merge_409_when_active_job: POST merge while a PENDING job exists → 409.
    - test_tag_merge_job_active_endpoint: active/ returns the caller's org active job only (not another org's).
    - test_tag_merge_job_dismiss: dismiss/ sets dismissed=True; active/ then returns null.
  </behavior>
  <action>
    First add RED view tests to apps/reviews/tests/test_views.py with EXACT names from 25-VALIDATION.md: `test_canonical_tags_list_query_count`, `test_canonical_tags_ordering`, `test_merge_409_when_active_job`, `test_tag_merge_job_active_endpoint`, `test_tag_merge_job_dismiss` (plus reuse existing fixtures for org_admin + a second org for cross-org assertions). Then implement. Add `OrgCanonicalTagReadSerializer` (fields: id, label, polarity_type, review_count, created_at exposed as `first_seen`) and `RenameSerializer(label=CharField(max_length=100, min_length=1))` and `TagMergeJobSerializer` (id, status, processed, total, source_label, target_label, error_message, dismissed) to apps/reviews/serializers.py (two-serializer rule §8). Add `OrgCanonicalTagViewSet(mixins.ListModelMixin, viewsets.GenericViewSet)` to apps/reviews/views.py per 25-PATTERNS.md: `permission_classes=[IsOrgAdmin]` (Staff → 403, D-01 — NOT IsOrgScoped), `pagination_class=DefaultPageNumberPagination`, `filter_backends=[OrderingFilter]`, `ordering_fields=["label","review_count","created_at"]`, `ordering=["-review_count"]`, `queryset=OrgCanonicalTag.objects.none()`, `get_queryset()` → `list_canonical_tags_for_org(organisation_id=request.user.organisation_id)`; `@action(detail=True, methods=["patch"], url_path="rename")` calling `rename_canonical_tag(...)` and returning the read serializer (DRF maps ValidationError → 400); `@action(detail=True, methods=["post"], url_path="merge")` calling `create_merge_job(...)` returning `{"job_id": job.pk}` status 201, and mapping the active-job ValidationError to HTTP 409 (catch and return Response(status=409) with the conflict copy). Add `TagMergeJobViewSet(viewsets.GenericViewSet)` with `permission_classes=[IsOrgAdmin]`, `get_queryset()` filtered to `request.user.organisation_id` (cross-org get_object → 404, T-25-AC1); `@action(detail=False, methods=["get"], url_path="active")` returning the most-recent non-dismissed PENDING/IN_PROGRESS job or null; `@action(detail=True, methods=["patch"], url_path="dismiss")` setting dismissed=True. Register all routes in apps/reviews/urls.py under the existing `/api/v1/reviews/` router as `canonical-tags` and `tag-merge-jobs`. Tests go GREEN.
  </action>
  <verify>
    <automated>pytest apps/reviews/tests/test_views.py -k "canonical_tags or tag_merge_job or merge_409" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "def test_(canonical_tags_list_query_count|canonical_tags_ordering|merge_409_when_active_job|tag_merge_job_active_endpoint|tag_merge_job_dismiss)" apps/reviews/tests/test_views.py` returns 5 matches.
    - `grep -n "class OrgCanonicalTagViewSet" apps/reviews/views.py` and `grep -n "class TagMergeJobViewSet" apps/reviews/views.py` both match.
    - `grep -n "IsOrgAdmin" apps/reviews/views.py` returns a match (Staff excluded) and `grep -c "IsOrgScoped" apps/reviews/views.py` for the new viewsets is not used for them.
    - `pytest apps/reviews/tests/test_views.py -k "canonical_tags or tag_merge_job or merge_409" -q` exits 0.
    - `python -c "from django.urls import resolve; resolve('/api/v1/reviews/tag-merge-jobs/active/')"` resolves.
  </acceptance_criteria>
  <done>Serializers + both viewsets + URLs implemented; list is paginated/sortable/≤3 queries; merge returns 201/409; poll + dismiss are org-scoped; all 5 view tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: ORG_ADMIN-only tags page view + tags.html + Staff redirect tests</name>
  <files>apps/reviews/views.py, apps/reviews/urls.py, templates/org_admin/tags.html, apps/reviews/tests/test_views.py</files>
  <read_first>
    - apps/common/views.py (analog — audit_log_view template view ~line 251; note it uses @login_required because Staff CAN access it — tags must NOT)
    - apps/accounts/permissions.py (@org_admin_required ~line 42)
    - templates/org-admin/audit-log.html (analog template referenced by audit-log entrypoint — root div + base extends)
    - apps/reviews/urls.py (modifying — add the template-page URL distinct from the API router)
    - .planning/phases/25-org-admin-tag-management-dashboard-polarity/25-PATTERNS.md (tags_page_view + template structure)
    - .planning/phases/25-org-admin-tag-management-dashboard-polarity/25-UI-SPEC.md (Surface 1 — template mounts #tag-management-root; no bootstrap beyond CSRF)
  </read_first>
  <action>
    Add the RED tests `test_tags_page_staff_redirected` (Staff GET /admin/org/tags/ → redirect, not 200) and `test_tags_page_org_admin_ok` (ORG_ADMIN GET → 200) to apps/reviews/tests/test_views.py per 25-VALIDATION.md. Add `tags_page_view(request)` to apps/reviews/views.py decorated with `@org_admin_required` (NOT `@login_required` — Staff must be redirected, D-01/Pitfall in RESEARCH) rendering `templates/org_admin/tags.html`. Create `templates/org_admin/tags.html` extending `base.html` with a single `<div id="tag-management-root"></div>` inside the content block (UI-SPEC Surface 1 — no `<script type="application/json">` bootstrap needed beyond the CSRF token already provided by base). Register the page URL `/admin/org/tags/` → `tags_page_view` in apps/reviews/urls.py (template URL, separate from the DRF router). Tests go GREEN.
  </action>
  <verify>
    <automated>pytest apps/reviews/tests/test_views.py -k "tags_page" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "def test_tags_page_(staff_redirected|org_admin_ok)" apps/reviews/tests/test_views.py` returns 2 matches.
    - `grep -n "org_admin_required" apps/reviews/views.py` returns a match on the tags page view; `grep -c "login_required" ` does not decorate tags_page_view.
    - `templates/org_admin/tags.html` exists and contains `tag-management-root`.
    - `python -c "from django.urls import resolve; resolve('/admin/org/tags/')"` resolves.
    - `pytest apps/reviews/tests/test_views.py -k tags_page -q` exits 0.
  </acceptance_criteria>
  <done>Tags page is ORG_ADMIN-only (Staff redirected), renders the widget root; both access tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Staff-gated Tags sidebar nav item</name>
  <files>templates/partials/sidebar_org.html</files>
  <read_first>
    - templates/partials/sidebar_org.html (modifying — existing {% if user.role != "STAFF_ADMIN" %} guards ~lines 30-41; Activity Log item)
    - .planning/phases/25-org-admin-tag-management-dashboard-polarity/25-UI-SPEC.md (Sidebar — Tags Nav Item: icon="tags", after Activity Log)
  </read_first>
  <action>
    Add a Tags nav item to templates/partials/sidebar_org.html inside a `{% if user.role != "STAFF_ADMIN" %}` guard, placed after the Activity Log item, using `{% include "partials/_nav_item.html" with href="/admin/org/tags/" icon="tags" label="Tags" %}` (lucide `tags` glyph, consistent with existing icon usage). This is the belt-and-braces UI layer (the authoritative defense is the @org_admin_required view + IsOrgAdmin API, both in Tasks 1-2). Do not wrap any existing item.
  </action>
  <verify>
    <automated>grep -n '/admin/org/tags/' templates/partials/sidebar_org.html && grep -n 'STAFF_ADMIN' templates/partials/sidebar_org.html | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'href="/admin/org/tags/"' templates/partials/sidebar_org.html` returns a match.
    - The Tags include is inside a `{% if user.role != "STAFF_ADMIN" %}` block (verify the guard wraps the new line).
    - `icon="tags"` and `label="Tags"` present on the new include.
  </acceptance_criteria>
  <done>Tags nav item renders for ORG_ADMIN and is hidden for Staff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → tags page/API | Staff or cross-org user may attempt direct access |
| client → merge/poll/dismiss | user may target another org's tags/jobs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-AC1 | Elevation of Privilege | Tags page + all viewsets | mitigate | `@org_admin_required` on the template view (Staff redirected); `IsOrgAdmin` on both viewsets (Staff → 403); sidebar nav gated by `user.role != "STAFF_ADMIN"`. Covered by test_tags_page_staff_redirected + DRF 403. |
| T-25-AC1b | Information Disclosure | TagMergeJobViewSet active/dismiss | mitigate | get_queryset filtered to `request.user.organisation_id`; cross-org get_object → 404. Covered by test_tag_merge_job_active_endpoint. |
| T-25-AC3 | Tampering | merge action | mitigate | active-job ValidationError mapped to HTTP 409; service-layer 409 guard from 25-01. Covered by test_merge_409_when_active_job. |
| T-25-V1 | Tampering | rename input | mitigate | RenameSerializer enforces 1–100 chars; service does Title-Case + iexact dedup (25-01). |
| T-25-SC | Tampering | npm/pip installs | accept | No new packages this plan. |
</threat_model>

<verification>
- `pytest apps/reviews/tests/test_views.py -q` green (all new view tests).
- `python manage.py check` passes (URL + view wiring).
- Per-wave: `pytest --cov=apps --cov-fail-under=85 -q`.
- Manual (UI-SPEC/VALIDATION): Staff login → Tags item absent from sidebar (belt-and-braces).
</verification>

<success_criteria>
- /admin/org/tags/ is 200 for ORG_ADMIN, redirect for Staff.
- canonical-tags list is paginated, sortable, ≤3 queries, org-scoped.
- rename/merge endpoints call 25-01 services; merge → 201 or 409.
- tag-merge-jobs active/ + dismiss/ are org-scoped.
- Tags sidebar item shown only to non-Staff.
</success_criteria>

<output>
Create `.planning/phases/25-org-admin-tag-management-dashboard-polarity/25-02-SUMMARY.md` when done.
</output>
