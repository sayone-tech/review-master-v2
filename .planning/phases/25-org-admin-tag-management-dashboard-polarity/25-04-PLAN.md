---
phase: 25-org-admin-tag-management-dashboard-polarity
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/selectors/aggregations.py
  - apps/dashboard/views.py
  - apps/dashboard/urls.py
  - apps/dashboard/tests/test_aggregations.py
  - frontend/src/widgets/dashboard/types.ts
  - frontend/src/widgets/dashboard/api.ts
  - frontend/src/widgets/dashboard/TagPolarityChart.tsx
  - frontend/src/widgets/dashboard/DashboardWidget.tsx
autonomous: true
requirements: [TDASH-01, TDASH-02]
must_haves:
  truths:
    - "The dashboard shows a tag-distribution chart: always_positive/always_negative tags render a single colored count bar; mixed tags render a stacked positive/negative split"
    - "The tag-polarity aggregation counts ONLY ReviewTag rows where canonical_tag is set, scoped to the org, in a single grouped query (no N+1)"
    - "The tag-polarity endpoint returns top-N tags by total_count descending with a has_more flag"
    - "Decisions covered: D-09 (extend dashboard tag chart — always_positive/always_negative single count bar, mixed → stacked positive/negative split from ReviewTag.polarity distribution), D-10 (TDASH-02: all canonical aggregation includes ONLY reviews where canonical_tag is set, org-scoped, query-bounded)"
  artifacts:
    - path: "apps/dashboard/selectors/aggregations.py"
      provides: "dashboard_tag_polarity selector"
      contains: "def dashboard_tag_polarity"
    - path: "apps/dashboard/views.py"
      provides: "DashboardTagPolarityView"
      contains: "class DashboardTagPolarityView"
    - path: "frontend/src/widgets/dashboard/TagPolarityChart.tsx"
      provides: "stacked recharts polarity chart"
      min_lines: 40
  key_links:
    - from: "apps/dashboard/selectors/aggregations.py"
      to: "ReviewTag canonical_tag"
      via: "grouped aggregate filtered to non-null canonical_tag"
      pattern: "canonical_tag__organisation_id"
    - from: "frontend/src/widgets/dashboard/TagPolarityChart.tsx"
      to: "/api/v1/dashboard/tag-polarity/"
      via: "fetch in dashboard api"
      pattern: "tag-polarity"
    - from: "frontend/src/widgets/dashboard/DashboardWidget.tsx"
      to: "TagPolarityChart"
      via: "rendered as a dashboard section"
      pattern: "TagPolarityChart"
---

<objective>
Add the polarity-aware tag-distribution chart to the existing dashboard: a backend selector + cached DRF view + URL, and the recharts stacked-bar React section wired into `DashboardWidget`. `always_positive`/`always_negative` tags show a single count bar; `mixed` tags show a positive/negative stacked split. All aggregation counts only reviews with a canonical tag set, org-scoped, query-bounded.

Purpose: Deliver TDASH-01 (polarity split) and TDASH-02 (canonical-only aggregation). This is an independent vertical slice with no file overlap with the tag-management plans, so it runs in Wave 1 alongside 25-01.
Output: `dashboard_tag_polarity` selector, `DashboardTagPolarityView` + `tag-polarity/` URL, `TagPolarityChart.tsx`, dashboard `types.ts`/`api.ts` additions, `DashboardWidget` wiring, and tag-polarity aggregation tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-org-admin-tag-management-dashboard-polarity/25-CONTEXT.md
@.planning/phases/25-org-admin-tag-management-dashboard-polarity/25-RESEARCH.md
@.planning/phases/25-org-admin-tag-management-dashboard-polarity/25-PATTERNS.md
@.planning/phases/25-org-admin-tag-management-dashboard-polarity/25-UI-SPEC.md
@.planning/phases/25-org-admin-tag-management-dashboard-polarity/25-VALIDATION.md

<interfaces>
<!-- Verified analogs. From RESEARCH.md Verification 7 + PATTERNS.md. -->

From apps/dashboard/views.py (VERIFIED):
  class DashboardApiView(APIView): permission_classes = [IsOrgScoped]; endpoint_name: str = ""
    def get(...) → reads dashboard_cache_key(endpoint, org_id, user_id, params), cache_get/cache_set(ttl), calls self._fetch(org_id, params, user)
  class KpisView(DashboardApiView): endpoint_name = "kpis"; def _fetch(...) → dashboard_kpis(...)

From apps/dashboard/selectors/aggregations.py (VERIFIED):
  dashboard_kpis / dashboard_sentiment_distribution — grouped values().annotate() pattern, no Python loop over querysets.

From apps/reviews/services/finalise.py (VERIFIED grouped-aggregate analog):
  ReviewTag.objects.filter(canonical_tag__organisation_id=org_id).values("canonical_tag_id").annotate(cnt=Count("id"))
  # NOTE: canonical_tag__organisation_id=org_id ALREADY implies canonical_tag IS NOT NULL (TDASH-02).

From frontend/src/widgets/dashboard/TopPerformingSection.tsx (VERIFIED):
  import { Bar, BarChart, Cell, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
  # Add Legend for this chart. Stacked bars use two <Bar stackId="a">. Skeleton: bg-line-soft animate-[sk-pulse...] h-[240px].
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dashboard_tag_polarity selector + tag-polarity tests (TDASH-01, TDASH-02)</name>
  <files>apps/dashboard/selectors/aggregations.py, apps/dashboard/tests/test_aggregations.py</files>
  <read_first>
    - apps/dashboard/selectors/aggregations.py (modifying — dashboard_kpis / dashboard_sentiment_distribution grouped-annotate patterns)
    - apps/dashboard/tests/test_aggregations.py (modifying — existing aggregation test + CaptureQueriesContext conventions)
    - apps/dashboard/tests/conftest.py (org/shop fixtures)
    - apps/reviews/tests/factories.py (OrgCanonicalTagFactory, ReviewTagFactory with polarity)
    - .planning/phases/25-org-admin-tag-management-dashboard-polarity/25-PATTERNS.md (full dashboard_tag_polarity body)
    - .planning/phases/25-org-admin-tag-management-dashboard-polarity/25-VALIDATION.md (exact test names)
  </read_first>
  <behavior>
    - test_tag_polarity_basic: a mixed tag with both positive and negative ReviewTags returns positive_count and negative_count > 0; an always_positive tag returns negative_count == 0.
    - test_tag_polarity_excludes_null_canonical: ReviewTag rows with canonical_tag=None are NOT counted in any bar.
    - test_tag_polarity_query_count: the selector runs in ≤2 queries via CaptureQueriesContext.
  </behavior>
  <action>
    First add the three RED tests to apps/dashboard/tests/test_aggregations.py with EXACT names from 25-VALIDATION.md (`test_tag_polarity_basic`, `test_tag_polarity_excludes_null_canonical`, `test_tag_polarity_query_count`), importing `dashboard_tag_polarity` from `apps.dashboard.selectors.aggregations`. Then implement `dashboard_tag_polarity(*, organisation_id, limit=10) -> dict` per 25-PATTERNS.md: a single grouped query `ReviewTag.objects.filter(canonical_tag__organisation_id=organisation_id).values("canonical_tag_id","canonical_tag__label","canonical_tag__polarity_type").annotate(positive_count=Count("id", filter=Q(polarity="positive")), negative_count=Count("id", filter=Q(polarity="negative")), total_count=Count("id")).order_by("-total_count")[:limit+1]`. The `canonical_tag__organisation_id` filter ALREADY enforces canonical_tag IS NOT NULL (TDASH-02) and org scoping (D-10) — do NOT add a redundant Python loop or per-tag count (Pitfall 7). Return `{"tags": [...top limit...], "has_more": len(rows) > limit}` with keys label/polarity_type/positive_count/negative_count/total_count. Tests transition GREEN after implementation.
  </action>
  <verify>
    <automated>pytest apps/dashboard/tests/test_aggregations.py -k tag_polarity -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "def test_tag_polarity_(basic|excludes_null_canonical|query_count)" apps/dashboard/tests/test_aggregations.py` returns 3 matches.
    - `grep -n "def dashboard_tag_polarity" apps/dashboard/selectors/aggregations.py` returns a match.
    - `pytest apps/dashboard/tests/test_aggregations.py -k tag_polarity -q` exits 0 (all 3 green, including the ≤2 query-count assertion).
    - `grep -n "canonical_tag__organisation_id" apps/dashboard/selectors/aggregations.py` returns a match (TDASH-02 enforced at the query).
  </acceptance_criteria>
  <done>Selector returns top-N tags with positive/negative split, excludes null canonical_tag, runs in ≤2 queries; all 3 tag-polarity tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: DashboardTagPolarityView + tag-polarity/ URL</name>
  <files>apps/dashboard/views.py, apps/dashboard/urls.py</files>
  <read_first>
    - apps/dashboard/views.py (modifying — DashboardApiView base + KpisView/SentimentView subclass pattern)
    - apps/dashboard/urls.py (modifying — existing dashboard endpoint URL registrations)
    - .planning/phases/25-org-admin-tag-management-dashboard-polarity/25-PATTERNS.md (DashboardTagPolarityView body)
  </read_first>
  <action>
    Add `DashboardTagPolarityView(DashboardApiView)` to apps/dashboard/views.py with `endpoint_name = "tag-polarity"` and `_fetch(self, *, org_id, params, user)` returning `dashboard_tag_polarity(organisation_id=org_id)` (ignores date/shop filter params per TDASH-02 — aggregates the full canonical vocabulary). It inherits `IsOrgScoped` + the cache read/write from the base (cache key already includes org_id + user_id per the v0.4 DASH-C1 isolation decision). Register the URL in apps/dashboard/urls.py as `tag-polarity/` → `DashboardTagPolarityView.as_view()` mirroring the existing `kpis/` / `sentiment/` registrations, resolving to `/api/v1/dashboard/tag-polarity/`.
  </action>
  <verify>
    <automated>python -c "from django.urls import resolve; print(resolve('/api/v1/dashboard/tag-polarity/').func)" && pytest apps/dashboard/tests/test_views.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class DashboardTagPolarityView" apps/dashboard/views.py` returns a match.
    - `grep -n "tag-polarity" apps/dashboard/urls.py` returns a match.
    - `resolve('/api/v1/dashboard/tag-polarity/')` resolves without error.
  </acceptance_criteria>
  <done>tag-polarity endpoint is registered, cached, IsOrgScoped, and returns the selector output.</done>
</task>

<task type="auto">
  <name>Task 3: TagPolarityChart.tsx + dashboard types/api + DashboardWidget wiring</name>
  <files>frontend/src/widgets/dashboard/types.ts, frontend/src/widgets/dashboard/api.ts, frontend/src/widgets/dashboard/TagPolarityChart.tsx, frontend/src/widgets/dashboard/DashboardWidget.tsx</files>
  <read_first>
    - frontend/src/widgets/dashboard/TopPerformingSection.tsx (analog — recharts BarChart, skeleton, error, role="img" wrapper, section card)
    - frontend/src/widgets/dashboard/DashboardWidget.tsx (modifying — grid composition; where to add the new section)
    - frontend/src/widgets/dashboard/types.ts (modifying — existing dashboard response types)
    - frontend/src/widgets/dashboard/api.ts (modifying — existing dashboard fetch functions)
    - .planning/phases/25-org-admin-tag-management-dashboard-polarity/25-UI-SPEC.md (Surface 5 — full chart contract, tooltip, legend, colors, empty/loading states, copy)
  </read_first>
  <action>
    Add `TagPolarityBar` and `TagPolarityResponse` interfaces to types.ts per UI-SPEC Surface 5 (label, polarity_type, positive_count, negative_count, total_count; tags + has_more). Add a `fetchTagPolarity()` function to api.ts that GETs `/api/v1/dashboard/tag-polarity/` following the existing dashboard fetch convention (CSRF/credentials as the other dashboard calls). Create `TagPolarityChart.tsx` per UI-SPEC Surface 5: recharts `ResponsiveContainer` height 240, `BarChart` with `barCategoryGap="35%"`, `XAxis dataKey="label"` (angle -35, 14-char truncate, fontSize 12 #71717A), `YAxis` (fontSize 12 #A1A1AA), `Tooltip` with a `TagPolarityTooltip` (mixed → pos/neg dot rows; non-mixed → total reviews), `Legend` (Positive/Negative), and two stacked bars `<Bar dataKey="positive_count" name="Positive" stackId="a" fill="#16A34A" radius={[0,0,0,0]} />` + `<Bar dataKey="negative_count" name="Negative" stackId="a" fill="#DC2626" radius={[4,4,0,0]} />` (always_* tags render single-color naturally since the opposite count is 0). Include the section card ("Tag Distribution" h2 + "Top tags by review volume · canonical tags only" subtitle), `role="img" aria-label="Tag distribution by polarity bar chart"`, the loading skeleton (`bg-line-soft ... h-[240px]`), the empty state ("No canonical tags yet. Tags will appear as reviews are enriched."), and the `has_more` "See all tags" footer link to `/admin/org/tags/`. Use the dashboard's existing data-fetching hook convention (e.g. useQuery as in KpiCards) to call `fetchTagPolarity`. Wire `<TagPolarityChart />` into DashboardWidget.tsx as a full-width section below the existing two-column grid (per RESEARCH Open Question 1 recommendation — simplest, preserves existing responsive grid).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "TagPolarity" || echo "TS_OK_NO_TAGPOLARITY_ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/widgets/dashboard/TagPolarityChart.tsx` exists and is ≥40 lines.
    - `grep -n "stackId" frontend/src/widgets/dashboard/TagPolarityChart.tsx` returns matches for both bars.
    - `grep -n "tag-polarity" frontend/src/widgets/dashboard/api.ts` returns a match.
    - `grep -n "TagPolarityChart" frontend/src/widgets/dashboard/DashboardWidget.tsx` returns a match.
    - `cd frontend && npx tsc --noEmit` reports no errors referencing TagPolarity types.
  </acceptance_criteria>
  <done>TagPolarityChart renders single bars for always_* tags and stacked pos/neg bars for mixed tags, fetches the new endpoint, and is mounted in DashboardWidget; TypeScript compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → dashboard API | org admin/staff requests aggregated tag data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-D1 | Information Disclosure | dashboard_tag_polarity selector | mitigate | Aggregate filters `canonical_tag__organisation_id=org_id` — org-scoped; cannot read another org's tag counts. Cache key includes org_id + user_id (DASH-C1). |
| T-25-D2 | Information Disclosure | tag-polarity endpoint | mitigate | `DashboardApiView` enforces `IsOrgScoped`; org_id read from `request.user.organisation_id`, never from params. |
| T-25-SC | Tampering | npm/pip installs | accept | No new packages (recharts/lucide already installed — RESEARCH Package Legitimacy Audit: none added). |
</threat_model>

<verification>
- `pytest apps/dashboard/tests/test_aggregations.py apps/dashboard/tests/test_views.py -q` green.
- `resolve('/api/v1/dashboard/tag-polarity/')` succeeds.
- `cd frontend && npx tsc --noEmit` clean for TagPolarity types.
- Per-wave: `pytest --cov=apps --cov-fail-under=85 -q`.
- Manual (UI-SPEC): open dashboard with a mixed tag having both polarities → stacked bar splits.
</verification>

<success_criteria>
- dashboard_tag_polarity returns top-N tags, pos/neg split, excludes null canonical_tag, ≤2 queries.
- tag-polarity endpoint registered, cached, org-scoped.
- TagPolarityChart renders single + stacked bars per UI-SPEC and is wired into the dashboard.
</success_criteria>

<output>
Create `.planning/phases/25-org-admin-tag-management-dashboard-polarity/25-04-SUMMARY.md` when done.
</output>
