---
phase: 20-ai-guardrails
plan: 05
type: execute
wave: 3
depends_on: [20-04]
files_modified:
  - apps/reviews/services/enrichment.py
  - apps/reviews/tests/test_enrichment_service.py
autonomous: true
requirements: [D-04, D-15, D-20, D-28, D-29, D-31, D-33]
must_haves:
  truths:
    - "enrich_review calls moderate_input(review.comment, review=review, request_type='enrichment') BEFORE call_openai_enrichment"
    - "When moderate_input raises ContentModeratedException, enrich_review sets Review.enrichment_status=FAILED and Review.enrichment_error_code='content_moderated' and returns without calling OpenAI"
    - "The moderate_input call site is OUTSIDE any transaction.atomic() block (D-33)"
    - "The MODERATED AiUsageLog row written by guardrails survives even if a later step rolls back"
  artifacts:
    - path: "apps/reviews/services/enrichment.py"
      provides: "Updated enrich_review with moderate_input wiring + _persist_moderated helper"
    - path: "apps/reviews/tests/test_enrichment_service.py"
      provides: "TestEnrichReviewModeration class covering input-moderation path"
  key_links:
    - from: "apps/reviews/services/enrichment.py"
      to: "apps/integrations/openai/guardrails.py"
      via: "from apps.integrations.openai.guardrails import moderate_input"
      pattern: "moderate_input"
    - from: "apps/reviews/services/enrichment.py"
      to: "apps/reviews/models.py"
      via: "Review.enrichment_error_code"
      pattern: "enrichment_error_code"
---

<objective>
Wire `moderate_input` into the enrichment pipeline and persist FAILED + denormalized error_code on block.

Purpose: Implements D-15 (call order: input moderation → OpenAI), D-31 (populate Review.enrichment_error_code), D-33 (call moderate_input outside atomic).
Output: Edits to enrich_review + new `_persist_moderated` helper + new test class.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-ai-guardrails/20-CONTEXT.md
@.planning/phases/20-ai-guardrails/20-RESEARCH.md
@./CLAUDE.md
@apps/reviews/services/enrichment.py
@apps/integrations/openai/guardrails.py

<interfaces>
From apps/reviews/services/enrichment.py (lines 382-461 per RESEARCH.md):
- `enrich_review(*, review_id: str) -> None` — main entry point.
- Existing `_persist_failure(review, *, error_code, error_message)` helper writes a FAILED AiUsageLog row AND sets `review.enrichment_status = FAILED`.
- Existing `_persist_success_no_comment` skip block ends around line 448.
- The call to `call_openai_enrichment` is at line 452.
- The body uses `transaction.atomic()` for the success persistence path. Verify line numbers when reading.

From apps/integrations/openai/guardrails.py (Plan 20-04):
- `moderate_input(text: str, *, review=None, request_type: str = "enrichment") -> str` — raises ContentModeratedException on block.

From apps/reviews/models.py (Plan 20-03):
- `Review.enrichment_error_code: CharField(max_length=32, blank=True, default="")`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _persist_moderated helper + wire moderate_input into enrich_review</name>
  <files>apps/reviews/services/enrichment.py</files>
  <read_first>
    - apps/reviews/services/enrichment.py (lines 1-50 imports; lines 350-470 around enrich_review and _persist_failure)
    - apps/integrations/openai/guardrails.py (Plan 20-04 output — confirm moderate_input signature)
    - .planning/phases/20-ai-guardrails/20-CONTEXT.md (D-15, D-31)
    - .planning/phases/20-ai-guardrails/20-RESEARCH.md (Section: Code Landmarks → enrichment.py; Pitfall 3)
  </read_first>
  <behavior>
    - On clean input: enrich_review unchanged — calls moderate_input, receives truncated text back, then proceeds to existing OpenAI call.
    - On ContentModeratedException: enrich_review catches it, calls new `_persist_moderated(review)` which sets `review.enrichment_status = Review.EnrichmentStatus.FAILED`, `review.enrichment_error_code = "content_moderated"`, `review.enrichment_attempted_at = timezone.now()`, calls `review.save(update_fields=["enrichment_status", "enrichment_error_code", "enrichment_attempted_at"])`, and returns. Note: AiUsageLog row is already written inside guardrails.py per D-33; _persist_moderated only updates the Review row.
    - The moderate_input call is placed BETWEEN the no-comment skip (line ~448) and call_openai_enrichment (line ~452). It MUST be outside any `with transaction.atomic():` block (Pitfall 3).
    - Existing `_persist_failure` is NOT used for the moderated path (its AiUsageLog write would duplicate the one from guardrails).
  </behavior>
  <action>
    Imports: add `from apps.integrations.openai.guardrails import moderate_input` and `from apps.integrations.openai.exceptions import ContentModeratedException` to the top of `enrichment.py`. Place beside existing OpenAI-related imports.

    In `enrich_review`, locate the position between the no-comment skip block (around line 448) and the `call_openai_enrichment(...)` call (around line 452). Wrap the moderation + OpenAI call sequence in a `try` block:

    `try: truncated_text = moderate_input(review.comment, review=review, request_type="enrichment"); ... existing OpenAI call using truncated_text instead of review.comment ... except ContentModeratedException: _persist_moderated(review); return`

    Pass `truncated_text` (the return value of moderate_input) into call_openai_enrichment in place of `review.comment`. This ensures the truncated form flows into the prompt assembly (D-03/D-21).

    Add a new helper at the appropriate location near `_persist_failure`:

    `def _persist_moderated(review: Review) -> None:` — body sets the three fields on review and saves with `update_fields=["enrichment_status", "enrichment_error_code", "enrichment_attempted_at"]`. Uses `timezone.now()` for the attempted_at field. Does NOT write AiUsageLog (guardrails already did). Does NOT wrap in `transaction.atomic()` (single row save).

    DO NOT modify _persist_failure or _persist_success. DO NOT add the moderate_input call inside any existing `transaction.atomic()` block — if needed, refactor to ensure the call is outside.
  </action>
  <acceptance_criteria>
    - `grep -nE 'from apps.integrations.openai.guardrails import moderate_input' apps/reviews/services/enrichment.py` matches.
    - `grep -nE 'from apps.integrations.openai.exceptions import.*ContentModeratedException' apps/reviews/services/enrichment.py` matches.
    - `grep -nE 'def _persist_moderated' apps/reviews/services/enrichment.py` matches exactly one definition.
    - `grep -nE '\.enrichment_error_code = "content_moderated"' apps/reviews/services/enrichment.py` matches.
    - `grep -B 10 'moderate_input(review.comment' apps/reviews/services/enrichment.py | grep -v '^#' | grep -c 'transaction.atomic'` equals 0 (Pitfall 3: moderate_input must not be inside an atomic block in its preceding context).
    - `python -c "import ast; tree = ast.parse(open('apps/reviews/services/enrichment.py').read()); print('ok')"` exits 0.
    - `ruff check apps/reviews/services/enrichment.py` exits 0.
    - `mypy apps/reviews/services/enrichment.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>ruff check apps/reviews/services/enrichment.py && python -c "import ast; ast.parse(open('apps/reviews/services/enrichment.py').read())"</automated>
  </verify>
  <done>Imports added, _persist_moderated defined, moderate_input wired between no-comment skip and call_openai_enrichment, outside any atomic block, and the truncated return value flows into the OpenAI call.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add TestEnrichReviewModeration class to test_enrichment_service.py</name>
  <files>apps/reviews/tests/test_enrichment_service.py</files>
  <read_first>
    - apps/reviews/tests/test_enrichment_service.py (existing fixtures, factory usage, mock patterns)
    - apps/reviews/services/enrichment.py (just updated — for the exact call site)
    - apps/integrations/openai/guardrails.py (signature confirmation)
    - .planning/phases/20-ai-guardrails/20-VALIDATION.md (Decisions → Test Map)
  </read_first>
  <behavior>
    `class TestEnrichReviewModeration` with these tests (decorate class with `@pytest.mark.django_db`):
    - `test_moderate_input_called_before_openai`: mock `apps.reviews.services.enrichment.moderate_input` (returns "clean text") and `apps.reviews.services.enrichment.call_openai_enrichment` (returns a valid EnrichmentResult). Run enrich_review; assert moderate_input called once with `review.comment`, then call_openai_enrichment called once with the truncated text as its review.comment input.
    - `test_moderate_input_blocks_sets_failed_with_error_code`: mock `moderate_input` to raise `ContentModeratedException("input flagged")`. Run enrich_review. Assert `call_openai_enrichment` NOT called. Refresh `review` from DB; assert `review.enrichment_status == Review.EnrichmentStatus.FAILED` AND `review.enrichment_error_code == "content_moderated"` AND `review.enrichment_attempted_at` is not None.
    - `test_moderate_input_does_not_use_atomic_block`: regression for Pitfall 3 / D-33. Mock moderate_input as a side-effect that records the value of `transaction.get_autocommit()` (or asserts `transaction.get_connection().in_atomic_block is False`). Verify the autocommit/atomic flag at the moderation call site indicates NOT inside an atomic block.
    - `test_already_success_review_skips_moderation`: regression for D-15 idempotency + CLAUDE.md §12.4 layer-3 short-circuit. Create a `Review` with `enrichment_status=Review.EnrichmentStatus.SUCCESS` (use factory). Mock `apps.reviews.services.enrichment.moderate_input` and `apps.reviews.services.enrichment.call_openai_enrichment`. Run `enrich_review(review_id=review.id)`. Assert: (a) `moderate_input.assert_not_called()`, (b) `call_openai_enrichment.assert_not_called()`, (c) `AiUsageLog.objects.filter(review=review).count()` is unchanged from baseline. This pins the contract that a SUCCESS-status review is a no-op — preventing a future refactor from moving `moderate_input` above the `select_for_update` SUCCESS guard and silently re-billing moderated traffic.

    Use existing factory patterns from `apps/reviews/tests/factories.py` (ReviewFactory etc.). Use `unittest.mock.patch` of imported symbols inside `apps.reviews.services.enrichment`.
  </behavior>
  <action>
    Append the `TestEnrichReviewModeration` class to `apps/reviews/tests/test_enrichment_service.py`. Use the same factory/import conventions as existing test classes in that file. Reuse any existing fixture for `EnrichmentResult` mock returns.

    For the atomic-block regression test: import `from django.db import transaction` in the test. Define a side-effect function that records `transaction.get_connection().in_atomic_block` into a list, and pass it as `side_effect` to the moderate_input mock. After enrich_review runs, assert the recorded value is `False`.
  </action>
  <acceptance_criteria>
    - `pytest apps/reviews/tests/test_enrichment_service.py::TestEnrichReviewModeration -x` exits 0.
    - `pytest apps/reviews/tests/test_enrichment_service.py::TestEnrichReviewModeration::test_moderate_input_blocks_sets_failed_with_error_code -x` exits 0.
    - `pytest apps/reviews/tests/test_enrichment_service.py::TestEnrichReviewModeration::test_moderate_input_does_not_use_atomic_block -x` exits 0.
    - `pytest apps/reviews/tests/test_enrichment_service.py::TestEnrichReviewModeration::test_already_success_review_skips_moderation -x` exits 0 (D-15 idempotency regression).
    - `ruff check apps/reviews/tests/test_enrichment_service.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pytest apps/reviews/tests/test_enrichment_service.py::TestEnrichReviewModeration -x -q</automated>
  </verify>
  <done>Four tests pass, covering call order, FAILED + error_code persistence, atomic-block-isolation regression, and SUCCESS-status idempotency short-circuit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Celery worker → moderate_input | Untrusted review text from Google API flows into the Moderation API; truncated and category-checked before any OpenAI call |
| moderate_input → enrich_review | Typed exception is the trusted signaling channel for "blocked" |
| enrich_review → Review row | Server-controlled write of enrichment_error_code; no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering (prompt injection) | enrich_review input path | mitigate | moderate_input invoked before any prompt assembly; truncated return value (D-21) flows into the prompt. |
| T-20-05 | Repudiation (audit-log loss) | moderate_input AiUsageLog write | mitigate | Call site is outside `transaction.atomic()`; regression test asserts `in_atomic_block is False` (Pitfall 3). |
| T-20-RT | Tampering (Celery task retry of moderated row) | enrich_review failure path | mitigate | Sets `enrichment_error_code = "content_moderated"`; Plan 20-08 retry-task exclusion uses this field. |
</threat_model>

<verification>
- `pytest apps/reviews/tests/test_enrichment_service.py -x` exits 0 (existing + new tests).
- `ruff check apps/reviews/services/enrichment.py apps/reviews/tests/test_enrichment_service.py` exits 0.
</verification>

<success_criteria>
Enrichment pipeline blocks high-severity reviews before any OpenAI call. Failed-moderation rows carry the error code needed by Plan 20-08.
</success_criteria>

<output>
Create `.planning/phases/20-ai-guardrails/20-05-SUMMARY.md` when done.
</output>
