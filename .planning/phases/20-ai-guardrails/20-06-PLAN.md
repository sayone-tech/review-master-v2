---
phase: 20-ai-guardrails
plan: 06
type: execute
wave: 3
depends_on: [20-04]
files_modified:
  - apps/reviews/services/reply_generation.py
  - apps/reviews/tests/test_reply_generation_service.py
autonomous: true
requirements: [D-07, D-08, D-14, D-22, D-29, D-33]
must_haves:
  truths:
    - "generate_reply_draft calls moderate_input(review.comment, review=review, request_type='reply_generation') BEFORE call_openai_reply_generation"
    - "generate_reply_draft calls moderate_output(draft, review=review, usage_data=usage_data) AFTER call_openai_reply_generation"
    - "After moderate_output passes, the draft is run through truncate_reply_at_sentence (D-08, D-22) BEFORE the SUCCESS AiUsageLog write"
    - "On output-moderation block, AiUsageLog row carries REAL prompt/completion tokens and REAL cost (D-29) with error_code='output_moderated'"
    - "Both moderate_input and moderate_output call sites are OUTSIDE any transaction.atomic() block (D-33)"
  artifacts:
    - path: "apps/reviews/services/reply_generation.py"
      provides: "Updated generate_reply_draft with input + output moderation + 300-word truncation"
    - path: "apps/reviews/tests/test_reply_generation_service.py"
      provides: "TestGenerateReplyDraftModeration class"
  key_links:
    - from: "apps/reviews/services/reply_generation.py"
      to: "apps/integrations/openai/guardrails.py"
      via: "moderate_input, moderate_output, truncate_reply_at_sentence"
      pattern: "moderate_input|moderate_output|truncate_reply_at_sentence"
---

<objective>
Wire input + output moderation and reply-length truncation into the reply-generation service.

Purpose: Implements D-14 (call order), D-07 (output moderation), D-08/D-22 (300-word sentence truncation), D-29 (output-moderation log carries real tokens), D-33 (outside atomic).
Output: Edits to generate_reply_draft + new test class.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-ai-guardrails/20-CONTEXT.md
@.planning/phases/20-ai-guardrails/20-RESEARCH.md
@./CLAUDE.md
@apps/reviews/services/reply_generation.py
@apps/integrations/openai/guardrails.py

<interfaces>
From apps/reviews/services/reply_generation.py (lines 68-128 per RESEARCH.md):
- `generate_reply_draft(*, review, tone) -> str` — entry point.
- Tone validation ends at ~line 78; the `try:` block begins at ~line 80.
- `call_openai_reply_generation(...)` returns `(draft_text, usage_data)` at ~line 85.
- AiUsageLog success write happens at ~lines 100-127.

From apps/integrations/openai/guardrails.py (Plan 20-04):
- `moderate_input(text, *, review, request_type) -> str` — returns truncated text or raises.
- `moderate_output(text, *, review=None, request_type="reply_generation", usage_data=None) -> str` — returns text unchanged or raises (writes MODERATED AiUsageLog with real tokens via usage_data on block per D-29).
- `truncate_reply_at_sentence(text) -> str` — applies D-08 300-word truncation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire moderate_input + moderate_output + truncate_reply_at_sentence into generate_reply_draft</name>
  <files>apps/reviews/services/reply_generation.py</files>
  <read_first>
    - apps/reviews/services/reply_generation.py (lines 1-150 — confirm imports, exact line numbers of: tone validation end, try-block start, call_openai_reply_generation invocation, AiUsageLog write)
    - apps/integrations/openai/guardrails.py (signatures of moderate_input, moderate_output, truncate_reply_at_sentence)
    - .planning/phases/20-ai-guardrails/20-CONTEXT.md (D-14, D-29)
    - .planning/phases/20-ai-guardrails/20-RESEARCH.md (Section: Code Landmarks → reply_generation.py; Pitfall 6)
  </read_first>
  <behavior>
    Call order in `generate_reply_draft` after the change:
    1. Existing tone validation.
    2. `truncated_text = moderate_input(review.comment, review=review, request_type="reply_generation")` — outside any atomic block. Raises ContentModeratedException on input block.
    3. Existing `call_openai_reply_generation(review_with_truncated_text, tone)` — adapt to pass truncated text into the prompt assembly (option: rebind `review.comment` locally OR pass `truncated_text` as a separate argument if call_openai_reply_generation accepts it; otherwise update the prompt assembly to use truncated_text). DO NOT mutate `review.comment` in place (would persist on subsequent saves).
    4. `moderate_output(draft, review=review, request_type="reply_generation", usage_data=usage_data)` — raises ContentModeratedException on output block; guardrails writes the MODERATED AiUsageLog row with REAL tokens (D-29).
    5. `draft = truncate_reply_at_sentence(draft)` — D-08 length cap applied AFTER output moderation passes.
    6. Existing SUCCESS AiUsageLog write proceeds with the truncated draft text logged as length but original token counts (the OpenAI call's actual usage).
    7. Return `draft`.

    Both moderate_* calls MUST be outside any `transaction.atomic()` block (D-33).

    `ContentModeratedException` is NOT caught here — it bubbles to the view (Plan 20-07).
  </behavior>
  <action>
    Imports: add `from apps.integrations.openai.guardrails import moderate_input, moderate_output, truncate_reply_at_sentence` to the top of `reply_generation.py`. ContentModeratedException is NOT imported here (no catch).

    Locate the body of `generate_reply_draft` per RESEARCH.md Code Landmarks lines 68-128. Insert step 2 (`truncated_text = moderate_input(...)`) immediately after tone validation (~line 78) and BEFORE the existing `try:` block. The moderate_input call itself is not wrapped in try — the exception is meant to propagate.

    Adapt the prompt assembly: either (a) pass `truncated_text` as a kwarg to call_openai_reply_generation if its signature allows; or (b) create a local shallow copy / SimpleNamespace exposing `.comment = truncated_text` and pass it; or (c) update the prompt-building helper to accept an explicit `comment_text` parameter. Choose the option that minimizes ripple — record the choice in the task SUMMARY. DO NOT do `review.comment = truncated_text` (it would risk persistence via accidental save).

    Insert step 4 (`moderate_output(draft, review=review, request_type="reply_generation", usage_data=usage_data)`) AFTER the call_openai_reply_generation return and BEFORE the SUCCESS AiUsageLog write (between lines ~85 and ~100 in the current numbering).

    Insert step 5 (`draft = truncate_reply_at_sentence(draft)`) between the output-moderation pass and the SUCCESS AiUsageLog write.

    Verify both moderate_* calls are outside any `with transaction.atomic():` block. If the existing function has an atomic block enclosing the OpenAI call (currently it does not, per RESEARCH.md), move the moderate_input call OUTSIDE it.
  </action>
  <acceptance_criteria>
    - `grep -nE 'from apps.integrations.openai.guardrails import .*moderate_input.*moderate_output' apps/reviews/services/reply_generation.py` matches (single line import).
    - `grep -c 'truncate_reply_at_sentence' apps/reviews/services/reply_generation.py` ≥ 1.
    - `grep -B 5 'moderate_input(' apps/reviews/services/reply_generation.py | grep -v '^#' | grep -c 'transaction.atomic'` equals 0 (no atomic before moderate_input).
    - `grep -B 5 'moderate_output(' apps/reviews/services/reply_generation.py | grep -v '^#' | grep -c 'transaction.atomic'` equals 0.
    - `python -c "import ast; ast.parse(open('apps/reviews/services/reply_generation.py').read())"` exits 0.
    - `ruff check apps/reviews/services/reply_generation.py` exits 0.
    - `mypy apps/reviews/services/reply_generation.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>ruff check apps/reviews/services/reply_generation.py && python -c "import ast; ast.parse(open('apps/reviews/services/reply_generation.py').read())"</automated>
  </verify>
  <done>Call order is moderate_input → OpenAI → moderate_output → truncate_reply_at_sentence → SUCCESS AiUsageLog → return; both moderate_* calls outside atomic; truncated text flows into prompt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add TestGenerateReplyDraftModeration class to test_reply_generation_service.py</name>
  <files>apps/reviews/tests/test_reply_generation_service.py</files>
  <read_first>
    - apps/reviews/tests/test_reply_generation_service.py (existing fixture/mock patterns; specifically lines 30-80)
    - apps/reviews/services/reply_generation.py (just updated)
    - .planning/phases/20-ai-guardrails/20-VALIDATION.md (Decisions → Test Map)
  </read_first>
  <behavior>
    `class TestGenerateReplyDraftModeration` decorated with `@pytest.mark.django_db`:
    - `test_moderate_input_called_before_openai_call`: mock `moderate_input` (returns "clean text"), `call_openai_reply_generation` (returns ("Nice reply.", usage_data)), `moderate_output` (returns "Nice reply."). Run generate_reply_draft. Assert call ordering using `unittest.mock.call_args_list` / `assert_called_once_with`, and that moderate_input was called before call_openai_reply_generation.
    - `test_input_moderation_block_raises_and_skips_openai`: mock moderate_input to raise ContentModeratedException; assert pytest.raises and that call_openai_reply_generation was NOT called.
    - `test_output_moderation_block_writes_aiusagelog_with_real_tokens`: mock moderate_output to raise ContentModeratedException; verify the AiUsageLog row created by guardrails (mock at the guardrails level OR observe via a real call where _persist_moderated_log is left unmocked) has `status="MODERATED"`, `error_code="output_moderated"`, AND non-zero `prompt_tokens`/`completion_tokens`/`estimated_cost_usd` matching usage_data (D-29).
    - `test_output_truncation_applied_after_moderation_pass`: mock call_openai_reply_generation to return a draft with 320 words across 4 sentences; mock moderate_output to return text unchanged (clean). Assert returned draft ends with " (Please review and complete before sending.)" AND word count ≤ 300.
    - `test_short_reply_not_truncated`: 150-word draft returned unchanged.
    - `test_truncated_input_flows_into_openai`: mock moderate_input to return "TRUNCATED_MARKER". Assert call_openai_reply_generation was invoked with the truncated marker (verify via mock's call_args).
    - `test_moderate_calls_outside_atomic_block`: runtime regression for Pitfall 3 / D-33 (mirrors the equivalent test in plan 20-05's `test_moderate_input_does_not_use_atomic_block`). Reply generation has TWO moderation call sites (input + output) — both must run outside any `transaction.atomic()` block so a downstream rollback never erases the moderated `AiUsageLog` audit row. Use side-effect functions on the `moderate_input` and `moderate_output` mocks that record `transaction.get_connection().in_atomic_block` into a list. After `generate_reply_draft` runs, assert both recorded values are `False`. This catches refactors the grep-gate acceptance criterion would miss (atomic block opened earlier than 5 lines, opened via `@transaction.atomic` decorator on a helper, or bound to a variable).

    Patch targets per RESEARCH.md mock convention: `apps.reviews.services.reply_generation.moderate_input`, `apps.reviews.services.reply_generation.moderate_output`, `apps.reviews.services.reply_generation.call_openai_reply_generation`, `apps.reviews.services.reply_generation.truncate_reply_at_sentence` (only if needed; otherwise let the real impl run for the truncation test).
  </behavior>
  <action>
    Append `TestGenerateReplyDraftModeration` class to `apps/reviews/tests/test_reply_generation_service.py`. Reuse the existing `_usage_data()` factory or equivalent (see lines 30-50 of that file for the existing convention). Use existing factory imports (ReviewFactory etc.).

    For the output-AiUsageLog test: since guardrails.moderate_output writes the row itself, do NOT mock moderate_output's side-effect for that test. Instead, mock only the underlying `_call_moderation_api` to return a flagged response and let `moderate_output` execute its real path (creating the row, then raising). Use the real guardrails module end-to-end for that test for fidelity.

    For the atomic-block regression test: import `from django.db import transaction`. Define a list `recorded = []` and a side-effect callable that appends `transaction.get_connection().in_atomic_block` to `recorded` then returns the mock's return value. Wire it to both `moderate_input` and `moderate_output` mocks via `side_effect`. After `generate_reply_draft` runs, assert `recorded == [False, False]`.
  </action>
  <acceptance_criteria>
    - `pytest apps/reviews/tests/test_reply_generation_service.py::TestGenerateReplyDraftModeration -x` exits 0.
    - `pytest apps/reviews/tests/test_reply_generation_service.py -k moderation -x` runs ≥ 7 tests, all pass.
    - `pytest apps/reviews/tests/test_reply_generation_service.py::TestGenerateReplyDraftModeration::test_moderate_calls_outside_atomic_block -x` exits 0 (D-33 runtime regression).
    - `pytest apps/reviews/tests/test_reply_generation_service.py::TestGenerateReplyDraftModeration::test_output_moderation_block_writes_aiusagelog_with_real_tokens -x` exits 0 (D-29 regression).
    - `pytest apps/reviews/tests/test_reply_generation_service.py::TestGenerateReplyDraftModeration::test_output_truncation_applied_after_moderation_pass -x` exits 0 (D-08 regression).
    - `ruff check apps/reviews/tests/test_reply_generation_service.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pytest apps/reviews/tests/test_reply_generation_service.py::TestGenerateReplyDraftModeration -x -q</automated>
  </verify>
  <done>Seven tests pass; output-AiUsageLog test asserts real tokens + output_moderated code; truncation test verifies sentence-boundary cut + canonical suffix; atomic-block runtime test asserts both moderation calls fire outside `transaction.atomic`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP view → generate_reply_draft | Org-scoped user request; review.comment is untrusted Google content |
| call_openai_reply_generation → moderate_output | LLM output is untrusted; must be moderated before reaching the user or being persisted |
| moderate_* → AiUsageLog | Audit-row writes must survive caller transaction rollback (D-33) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering (prompt injection) | generate_reply_draft input path | mitigate | moderate_input invoked before prompt assembly; truncated text flows into the prompt. |
| T-20-03 | Information Disclosure (harmful generated reply to Google) | generate_reply_draft output path | mitigate | moderate_output executed before draft is returned to the view; flagged drafts raise ContentModeratedException → 422 (Plan 20-07). |
| T-20-CO | Repudiation (under-counted cost on output-moderation) | output-moderation AiUsageLog write | mitigate | D-29 — output-moderation log carries REAL tokens/cost from `usage_data`; regression test enforces non-zero values. |
| T-20-05 | Repudiation (audit-log loss) | moderate_* call sites | mitigate | Both calls outside transaction.atomic(); grep gate in acceptance criteria. |
</threat_model>

<verification>
- `pytest apps/reviews/tests/test_reply_generation_service.py -x` exits 0 (existing + new tests).
- `ruff check apps/reviews/services/reply_generation.py apps/reviews/tests/test_reply_generation_service.py` exits 0.
</verification>

<success_criteria>
Reply-generation service blocks flagged inputs and outputs; output truncation applied after moderation passes; cost accounting reflects reality for output-moderation events.
</success_criteria>

<output>
Create `.planning/phases/20-ai-guardrails/20-06-SUMMARY.md` when done.
</output>
