---
phase: 20-ai-guardrails
plan: 07
type: execute
wave: 4
depends_on: [20-02, 20-06]
files_modified:
  - apps/reviews/views.py
  - apps/reviews/tests/test_views.py
autonomous: true
requirements: [D-16, D-26, D-32]
must_haves:
  truths:
    - "ReviewViewSet.generate_reply catches ContentModeratedException and returns HTTP 422"
    - "Response body is exactly {'code': 'content_moderated', 'detail': 'AI reply isn\\'t available for this review. Please write your reply manually.'}"
    - "The except ContentModeratedException block is placed BEFORE except (OpenAITransientError, OpenAIPermanentError) for correct exception ordering"
  artifacts:
    - path: "apps/reviews/views.py"
      provides: "ContentModeratedException → 422 mapping"
    - path: "apps/reviews/tests/test_views.py"
      provides: "test_generate_reply_returns_422_on_content_moderated"
  key_links:
    - from: "apps/reviews/views.py"
      to: "apps/integrations/openai/exceptions.py"
      via: "except ContentModeratedException"
      pattern: "ContentModeratedException"
---

<objective>
Add the HTTP 422 mapping for `ContentModeratedException` in the generate_reply view.

Purpose: Implements D-16, D-26 (canonical user-facing copy), D-32 (catch hierarchy via OpenAIError base).
Output: One new `except` block in views.py + one new view test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-ai-guardrails/20-CONTEXT.md
@.planning/phases/20-ai-guardrails/20-RESEARCH.md
@apps/reviews/views.py
@apps/integrations/openai/exceptions.py

<interfaces>
From apps/reviews/views.py (lines 253-317 per RESEARCH.md):
- `ReviewViewSet.generate_reply` action; exception-to-HTTP mapping is INLINE via `return Response(..., status=status.HTTP_xxx)`.
- Existing `except (OpenAITransientError, OpenAIPermanentError):` block at lines 276-295.
- New catch block must precede the existing block because ContentModeratedException inherits from OpenAIError; most-specific first.

From apps/integrations/openai/exceptions.py:
- `ContentModeratedException(OpenAIError)` — Plan 20-02.

Canonical copy (D-26):
- code: `"content_moderated"`
- detail: `"AI reply isn't available for this review. Please write your reply manually."`
- HTTP status: 422 UNPROCESSABLE_ENTITY
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add except ContentModeratedException → 422 mapping in generate_reply view</name>
  <files>apps/reviews/views.py</files>
  <read_first>
    - apps/reviews/views.py (lines 240-320 — full generate_reply action)
    - apps/integrations/openai/exceptions.py (confirm import path)
    - .planning/phases/20-ai-guardrails/20-CONTEXT.md (D-26)
    - .planning/phases/20-ai-guardrails/20-RESEARCH.md (Section: Code Landmarks → views.py)
  </read_first>
  <action>
    In `apps/reviews/views.py`:
    1. Add `ContentModeratedException` to the existing OpenAI exceptions import line. Keep imports sorted (ruff isort).
    2. Inside `ReviewViewSet.generate_reply` action (around lines 253-317), locate the `except (OpenAITransientError, OpenAIPermanentError) as exc:` block. INSERT a new `except ContentModeratedException:` block IMMEDIATELY BEFORE it (most-specific exception first — required because ContentModeratedException inherits from OpenAIError which is the base of OpenAITransientError/OpenAIPermanentError).
    3. The new block body: `return Response({"code": "content_moderated", "detail": "AI reply isn't available for this review. Please write your reply manually."}, status=status.HTTP_422_UNPROCESSABLE_ENTITY,)`
    4. Use the EXACT copy from D-26 — single canonical string for both input-moderation and output-moderation cases (frontend renders detail verbatim per D-27). No `as exc:` variable needed (the exception carries no payload).
    5. Do NOT log review text. A `logger.info("generate_reply blocked by moderation review_id=%s", review.pk)` is acceptable; INFO level only, never WARNING+ with text.
  </action>
  <acceptance_criteria>
    - `grep -n 'except ContentModeratedException' apps/reviews/views.py` returns exactly one match.
    - `grep -n 'except (OpenAITransientError, OpenAIPermanentError)' apps/reviews/views.py` still present (untouched).
    - `awk '/except ContentModeratedException/{cm=NR} /except \(OpenAITransientError/{tp=NR} END{exit !(cm>0 && cm<tp)}' apps/reviews/views.py` exits 0 (ContentModeratedException catch precedes the OpenAITransient/Permanent catch).
    - `grep -F "AI reply isn't available for this review. Please write your reply manually." apps/reviews/views.py` matches exactly one line (canonical copy per D-26).
    - `grep -n 'HTTP_422_UNPROCESSABLE_ENTITY' apps/reviews/views.py` includes the new block.
    - `python -c "import ast; ast.parse(open('apps/reviews/views.py').read())"` exits 0.
    - `ruff check apps/reviews/views.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>ruff check apps/reviews/views.py && grep -q "except ContentModeratedException" apps/reviews/views.py && grep -F "AI reply isn't available for this review. Please write your reply manually." apps/reviews/views.py</automated>
  </verify>
  <done>Catch block added, placed before OpenAITransient/Permanent catch, returns exact canonical body with HTTP 422.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add test_generate_reply_returns_422_on_content_moderated to test_views.py</name>
  <files>apps/reviews/tests/test_views.py</files>
  <read_first>
    - apps/reviews/tests/test_views.py (existing generate_reply view tests for auth + factory setup)
    - apps/reviews/views.py (just updated)
    - .planning/phases/20-ai-guardrails/20-VALIDATION.md
  </read_first>
  <behavior>
    Add tests (decorate with `@pytest.mark.django_db` as the existing file does):
    - `test_generate_reply_returns_422_on_content_moderated`: authenticated org-admin user POSTs to the generate_reply endpoint for an in-org review. Mock `apps.reviews.views.generate_reply_draft` (or wherever the view imports it from) to raise `ContentModeratedException("input flagged")`. Assert response.status_code == 422, response.json() == {"code": "content_moderated", "detail": "AI reply isn't available for this review. Please write your reply manually."}.
    - `test_generate_reply_422_canonical_detail_string_byte_exact`: same setup; assert the response body's `detail` field equals the literal D-26 string byte-for-byte (regression for accidental rewording).
    - `test_generate_reply_422_does_not_leak_categories_or_review_text`: same setup; assert response.json() keys are exactly `{"code", "detail"}` (no extra fields like `categories`, `text`, `flagged`).
  </behavior>
  <action>
    Append the three test methods to the existing generate_reply test class in `apps/reviews/tests/test_views.py` (or add a new `class TestGenerateReplyModeration` if no parent class exists for generate_reply view tests). Reuse existing authenticated-client and Review factory fixtures.

    Use `unittest.mock.patch` at the view's import path of the service function (the path is whatever the view uses — typically `apps.reviews.views.generate_reply_draft`). If the view imports as `from apps.reviews.services.reply_generation import generate_reply_draft`, the patch target is `apps.reviews.views.generate_reply_draft`.
  </action>
  <acceptance_criteria>
    - `pytest apps/reviews/tests/test_views.py -k generate_reply_moderated -x` exits 0.
    - `pytest apps/reviews/tests/test_views.py -k generate_reply_returns_422_on_content_moderated -x` exits 0.
    - `pytest apps/reviews/tests/test_views.py -k canonical_detail_string_byte_exact -x` exits 0.
    - `pytest apps/reviews/tests/test_views.py -k does_not_leak_categories -x` exits 0.
    - `ruff check apps/reviews/tests/test_views.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pytest apps/reviews/tests/test_views.py -k "generate_reply and (moderated or content_moderated)" -x -q</automated>
  </verify>
  <done>Three tests pass; canonical D-26 string asserted byte-exact; no information leakage in error body.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated request → ReviewViewSet | Existing tenant + permission scoping applies; moderation does not bypass it |
| Service exception → HTTP response | ContentModeratedException maps to fixed body; no LLM categories or review text leak through |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-LK | Information Disclosure (leak moderation criteria) | generate_reply 422 response body | mitigate | Response body has exactly two keys (`code`, `detail`); regression test asserts no extra keys. User is told "what they can do", not "why blocked". |
| T-20-OR | Tampering (wrong exception catch order) | generate_reply except chain | mitigate | ContentModeratedException(OpenAIError) caught BEFORE OpenAITransient/Permanent (which also inherit from OpenAIError). Grep gate enforces ordering. |
</threat_model>

<verification>
- `pytest apps/reviews/tests/test_views.py -x` exits 0 (existing + new tests).
- Manual: in dev, run a flagged-input flow and verify the React UI renders the canonical D-26 string (per VALIDATION.md manual section — out of scope for automated tests).
</verification>

<success_criteria>
View-layer mapping complete; HTTP 422 with canonical copy returned on any ContentModeratedException; no information leakage.
</success_criteria>

<output>
Create `.planning/phases/20-ai-guardrails/20-07-SUMMARY.md` when done.
</output>
