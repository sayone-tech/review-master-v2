---
phase: 20-ai-guardrails
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/integrations/openai/exceptions.py
autonomous: true
requirements: [D-16, D-32]
must_haves:
  truths:
    - "ContentModeratedException exists and inherits from OpenAIError"
    - "It can be caught via `except OpenAIError`"
  artifacts:
    - path: "apps/integrations/openai/exceptions.py"
      provides: "ContentModeratedException class"
      contains: "class ContentModeratedException"
  key_links:
    - from: "apps/integrations/openai/exceptions.py"
      to: "apps/reviews/views.py (Plan 20-07)"
      via: "import + except clause"
      pattern: "ContentModeratedException"
---

<objective>
Add the `ContentModeratedException` exception class that the guardrails module (Plan 20-04) raises and the view (Plan 20-07) catches.

Purpose: Implements D-16 + D-32. A typed exception is the contract that connects the guardrail layer to the HTTP layer.
Output: One new class in `apps/integrations/openai/exceptions.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-ai-guardrails/20-CONTEXT.md
@.planning/phases/20-ai-guardrails/20-RESEARCH.md
@./CLAUDE.md

<interfaces>
From apps/integrations/openai/exceptions.py:
- `class OpenAIError(Exception)` is defined at line 11 per RESEARCH.md Code Landmarks. All other exceptions in the module subclass it.
- Existing siblings include `OpenAITransientError`, `OpenAIPermanentError` (per Phase 19).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ContentModeratedException</name>
  <files>apps/integrations/openai/exceptions.py</files>
  <read_first>
    - apps/integrations/openai/exceptions.py (lines 1-29 — entire file; verify `OpenAIError` is at line 11 per RESEARCH.md)
    - .planning/phases/20-ai-guardrails/20-CONTEXT.md (D-16, D-32)
    - .planning/phases/20-ai-guardrails/20-RESEARCH.md (Section: Code Landmarks → exceptions.py)
  </read_first>
  <action>
    Append a new class to `apps/integrations/openai/exceptions.py`:

    Name: `ContentModeratedException`. Base: `OpenAIError` (per D-32 — NOT bare `Exception`). Body: `pass`. Add a single-line docstring: "Raised when input or output content is blocked by moderation (Phase 20, D-16/D-32)."

    No constructor; no extra attributes. The exception is a pure signal; stage/entity are conveyed via logs and AiUsageLog rows written by the guardrails module (D-33).

    Do not modify any other class.
  </action>
  <acceptance_criteria>
    - `grep -nE "class ContentModeratedException\(OpenAIError\)" apps/integrations/openai/exceptions.py` returns exactly one match.
    - `python -c "from apps.integrations.openai.exceptions import ContentModeratedException, OpenAIError; assert issubclass(ContentModeratedException, OpenAIError); assert issubclass(ContentModeratedException, Exception)"` exits 0.
    - `ruff check apps/integrations/openai/exceptions.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python -c "from apps.integrations.openai.exceptions import ContentModeratedException, OpenAIError; assert issubclass(ContentModeratedException, OpenAIError)" && ruff check apps/integrations/openai/exceptions.py</automated>
  </verify>
  <done>ContentModeratedException is importable and inherits from OpenAIError.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guardrails → view | Typed exception is the trusted signaling channel; no untrusted data flows through |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-EX | Repudiation | ContentModeratedException | accept | Pure signal class; no payload to tamper with. Audit context lives in AiUsageLog (Plan 20-04). |
</threat_model>

<verification>
- Import succeeds.
- Inheritance chain `Exception ← OpenAIError ← ContentModeratedException` verified.
</verification>

<success_criteria>
Downstream plans (20-04, 20-05, 20-06, 20-07) can `from apps.integrations.openai.exceptions import ContentModeratedException`.
</success_criteria>

<output>
Create `.planning/phases/20-ai-guardrails/20-02-SUMMARY.md` when done.
</output>
