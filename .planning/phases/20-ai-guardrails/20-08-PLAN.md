---
phase: 20-ai-guardrails
plan: 08
type: execute
wave: 4
depends_on: [20-03, 20-05]
files_modified:
  - apps/reviews/tasks.py
  - apps/reviews/tests/test_tasks.py
autonomous: true
requirements: [D-25, D-31]
must_haves:
  truths:
    - "retry_failed_enrichments_task queryset excludes rows where Review.enrichment_error_code == 'content_moderated'"
    - "Other FAILED rows (openai_5xx, parse_error, openai_timeout) remain retry-eligible"
  artifacts:
    - path: "apps/reviews/tasks.py"
      provides: "Updated retry_failed_enrichments_task queryset"
    - path: "apps/reviews/tests/test_tasks.py"
      provides: "test_retry_failed_enrichments_excludes_moderated"
  key_links:
    - from: "apps/reviews/tasks.py"
      to: "apps/reviews/models.py"
      via: "Review.enrichment_error_code"
      pattern: "enrichment_error_code"
---

<objective>
Exclude `content_moderated` rows from the Beat-scheduled retry task so the platform never re-runs moderation on immutable reviewer text.

Purpose: Implements D-25 (no retry on moderated rows) using the D-31 denormalized field.
Output: One filter line added + one new test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-ai-guardrails/20-CONTEXT.md
@.planning/phases/20-ai-guardrails/20-RESEARCH.md
@apps/reviews/tasks.py

<interfaces>
From apps/reviews/tasks.py lines 217-246 (per RESEARCH.md):
- `retry_failed_enrichments_task` queryset (lines 233-239):
  `Review.objects.filter(enrichment_status=Review.EnrichmentStatus.FAILED, enrichment_version__lt=MAX_TOTAL_ENRICH_ATTEMPTS, deleted_at__isnull=True)`

From apps/reviews/models.py (Plan 20-03):
- `enrichment_error_code = CharField(max_length=32, blank=True, default="")`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exclude content_moderated rows from retry_failed_enrichments_task queryset</name>
  <files>apps/reviews/tasks.py</files>
  <read_first>
    - apps/reviews/tasks.py (lines 200-260 — full retry_failed_enrichments_task)
    - apps/reviews/models.py (confirm enrichment_error_code field exists from Plan 20-03)
    - .planning/phases/20-ai-guardrails/20-CONTEXT.md (D-25, D-31)
  </read_first>
  <action>
    In `apps/reviews/tasks.py`, locate the `retry_failed_enrichments_task` queryset (around lines 233-239). Add an `.exclude(enrichment_error_code="content_moderated")` call immediately after the existing `.filter(...)` chain. Final shape:

    `Review.objects.filter(enrichment_status=Review.EnrichmentStatus.FAILED, enrichment_version__lt=MAX_TOTAL_ENRICH_ATTEMPTS, deleted_at__isnull=True).exclude(enrichment_error_code="content_moderated")`

    Add a comment above the line: "# D-25: skip content_moderated rows — reviewer text is immutable, retry is pure waste."

    No other change to the task body. Do not add a `.filter(enrichment_error_code__in=[...])` — the exclude form is clearer and matches the original D-25/D-31 wording.
  </action>
  <acceptance_criteria>
    - `grep -n 'exclude(enrichment_error_code="content_moderated")' apps/reviews/tasks.py` returns exactly one match.
    - `grep -B 2 'exclude(enrichment_error_code="content_moderated")' apps/reviews/tasks.py | grep -c 'D-25'` ≥ 1.
    - `python -c "import ast; ast.parse(open('apps/reviews/tasks.py').read())"` exits 0.
    - `ruff check apps/reviews/tasks.py` exits 0.
    - `mypy apps/reviews/tasks.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>ruff check apps/reviews/tasks.py && grep -q 'exclude(enrichment_error_code="content_moderated")' apps/reviews/tasks.py</automated>
  </verify>
  <done>One exclude clause added with D-25 comment; nothing else changed in the task.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add test_retry_failed_enrichments_excludes_moderated</name>
  <files>apps/reviews/tests/test_tasks.py</files>
  <read_first>
    - apps/reviews/tests/test_tasks.py (existing retry-task tests; factory imports; CELERY_TASK_ALWAYS_EAGER usage)
    - apps/reviews/tasks.py (just updated)
    - .planning/phases/20-ai-guardrails/20-VALIDATION.md
  </read_first>
  <behavior>
    `class TestRetryFailedEnrichmentsExcludesModerated` (or extend existing retry-test class) with these tests, decorated `@pytest.mark.django_db`:

    - `test_retry_excludes_moderated`: create three FAILED reviews:
      - R1: enrichment_error_code="content_moderated"
      - R2: enrichment_error_code="openai_5xx"
      - R3: enrichment_error_code="" (legacy row, no code)
      Mock `apps.reviews.tasks.enrich_review_task` (or whatever task it delegates to — read the actual implementation). Run `retry_failed_enrichments_task` synchronously. Assert the enqueued/called set includes R2 and R3 but NOT R1.

    - `test_retry_includes_other_failure_codes`: create FAILED reviews with enrichment_error_code in {"openai_5xx", "parse_error", "openai_timeout", ""}. Run retry; assert all four are retried.

    Use existing ReviewFactory; build reviews with `enrichment_status=Review.EnrichmentStatus.FAILED`, `enrichment_version__lt=MAX_TOTAL_ENRICH_ATTEMPTS`, `deleted_at=None`.
  </behavior>
  <action>
    Append the test class/methods to `apps/reviews/tests/test_tasks.py`. Patch the per-review enqueue helper (likely `apps.reviews.tasks.enrich_review_task.delay` or a direct call to the service function — confirm by reading the task body in this task's read_first). Collect the IDs passed to the patched function and assert membership.

    Reuse `CELERY_TASK_ALWAYS_EAGER = True` from `config/settings/test.py` (already configured per CLAUDE.md §16).
  </action>
  <acceptance_criteria>
    - `pytest apps/reviews/tests/test_tasks.py -k retry_excludes_moderated -x` exits 0.
    - `pytest apps/reviews/tests/test_tasks.py -k retry_includes_other_failure_codes -x` exits 0.
    - `pytest apps/reviews/tests/test_tasks.py -k "retry and moderated" -x` runs ≥ 1 test (D-25 regression).
    - `ruff check apps/reviews/tests/test_tasks.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pytest apps/reviews/tests/test_tasks.py -k "retry and (moderated or other_failure_codes)" -x -q</automated>
  </verify>
  <done>Two tests pass; content_moderated rows are excluded; other failure codes are still retried.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Beat scheduler → retry_failed_enrichments_task | Trusted internal scheduler; no untrusted input crosses |
| retry_failed_enrichments_task → enrich_review | Only filtered rows enqueued; moderated rows never re-trigger OpenAI calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-RT | Denial of Service (wasted OpenAI calls on immutable moderated content) | retry queryset | mitigate | `.exclude(enrichment_error_code="content_moderated")` filter ensures moderated rows never re-enqueue. Regression test enforces. |
</threat_model>

<verification>
- `pytest apps/reviews/tests/test_tasks.py -x` exits 0 (existing + new).
- `python manage.py check` exits 0.
</verification>

<success_criteria>
Beat-scheduled retry task no longer wastes OpenAI calls on content-moderated reviews; other transient failures continue to retry up to MAX_TOTAL_ENRICH_ATTEMPTS.
</success_criteria>

<output>
Create `.planning/phases/20-ai-guardrails/20-08-SUMMARY.md` when done.

## Phase 20 final verification (after Plan 20-08)

When this plan completes, the executor should run the full Phase 20 suite as documented in VALIDATION.md:
`pytest apps/integrations/openai/tests/test_guardrails.py apps/reviews/tests/test_enrichment_service.py apps/reviews/tests/test_reply_generation_service.py apps/reviews/tests/test_views.py apps/reviews/tests/test_tasks.py -x`

Followed by `pre-commit run --all-files` and `python manage.py makemigrations --check --dry-run`.
</output>
