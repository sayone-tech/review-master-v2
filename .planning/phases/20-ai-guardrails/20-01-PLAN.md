---
phase: 20-ai-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/settings/base.py
  - .env.example
autonomous: true
requirements: [D-03, D-21]
must_haves:
  truths:
    - "OPENAI_REVIEW_TEXT_MAX_CHARS is read from env at settings load with default 4000"
    - ".env.example documents the new variable"
  artifacts:
    - path: "config/settings/base.py"
      provides: "OPENAI_REVIEW_TEXT_MAX_CHARS setting"
      contains: "OPENAI_REVIEW_TEXT_MAX_CHARS"
    - path: ".env.example"
      provides: "Documented OPENAI_REVIEW_TEXT_MAX_CHARS=4000"
      contains: "OPENAI_REVIEW_TEXT_MAX_CHARS"
  key_links:
    - from: "config/settings/base.py"
      to: "apps/integrations/openai/guardrails.py (future)"
      via: "settings.OPENAI_REVIEW_TEXT_MAX_CHARS"
      pattern: "OPENAI_REVIEW_TEXT_MAX_CHARS"
---

<objective>
Add the env-configurable input length cap setting that downstream guardrails code (Plan 20-04) will consume for truncation.

Purpose: Implements D-21 — operations can tune the input truncation cap without code change.
Output: One new setting line in `config/settings/base.py` and a matching entry in `.env.example`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-ai-guardrails/20-CONTEXT.md
@.planning/phases/20-ai-guardrails/20-RESEARCH.md
@./CLAUDE.md

<interfaces>
From config/settings/base.py around line 158:
- `OPENAI_MAX_RETRIES = env.int("OPENAI_MAX_RETRIES", default=3)` — insert the new setting immediately after this line for natural grouping.
- Project uses `django-environ` style `env.int(...)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OPENAI_REVIEW_TEXT_MAX_CHARS setting + .env.example entry</name>
  <files>config/settings/base.py, .env.example</files>
  <read_first>
    - config/settings/base.py (lines 150-170 — locate OPENAI_MAX_RETRIES at line 158 per RESEARCH.md Code Landmarks)
    - .env.example (full file — match existing comment/grouping style)
    - .planning/phases/20-ai-guardrails/20-CONTEXT.md (D-03, D-21)
  </read_first>
  <action>
    In `config/settings/base.py`, immediately after the `OPENAI_MAX_RETRIES = env.int("OPENAI_MAX_RETRIES", default=3)` line (line 158 per RESEARCH.md), add:
    `OPENAI_REVIEW_TEXT_MAX_CHARS = env.int("OPENAI_REVIEW_TEXT_MAX_CHARS", default=4000)`

    In `.env.example`, append a line near the existing `OPENAI_*` block:
    `OPENAI_REVIEW_TEXT_MAX_CHARS=4000`

    Add a short Python comment above the setting line: "# Input cap for review text fed to OpenAI (D-21)."

    No other files modified. Do NOT pre-create `guardrails.py` — that lives in Plan 20-04.
  </action>
  <acceptance_criteria>
    - `grep -n "OPENAI_REVIEW_TEXT_MAX_CHARS" config/settings/base.py` returns exactly one match referencing `env.int(...)` with `default=4000`.
    - `grep -n "OPENAI_REVIEW_TEXT_MAX_CHARS" .env.example` returns exactly one match with value `4000`.
    - `python -c "import django; import os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','config.settings.base'); django.setup(); from django.conf import settings; assert settings.OPENAI_REVIEW_TEXT_MAX_CHARS == 4000"` exits 0 (run via `DJANGO_SETTINGS_MODULE=config.settings.local python -c "..."` in repo's container).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "OPENAI_REVIEW_TEXT_MAX_CHARS" config/settings/base.py && grep -q "OPENAI_REVIEW_TEXT_MAX_CHARS=4000" .env.example && python manage.py check --settings=config.settings.local</automated>
  </verify>
  <done>Setting exists, default is 4000, .env.example documents it, `manage.py check` passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → settings | Operator-controlled value; trusted but must be sanity-checked (int) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02 | Tampering | OPENAI_REVIEW_TEXT_MAX_CHARS env var | mitigate | `env.int()` coerces and rejects non-integer values; downstream consumer in Plan 20-04 enforces the cap before any OpenAI call (defense against indirect prompt injection via very long reviews) |
</threat_model>

<verification>
- `python manage.py check` exits 0.
- `python -c "from django.conf import settings; print(settings.OPENAI_REVIEW_TEXT_MAX_CHARS)"` prints `4000`.
- `.env.example` diff shows a single added line.
</verification>

<success_criteria>
Setting available to all downstream plans via `from django.conf import settings; settings.OPENAI_REVIEW_TEXT_MAX_CHARS`.
</success_criteria>

<output>
Create `.planning/phases/20-ai-guardrails/20-01-SUMMARY.md` when done.
</output>
