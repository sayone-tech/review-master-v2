---
phase: 20-ai-guardrails
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/integrations/openai/models.py
  - apps/integrations/openai/migrations/
  - apps/reviews/models.py
  - apps/reviews/migrations/
autonomous: true
requirements: [D-20, D-28, D-31]
must_haves:
  truths:
    - "AiUsageLog.Status enum has a MODERATED member equal to 'MODERATED'"
    - "Review model has an enrichment_error_code CharField (max_length=32, blank, default='')"
    - "Migrations apply cleanly on a fresh database"
  artifacts:
    - path: "apps/integrations/openai/models.py"
      provides: "AiUsageLog.Status.MODERATED enum value"
      contains: "MODERATED"
    - path: "apps/reviews/models.py"
      provides: "Review.enrichment_error_code field"
      contains: "enrichment_error_code"
    - path: "apps/integrations/openai/migrations/"
      provides: "AlterField migration adding MODERATED choice"
    - path: "apps/reviews/migrations/"
      provides: "AddField migration for enrichment_error_code"
  key_links:
    - from: "apps/integrations/openai/models.py"
      to: "apps/integrations/openai/guardrails.py (Plan 20-04)"
      via: "AiUsageLog.Status.MODERATED"
      pattern: "Status.MODERATED"
    - from: "apps/reviews/models.py (enrichment_error_code)"
      to: "apps/reviews/tasks.py (Plan 20-08)"
      via: ".exclude(enrichment_error_code='content_moderated')"
      pattern: "enrichment_error_code"
---

<objective>
Add the schema changes Phase 20 needs: a new `MODERATED` TextChoice on `AiUsageLog.Status` and a denormalized `enrichment_error_code` field on `Review`. Generate two `AlterField`/`AddField` migrations.

Purpose: Implements D-28 (uppercase enum casing — supersedes D-20 lowercase wording) and D-31 (denormalized error code field for the retry-task exclusion in D-25). Both are prerequisites for the guardrails module (Plan 20-04) and the retry-task change (Plan 20-08).
Output: Two model changes + two migrations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-ai-guardrails/20-CONTEXT.md
@.planning/phases/20-ai-guardrails/20-RESEARCH.md
@./CLAUDE.md

<interfaces>
From apps/integrations/openai/models.py (lines 55-101 per RESEARCH.md):
- `class AiUsageLog(models.Model)` has `class Status(models.TextChoices): SUCCESS = "SUCCESS", "Success"; FAILED = "FAILED", "Failed"` (verify exact form).
- `status = models.CharField(max_length=10, choices=Status.choices)` — `max_length=10` already accommodates `"MODERATED"` (9 chars). No max_length change.

From apps/reviews/models.py:
- `class Review(models.Model)` already has `enrichment_status` and `enrichment_version` fields. `enrichment_error_code` is NEW.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MODERATED to AiUsageLog.Status and generate migration</name>
  <files>apps/integrations/openai/models.py, apps/integrations/openai/migrations/</files>
  <read_first>
    - apps/integrations/openai/models.py (lines 55-101 — Status enum + status field)
    - .planning/phases/20-ai-guardrails/20-RESEARCH.md (Section: Code Landmarks → models.py; Pitfall 2)
    - .planning/phases/20-ai-guardrails/20-CONTEXT.md (D-20, D-28)
  </read_first>
  <action>
    In `apps/integrations/openai/models.py`, inside `class AiUsageLog.Status(models.TextChoices)`, add a new member: `MODERATED = "MODERATED", "Moderated"`. Place it after `FAILED` to maintain enum-declaration order matching state-machine order.

    Do NOT change `max_length` on the `status` field — 10 is sufficient (MODERATED is 9 chars).
    Do NOT change any other field, manager, or method.

    Generate the migration: `python manage.py makemigrations openai`. Migration file MUST be named descriptively — rename the auto-generated file to something like `NNNN_add_moderated_status_choice.py` (replace `NNNN` with the next sequence number) per CLAUDE.md §18.

    Verify the migration is a pure `AlterField` on `status` and contains exactly the three choices (SUCCESS, FAILED, MODERATED). No data migration.
  </action>
  <acceptance_criteria>
    - `grep -n 'MODERATED = "MODERATED"' apps/integrations/openai/models.py` returns exactly one match.
    - `grep -c "MODERATED" apps/integrations/openai/migrations/*add_moderated*.py` (after rename) ≥ 1.
    - `python manage.py makemigrations --check --dry-run` exits 0 (no further migrations needed).
    - `python manage.py migrate openai --plan` lists the new migration.
    - `python -c "from apps.integrations.openai.models import AiUsageLog; assert AiUsageLog.Status.MODERATED == 'MODERATED'"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python manage.py makemigrations --check --dry-run && python -c "from apps.integrations.openai.models import AiUsageLog; assert AiUsageLog.Status.MODERATED == 'MODERATED'"</automated>
  </verify>
  <done>Status enum updated, migration created with descriptive name, makemigrations --check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add Review.enrichment_error_code field and migration</name>
  <files>apps/reviews/models.py, apps/reviews/migrations/</files>
  <read_first>
    - apps/reviews/models.py (locate `class Review` and the existing `enrichment_status` / `enrichment_version` fields; place new field adjacent)
    - .planning/phases/20-ai-guardrails/20-RESEARCH.md (Section: Code Landmarks → tasks.py; Open Question 9)
    - .planning/phases/20-ai-guardrails/20-CONTEXT.md (D-31)
  </read_first>
  <action>
    In `apps/reviews/models.py`, on the `Review` model, add a new field directly after the existing `enrichment_status` / `enrichment_version` fields:

    `enrichment_error_code = models.CharField(max_length=32, blank=True, default="")`

    Add a docstring comment above the field: "Denormalized last-failure code; consumed by retry_failed_enrichments_task to skip 'content_moderated' rows (D-25, D-31). Populated by enrichment service helpers." Do NOT add db_index — the retry task scans a small filtered set already keyed on `enrichment_status=FAILED`.

    Generate the migration: `python manage.py makemigrations reviews`. Rename the auto-generated file to `NNNN_add_enrichment_error_code.py`. Verify it is a single `AddField`. Backfill is implicit via the `default=""`.
  </action>
  <acceptance_criteria>
    - `grep -n "enrichment_error_code = models.CharField" apps/reviews/models.py` returns exactly one match with `max_length=32`, `blank=True`, `default=""`.
    - `grep -l "enrichment_error_code" apps/reviews/migrations/*.py` lists exactly one new migration file.
    - `python manage.py makemigrations --check --dry-run` exits 0.
    - `python -c "from apps.reviews.models import Review; assert Review._meta.get_field('enrichment_error_code').max_length == 32"` exits 0.
    - `python manage.py migrate --plan` includes both new migrations (openai + reviews) in order.
  </acceptance_criteria>
  <verify>
    <automated>python manage.py makemigrations --check --dry-run && python -c "from apps.reviews.models import Review; f=Review._meta.get_field('enrichment_error_code'); assert f.max_length==32 and f.blank and f.default==''"</automated>
  </verify>
  <done>Field added, migration created and named descriptively, no other migrations pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live DB | Schema-only change; no PII flows through |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Repudiation | AiUsageLog moderated rows | mitigate | New MODERATED enum value preserves audit-row distinguishability; Plan 20-04 writes the row outside any atomic block (D-33) |
| T-20-DB | Tampering | enrichment_error_code | accept | Server-written only; never sourced from user input |
</threat_model>

<verification>
- `python manage.py migrate` applies cleanly on a fresh test DB.
- `python manage.py makemigrations --check --dry-run` exits 0 after the two migrations.
</verification>

<success_criteria>
Schema is in place for Plan 20-04 to write `AiUsageLog.Status.MODERATED` rows and Plan 20-08 to filter on `enrichment_error_code`.
</success_criteria>

<output>
Create `.planning/phases/20-ai-guardrails/20-03-SUMMARY.md` when done.
</output>
