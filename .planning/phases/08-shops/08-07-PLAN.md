---
phase: 08-shops
plan: 07
type: execute
wave: 2
depends_on:
  - 08-06
files_modified:
  - frontend/src/widgets/shop-management/types.ts
  - frontend/src/widgets/shop-management/api.ts
  - frontend/src/widgets/shop-management/CreateShopModal.tsx
  - frontend/src/widgets/shop-management/CreateShopModal.test.tsx
  - frontend/src/widgets/shop-management/EditShopModal.tsx
  - frontend/src/widgets/shop-management/ShopDetailsModal.tsx
  - frontend/src/widgets/shop-management/ShopTable.tsx
  - frontend/src/widgets/shop-management/ShopTable.test.tsx
  - frontend/src/widgets/shop-management/ShopRowActionsMenu.tsx
  - frontend/src/widgets/shop-management/ConnectionStatusPill.tsx
  - frontend/src/widgets/shop-management/ShopModals.tsx
  - frontend/src/widgets/shop-management/ShopModals.test.tsx
  - frontend/src/widgets/shop-management/RevealKeyModal.tsx
  - frontend/src/widgets/shop-management/RotateKeyModal.tsx
  - frontend/src/widgets/shop-management/OAuthConnectionSection.tsx
autonomous: true
gap_closure: true
requirements:
  - SHOP-04
  - SHOP-08
  - SHOP-09
  - SHOP-10
  - SHOP-11
  - SHOP-13
  - SHOP-15
  - SHOP-16
  - SHOP-19
  - SHOP-20

must_haves:
  truths:
    - "ConnectionMethod TypeScript type is 'GOOGLE_OAUTH' | 'NOT_CONNECTED' — no MANUAL"
    - "ShopRow has no city, state, zip_code, or api_key_masked fields"
    - "ShopCreatePayload has no city, state, zip_code, or api_key — only place_id, google_refresh_token (state token), region, name, phone, street_address, connection_method"
    - "ShopUpdatePayload has no city, state, zip_code"
    - "RotateKeyPayload and RevealKeyResponse types are deleted"
    - "api.ts has no revealKey, rotateKey functions"
    - "RevealKeyModal.tsx and RotateKeyModal.tsx are deleted"
    - "ShopRowActionsMenu list contains no Reveal Key, Rotate Key entries"
    - "ShopTable column list omits the api_key column"
    - "CreateShopModal has no Enter manually radio, no Place ID input, no API Key input — only the OAuth path"
    - "EditShopModal has no city/state/zip inputs and no MANUAL display logic"
    - "ShopDetailsModal grid omits City/State/ZIP, Place ID API Key row, and Connection Method 'Manual' branch"
    - "ConnectionStatusPill has no MANUAL/Connected via API key branch"
    - "ShopModals.tsx no longer subscribes to shop:open-reveal-key or shop:open-rotate-key events; no RevealKeyModal/RotateKeyModal imports"
    - "OAuthConnectionSection 'Connect Google Business Profile' button uses the yellow primary style"
    - "Vitest suite passes; Vite build produces a valid shop-management bundle"
  artifacts:
    - path: frontend/src/widgets/shop-management/types.ts
      provides: "Trimmed type surface — no MANUAL, no city/state/zip, no api_key types"
    - path: frontend/src/widgets/shop-management/CreateShopModal.tsx
      provides: "OAuth-only create flow — no connection method radio, no manual fields, no city/state/zip inputs"
    - path: frontend/src/widgets/shop-management/OAuthConnectionSection.tsx
      provides: "Yellow primary style on the Connect button (matches brand CTA)"
    - path: frontend/src/widgets/shop-management/ShopModals.tsx
      provides: "Orchestrator without reveal/rotate event handling"
  key_links:
    - from: frontend/src/widgets/shop-management/types.ts
      to: apps/shops/serializers.py
      via: "ShopRow / ShopCreatePayload / ShopUpdatePayload mirror the new Django serializer surface"
      pattern: "city|zip_code|api_key_masked"
    - from: frontend/src/widgets/shop-management/OAuthConnectionSection.tsx
      to: "Brand button style"
      via: "className uses bg-yellow text-black border border-yellow-hover hover:bg-yellow-hover"
      pattern: "bg-yellow"
    - from: frontend/src/widgets/shop-management/ShopRowActionsMenu.tsx
      to: frontend/src/widgets/shop-management/ShopTable.tsx
      via: "Action list excludes reveal/rotate"
      pattern: "Reveal Key|Rotate Key"
---

<objective>
Surgical frontend cleanup of Phase 8 to drop every reference to MANUAL connection, api_key UI, and city/state/zip_code address fields. Two whole modal files (RevealKeyModal.tsx, RotateKeyModal.tsx) are deleted; the Create modal is reduced to the OAuth-only path; the Details modal grid is trimmed; ShopTable + ShopRowActionsMenu lose their reveal/rotate items and the api_key column. Finally, the "Connect Google Business Profile" button is restyled from the outline (white) treatment to the brand yellow primary.

Purpose: Plan 06 has just shipped a backend that no longer accepts MANUAL/api_key/city/state/zip_code. Frontend types currently still encode the old contract; without this cleanup the UI would crash on the first list response (api_key_masked missing) and the modals would expose fields that the backend silently ignores. The OAuth button restyle is a UX correctness fix — it currently renders as a grey "Cancel-style" button on a CTA-heavy modal, breaking the brand language seen in every other primary button across the Org Admin UI (Add Shop, Save, etc.).

Output: A frontend that mirrors the new backend contract exactly: GOOGLE_OAUTH-only connection flow, single Address line in the form, no Reveal/Rotate UX surface, and a primary-yellow CTA in the OAuth section. Vitest passes; Vite build produces a valid bundle.
</objective>

<execution_context>
@/Users/renjith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/renjith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/08-shops/08-CONTEXT.md
@.planning/phases/08-shops/08-VERIFICATION.md
@.planning/phases/08-shops/08-06-PLAN.md

# Files to read before editing — current state
@frontend/src/widgets/shop-management/types.ts
@frontend/src/widgets/shop-management/api.ts
@frontend/src/widgets/shop-management/CreateShopModal.tsx
@frontend/src/widgets/shop-management/CreateShopModal.test.tsx
@frontend/src/widgets/shop-management/EditShopModal.tsx
@frontend/src/widgets/shop-management/ShopDetailsModal.tsx
@frontend/src/widgets/shop-management/ShopTable.tsx
@frontend/src/widgets/shop-management/ShopTable.test.tsx
@frontend/src/widgets/shop-management/ShopRowActionsMenu.tsx
@frontend/src/widgets/shop-management/ConnectionStatusPill.tsx
@frontend/src/widgets/shop-management/ShopModals.tsx
@frontend/src/widgets/shop-management/ShopModals.test.tsx
@frontend/src/widgets/shop-management/OAuthConnectionSection.tsx

# Brand reference for the yellow primary button (canonical pattern)
@frontend/src/widgets/region-management/RegionEmptyState.tsx

<interfaces>
<!-- Backend contract after Plan 06 (mirror this on frontend): -->
ShopRow fields: id, name, phone, street_address, place_id, connection_method ("GOOGLE_OAUTH"|"NOT_CONNECTED"),
                connection_status, is_active, region, region_name, region_region_id, created_at, updated_at.
ShopCreatePayload fields: name, region (required), connection_method ("GOOGLE_OAUTH"|"NOT_CONNECTED"),
                           place_id?, google_refresh_token? (state token, see Plan 08-03 for the resolution flow),
                           phone?, street_address?.
ShopUpdatePayload fields: name?, phone?, region?, street_address?.
Endpoints removed: /api/v1/shops/{id}/reveal_key/, /api/v1/shops/{id}/rotate_key/.
Endpoints retained: /api/v1/shops/, /api/v1/shops/{id}/, .../activate/, .../deactivate/, .../reconnect/, .../oauth_result/.

<!-- Yellow primary button class (copy verbatim) -->
className="inline-flex items-center gap-1.5 px-3.5 py-2 bg-yellow text-black border border-yellow-hover rounded-md text-[13.5px] font-semibold hover:bg-yellow-hover"
(use this exact set on the "Connect Google Business Profile" button in OAuthConnectionSection.tsx)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Types + API client + delete reveal/rotate modals</name>
  <files>
    frontend/src/widgets/shop-management/types.ts,
    frontend/src/widgets/shop-management/api.ts,
    frontend/src/widgets/shop-management/RevealKeyModal.tsx,
    frontend/src/widgets/shop-management/RotateKeyModal.tsx
  </files>
  <read_first>
    frontend/src/widgets/shop-management/types.ts,
    frontend/src/widgets/shop-management/api.ts,
    frontend/src/widgets/shop-management/RevealKeyModal.tsx,
    frontend/src/widgets/shop-management/RotateKeyModal.tsx
  </read_first>
  <action>
    1. Edit `frontend/src/widgets/shop-management/types.ts`:
       a. Change line 1:
          ```typescript
          export type ConnectionMethod = "GOOGLE_OAUTH" | "MANUAL" | "NOT_CONNECTED";
          ```
          to:
          ```typescript
          export type ConnectionMethod = "GOOGLE_OAUTH" | "NOT_CONNECTED";
          ```
       b. In `interface ShopRow`: DELETE the four lines: `city: string;`, `state: string;`, `zip_code: string;`, `api_key_masked: string;`. The remaining fields are: id, name, phone, street_address, place_id, connection_method, connection_status, is_active, region, region_name, region_region_id, created_at, updated_at.
       c. In `interface ShopCreatePayload`: DELETE the four lines: `api_key?: string;`, `city?: string;`, `state?: string;`, `zip_code?: string;`. Remaining: name, region, connection_method, place_id?, google_refresh_token?, phone?, street_address?.
       d. In `interface ShopUpdatePayload`: DELETE the three lines: `city?: string;`, `state?: string;`, `zip_code?: string;`. Remaining: name?, phone?, region?, street_address?.
       e. DELETE the entire `interface RotateKeyPayload { ... }`.
       f. DELETE the entire `interface RevealKeyResponse { ... }`.

    2. Edit `frontend/src/widgets/shop-management/api.ts`:
       a. In the import block at top, remove `RevealKeyResponse,` and `RotateKeyPayload,` from the type-only import.
       b. DELETE the entire `export async function revealKey(id: number): Promise<RevealKeyResponse> { ... }`.
       c. DELETE the entire `export async function rotateKey(id: number, payload: RotateKeyPayload): Promise<ShopRow> { ... }`.
       d. Leave all other exports (listShops, createShop, updateShop, activateShop, deactivateShop, reconnectShop, getOAuthResult) unchanged.

    3. DELETE the file `frontend/src/widgets/shop-management/RevealKeyModal.tsx` entirely (use `rm`).

    4. DELETE the file `frontend/src/widgets/shop-management/RotateKeyModal.tsx` entirely (use `rm`).
  </action>
  <verify>
    <!-- tsc --noEmit is intentionally NOT used here: it would FAIL because Tasks 2 + 3 still need
         to clean up consumer files. Use structural grep checks to confirm Task 1's surgical
         deletions instead. The authoritative type-check happens at the end of Task 3. -->
    <automated>test ! -f frontend/src/widgets/shop-management/RevealKeyModal.tsx && test ! -f frontend/src/widgets/shop-management/RotateKeyModal.tsx && ! grep -q '"MANUAL"' frontend/src/widgets/shop-management/types.ts && ! grep -q 'api_key_masked' frontend/src/widgets/shop-management/types.ts && ! grep -q 'RotateKeyPayload\|RevealKeyResponse' frontend/src/widgets/shop-management/types.ts && ! grep -q 'revealKey\|rotateKey' frontend/src/widgets/shop-management/api.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"MANUAL"' frontend/src/widgets/shop-management/types.ts` returns NON-ZERO
    - `grep -q 'api_key_masked' frontend/src/widgets/shop-management/types.ts` returns NON-ZERO
    - `grep -qE '\s+city: string' frontend/src/widgets/shop-management/types.ts` returns NON-ZERO
    - `grep -qE '\s+zip_code' frontend/src/widgets/shop-management/types.ts` returns NON-ZERO
    - `grep -q 'RotateKeyPayload\|RevealKeyResponse' frontend/src/widgets/shop-management/types.ts` returns NON-ZERO
    - `grep -q 'GOOGLE_OAUTH' frontend/src/widgets/shop-management/types.ts` exits 0 (still present)
    - `grep -q 'NOT_CONNECTED' frontend/src/widgets/shop-management/types.ts` exits 0
    - `grep -q 'revealKey\|rotateKey' frontend/src/widgets/shop-management/api.ts` returns NON-ZERO
    - `grep -q 'RotateKeyPayload\|RevealKeyResponse' frontend/src/widgets/shop-management/api.ts` returns NON-ZERO
    - `test ! -f frontend/src/widgets/shop-management/RevealKeyModal.tsx` exits 0
    - `test ! -f frontend/src/widgets/shop-management/RotateKeyModal.tsx` exits 0

    NOTE: TypeScript compilation is intentionally NOT checked at this point — Tasks 2 + 3 still
    need to clean up consumer files (CreateShopModal/EditShopModal/ShopModals/ShopTable). The
    authoritative `tsc --noEmit` + `npm test` + `npm run build` gate runs at the end of Task 3.
  </acceptance_criteria>
  <done>
    Type definitions and API client mirror the new backend contract. Reveal/Rotate modal files are deleted. Subsequent tasks fix the call sites.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Modals — Create, Edit, Details, Connection pill, OAuth button restyle</name>
  <files>
    frontend/src/widgets/shop-management/CreateShopModal.tsx,
    frontend/src/widgets/shop-management/CreateShopModal.test.tsx,
    frontend/src/widgets/shop-management/EditShopModal.tsx,
    frontend/src/widgets/shop-management/ShopDetailsModal.tsx,
    frontend/src/widgets/shop-management/ConnectionStatusPill.tsx,
    frontend/src/widgets/shop-management/OAuthConnectionSection.tsx
  </files>
  <read_first>
    frontend/src/widgets/shop-management/CreateShopModal.tsx,
    frontend/src/widgets/shop-management/CreateShopModal.test.tsx,
    frontend/src/widgets/shop-management/EditShopModal.tsx,
    frontend/src/widgets/shop-management/ShopDetailsModal.tsx,
    frontend/src/widgets/shop-management/ConnectionStatusPill.tsx,
    frontend/src/widgets/shop-management/OAuthConnectionSection.tsx,
    frontend/src/widgets/region-management/RegionEmptyState.tsx
  </read_first>
  <action>
    1. Edit `frontend/src/widgets/shop-management/CreateShopModal.tsx`:
       a. Remove all city / state / zip_code state hooks: delete `const [city, setCity] = useState(""); const [stateField, setStateField] = useState(""); const [zipCode, setZipCode] = useState("");`.
       b. Remove the corresponding lines in `reset()`: `setCity(""); setStateField(""); setZipCode("");`.
       c. In the `payload: ShopCreatePayload = { ... }` object inside `handleSubmit`, REMOVE the lines: `city,`, `state: stateField,`, `zip_code: zipCode,`.
       d. In the JSX form, DELETE the entire `<div className="grid grid-cols-3 gap-2">` block (the 3-col City/State/ZIP grid). Final form has only: connected listing pill, Shop Name, Region, Phone, Street Address — no city/state/zip row.
       e. Re-check the file: it currently contains a 3-step OAuth flow ("connect" → "pick" → "form"). Per CONTEXT.md the user decision is GOOGLE_OAUTH only; MANUAL is removed. The current modal is already OAuth-only (no MANUAL radio) — it just had MANUAL fields in scope from the older spec. After deleting city/state/zip and the api_key/place_id input pair (which the current file does not have at top level — re-read), the modal is correct. Confirm by grepping: `grep -n 'Enter manually\|Place ID\|API Key' frontend/src/widgets/shop-management/CreateShopModal.tsx` — current file has none of those strings; ONLY remove the city/state/zip block.

    2. Edit `frontend/src/widgets/shop-management/CreateShopModal.test.tsx`:
       a. The current test file has tests like `clicking Enter manually shows place_id and api_key fields` and `renders place_id field error when API responds with field errors` — these reference UI elements that don't exist in the current CreateShopModal (the modal was already OAuth-only). DELETE these now-invalid test cases:
          - DELETE: `it("clicking Enter manually shows place_id and api_key fields", ...)`
          - DELETE: `it("renders place_id field error when API responds with field errors", ...)`
          - DELETE: `it("renders non-field error at top when API responds with non_field_errors", ...)` — current modal has no manual submit path that would surface this; the OAuth flow is exercised via OAuthConnectionSection mocks which is out of scope here.
          - KEEP: `it("Google selected by default", ...)` — but rewrite the assertion: the current modal does NOT render a "Connect with Google" radio (it was already OAuth-only). Change this test to verify a meaningful smoke fact: rename to `it("renders the OAuth connection step initially")` and assert that the OAuth connect button or its testid is present:
            ```typescript
            it("renders the OAuth connect step on open", () => {
              render(<CreateShopModal open onClose={() => {}} onCreated={() => {}} regions={REGIONS} />);
              expect(screen.getByTestId("oauth-connect-button")).toBeInTheDocument();
            });
            ```
       b. Final test file has exactly 1 test that asserts the OAuth connect button is rendered. Remove unused imports (`fireEvent`, `waitFor`, `vi`, `api`, `ApiError`) if no longer referenced.

    3. Edit `frontend/src/widgets/shop-management/EditShopModal.tsx`:
       a. Remove the three useState hooks: `const [city, setCity] = useState(""); const [stateField, setStateField] = useState(""); const [zipCode, setZipCode] = useState("");`.
       b. Remove the three lines from the pre-fill useEffect: `setCity(shop.city ?? ""); setStateField(shop.state ?? ""); setZipCode(shop.zip_code ?? "");`. (These currently read removed properties — they'd be `undefined` and the `?? ""` keeps it safe, but it's cleaner to remove.)
       c. Remove from `reset()`: `setCity(""); setStateField(""); setZipCode("");`.
       d. Remove from `payload: ShopUpdatePayload = { ... }`: `city,`, `state: stateField,`, `zip_code: zipCode,`.
       e. DELETE the entire `<div className="grid grid-cols-3 gap-2">` block in JSX (the 3-col City/State/ZIP inputs).
       f. The locked Connection Method fieldset currently shows two options: "Connect with Google" and "Enter manually". DELETE the second `<label>` for "Enter manually" (lines around 170-181 of the current file). The legend stays but only Google option remains, which is shown checked when `shop?.connection_method === "GOOGLE_OAUTH"`.
       g. Remove the `connectionMethodLabel` const and the corresponding hidden `<input type="hidden" value={connectionMethodLabel} />` (no longer needed; the label no longer references MANUAL).

    4. Edit `frontend/src/widgets/shop-management/ShopDetailsModal.tsx`:
       a. DELETE the `<Row label="City / State / ZIP">` row (lines ~136-138).
       b. DELETE the `<Row label="API Key">` row (lines ~142-144).
       c. In the `<Row label="Connection Method">` cell, simplify the ternary to remove the MANUAL branch:
          ```tsx
          <Row label="Connection Method">
            {shop.connection_method === "GOOGLE_OAUTH" ? "Google OAuth" : "Not connected"}
          </Row>
          ```

    5. Edit `frontend/src/widgets/shop-management/ConnectionStatusPill.tsx`:
       a. DELETE the entire `if (method === "MANUAL" && status === "CONNECTED") { ... }` branch (lines 21-23) — returns the blue "Connected via API key" pill.
       b. The remaining branches handle ERROR/EXPIRED, QUOTA_EXCEEDED, GOOGLE_OAUTH+CONNECTED, and the NOT_CONNECTED fallback. Confirm via re-read.

    6. Edit `frontend/src/widgets/shop-management/OAuthConnectionSection.tsx`:
       a. Replace the existing button className. Find the `return ( <button ... className="..." ...>` block at the bottom (around line 141-150). Change className to:
          ```typescript
          className="inline-flex items-center gap-1.5 px-3.5 py-2 bg-yellow text-black border border-yellow-hover rounded-md text-[13.5px] font-semibold hover:bg-yellow-hover"
          ```
          (Removes the `bg-white text-ink border border-line hover:bg-line-soft` outline style; replaces with brand yellow primary matching `RegionEmptyState.tsx` and other CTAs.)
       b. Keep the `<ExternalLink size={14} aria-hidden="true" />` icon and "Connect Google Business Profile" label intact.
       c. Keep `data-testid="oauth-connect-button"`.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'setCity\|setZipCode\|setStateField' frontend/src/widgets/shop-management/CreateShopModal.tsx` returns NON-ZERO
    - `grep -q 'cs-city\|cs-state\|cs-zip' frontend/src/widgets/shop-management/CreateShopModal.tsx` returns NON-ZERO
    - `grep -q 'grid-cols-3' frontend/src/widgets/shop-management/CreateShopModal.tsx` returns NON-ZERO
    - `grep -q 'setCity\|setZipCode\|setStateField' frontend/src/widgets/shop-management/EditShopModal.tsx` returns NON-ZERO
    - `grep -q 'es-city\|es-state\|es-zip' frontend/src/widgets/shop-management/EditShopModal.tsx` returns NON-ZERO
    - `grep -q 'connection_method === "MANUAL"' frontend/src/widgets/shop-management/EditShopModal.tsx` returns NON-ZERO
    - `grep -q 'Enter manually' frontend/src/widgets/shop-management/EditShopModal.tsx` returns NON-ZERO
    - `grep -q 'City / State / ZIP\|api_key_masked\|"MANUAL"' frontend/src/widgets/shop-management/ShopDetailsModal.tsx` returns NON-ZERO
    - `grep -q 'API Key' frontend/src/widgets/shop-management/ShopDetailsModal.tsx` returns NON-ZERO
    - `grep -q 'Connected via API key\|method === "MANUAL"' frontend/src/widgets/shop-management/ConnectionStatusPill.tsx` returns NON-ZERO
    - `grep -q 'bg-yellow text-black border border-yellow-hover' frontend/src/widgets/shop-management/OAuthConnectionSection.tsx` exits 0
    - `grep -q 'Connect Google Business Profile' frontend/src/widgets/shop-management/OAuthConnectionSection.tsx` exits 0
    - `grep -q 'bg-white.*hover:bg-line-soft' frontend/src/widgets/shop-management/OAuthConnectionSection.tsx` returns NON-ZERO (old outline style removed)
    - `cd frontend && npm run build` exits 0 — Vite build succeeds
  </acceptance_criteria>
  <done>
    All modals reflect the OAuth-only, single-address contract. Details modal grid no longer shows City/State/ZIP or API Key. Connection pill no longer renders the blue "Connected via API key" state. OAuth connect button uses the brand yellow primary style.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: ShopTable + ShopRowActionsMenu + ShopModals + tests</name>
  <files>
    frontend/src/widgets/shop-management/ShopTable.tsx,
    frontend/src/widgets/shop-management/ShopTable.test.tsx,
    frontend/src/widgets/shop-management/ShopRowActionsMenu.tsx,
    frontend/src/widgets/shop-management/ShopModals.tsx,
    frontend/src/widgets/shop-management/ShopModals.test.tsx
  </files>
  <read_first>
    frontend/src/widgets/shop-management/ShopTable.tsx,
    frontend/src/widgets/shop-management/ShopTable.test.tsx,
    frontend/src/widgets/shop-management/ShopRowActionsMenu.tsx,
    frontend/src/widgets/shop-management/ShopModals.tsx,
    frontend/src/widgets/shop-management/ShopModals.test.tsx
  </read_first>
  <action>
    1. Edit `frontend/src/widgets/shop-management/ShopTable.tsx`:
       a. Remove imports of `Eye`, `Key` from lucide-react if no longer used after deletions (re-check after step 1c). The "details" action uses Eye — KEEP. The "reveal" + "rotate" actions used Eye + Key — those entries get deleted; Key icon import becomes unused → remove.
       b. In `SHOP_ACTIONS: RowAction[]`, DELETE three entries:
          - The `key: "reveal"` entry (uses Eye, separatorBefore, MANUAL visibility)
          - The `key: "rotate"` entry (uses Key, MANUAL visibility)
          - Update the `key: "reconnect"` entry's `separatorBefore: true` to remain (it groups Reconnect under a separator after activate/deactivate now that reveal/rotate are gone).
       c. In the JSX, find the columns array declaration. DELETE the entire column block:
          ```tsx
          {
            key: "api_key",
            label: "API KEY",
            ...
            accessor: (row: ShopRow) => row.api_key_masked ? ... : ...,
          },
          ```
          The column at index 5 disappears; the rest shifts up.
       d. Update the LOCATION column accessor to drop city/state/zip rendering. Change:
          ```tsx
          accessor: (row: ShopRow) => (
            <div>
              <div className="text-[13px]">{row.street_address || "—"}</div>
              <div className="text-[12px] text-muted">
                {[row.city, row.state, row.zip_code].filter(Boolean).join(", ")}
              </div>
            </div>
          ),
          ```
          to:
          ```tsx
          accessor: (row: ShopRow) =>
            row.street_address ? row.street_address : <span className="text-muted">—</span>,
          ```
          (Single-line; the secondary city/state/zip line is gone because those fields no longer exist.)
       e. Update the search placeholder text: change `placeholder="Search name, address, city…"` to `placeholder="Search name, address…"`.

    2. Edit `frontend/src/widgets/shop-management/ShopTable.test.tsx`:
       a. In the `fakeRow()` helper, DELETE the four lines: `city: "",`, `state: "",`, `zip_code: "",`, `api_key_masked: "",`.
       b. DELETE the test `it("Reveal Key item visible only for MANUAL shop", ...)`.
       c. DELETE the test `it("Reveal Key NOT visible for OAuth shop", ...)`.
       d. KEEP all other tests (rendering, empty states, click dispatch, pagination).

    3. Edit `frontend/src/widgets/shop-management/ShopRowActionsMenu.tsx`:
       a. No structural changes needed — this file is a generic actions menu and doesn't reference reveal/rotate by name. RE-READ to confirm. (The action LIST is defined in ShopTable.tsx; the menu just iterates `actions: RowAction[]`.) NO EDITS to this file unless the TypeScript compiler flags an unused import. If `RowAction` interface is unchanged, this file stays as-is.

    4. Edit `frontend/src/widgets/shop-management/ShopModals.tsx`:
       a. Remove imports: `import { RevealKeyModal } from "./RevealKeyModal";`, `import { RotateKeyModal } from "./RotateKeyModal";` — both delete; the imported files no longer exist.
       b. Remove state declarations: `const [revealOpen, setRevealOpen] = useState(false);`, `const [rotateOpen, setRotateOpen] = useState(false);`.
       c. In the `useEffect` that subscribes to events, REMOVE the two array entries for `"shop:open-reveal-key"` and `"shop:open-rotate-key"`. The remaining map has 5 events: details, edit, deactivate, activate, reconnect.
       d. DELETE the JSX:
          ```tsx
          <RevealKeyModal open={revealOpen} shop={selected} onClose={...} />
          <RotateKeyModal open={rotateOpen} shop={selected} onClose={...} onRotated={...} />
          ```
       e. Remove unused imports if any (`activateShop`, `deactivateShop`, `reconnectShop` all stay).

    5. Edit `frontend/src/widgets/shop-management/ShopModals.test.tsx`:
       a. In the `row()` helper, DELETE the four lines: `city: "",`, `state: "",`, `zip_code: "",`, `api_key_masked: "",`.
       b. KEEP all existing tests — they exercise deactivate / activate / details / refresh / at-limit, none of which touch reveal/rotate.

    6. Run the full Vitest suite:
       ```bash
       cd frontend && npm test -- --run src/widgets/shop-management/
       ```
       All tests must pass.

    7. Run Vite build:
       ```bash
       cd frontend && npm run build
       ```
       Build must succeed with no TypeScript errors.

    8. Run pre-commit on all modified frontend files (no Python hooks fire on .ts/.tsx, but format hooks may):
       ```bash
       pre-commit run --files frontend/src/widgets/shop-management/types.ts frontend/src/widgets/shop-management/api.ts frontend/src/widgets/shop-management/CreateShopModal.tsx frontend/src/widgets/shop-management/CreateShopModal.test.tsx frontend/src/widgets/shop-management/EditShopModal.tsx frontend/src/widgets/shop-management/ShopDetailsModal.tsx frontend/src/widgets/shop-management/ConnectionStatusPill.tsx frontend/src/widgets/shop-management/OAuthConnectionSection.tsx frontend/src/widgets/shop-management/ShopTable.tsx frontend/src/widgets/shop-management/ShopTable.test.tsx frontend/src/widgets/shop-management/ShopModals.tsx frontend/src/widgets/shop-management/ShopModals.test.tsx
       ```
       All hooks pass.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/widgets/shop-management/ && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'key: "reveal"\|key: "rotate"' frontend/src/widgets/shop-management/ShopTable.tsx` returns NON-ZERO
    - `grep -q 'shop:open-reveal-key\|shop:open-rotate-key' frontend/src/widgets/shop-management/ShopTable.tsx` returns NON-ZERO
    - `grep -q 'key: "api_key"\|API KEY' frontend/src/widgets/shop-management/ShopTable.tsx` returns NON-ZERO
    - `grep -q 'row.city\|row.state\|row.zip_code\|row.api_key_masked' frontend/src/widgets/shop-management/ShopTable.tsx` returns NON-ZERO
    - `grep -q 'key: "details"' frontend/src/widgets/shop-management/ShopTable.tsx` exits 0 (kept)
    - `grep -q 'key: "edit"' frontend/src/widgets/shop-management/ShopTable.tsx` exits 0 (kept)
    - `grep -q 'key: "reconnect"' frontend/src/widgets/shop-management/ShopTable.tsx` exits 0 (kept)
    - `grep -q 'RevealKeyModal\|RotateKeyModal' frontend/src/widgets/shop-management/ShopModals.tsx` returns NON-ZERO
    - `grep -q 'shop:open-reveal-key\|shop:open-rotate-key' frontend/src/widgets/shop-management/ShopModals.tsx` returns NON-ZERO
    - `grep -q 'shop:open-deactivate' frontend/src/widgets/shop-management/ShopModals.tsx` exits 0 (kept)
    - `grep -q 'shop:open-reconnect' frontend/src/widgets/shop-management/ShopModals.tsx` exits 0 (kept)
    - `grep -q 'api_key_masked' frontend/src/widgets/shop-management/ShopTable.test.tsx` returns NON-ZERO
    - `grep -q 'api_key_masked' frontend/src/widgets/shop-management/ShopModals.test.tsx` returns NON-ZERO
    - `grep -q 'Reveal Key item visible only for MANUAL\|Reveal Key NOT visible for OAuth' frontend/src/widgets/shop-management/ShopTable.test.tsx` returns NON-ZERO
    - `cd frontend && npx tsc --noEmit` exits 0 — full TypeScript compile is now clean
    - `cd frontend && npm test -- --run src/widgets/shop-management/` exits 0 — Vitest suite green
    - `cd frontend && npm run build` exits 0 — Vite build succeeds
    - `test ! -f frontend/src/widgets/shop-management/RevealKeyModal.tsx` exits 0
    - `test ! -f frontend/src/widgets/shop-management/RotateKeyModal.tsx` exits 0
  </acceptance_criteria>
  <done>
    ShopTable column list and row action menu omit api_key/reveal/rotate. ShopModals orchestrator no longer subscribes to reveal/rotate events. All tests pass; Vite build succeeds; TypeScript compile is clean. Frontend fully mirrors the new backend contract.
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npm test` — full Vitest suite green
- `cd frontend && npm run build` — Vite build produces a valid `shop-management` bundle
- `cd frontend && npx tsc --noEmit` — clean TypeScript compile
- `pytest apps/shops/ apps/integrations/ -x -q` — backend still green (sanity re-check; Plan 06 ensures this)
- Manual smoke (post-deploy): visit `/admin/org/shops/`, click "+ Add Shop", verify the modal shows ONLY the OAuth Connect button (no manual radio); the button is yellow; the form has Shop Name, Region, Phone, Street Address (no city/state/zip).
</verification>

<success_criteria>
- ConnectionMethod type is `"GOOGLE_OAUTH" | "NOT_CONNECTED"`.
- ShopRow has no city, state, zip_code, api_key_masked.
- ShopCreatePayload / ShopUpdatePayload have no city/state/zip/api_key.
- RotateKeyPayload + RevealKeyResponse types are deleted.
- api.ts has no revealKey/rotateKey functions.
- RevealKeyModal.tsx + RotateKeyModal.tsx files are deleted.
- CreateShopModal renders only the OAuth path (no manual radio, no API key field).
- EditShopModal has no city/state/zip inputs and no MANUAL display branch.
- ShopDetailsModal grid omits City/State/ZIP and API Key rows; Connection Method ternary has only Google/Not connected.
- ConnectionStatusPill has no MANUAL/Connected via API key branch.
- ShopTable columns omit API KEY column; LOCATION renders street_address only; search placeholder updated.
- ShopRowActionsMenu list contains no Reveal/Rotate entries.
- ShopModals doesn't subscribe to shop:open-reveal-key or shop:open-rotate-key.
- "Connect Google Business Profile" button uses `bg-yellow text-black border border-yellow-hover hover:bg-yellow-hover`.
- All Vitest tests pass; Vite build succeeds; tsc --noEmit clean.
</success_criteria>

<output>
After completion, create `.planning/phases/08-shops/08-07-SUMMARY.md` capturing:
- Files deleted (RevealKeyModal.tsx, RotateKeyModal.tsx) and lines removed
- Test cases deleted (count and names)
- Confirmation that the OAuth Connect button now uses brand yellow primary
- Vitest test count + duration
- Vite bundle size delta (before vs after)
- Any unexpected discoveries during cleanup
</output>
