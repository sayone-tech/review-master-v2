---
phase: 14
slug: dashboard
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-07
---

# Phase 14 — UI Design Contract

> Visual and interaction contract for the Dashboard phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — Tailwind CSS v4 with custom @theme tokens |
| Preset | not applicable |
| Component library | none — hand-crafted Tailwind components |
| Icon library | lucide-react 1.8.0 (already installed) |
| Font | Geist (loaded via CDN or bundled); system-ui fallback chain |

No new design system tooling is introduced. This phase reuses the established token set from `frontend/src/styles/tailwind.css`.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gaps, tight inline padding |
| sm | 8px | Within-card spacing, badge padding |
| md | 16px | Default element and card padding (`p-4`) |
| lg | 24px | Between filter bar and widget grid, card-to-card gaps |
| xl | 32px | Page section vertical separation |
| 2xl | 48px | Error page vertical centering offset |
| 3xl | 64px | Sidebar rail width (spacing-sidebar-rail — existing token) |

Exceptions:
- Filter bar row gap: 8px (`gap-2`) — compact row matching `ReviewFilters.tsx` precedent
- KPI card internal gap between title row and metric value: 6px (`gap-1.5`) — matches `ReviewStatsCards.tsx` precedent
- Bar chart `ResponsiveContainer` min-height: 240px — chart-specific, not a spacing token
- Donut chart `ResponsiveContainer` height: 200px — chart-specific
- Touch targets minimum: 44px height for all interactive filter controls (WCAG 2.5.5)

---

## Typography

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 13.5px | 400 | 1.5 | Filter dropdowns, tooltip text, coverage footer, error page body copy |
| Label | 11px | 600 (semibold) | 1.4 | KPI card titles (all-caps + letter-spacing 0.06em), chart axis labels |
| Heading | 20px | 700 | 1.2 | KPI metric values, error page code number (48px for the large 404/500 numeral — display only) |
| Sub-heading | 14px | 600 (semibold) | 1.4 | Section headings ("Best Performing Outlets", "Sentiment Distribution"), performance highlights sub-card labels, "Your Store" card shop name |

Display variant for error pages only:
- Error code numeral (404 / 500): 48px, weight 700, color `--color-faint` (#A1A1AA), line-height 1.0

All font sizes use `text-[Npx]` Tailwind arbitrary values, consistent with the established codebase pattern (see `ReviewStatsCards.tsx`).

---

## Color

| Role | Value | Token | Usage |
|------|-------|-------|-------|
| Dominant (60%) | #FAFAFA | `--color-bg` | Page background, main content area |
| Surface | #FFFFFF | white | Card backgrounds, filter control backgrounds, date picker panel |
| Secondary (30%) | #F4F4F5 | `--color-line-soft` | Skeleton loading backgrounds, muted inner areas, chart background |
| Border | #E4E4E7 | `--color-line` | Card borders, input borders, dividers |
| Accent (10%) | #FACC15 | `--color-yellow` | Active filter indicator dot, focus ring, primary CTA button text |
| Text primary | #18181B | `--color-ink` | Metric values, headings |
| Text secondary | #27272A | `--color-text` | Standard body text |
| Text muted | #52525B | `--color-muted` | Subtitles, footer text, chart axis labels |
| Text subtle | #71717A | `--color-subtle` | Label captions, date range subtitle |
| Text faint | #A1A1AA | `--color-faint` | Error code numeral, placeholder text |
| Success green | #16A34A | `--color-green` | Bar threshold ≥4.0, Sentiment Positive segment, highlights top-performer sub-card border/icon, trend up indicator |
| Success tint | #DCFCE7 | `--color-green-tint` | Top performer sub-card background |
| Warning amber | #D97706 | `--color-amber` | Bar threshold 3.0–3.99, Sentiment Neutral segment |
| Warning tint | #FEF3C7 | `--color-amber-tint` | Coverage warning callout background |
| Danger red | #DC2626 | `--color-red` | Bar threshold <3.0, Sentiment Negative segment, highlights bottom-performer sub-card border/icon, trend down indicator, Negative KPI value |
| Danger tint | #FEE2E2 | `--color-red-tint` | Bottom performer sub-card background |
| CTA black | #0A0A0A | `--color-black` | Primary button background (Apply, bar chart click), error page CTA |

NOTE: The requirements doc (SENT-02) specifies Positive #22C55E, Neutral #F59E0B, Negative #EF4444 for the sentiment chart segments. These are the Tailwind 500-level palette values. The project's custom tokens `--color-green` (#16A34A) and `--color-red` (#DC2626) are the 600-level and are already used in `ReviewStatsCards.tsx` for sentiment. For chart segment fills, use the exact hex values from the requirements doc (#22C55E, #F59E0B, #EF4444) as hardcoded recharts fill props — these are not Tailwind class usages but SVG fill values. For all non-chart color usage (borders, sub-card tints, badges), use the project tokens above.

Accent (#FACC15) reserved for:
- Focus ring on all interactive elements (`:focus-visible` — already global)
- Active filter dropdown indicator (a 6px yellow dot or underline when a non-default value is selected)
- Star icon fill inside Average Rating KPI card (consistent with `ReviewStatsCards.tsx`)
- Primary CTA button text color when button background is `--color-black`

---

## Component Inventory

### 1. Dashboard Page Shell
- Extends `base_org.html` — inherits sidebar, topbar, content block
- Content block: `px-6 py-6` padding, `bg-bg` background, `max-w-[1400px] mx-auto`
- Two-column layout for the top section (Top Performers + KPI/Sentiment): `grid grid-cols-[1fr_320px] gap-6` on desktop (≥1280px), single column on tablet/mobile
- UTC notice: 12px `text-subtle` text positioned inline in the filter bar row, right-aligned: "All dates in UTC"

### 2. Filter Bar
Source: `frontend/src/widgets/dashboard/FilterBar.tsx`

Structure: single horizontal row of flex controls, wraps on mobile
- Region select: `<select>` element, same Tailwind classes as `ReviewFilters.tsx` (`px-3 py-2 text-[13.5px] bg-white border border-line rounded-md`), `min-w-[160px]`
- Store select: same classes, `min-w-[160px]`, options cascade from selected region
- Date Range select: same classes, `min-w-[160px]`, options: "Last 7 days" / "Last 30 days" (default) / "Last 90 days" / "Custom"
- Custom date panel: appears directly below the Date Range select via `absolute` positioning, `top-[calc(100%+4px)]`, `z-50`, white card `rounded-card border border-line shadow-sm p-4`, width `320px`
  - Two `<input type="date">` inputs side by side: `w-full px-3 py-2 text-[13.5px] bg-white border border-line rounded-md`
  - Inline validation error: `text-[12px] text-red` below the inputs
  - Apply button: `px-4 py-2 bg-black text-yellow text-[13.5px] font-semibold rounded-md`
- Clear Filters button: disabled appearance (`opacity-40 cursor-not-allowed`) when all filters are at defaults; enabled appearance matches the Reset button in `ReviewFilters.tsx` (`text-muted hover:text-ink border border-line rounded-md bg-white`)
- UTC notice: `text-[12px] text-subtle ml-auto self-center` ("All dates in UTC")

### 3. KPI Cards
Source: `frontend/src/widgets/dashboard/KpiCards.tsx`

Three equal-width cards in a CSS grid: `grid grid-cols-3 gap-4`
Card structure (identical to `ReviewStatsCards.tsx` precedent):
- Container: `bg-white border border-line rounded-card p-4 flex flex-col gap-1.5`
- Title row: label `text-[11px] font-semibold text-subtle uppercase tracking-[0.06em]` + icon `size={16}` top-right
- Metric value: `text-[26px] font-bold leading-none tabular-nums`
- Footer text: `text-[12px] text-muted`

Card-specific colors:
- Total Reviews: icon `FileText` color `--color-faint`, value color `--color-ink`
- Average Rating: icon `Star` filled `--color-yellow`, value color `--color-ink`; half-star at decimal .25–.74 uses a split SVG star (2 `Star` halves or a single `StarHalf` icon)
- Negative Reviews: icon `TrendingDown` color `--color-red`, value color `--color-red`; percentage shown as `text-[14px] text-red` below the count

Loading skeleton: `bg-line-soft rounded animate-[sk-pulse_1.6s_ease-in-out_infinite]` blocks matching the exact card dimensions — title row skeleton `h-3 w-24 mb-1.5`, value skeleton `h-7 w-16`, footer skeleton `h-3 w-32`

Empty state (no reviews in period): value shows "—", footer shows "No reviews in this period"

Error state (fetch failed): replaces card content with `text-[12px] text-muted` message "Could not load." + `<button>` "Retry" in `text-[12px] font-semibold text-ink underline`

### 4. Top Performing Outlets — Bar Chart
Source: `frontend/src/widgets/dashboard/TopPerformingSection.tsx`

Container card: `bg-white border border-line rounded-card p-4`
Section heading: `text-[14px] font-semibold text-ink mb-4` — "Best Performing Outlets"
Date scope note: `text-[12px] text-subtle` — "(date range only)"

recharts `BarChart` configuration:
- `ResponsiveContainer width="100%" height={240}`
- Bar orientation: vertical bars (shop name on X-axis, rating on Y-axis, 0–5 scale)
- Y-axis: domain `[0, 5]`, ticks `[1, 2, 3, 4, 5]`, `tick={{ fontSize: 11 }}`
- X-axis: shop name, `tick={{ fontSize: 11 }}`, `interval={0}`, truncate to 16 chars + ellipsis if longer
- Bar: `<Bar dataKey="avg_rating" radius={[4, 4, 0, 0]}>`; fill determined per-bar by `<Cell fill={barColor(rating)}>`
- Bar color function: rating ≥ 4.0 → `#22C55E`; 3.0–3.99 → `#F59E0B`; < 3.0 → `#EF4444`
- `<Tooltip>` custom content component: white card, `rounded-md shadow border border-line p-2`, 12px text, shows shop name bold + "Avg: {rating}" + "{N} reviews"
- Visual gap between Top 5 and Worst 5 (when >10 shops): render a placeholder `<Cell fill="transparent">` separator bar or a thin vertical rule with `text-[11px] text-subtle` label "· · ·"
- Cursor: `cursor-pointer` on bar hover; `onClick` calls `window.location.href` navigation

### 5. Performance Highlights
Source: `frontend/src/widgets/dashboard/PerformanceHighlights.tsx`

Container: stacks vertically in the right ~1/3 of the top section (`flex flex-col gap-4`)

Top sub-card (highest rated): `bg-green-tint border border-green/30 rounded-card p-4`
- Header: `TrendingUp` icon 16px `text-green` + label `text-[13px] font-semibold text-green` "Top Performer"
- Shop name: `text-[14px] font-semibold text-ink`
- Rating: `text-[22px] font-bold text-green tabular-nums`
- Positive review count: `text-[12px] text-muted` "N positive reviews (AI-derived)"

Bottom sub-card (lowest rated): `bg-red-tint border border-red/30 rounded-card p-4`
- Header: `TrendingDown` icon 16px `text-red` + label `text-[13px] font-semibold text-red` "Needs Attention"
- Shop name: `text-[14px] font-semibold text-ink`
- Rating: `text-[22px] font-bold text-red tabular-nums`
- Negative review count: `text-[12px] text-muted` "N negative reviews (AI-derived)"

### 6. Your Store Card (single-shop variant)
Source: `frontend/src/widgets/dashboard/YourStore.tsx`

Replaces bar chart + highlights when exactly 1 accessible shop.
Container: `bg-white border border-line rounded-card p-6`
Section heading: `text-[14px] font-semibold text-ink mb-1` "Your Store"

Layout: two-column `grid grid-cols-[1fr_auto] gap-4 items-start`

Left column:
- Shop name: `text-[18px] font-bold text-ink`
- Region badge: `inline-flex items-center px-2 py-0.5 bg-line-soft text-[11px] font-semibold text-subtle rounded-full` (matches existing badge pattern)
- Avg rating: `text-[36px] font-bold text-ink tabular-nums` + `text-[14px] text-muted` "out of 5"
- Total reviews: `text-[13px] text-muted` "N reviews"
- Positive count: `text-[13px] text-green font-semibold` "N positive (P%)"
- Negative count: `text-[13px] text-red font-semibold` "N negative (P%)"

Right column — trend indicator:
- Up: `↑` `text-[16px] font-bold text-green` + delta value `text-[13px] text-green`
- Down: `↓` `text-[16px] font-bold text-red` + delta value `text-[13px] text-red`
- Flat / no previous data: `—` `text-[16px] text-faint` + `text-[12px] text-subtle` "No previous data"

5-star rating distribution mini-bars:
- 5 rows, each: star label `text-[11px] text-muted w-4` + bar track `bg-line-soft h-2 rounded-full flex-1` + filled segment `bg-yellow h-2 rounded-full` proportional to count + count `text-[11px] text-muted w-6 text-right`

### 7. Sentiment Distribution Donut
Source: `frontend/src/widgets/dashboard/SentimentDonut.tsx`

Container card: `bg-white border border-line rounded-card p-4`
Section heading: `text-[14px] font-semibold text-ink mb-4` "Sentiment Distribution"

recharts `PieChart` configuration:
- `ResponsiveContainer width="100%" height={200}`
- `<Pie dataKey="value" innerRadius="55%" outerRadius="80%" paddingAngle={2} startAngle={90} endAngle={-270}`
- Three `<Cell>`: Positive `#22C55E`, Neutral `#F59E0B`, Negative `#EF4444`
- `<Tooltip>` custom: label + count + percentage, same card style as bar chart tooltip
- No inner text label (hollow center per requirements)
- ARIA: `role="img" aria-label="Sentiment distribution donut chart"`

Legend (below donut): three rows, each `flex items-center justify-between gap-2`
- Color swatch: `w-3 h-3 rounded-sm shrink-0` with matching fill
- Label: `text-[13px] text-text`
- Count: `text-[13px] font-semibold text-ink tabular-nums`
- Percentage: `text-[12px] text-muted`
- Progress bar: `bg-line-soft h-2 rounded-full` track, filled `h-2 rounded-full` with matching color

Coverage footer: `text-[11px] text-subtle mt-3 pt-3 border-t border-line`
- Default: "Based on N enriched reviews (X% of total)"
- Below 50% enrichment: adds `Loader2` icon 12px spinning + " Analysis is still in progress."

Empty states:
- No reviews in window: `text-[13px] text-muted text-center py-8` "No reviews to analyze in this period."
- Reviews exist but none enriched: `text-[13px] text-muted text-center py-8` with `Loader2` spinner icon above + "Sentiment analysis is in progress. Check back shortly."

### 8. Combined Empty State (Top Performers section)
When no shops qualify (< 3 reviews each in the date window):
- Centered within the card area: `text-center py-12`
- `BarChart2` icon 32px `text-faint` mb-3
- Heading: `text-[14px] font-semibold text-ink` "No qualifying stores in this period"
- Body: `text-[13px] text-muted` "Stores need at least 3 reviews in the selected period to appear here."
- CTA button: `mt-4 inline-flex items-center gap-1.5 px-4 py-2 bg-black text-yellow text-[13.5px] font-semibold rounded-md` "View last 90 days"

### 9. Loading Skeletons
All skeletons use `bg-line-soft rounded animate-[sk-pulse_1.6s_ease-in-out_infinite]` (reuses existing `@keyframes sk-pulse` from tailwind.css).

KPI card skeleton (per card, see §3 above): matches exact card dimensions.

Bar chart skeleton: single `bg-line-soft rounded-card` block at full card height (280px including header).

Donut skeleton: centered `bg-line-soft rounded-full` circle 160px × 160px within the card.

Highlights skeleton: two stacked rectangular blocks matching sub-card heights.

### 10. Error Pages (404 / 500)
Source: `templates/404.html`, `templates/500.html`

Standalone full-page — does NOT extend `base_org.html`. Extends a minimal `base_error.html` or is self-contained with `<!DOCTYPE html>`.

Layout: `min-h-screen bg-white flex flex-col items-center justify-center p-8`

Structure (top to bottom, centered, max-width 480px):
1. Logo: `<img src="{% static 'logo/main_logo.png' %}" alt="Review Master" class="h-10 mb-8">`
2. Error code numeral: `text-[48px] font-bold text-faint leading-none mb-2` — "404" or "500"
3. Title: `text-[20px] font-bold text-ink mb-2` — "Page Not Found" or "Something went wrong"
4. Body: `text-[14px] text-muted text-center mb-6 max-w-[320px]`
   - 404: "The page you're looking for doesn't exist or has been moved."
   - 500: "We encountered an unexpected error. Our team has been notified."
5. CTA button: `px-6 py-3 bg-black text-yellow font-semibold rounded-md text-[14px] hover:bg-ink/90`
   - Authenticated → label "Go to Dashboard", href `/admin/org/dashboard/`
   - Unauthenticated → label "Go to Login", href `/accounts/login/`

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Filter bar — Region default | "All Regions" |
| Filter bar — Store default | "All Stores" |
| Filter bar — Date Range default | "Last 30 days" |
| Filter bar — Date Range options | "Last 7 days" / "Last 30 days" / "Last 90 days" / "Custom range" |
| Filter bar — Apply (custom dates) | "Apply" |
| Filter bar — Clear Filters | "Clear Filters" |
| Filter bar — UTC notice | "All dates in UTC" |
| Custom date — from > to error | "End date must be after start date." |
| Custom date — range > 365d error | "Date range cannot exceed 365 days." |
| KPI card — Total Reviews title | "Total Reviews" |
| KPI card — Total Reviews footer (multi-store) | "Across N stores" |
| KPI card — Total Reviews footer (single-store) | "{Shop Name}" |
| KPI card — Avg Rating title | "Average Rating" |
| KPI card — Avg Rating footer | "out of 5 stars" |
| KPI card — Negative Reviews title | "Negative Reviews" |
| KPI card — Negative Reviews footer | "N% of enriched reviews" |
| KPI empty state | "No reviews in this period" |
| KPI error state | "Could not load." + inline link "Retry" |
| Top performers heading | "Best Performing Outlets" |
| Top performers date scope note | "(date range only)" |
| Top performers empty state heading | "No qualifying stores in this period" |
| Top performers empty state body | "Stores need at least 3 reviews in the selected period to appear here." |
| Top performers empty state CTA | "View last 90 days" |
| Your Store card heading | "Your Store" |
| Your Store trend — no previous data | "No previous data" |
| Performance highlights — top label | "Top Performer" |
| Performance highlights — bottom label | "Needs Attention" |
| Performance highlights positive count | "N positive reviews (AI-derived)" |
| Performance highlights negative count | "N negative reviews (AI-derived)" |
| Sentiment heading | "Sentiment Distribution" |
| Sentiment coverage footer (< 100%) | "Based on N enriched reviews (X% of total)" |
| Sentiment coverage — in progress | "Analysis is still in progress." |
| Sentiment empty — no reviews | "No reviews to analyze in this period." |
| Sentiment empty — none enriched yet | "Sentiment analysis is in progress. Check back shortly." |
| 403 inline filter error | "You don't have access to the selected {Region/Store}. Filter has been cleared." |
| 404 page title | "Page Not Found" |
| 404 page body | "The page you're looking for doesn't exist or has been moved." |
| 404 CTA (authenticated) | "Go to Dashboard" |
| 404 CTA (unauthenticated) | "Go to Login" |
| 500 page title | "Something went wrong" |
| 500 page body | "We encountered an unexpected error. Our team has been notified." |
| 500 CTA (authenticated) | "Go to Dashboard" |
| 500 CTA (unauthenticated) | "Go to Login" |

No destructive actions in this phase — dashboard is entirely read-only.

---

## Interaction States

| Component | State | Visual Treatment |
|-----------|-------|-----------------|
| Filter dropdown | Default | `border-line bg-white` |
| Filter dropdown | Focus | `border-ink ring ring-black/[0.06]` (matches `ReviewFilters.tsx`) |
| Filter dropdown | Active filter | 6px yellow dot (`bg-yellow rounded-full`) appended to dropdown label |
| Custom date panel | Closed | `display: none` (not `visibility: hidden`) |
| Custom date panel | Open | slide-in via `transition-[opacity,transform] duration-150` — opacity 0→1, translateY(-4px→0) |
| Custom date panel | Validation error | Red border `border-red` on offending input + `text-[12px] text-red` below |
| Apply button | Disabled (incomplete dates) | `opacity-50 cursor-not-allowed` |
| Clear Filters | Disabled (at defaults) | `opacity-40 cursor-not-allowed pointer-events-none` |
| Bar chart bar | Hover | recharts built-in `activeBar` with `opacity-80` + tooltip appears |
| Bar chart bar | Click | `cursor-pointer`; `window.location.href` navigation |
| KPI card | Loading | skeleton pulse animation |
| KPI card | Error | card content replaced by error + retry text |
| Donut segment | Hover | recharts built-in active state + tooltip |
| Your Store trend | Up | `text-green` arrow + delta |
| Your Store trend | Down | `text-red` arrow + delta |
| Your Store trend | No data | `text-faint` em dash + "No previous data" |
| Error page CTA | Hover | `bg-ink/90` (matches primary button convention) |
| All interactive elements | Focus | `outline: 2px solid #facc15; outline-offset: 2px` (global `:focus-visible`) |

---

## Layout Specifications

### Dashboard page grid (desktop ≥ 1280px)

```
┌─────────────────────────────────────────────────────────────────────┐
│ Filter Bar (full width)                               All dates UTC │
├─────────────────────────────────────────────────────┬───────────────┤
│ Top Performing Outlets (bar chart)         ~65%      │ KPI Cards ↕  │
│                                                      │ ~35%         │
│                                                      ├───────────────┤
│                                                      │ Sentiment    │
│                                                      │ Donut        │
├─────────────────────────────────────────────────────┴───────────────┤
│ Performance Highlights (two sub-cards, full width at bottom OR       │
│ stacked in right column beside bar chart — see below)               │
└─────────────────────────────────────────────────────────────────────┘
```

Exact grid:
- Top section: `grid grid-cols-[1fr_320px] gap-6` — bar chart fills `1fr`, KPI cards + sentiment fill `320px` column
- KPI cards + sentiment stacked vertically: `flex flex-col gap-6`
- Performance highlights: stacked vertically `flex flex-col gap-4` inside the `320px` column, below sentiment donut
- On tablet (768–1279px): single column, all sections stacked: filter → bar chart → KPI cards → sentiment → highlights
- On mobile (<768px): same single column; filter bar wraps; bar chart `height={180}`; donut `height={160}`

Single-shop variant (Your Store):
- Replaces bar chart in the `1fr` column; highlights card is NOT rendered
- Your Store card occupies the full `1fr` column

---

## Animation Tokens

| Token | Value | Usage |
|-------|-------|-------|
| Skeleton pulse | `sk-pulse 1.6s ease-in-out infinite` | All loading skeletons (reuses existing keyframe) |
| Filter panel open | `opacity + translateY, duration 150ms ease-out` | Custom date picker panel appear |
| Filter panel close | `opacity + translateY, duration 100ms ease-in` | Custom date picker panel dismiss |
| Button hover | CSS `transition-colors duration-150` | All buttons |

No new `@keyframes` definitions required — `sk-pulse` already exists in `tailwind.css`.

---

## Accessibility Contract

| Requirement | Implementation |
|-------------|----------------|
| WCAG AA contrast | All text/background combinations use established tokens verified in Phase 1 |
| Focus ring | Global `:focus-visible` yellow ring (already in tailwind.css) — no per-component override |
| Chart accessibility | Donut: `role="img" aria-label="Sentiment distribution donut chart"`. Bar chart: `role="img" aria-label="Best performing outlets bar chart"`. Tooltips supplement, not replace, accessible data |
| KPI error retry | `<button>` element with `aria-label="Retry loading {Card Name}"` |
| Icon-only elements | All `lucide-react` icons: `aria-hidden="true"` on decorative, `aria-label` on standalone interactive |
| Date inputs | `aria-label="From date"` and `aria-label="To date"` (matches `ReviewFilters.tsx`) |
| Filter selects | `aria-label="Filter by region"`, `aria-label="Filter by store"`, `aria-label="Filter by date range"` |
| Empty states | Rendered in the DOM (not CSS hidden) so screen readers encounter them |
| Color not sole indicator | Bar threshold colors supplemented by exact value tooltip (not color-only) |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable |
| Third-party | none | not applicable |

No third-party registries. All new UI is hand-crafted Tailwind + recharts + lucide-react (both already approved and in use / being added per RESEARCH.md).

New npm packages for this phase:
- `recharts@^3.8.1` — added to `frontend/package.json` dependencies (source: RESEARCH.md; React 19 peer dep verified)
- `@tanstack/react-query` v5 — added to `frontend/package.json` dependencies (source: RESEARCH.md; overrides STACK.md per documented conflict resolution)

---

## Pre-Population Sources

| Field | Source |
|-------|--------|
| Brand colors (yellow, black, bg, ink, etc.) | `frontend/src/styles/tailwind.css` @theme tokens — direct codebase scan |
| Chart segment colors (#22C55E, #F59E0B, #EF4444) | `docs/Requirements_Phase4_Dashboard.docx` via REQUIREMENTS.md SENT-02, TOP-03 |
| Font (Geist) | `frontend/src/styles/tailwind.css` `--font-sans` token |
| Icon library (lucide-react) | `frontend/package.json` — already installed |
| Card pattern (padding, border, radius) | `ReviewStatsCards.tsx` — direct codebase inspection |
| Filter control pattern | `ReviewFilters.tsx` — direct codebase inspection |
| Spacing scale | Phase 1 CONTEXT.md + tailwind.css @theme |
| KPI card layout and skeleton pattern | `ReviewStatsCards.tsx` — direct codebase inspection |
| Date input pattern | `ReviewFilters.tsx` — direct codebase inspection |
| Custom date picker — inline panel, Apply button | 14-CONTEXT.md decisions |
| Error page structure | 14-CONTEXT.md decisions |
| Widget island — single #dashboard-root | 14-CONTEXT.md decisions |
| Bar click navigation (same tab, absolute ISO dates) | 14-CONTEXT.md decisions |
| Two filter objects (fullFilters / dateOnlyFilters) | RESEARCH.md DASH-C2 prevention |
| Recharts + React Query addition | RESEARCH.md recommended stack |
| UTC notice | RESEARCH.md DASH-C7 prevention |
| Typography scale (px sizes, weight 400/600/700) | Extracted from `ReviewStatsCards.tsx` and `ReviewFilters.tsx` patterns |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
