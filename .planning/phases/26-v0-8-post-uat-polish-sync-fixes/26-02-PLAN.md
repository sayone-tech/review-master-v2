---
phase: 26-v0-8-post-uat-polish-sync-fixes
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - frontend/src/widgets/tag-management/types.ts
  - frontend/src/widgets/tag-management/api.ts
  - frontend/src/widgets/tag-management/useTagList.ts
  - frontend/src/widgets/tag-management/TagManagementWidget.tsx
  - frontend/src/widgets/review-management/ProgressModal.tsx
autonomous: true
requirements: [TMGT-07, TMGT-08, SEED-05]
must_haves:
  truths:
    - "D-01 (TMGT-07): the Tags widget has a debounced server-side search box that sends ?search= to the canonical-tag list endpoint and shows matching tags"
    - "D-02 (TMGT-08): the Tags page shows a 'Tags (N)' header count and a 'Showing X–Y of N · Rows: N' pagination footer with a rows selector + page controls, matching /admin/org/team/"
    - "D-03 (SEED-05a): the sync progress modal step-1 label reads 'Fetching from Google' (not 'Fetched from Google')"
    - "D-04 (SEED-05b): each completed sync stage shows its wall-clock duration (e.g. 'Building Tag Vocabulary · 10m 16s') read from the snapshot timing fields produced in 26-01"
  artifacts:
    - path: "frontend/src/widgets/tag-management/TagManagementWidget.tsx"
      provides: "search input + debounce, Tags (N) header, Showing X–Y of N footer"
      contains: "Showing"
    - path: "frontend/src/widgets/tag-management/api.ts"
      provides: "fetchTags appends ?search= when set"
      contains: "search"
    - path: "frontend/src/widgets/review-management/ProgressModal.tsx"
      provides: "Fetching from Google label + per-stage duration display"
      contains: "Fetching from Google"
  key_links:
    - from: "frontend/src/widgets/tag-management/TagManagementWidget.tsx"
      to: "frontend/src/widgets/tag-management/useTagList.ts setSearch"
      via: "debounced onChange → setSearch"
      pattern: "setSearch"
    - from: "frontend/src/widgets/tag-management/api.ts fetchTags"
      to: "OrgCanonicalTagViewSet ?search="
      via: "URLSearchParams search param"
      pattern: "set\\(\"search\""
    - from: "frontend/src/widgets/review-management/ProgressModal.tsx"
      to: "sync snapshot timing fields (26-01)"
      via: "snapshot.*_duration_seconds"
      pattern: "duration_seconds"
---

<objective>
Frontend for Phase 26 polish: add a debounced server-side label search box to the
`tag-management` widget (D-01/TMGT-07); add a "Tags (N)" header count and a
"Showing X–Y of N · Rows: N" pagination footer matching the team page
(D-02/TMGT-08); rename the sync progress modal step-1 label to "Fetching from
Google" (D-03/SEED-05a); and render each completed stage's wall-clock duration
from the snapshot timing fields produced in 26-01 (D-04/SEED-05b).

Purpose: surface the backend `search` param and snapshot timing fields to the user.
Output: extended tag-management widget + surgical ProgressModal edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-v0-8-post-uat-polish-sync-fixes/26-CONTEXT.md
@.planning/phases/26-v0-8-post-uat-polish-sync-fixes/26-PATTERNS.md
@.planning/phases/26-v0-8-post-uat-polish-sync-fixes/26-01-PLAN.md

<interfaces>
<!-- Analog files to clone, and the backend contract from 26-01. -->

Backend contract from 26-01 (already shipped this wave):
  - GET canonical-tag list accepts `?search=<label substring>` (org-scoped, query-count-bounded).
  - The sync progress snapshot now carries: fetch_started_at, fetch_duration_seconds,
    vocab_started_at, vocab_duration_seconds, enriching_started_at (all nullable);
    finalising uses the existing duration_seconds on sync.complete.

Analogs to clone:
  - frontend/src/widgets/team-management/useTeam.ts (search state + setSearch, lines ~32–36)
  - frontend/src/widgets/team-management/api.ts (buildQs search param, lines ~41–50)
  - frontend/src/widgets/team-management/TeamTable.tsx (debounce lines ~42–54; search input ~220–227;
    "Showing X–Y of N · Rows: N" footer + buildPageRange ~24–29 + ~209–337; rows options [10,25,50,100])
  - templates/team/team_list.html (header count "Team (N)" markup, lines ~7–9)

Widget being extended:
  - frontend/src/widgets/tag-management/{types.ts, api.ts, useTagList.ts, TagManagementWidget.tsx}
    (useTagList already returns count/page/pageSize + pagination helpers; lacks search state)

ProgressModal (surgical edit only):
  - frontend/src/widgets/review-management/ProgressModal.tsx
    step-1 label "Fetched from Google" at the pending/complete/active <label> occurrences → "Fetching from Google"
    SnapshotState interface (~lines 5–24) gains the nullable timing fields.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Debounced search box wired to ?search= (D-01/TMGT-07)</name>
  <files>frontend/src/widgets/tag-management/types.ts, frontend/src/widgets/tag-management/api.ts, frontend/src/widgets/tag-management/useTagList.ts, frontend/src/widgets/tag-management/TagManagementWidget.tsx</files>
  <read_first>
    - frontend/src/widgets/tag-management/types.ts (FetchTagsParams — add search?)
    - frontend/src/widgets/tag-management/api.ts (fetchTags — the function being changed)
    - frontend/src/widgets/tag-management/useTagList.ts (the hook being changed)
    - frontend/src/widgets/tag-management/TagManagementWidget.tsx (the component being changed)
    - frontend/src/widgets/team-management/useTeam.ts (analog: setSearch, lines ~32–36)
    - frontend/src/widgets/team-management/api.ts (analog: buildQs search param, lines ~41–50)
    - frontend/src/widgets/team-management/TeamTable.tsx (analog: debounce ~42–54, search input ~220–227)
  </read_first>
  <action>
    Add `search?: string` to `FetchTagsParams` in `types.ts`. In `api.ts` `fetchTags`, append
    `if (params.search) qs.set("search", params.search);` inside the existing `URLSearchParams` block so
    the request carries `?search=`. In `useTagList.ts`, add `const [search, setSearchState] = useState("")`
    and a `setSearch(s: string)` that sets the search state and resets page to 1, then pass `search` to
    `fetchTags` in the load/refresh callback; expose `setSearch` (the existing `count` handling is unchanged).
    In `TagManagementWidget.tsx`, add a controlled `searchInput` state + a 300ms debounce `useEffect`
    (clone TeamTable.tsx lines ~42–54, including the `react-hooks/exhaustive-deps` disable comment) that
    calls `setSearch(searchInput)`; render a `type="search"` input with placeholder "Search tags…",
    `data-testid="tag-search"`, styled like the team search box. The debounce interval is 300ms (within
    the 250–300ms discretion). Server-side: the box drives the backend `?search=` from 26-01 — no
    client-side filtering. Empty search returns the full list (copy is executor discretion per CONTEXT
    Claude's-Discretion).
  </action>
  <acceptance_criteria>
    - `grep -n "set(\"search\"" frontend/src/widgets/tag-management/api.ts` shows fetchTags appending the param.
    - `grep -n "setSearch" frontend/src/widgets/tag-management/useTagList.ts` shows the hook exposes setSearch and resets page to 1.
    - `grep -n "data-testid=\"tag-search\"" frontend/src/widgets/tag-management/TagManagementWidget.tsx` shows the input; a 300ms debounce useEffect is present.
    - Typing in the box (after debounce) issues a request with `?search=` and re-renders with the matching subset (no client-side filter).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>The Tags widget has a debounced server-side search box matching the team page UX; empty search shows the full list.</done>
</task>

<task type="auto">
  <name>Task 2: "Tags (N)" header count + "Showing X–Y of N · Rows: N" footer (D-02/TMGT-08)</name>
  <files>frontend/src/widgets/tag-management/TagManagementWidget.tsx</files>
  <read_first>
    - frontend/src/widgets/tag-management/TagManagementWidget.tsx (the component being changed — currently "Page X of Y")
    - frontend/src/widgets/tag-management/useTagList.ts (count/page/pageSize already returned)
    - frontend/src/widgets/team-management/TeamTable.tsx (analog: footer + buildPageRange, lines ~24–29 and ~209–337; rows options [10,25,50,100])
    - templates/team/team_list.html (analog: header count "Team (N)", lines ~7–9)
  </read_first>
  <action>
    In `TagManagementWidget.tsx`, render a header count from `useTagList`'s `count`:
    `Tags (count)` styled like "Team (N)" (muted, smaller, parenthesised — clone team_list.html markup
    into JSX since the widget has no SSR context). Replace the existing "Page X of Y" pagination nav with
    the team page's richer footer: derive `start = count === 0 ? 0 : (page - 1) * pageSize + 1`,
    `end = Math.min(page * pageSize, count)`, `totalPages = count > 0 ? Math.ceil(count / pageSize) : 1`;
    render "Showing X–Y of N · Rows: <selector>" with a rows-per-page `<select>` (options `[10, 25, 50, 100]`
    verbatim per CONTEXT) wired to the existing pageSize setter, plus numbered page buttons. Copy the
    `buildPageRange` helper (TeamTable.tsx lines ~24–29) verbatim for ellipsis handling. Total count comes
    from the existing paginated `count` — no new endpoint. Keep changes surgical (§24): touch only the
    header and the pagination region of the widget.
  </action>
  <acceptance_criteria>
    - `grep -n "Tags (" frontend/src/widgets/tag-management/TagManagementWidget.tsx` (or the templated equivalent rendering `({count})`) shows the header count bound to `count`.
    - `grep -n "Showing" frontend/src/widgets/tag-management/TagManagementWidget.tsx` shows the footer; `buildPageRange` is present.
    - The rows-per-page selector offers exactly 10/25/50/100 and changing it re-fetches with the new page_size and resets to a valid page.
    - With count=0 the footer reads "Showing 0–0 of 0" (no NaN / negative range).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>The Tags page shows "Tags (N)" and "Showing X–Y of N · Rows: N" with a rows selector + page controls, matching the team page.</done>
</task>

<task type="auto">
  <name>Task 3: ProgressModal — "Fetching from Google" label + per-stage durations (D-03, D-04)</name>
  <files>frontend/src/widgets/review-management/ProgressModal.tsx</files>
  <read_first>
    - frontend/src/widgets/review-management/ProgressModal.tsx (the file being edited — SnapshotState ~5–24; step-1 label occurrences ~336/348/367; success duration_seconds display ~323–327; per-step branches for vocab/enriching/finalising)
    - .planning/phases/26-v0-8-post-uat-polish-sync-fixes/26-01-PLAN.md (the snapshot timing fields contract: fetch/vocab/enriching start + duration)
  </read_first>
  <action>
    D-03: change the step-1 `<label>` text "Fetched from Google" → "Fetching from Google" at all three
    occurrences (pending/complete/active — ~lines 336, 348, 367). Leave the active-state aria-label (already
    "Fetching from Google") and the other three step labels unchanged. D-04: extend the `SnapshotState`
    interface with the nullable timing fields produced by 26-01 — `fetch_started_at`, `fetch_duration_seconds`,
    `vocab_started_at`, `vocab_duration_seconds`, `enriching_started_at` (`enriching`/finalising end uses the
    existing `duration_seconds` on sync.complete). Add a small `formatDuration(s: number): string` helper
    ("Xm Ys", or "Ys" when < 60s). In each step's complete-state branch, when the matching
    `*_duration_seconds` is non-null, render " · {formatDuration(...)}" next to the completed step summary
    (clone the existing `duration_seconds` success-banner display pattern at ~323–327 for formatting style).
    Visual treatment is executor discretion (CONTEXT) but it must read each stage's real elapsed time from
    the snapshot — no hardcoded or client-computed wall clock. Keep the edit surgical: only labels, the
    interface fields, the helper, and the per-step complete branches.
  </action>
  <acceptance_criteria>
    - `grep -c "Fetched from Google" frontend/src/widgets/review-management/ProgressModal.tsx` returns 0 in `<label>` text (the gerund "Fetching from Google" is present at the step-1 label sites).
    - `SnapshotState` includes `fetch_duration_seconds`, `vocab_duration_seconds` (and the start timestamps); a `formatDuration` helper exists.
    - When `vocab_duration_seconds` is set, the completed Vocabulary step renders "· Xm Ys"; when null, no duration text renders (no "· NaN").
    - The other three step labels ("Building Tag Vocabulary", "Analysing…", "Finalising") are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Step 1 reads "Fetching from Google"; each completed stage shows its real elapsed time from the snapshot timing fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (Org Admin) → canonical-tag list `?search=` | Search term sent to a server-side, org-scoped endpoint |
| Sync snapshot (server) → ProgressModal | Read-only display of per-shop progress timing fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05 | Information Disclosure | tag search box | mitigate | Search is server-side only — the widget sends `?search=` to the org-scoped, `IsOrgAdmin`-guarded endpoint (T-26-01 in 26-01); no client-side dataset is fetched unfiltered, so no other org's tags reach the browser. |
| T-26-06 | Information Disclosure | ProgressModal timing display | accept | Renders only the already-authorised per-shop sync snapshot (timing seconds + the user's own sync); no new data crosses a tenant boundary. |
| T-26-SC | Tampering | npm installs | mitigate | No new frontend dependencies — clones existing widget patterns only. No install task. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.json`
- `cd frontend && npm run build` (Vite entrypoints compile)
- Manual: search box filters server-side; header/footer match the team page; modal step 1 reads "Fetching from Google" with per-stage durations.
</verification>

<success_criteria>
- D-01: debounced server-side search box sends `?search=` and shows matching tags.
- D-02: "Tags (N)" header + "Showing X–Y of N · Rows: N" footer with rows selector + page controls.
- D-03: step-1 label is "Fetching from Google".
- D-04: each completed stage shows its real wall-clock duration from the snapshot.
</success_criteria>

<output>
Create `.planning/phases/26-v0-8-post-uat-polish-sync-fixes/26-02-SUMMARY.md` when done.
</output>
