---
phase: 18
slug: action-item-duplicate-merge
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-22
---

# Phase 18 — UI Design Contract: Action Item Duplicate Merge

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (manual Tailwind CSS) |
| Icon library | lucide-react (already installed) |
| Font | Geist, system-ui fallback |

No new packages. No shadcn. No third-party component libraries.

---

## Spacing Scale

All Tailwind classes used in this phase must map to multiples of 4px. The project uses Tailwind's default 4px base unit. No custom spacing tokens are declared beyond the two named sidebar tokens in tailwind.config.js.

| Token | Value | Tailwind | Usage |
|-------|-------|----------|-------|
| xs | 4px | `p-1` / `gap-1` | Checkbox-to-content gap inside a cell |
| sm | 8px | `p-2` / `gap-2` | Toolbar button internal padding, badge gap |
| md | 16px | `p-4` / `gap-4` | Modal section padding (matches existing `p-4` pattern) |
| lg | 24px | `p-6` / `gap-6` | Modal outer padding (matches existing `px-6 py-5` pattern) |
| xl | 32px | `p-8` | Large vertical separation in modals (empty states) |
| 2xl | 48px | `py-12` | Empty state centering (matches existing `py-12` pattern) |

Exceptions: none. Existing code uses `py-[11px]` and `py-[14px]` in DataTable headers/rows — these are pre-existing table row heights that must be preserved. Do not introduce new arbitrary values for the new UI elements in this phase.

---

## Typography

Extracted directly from the existing action items codebase. Maximum 2 font weights used across the entire widget.

| Role | Size | Weight | Line Height | Tailwind |
|------|------|--------|-------------|----------|
| Body / cell text | 14px | 400 (normal) | 1.5 | `text-[14px] font-normal` |
| Semibold body (titles, CTAs, labels) | 14px | 600 (semibold) | 1.5 | `text-[14px] font-semibold` |
| Small label (uppercase section headers) | 12px | 600 (semibold) | 1.4 | `text-[12px] font-semibold uppercase tracking-[0.05em]` |
| Small body (badges, counts, meta) | 12px | 600 (semibold) | 1.4 | `text-[12px] font-semibold` |
| Micro (badge text for +N count) | 11px | 600 (semibold) | — | `text-[11px] font-semibold` |
| Modal title | 18px | 600 (semibold) | 1.2 | `text-[18px] font-semibold tracking-[-0.01em]` |

**Permitted weights: 400 (normal) and 600 (semibold) only.** No 500/medium weight. This matches the existing codebase exactly.

---

## Color

All hex values extracted from `frontend/tailwind.config.js`.

| Role | Token | Hex | Usage |
|------|-------|-----|-------|
| Dominant (60%) | `bg` / `white` | `#FAFAFA` / `#FFFFFF` | Page background, modal surface, table rows |
| Secondary (30%) | `line-soft` | `#F4F4F5` | Table header background (`#FBFBFB`), hover states, modal footer |
| Accent (10%) | `yellow` | `#FACC15` | Primary CTA buttons ("Merge" confirm button) |
| Accent hover | `yellow-hover` | `#EAB308` | Hover on yellow CTA buttons |
| Duplicate badge | `amber-tint` + `amber` | `#FEF3C7` / `#D97706` | `+N` duplicate count badge background and text |
| Destructive | `red` | `#DC2626` | Not used in this phase (merge is permanent but not destructive-red — see note) |
| Border | `line` | `#E4E4E7` | Table borders, modal borders, input borders |
| Border soft | `line-soft` | `#F4F4F5` | Row dividers, section dividers |
| Body text | `ink` | `#18181B` | Primary text: item titles, modal body text |
| Secondary text | `muted` | `#52525B` | Assignee placeholders, meta text, cancel buttons |
| Subtle text | `subtle` | `#71717A` | Column headers, toolbar hint text |
| Checkbox accent | `yellow` | `#FACC15` | Checked checkbox indicator (focus ring: `focus:ring-yellow`) |
| Toolbar background | `white` | `#FFFFFF` | Merge toolbar bar, same surface as table |
| Radio selected | `ink` | `#18181B` | Selected radio button in MergeModal (black dot) |
| Search input focus | `yellow` | `#FACC15` | `focus:ring-2 focus:ring-yellow` on picker input |

**Accent reserved for:** primary CTA "Merge" button only. The `+N` badge uses `amber` (not yellow) to distinguish it from interactive elements.

**Note on merge confirmation styling:** The merge operation is permanent (D-07) but the confirmation uses the `amber` ConfirmModal variant (AlertTriangle icon, amber icon background, red confirm button per existing `CONFIRM_BTN_CLASS.amber`). This is consistent with the existing `ConfirmModal` — the amber variant already maps to the red confirm button class. Do not create a new variant.

---

## Component Inventory

All new components follow existing modal/table patterns exactly.

### 1. Checkbox column in DataTable (additive)

- **Where:** Leading column in `ActionItemTable`, visible only when `isOrgAdmin === true`
- **What:** `<input type="checkbox">` element per row, plus a header "select all" checkbox
- **Header checkbox:** Selects all `source === 'AI'` rows on the current page. If any AI rows are selected, shows indeterminate state (`ref.current.indeterminate = true`)
- **Row checkbox:** Disabled (greyed, `opacity-40 cursor-not-allowed`) when `row.source === 'MANUAL'`. Enabled for `source === 'AI'` rows
- **Checked appearance:** `accent-yellow` via inline style or Tailwind `accent-[#FACC15]`
- **Cell width:** `w-10` (40px) — matches the existing actions column `w-12`
- **Cell padding:** `px-4 py-[14px]` — matches existing row cells exactly
- **Header cell padding:** `px-4 py-[11px]` — matches existing header cells

### 2. Merge toolbar

- **Where:** Between `ActionItemFilters` and the table container, inside `ActionItemManagementWidget`
- **Visibility:** Rendered only when `selectedIds.size >= 2 && isOrgAdmin`. Hidden (not just disabled) when fewer than 2 items are selected
- **Layout:** `flex items-center justify-between px-4 py-2 bg-white border border-line rounded-card mb-2` (a contained bar, not full-width banner)
- **Left side:** Selection count text — `text-[14px] font-semibold text-ink` — copy: "{N} items selected"
- **Right side:** Two buttons side by side with `gap-2`
  - "Clear selection" — `px-3 py-1.5 text-[13.5px] text-muted border border-line rounded-md hover:bg-line-soft`
  - "Merge duplicates" — `px-3 py-1.5 text-[13.5px] bg-yellow text-black font-semibold rounded-md hover:bg-yellow-hover`

### 3. `+N` duplicate count badge

- **Where:** Inline with the item title in the TITLE column cell, appended immediately after the title button
- **Layout:** `inline-flex items-center justify-center px-1.5 py-0.5 rounded-full bg-amber-tint text-amber text-[11px] font-semibold`
- **Content:** `+{duplicate_count}` — e.g., "+3"
- **Visibility:** Rendered only when `duplicate_count > 0`
- **Wrapper:** `<div className="flex items-center gap-2">` wrapping the existing title button + the badge

### 4. MergeModal

- **Size:** `default` (max-w-[520px]) using the existing `<Modal>` component
- **Title:** "Merge action items" — `text-[18px] font-semibold text-ink`
- **Subtitle:** "{N} items selected" — `text-[13.5px] text-muted`
- **Body layout:** `p-4 space-y-4`
- **Section header:** "Pick the primary item" — `text-[12px] font-semibold uppercase tracking-[0.05em] text-muted`
- **Instruction text:** "The primary item will remain active. All others will be merged into it." — `text-[13.5px] text-muted`
- **Radio list:** One `<label>` per selected item, `flex items-start gap-3 p-3 rounded-md border border-line cursor-pointer hover:bg-line-soft`
  - Radio input: `mt-0.5 w-4 h-4 accent-[#FACC15]`
  - Item title: `text-[14px] font-semibold text-ink`
  - Item meta (scope + shop name): `text-[12px] text-muted mt-0.5` — e.g., "Shop · Central Market"
- **Selected radio row highlight:** `border-yellow bg-yellow-tint` instead of `border-line`
- **Validation:** Confirm button disabled until a primary radio is selected
- **Footer (reuse Modal footer slot):**
  - Cancel: `px-3.5 py-2 bg-white text-ink border border-line rounded-md text-[13.5px] font-medium hover:bg-line-soft`
  - "Merge" (primary, disabled until radio selected): `px-3.5 py-2 bg-yellow text-black font-semibold rounded-md text-[13.5px] hover:bg-yellow-hover disabled:opacity-50`

### 5. Confirmation step within MergeModal

After the user picks a primary and clicks "Merge", show a confirmation state **within the same modal** (do not open a nested modal):

- **Transition:** Replace the radio list with a single confirmation message block
- **Icon block:** `w-11 h-11 rounded-[12px] bg-amber-tint flex items-center justify-center` — `<AlertTriangle size={22} className="text-amber" />`
- **Confirmation title:** "Confirm merge" — `text-[18px] font-semibold text-ink mb-1.5`
- **Confirmation message:** `text-[13.5px] text-muted leading-[1.5]` — copy: "Merge {N} items into '{primary title}'? This cannot be undone."
- **Footer buttons:**
  - "Back" (returns to radio selection): `px-3.5 py-2 bg-white text-ink border border-line rounded-md text-[13.5px] font-medium hover:bg-line-soft`
  - "Merge" (final confirm, calls API): `px-3.5 py-2 bg-yellow text-black font-semibold rounded-md text-[13.5px] hover:bg-yellow-hover disabled:opacity-50`
- **Loading state:** "Merging…" text on the Merge button, button disabled

### 6. DuplicatePickerModal

- **Size:** `default` (max-w-[520px]) using the existing `<Modal>` component
- **Title:** "Mark as duplicate of" — `text-[18px] font-semibold text-ink`
- **Subtitle:** Current item title — `text-[13.5px] text-muted`
- **Body layout:** `p-4 space-y-3`
- **Search input:**
  - Full-width: `w-full px-3 py-2 text-[14px] bg-white border border-line rounded-md focus:outline-none focus:ring-2 focus:ring-yellow`
  - Placeholder: "Search action items…"
  - Icon: `<Search size={14} className="text-muted" />` inset on the left using `relative` wrapper with `pl-9` on input and `absolute left-3 top-1/2 -translate-y-1/2` on icon
- **Results list:** `mt-2 max-h-[280px] overflow-y-auto space-y-1 border border-line rounded-md`
  - Each result row: `flex flex-col px-3 py-2.5 cursor-pointer hover:bg-line-soft border-b border-line-soft last:border-b-0`
  - Title: `text-[14px] font-semibold text-ink`
  - Meta: `text-[12px] text-muted mt-0.5` — format: "Shop · {shop_name}" or "Brand"
  - Selected row: `bg-yellow-tint` background
- **Empty search result:** Single row in the list: `text-[14px] text-muted px-3 py-4 text-center` — copy: "No matching items found."
- **Footer:**
  - Cancel: same secondary button style as MergeModal
  - "Select as primary" (disabled until an item is selected): same primary yellow button style

### 7. "Also reported in" section (ActionItemModal details tab)

- **Where:** Appended at the bottom of the `DetailsTab` view mode content (before the "Edit Details" link), visible only when `item.duplicates.length > 0`
- **Section separator:** `<div className="border-t border-line-soft pt-4 mt-4">`
- **Section header:** `text-[12px] font-semibold uppercase tracking-[0.05em] text-muted mb-2` — copy: "Also reported in"
- **Duplicate rows:** `space-y-2`
  - Each row: `flex items-center justify-between px-3 py-2 rounded-md bg-line-soft text-[13.5px]`
  - Left: shop name `text-ink font-semibold` + date `text-muted ml-2 text-[12px]`
  - Right: star rating display — `text-amber text-[12px]` — e.g., "★ 3"
  - Full row format: `[shop name] · [formatted date]` on left, `★ {rating}` on right
  - Date format: `toLocaleDateString(undefined, { day: 'numeric', month: 'short', year: 'numeric' })` — consistent with existing `formatDate()` in ActionItemTable

### 8. "Mark as duplicate of…" button (ActionItemModal)

- **Where:** In the `DetailsTab` view mode, below the "Edit Details" link, visible only when `isOrgAdmin && item.source === 'AI'`
- **Button style:** `text-[14px] text-muted underline hover:text-ink` — matches existing "Edit Details" button pattern exactly
- **Copy:** "Mark as duplicate of…"
- **Placement:** `<div className="pt-1 text-right">` immediately below the `<div className="pt-2 text-right">` that holds "Edit Details"
- **Behavior:** Opens `DuplicatePickerModal` with the current item as the duplicate (it will become the duplicate; the selected item becomes primary)

---

## Copywriting Contract

All user-facing strings, exact as they appear in the UI.

| Element | Copy |
|---------|------|
| Toolbar selection count | "{N} items selected" |
| Toolbar clear button | "Clear selection" |
| Toolbar merge button | "Merge duplicates" |
| Duplicate badge | "+{N}" (e.g. "+3") |
| MergeModal title | "Merge action items" |
| MergeModal subtitle | "{N} items selected" |
| MergeModal section header | "Pick the primary item" |
| MergeModal instruction | "The primary item will remain active. All others will be merged into it." |
| MergeModal confirm step title | "Confirm merge" |
| MergeModal confirm step message | "Merge {N} items into '{primary title}'? This cannot be undone." |
| MergeModal back button | "Back" |
| MergeModal primary CTA (pre-pick) | "Merge" (disabled) |
| MergeModal primary CTA (confirmed) | "Merge" (active) |
| MergeModal loading state | "Merging…" |
| Cancel buttons (all modals) | "Cancel" |
| DuplicatePickerModal title | "Mark as duplicate of" |
| DuplicatePickerModal search placeholder | "Search action items…" |
| DuplicatePickerModal empty result | "No matching items found." |
| DuplicatePickerModal primary CTA | "Select as primary" |
| "Also reported in" section header | "Also reported in" |
| "Mark as duplicate of" button | "Mark as duplicate of…" |
| Success toast — list merge | "Items merged successfully" |
| Success toast — detail merge | "Marked as duplicate" |
| Error toast — merge failed | "Could not merge items. Please try again." |
| Error toast — same scope violation | "Items must have the same scope to merge." |
| Error toast — manual source | "Only AI-extracted items can be merged." |
| Merge toolbar empty state heading | "No action items yet" (unchanged — toolbar hidden) |

---

## Interaction States

### Checkbox column

| State | Visual |
|-------|--------|
| Unchecked, AI item | Empty checkbox, `opacity-100` |
| Checked, AI item | Checked, `accent-[#FACC15]` |
| Disabled, MANUAL item | `opacity-40 cursor-not-allowed` checkbox |
| Header: none selected | Unchecked |
| Header: some AI selected | `indeterminate` |
| Header: all AI selected | Checked |

### Merge toolbar

| Condition | State |
|-----------|-------|
| `selectedIds.size < 2` | Toolbar not rendered |
| `selectedIds.size >= 2` | Toolbar rendered, "Merge duplicates" active |
| Post-merge success | Toolbar disappears (selection cleared), list refetched |
| Post-merge error | Toolbar remains, error toast shown |

### MergeModal

| Step | State |
|------|-------|
| Opened, no radio selected | "Merge" button disabled |
| Radio selected | "Merge" button enabled |
| "Merge" clicked | Transitions to confirmation step |
| "Back" clicked from confirm | Returns to radio selection step, radio stays selected |
| "Merge" on confirmation clicked | Button shows "Merging…", disabled; API call fires |
| API success | Modal closes, success toast, list refetched, selection cleared |
| API error | Modal stays open on the confirmation step, error toast shown |

### DuplicatePickerModal

| State | Behavior |
|-------|----------|
| No search query | Shows all eligible items (same scope, source=AI, `canonical__isnull` on API) up to first page |
| Search query typed | Filters client-side by title substring (case-insensitive) over pre-fetched list |
| No results | Shows "No matching items found." row |
| Item clicked | Row gets `bg-yellow-tint`, "Select as primary" button becomes active |
| "Select as primary" clicked | Opens MergeModal pre-populated with selected primary and current item as duplicate |
| "Cancel" clicked | Modal closes, no state change |

---

## State Cleared On

- Page change (`params.page` changes) → `selectedIds` reset to empty `Set`
- Filter reset (any filter param changes) → `selectedIds` reset to empty `Set`
- Post-merge success → `selectedIds` reset to empty `Set`

---

## Accessibility

- Checkbox inputs have `aria-label={`Select ${row.title}`}` per row
- Header checkbox has `aria-label="Select all AI-extracted items"` and `aria-checked={...}` with `"mixed"` for indeterminate
- MergeModal radio group has `role="radiogroup"` with `aria-labelledby` pointing to the "Pick the primary item" heading
- DuplicatePickerModal search input has `aria-label="Search action items"` and `aria-controls` pointing to the results list (`role="listbox"`)
- Each result row in DuplicatePickerModal has `role="option"` and `aria-selected`
- Merge toolbar has `role="toolbar"` with `aria-label="Bulk actions"`
- All modals use the existing `<Modal>` component which wraps `<FocusTrap>` and sets `role="dialog" aria-modal="true"`
- Error messages use `role="alert"`
- Loading states use `aria-busy="true"` on the Merge button

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| None | — | not applicable |

No third-party registries. No new npm packages. All new components use:
- Existing `<Modal>` from `frontend/src/widgets/modal/Modal.tsx`
- Existing `lucide-react` icons already installed (add: `Search`, `AlertTriangle` — both available in lucide-react)
- Existing Tailwind CSS tokens only

**New lucide icons needed:** `Search` (for DuplicatePickerModal input), `AlertTriangle` (already imported in `ConfirmModal.tsx`). Both are in the installed `lucide-react` package — no new installation.

---

## Files to Create / Modify (Frontend)

| File | Action | What Changes |
|------|--------|-------------|
| `frontend/src/widgets/action-items/types.ts` | MODIFY | Add `duplicate_count: number` to `ActionItemListRow`; add `ActionItemDuplicate` interface; add `duplicates: ActionItemDuplicate[]` to `ActionItemDetail`; add `MergePayload` type |
| `frontend/src/widgets/action-items/api.ts` | MODIFY | Add `mergeActionItems(payload: MergePayload): Promise<ActionItemDetail>` |
| `frontend/src/widgets/data-table/DataTable.tsx` | MODIFY | Add optional `selectedIds?: Set<string>`, `onToggleRow?: (key: string) => void`, `onToggleAll?: (allKeys: string[]) => void` — all optional, no breaking changes |
| `frontend/src/widgets/action-items/ActionItemTable.tsx` | MODIFY | Pass checkbox props through to DataTable; disable for MANUAL rows; add `+N` badge to title cell |
| `frontend/src/widgets/action-items/MergeModal.tsx` | CREATE | Radio-pick primary + inline confirmation step |
| `frontend/src/widgets/action-items/DuplicatePickerModal.tsx` | CREATE | Search-as-you-type picker with pre-loaded items |
| `frontend/src/widgets/action-items/ActionItemManagementWidget.tsx` | MODIFY | Add `selectedIds` state; render toolbar; handle merge flow; clear selection on page/filter change |
| `frontend/src/widgets/action-items/ActionItemModal.tsx` | MODIFY | Add "Also reported in" section; add "Mark as duplicate of…" button; integrate DuplicatePickerModal |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
