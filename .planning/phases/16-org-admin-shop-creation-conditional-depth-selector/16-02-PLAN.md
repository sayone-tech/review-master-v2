---
phase: "16"
plan: "02"
type: execute
wave: 2
depends_on:
  - "16-01"
files_modified:
  - frontend/src/widgets/shop-management/types.ts
  - frontend/src/entrypoints/shop-management.tsx
  - frontend/src/widgets/shop-management/ShopModals.tsx
  - frontend/src/widgets/shop-management/CreateShopModal.tsx
autonomous: false
requirements:
  - SDEP-01

must_haves:
  truths:
    - "Org Admin with allow_custom_sync_depth=True sees a Review History dropdown in Step 3 of the shop creation wizard"
    - "Org Admin with allow_custom_sync_depth=False sees no Review History dropdown and no cs-sync-depth element exists in the DOM"
    - "Dropdown shows three options: Last 1 year, Last 2 years, All time; defaults to Last 2 years"
    - "Closing and re-opening the modal resets the dropdown to Last 2 years"
    - "After shop creation with Last 1 year, the shop detail page shows Last 1 year confirming the value was persisted"
  artifacts:
    - path: frontend/src/widgets/shop-management/types.ts
      provides: "sync_depth?: string added to ShopCreatePayload"
      contains: "sync_depth"
    - path: frontend/src/entrypoints/shop-management.tsx
      provides: "parseJson('shop-org-data') + allowCustomSyncDepth prop to ShopModals"
      contains: "shop-org-data"
    - path: frontend/src/widgets/shop-management/ShopModals.tsx
      provides: "allowCustomSyncDepth prop threaded to CreateShopModal"
      contains: "allowCustomSyncDepth"
    - path: frontend/src/widgets/shop-management/CreateShopModal.tsx
      provides: "conditional Review History <select> in Step 3"
      contains: "cs-sync-depth"
  key_links:
    - from: frontend/src/entrypoints/shop-management.tsx
      to: frontend/src/widgets/shop-management/ShopModals.tsx
      via: "allowCustomSyncDepth prop"
      pattern: "allowCustomSyncDepth"
    - from: frontend/src/widgets/shop-management/ShopModals.tsx
      to: frontend/src/widgets/shop-management/CreateShopModal.tsx
      via: "allowCustomSyncDepth prop pass-through"
      pattern: "allowCustomSyncDepth"
    - from: frontend/src/widgets/shop-management/CreateShopModal.tsx
      to: POST /api/v1/shops/
      via: "syncDepth state in ShopCreatePayload.sync_depth"
      pattern: "sync_depth.*syncDepth"
---

<objective>
Wire the allow_custom_sync_depth flag from the shop-org-data bootstrap tag (set by Plan 01)
through the React component tree and render a conditional Review History dropdown in Step 3
of the CreateShopModal. The selected depth is included in the shop creation payload as
sync_depth, which the backend (also wired in Plan 01) accepts and persists.

Purpose: Complete SDEP-01 — Org Admins whose org has the flag see and use the dropdown;
those without it never see it and always get the TWO_YEARS default.

Output: Four frontend files modified; TypeScript must compile without errors; the
conditional render is completely absent from the DOM when the flag is false.
</objective>

<execution_context>
@/Users/renjith/Documents/Accounts/review-master/.claude/get-shit-done/workflows/execute-plan.md
@/Users/renjith/Documents/Accounts/review-master/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/renjith/Documents/Accounts/review-master/.planning/ROADMAP.md
@/Users/renjith/Documents/Accounts/review-master/.planning/phases/16-org-admin-shop-creation-conditional-depth-selector/16-CONTEXT.md
@/Users/renjith/Documents/Accounts/review-master/.planning/phases/16-org-admin-shop-creation-conditional-depth-selector/16-UI-SPEC.md
@/Users/renjith/Documents/Accounts/review-master/.planning/phases/16-org-admin-shop-creation-conditional-depth-selector/16-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/widgets/shop-management/types.ts (current — SyncDepth already defined):
  export type SyncDepth = "ONE_YEAR" | "TWO_YEARS" | "ALL_TIME";

  export const SYNC_DEPTH_LABELS: Record<SyncDepth, string> = {
    ONE_YEAR: "Last 1 year",
    TWO_YEARS: "Last 2 years",
    ALL_TIME: "All time",
  };

  // ShopCreatePayload — current (no sync_depth yet; Phase 16 adds it):
  export interface ShopCreatePayload {
    name: string;
    connection_method: string;
    place_id: string;
    google_refresh_token?: string;
    google_account_name?: string;
    google_location_name?: string;
    region?: number | "";
    phone?: string;
    street_address?: string;
    // Phase 16 adds: sync_depth?: string;
  }

From frontend/src/entrypoints/shop-management.tsx (parseJson already defined — reuse):
  function parseJson<T>(id: string, fallback: T): T {
    const el = document.getElementById(id);
    if (!el) return fallback;
    try {
      return JSON.parse(el.textContent ?? "") as T;
    } catch {
      return fallback;
    }
  }
  // Already reads: shop-data, shop-allocation, shop-has-regions, shop-regions-data
  // Phase 16 adds: shop-org-data (reads allow_custom_sync_depth boolean)

From frontend/src/widgets/shop-management/ShopModals.tsx (current ShopModalsProps):
  interface ShopModalsProps {
    allocation: AllocationStatus;
    regions: RegionLite[];
    initialPlaceIds?: string[];
    isOrgAdmin?: boolean;
    // Phase 16 adds: allowCustomSyncDepth?: boolean;
  }
  // Phase 16: thread allowCustomSyncDepth to CreateShopModal

From frontend/src/widgets/shop-management/CreateShopModal.tsx (current — key constants):
  // Lines 9-13 (inputCls and labelCls — use unchanged for Review History field):
  const labelCls = "block text-[12px] font-semibold text-subtle tracking-[0.05em] uppercase mb-1";
  const inputCls = "w-full px-3 py-2 text-[13.5px] bg-white border border-line rounded-md focus:outline-none focus:ring focus:ring-black/[0.06] focus:border-ink";

  // Current Props interface (no allowCustomSyncDepth yet):
  interface Props {
    open: boolean;
    onClose: () => void;
    onCreated: (shop: ShopRow) => void;
    regions: RegionLite[];
    existingPlaceIds?: Set<string>;
    // Phase 16 adds: allowCustomSyncDepth?: boolean;
  }

  // Region <select> in Step 3 (template for Review History select — lines 432-455):
  <div>
    <label htmlFor="cs-region" className={labelCls}>Region</label>
    <select id="cs-region" value={...} onChange={...} className={...} aria-label="Region">
      ...
    </select>
  </div>

  // Existing reset() function — Phase 16 adds setSyncDepth("TWO_YEARS") to this function

  // Step 3 field order after Phase 16:
  // 1. Non-field error banner (existing)
  // 2. Connected listing pill (existing)
  // 3. Shop Name (existing)
  // 4. Region <select> (existing)
  // 5. Review History <select> (NEW — conditional on allowCustomSyncDepth)
  // 6. Phone (existing)
  // 7. Street Address (existing)
  // 8. Footer (existing)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sync_depth to ShopCreatePayload in types.ts and wire allowCustomSyncDepth through entrypoint and ShopModals</name>
  <files>
    frontend/src/widgets/shop-management/types.ts,
    frontend/src/entrypoints/shop-management.tsx,
    frontend/src/widgets/shop-management/ShopModals.tsx
  </files>
  <read_first>
    frontend/src/widgets/shop-management/types.ts — read the full file; locate ShopCreatePayload interface (NOT ShopUpdatePayload); confirm SyncDepth type and SYNC_DEPTH_LABELS are already present; find the correct insertion point for sync_depth?: string
    frontend/src/entrypoints/shop-management.tsx — read the full file; locate all existing parseJson calls (for shop-data, shop-allocation, shop-has-regions, shop-regions-data); find where ShopModals is rendered and what props it currently receives; locate the insertion point for the new orgData parseJson call
    frontend/src/widgets/shop-management/ShopModals.tsx — read the full file; locate ShopModalsProps interface; locate where CreateShopModal is rendered inside ShopModals; confirm which props CreateShopModal currently receives
  </read_first>
  <action>
    In frontend/src/widgets/shop-management/types.ts — add sync_depth?: string as an optional field to ShopCreatePayload (not ShopUpdatePayload — that interface must NOT be modified). Place it after street_address?: string to keep it logically last.

    In frontend/src/entrypoints/shop-management.tsx — after the last existing parseJson call (for shop-regions-data), add:
      const orgData = parseJson<{ allow_custom_sync_depth: boolean }>(
        "shop-org-data",
        { allow_custom_sync_depth: false },
      );
    Then pass allowCustomSyncDepth={orgData.allow_custom_sync_depth} to the ShopModals JSX element.

    In frontend/src/widgets/shop-management/ShopModals.tsx — add allowCustomSyncDepth?: boolean to ShopModalsProps (optional, so no existing callers break without the prop). Destructure it in the component function signature. Pass allowCustomSyncDepth={allowCustomSyncDepth} to CreateShopModal. The defaulting to false happens naturally via the optional prop — do not add explicit ?? false unless TypeScript requires it.

    Do NOT modify ShopUpdatePayload or any edit/update component.
  </action>
  <verify>
    <automated>cd /Users/renjith/Documents/Accounts/review-master/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/widgets/shop-management/types.ts ShopCreatePayload interface contains sync_depth?: string
    - frontend/src/widgets/shop-management/types.ts ShopUpdatePayload interface does NOT contain sync_depth
    - frontend/src/entrypoints/shop-management.tsx contains parseJson with "shop-org-data" string
    - frontend/src/entrypoints/shop-management.tsx passes allowCustomSyncDepth to ShopModals component
    - frontend/src/widgets/shop-management/ShopModals.tsx ShopModalsProps contains allowCustomSyncDepth?: boolean
    - frontend/src/widgets/shop-management/ShopModals.tsx passes allowCustomSyncDepth to CreateShopModal
    - npx tsc --noEmit exits 0 (no TypeScript compilation errors)
  </acceptance_criteria>
  <done>ShopCreatePayload has sync_depth?; the org flag is read from shop-org-data at startup; allowCustomSyncDepth prop threads from entrypoint through ShopModals to CreateShopModal; TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add conditional Review History <select> to CreateShopModal Step 3 and include sync_depth in submission payload</name>
  <files>
    frontend/src/widgets/shop-management/CreateShopModal.tsx
  </files>
  <read_first>
    frontend/src/widgets/shop-management/CreateShopModal.tsx — read the full file end to end; locate:
      1. Props interface (to add allowCustomSyncDepth?: boolean)
      2. All useState declarations (to add syncDepth state after the last existing state)
      3. The reset() function body (to add setSyncDepth("TWO_YEARS") alongside other resets)
      4. The Step 3 JSX block — specifically the Region <select> (lines ~432-455) and the Phone field immediately after it; the Review History field is inserted between them
      5. The handleSubmit or payload assembly location where ShopCreatePayload is constructed (to add sync_depth: syncDepth)
    frontend/src/widgets/shop-management/types.ts — confirm SyncDepth type is exported (to use as the useState generic and onChange cast)
  </read_first>
  <action>
    In frontend/src/widgets/shop-management/CreateShopModal.tsx:

    1. Add allowCustomSyncDepth?: boolean to the Props interface (optional, defaults to false).

    2. Add state declaration: const [syncDepth, setSyncDepth] = useState<SyncDepth>("TWO_YEARS"); — placed after the existing useState declarations. Import SyncDepth from "../types" if not already imported.

    3. In the reset() function, add setSyncDepth("TWO_YEARS"); alongside the other setState reset calls (per RESEARCH pitfall 2 — prevents stale state on modal reopen).

    4. In the Step 3 JSX, insert the following conditional block immediately after the Region <select> closing </div> and before the Phone field opening <div>:
      {allowCustomSyncDepth && (
        <div>
          <label htmlFor="cs-sync-depth" className={labelCls}>
            Review History
          </label>
          <p className="mt-1 text-[12px] text-muted">
            Sets how far back this shop&#39;s initial review sync will go.
          </p>
          <select
            id="cs-sync-depth"
            value={syncDepth}
            onChange={(e) => setSyncDepth(e.target.value as SyncDepth)}
            className={inputCls}
            aria-label="Review History"
          >
            <option value="ONE_YEAR">Last 1 year</option>
            <option value="TWO_YEARS">Last 2 years</option>
            <option value="ALL_TIME">All time</option>
          </select>
        </div>
      )}
    The outer wrapper uses React conditional rendering ({condition && <element>}) — not CSS display:none, not disabled, not aria-hidden. The element must be completely absent from the DOM when allowCustomSyncDepth is false (per D-10 and UI-SPEC).

    5. In the payload assembly (the object passed to the POST /api/v1/shops/ call), add sync_depth: syncDepth to the ShopCreatePayload object. Always include it (not conditionally) — the backend accepts any valid value regardless of the org flag per D-05. This avoids a conditional spread and keeps the payload simple.

    Do not modify EditShopModal, ShopUpdatePayload, or any update/edit path. Do not add a validation error state to the Review History field — the select always has a valid value due to the TWO_YEARS default (per UI-SPEC).
  </action>
  <verify>
    <automated>cd /Users/renjith/Documents/Accounts/review-master/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/widgets/shop-management/CreateShopModal.tsx Props interface contains allowCustomSyncDepth?: boolean
    - frontend/src/widgets/shop-management/CreateShopModal.tsx contains const [syncDepth, setSyncDepth] = useState<SyncDepth>("TWO_YEARS")
    - frontend/src/widgets/shop-management/CreateShopModal.tsx reset() function body contains setSyncDepth("TWO_YEARS")
    - frontend/src/widgets/shop-management/CreateShopModal.tsx contains {allowCustomSyncDepth && ( with id="cs-sync-depth" inside (DOM-absent conditional render)
    - frontend/src/widgets/shop-management/CreateShopModal.tsx does NOT contain display:none or hidden or aria-hidden on the Review History block
    - frontend/src/widgets/shop-management/CreateShopModal.tsx contains option value="ONE_YEAR">Last 1 year, value="TWO_YEARS">Last 2 years, value="ALL_TIME">All time
    - frontend/src/widgets/shop-management/CreateShopModal.tsx payload assembly contains sync_depth: syncDepth
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>CreateShopModal conditionally renders the Review History select when allowCustomSyncDepth is true; state defaults to TWO_YEARS; reset() restores the default; sync_depth is always included in the submission payload; TypeScript compiles without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 01 wired allow_custom_sync_depth into the shop_list view context and the shop-org-data
    bootstrap tag. Plan 02 threaded it through the React component tree and rendered a conditional
    Review History dropdown in CreateShopModal Step 3, wired to the POST /api/v1/shops/ payload.
  </what-built>
  <how-to-verify>
    1. Start the dev server (make up or docker-compose up).

    2. Log in as an Org Admin whose organisation has allow_custom_sync_depth = False.
       Navigate to /shops/ and open the Add Shop wizard (if allocation permits).
       Complete Steps 1 and 2, then reach Step 3.
       Verify: NO Review History dropdown is visible. Inspect the DOM — there must be no
       element with id="cs-sync-depth" anywhere in the page source.

    3. Log in as an Org Admin whose organisation has allow_custom_sync_depth = True
       (use Django admin or shell to set org.allow_custom_sync_depth = True and save).
       Navigate to /shops/ and open the Add Shop wizard.
       Complete Steps 1 and 2, then reach Step 3.
       Verify: Review History dropdown IS visible, positioned after Region and before Phone.
       Verify: Default selected option is "Last 2 years".
       Verify: Dropdown contains exactly three options: "Last 1 year", "Last 2 years", "All time".
       Verify: Helper text "Sets how far back this shop's initial review sync will go." appears
               between the label and the select.

    4. With the allow_custom_sync_depth = True org:
       Select "Last 1 year" in the Review History dropdown.
       Click Cancel (or close the modal).
       Re-open the Add Shop wizard.
       Reach Step 3 again.
       Verify: The dropdown has reset to "Last 2 years" (not "Last 1 year").

    5. With the allow_custom_sync_depth = True org:
       Complete the full shop creation flow, selecting "Last 1 year" as Review History.
       After the shop is created, navigate to the shop detail page (or open the shop details modal).
       Verify: The "Review history" field shows "Last 1 year".
  </how-to-verify>
  <resume-signal>Type "approved" if all five verification steps pass, or describe any issue found (e.g., "Step 3 fails: dropdown not visible" or "Step 4 fails: reset to All time instead of Last 2 years").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bootstrap tag → React entrypoint | allow_custom_sync_depth boolean crosses from server-rendered HTML into JS via parseJson |
| React component → POST /api/v1/shops/ | sync_depth string crosses from browser into the API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Tampering | ShopCreatePayload.sync_depth (client-assembled) | accept | Backend (Plan 01) validates via ChoiceField; invalid values already return 400; the frontend sends a value from a closed enum (SyncDepth type). No additional frontend validation needed. |
| T-16-05 | Information Disclosure | parseJson fallback for shop-org-data | accept | Fallback is false — if the tag is missing, the dropdown is absent and the shop silently gets TWO_YEARS. Safe-fail direction: more restrictive (no custom depth), not more permissive. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced in this plan. Zero install risk. |
</threat_model>

<verification>
TypeScript compilation:
  cd frontend && npx tsc --noEmit

Build the frontend bundle and confirm no build errors:
  cd frontend && npm run build

Full backend test suite (must remain green — no regressions from template or view changes):
  pytest --cov=apps --cov-fail-under=85 -q
</verification>

<success_criteria>
1. Org Admin with allow_custom_sync_depth=True: Review History select visible in Step 3 with id="cs-sync-depth", three options, default "Last 2 years"
2. Org Admin with allow_custom_sync_depth=False: no element with id="cs-sync-depth" exists anywhere in the DOM
3. Selecting "Last 1 year" → Cancel → reopen → select shows "Last 2 years" (state reset confirmed)
4. Creating a shop with "Last 1 year" → shop detail shows "Last 1 year" (end-to-end persistence)
5. npx tsc --noEmit exits 0
6. npm run build exits 0
7. pytest --cov=apps --cov-fail-under=85 -q exits 0
</success_criteria>

<output>
Create .planning/phases/16-org-admin-shop-creation-conditional-depth-selector/16-02-SUMMARY.md when done.
</output>
