---
phase: 23-four-step-initial-sync-seeding-queue-split
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/widgets/review-management/ProgressModal.tsx
  - frontend/src/widgets/review-management/TopbarSyncIndicator.tsx
autonomous: false
requirements: [SEED-01]
must_haves:
  truths:
    - "The ProgressModal renders four named steps — Fetching Reviews → Building Tag Vocabulary → AI Enrichment → Finalising — visible from open, with per-step counters and step-specific bar colors"
    - "The modal advances steps from incoming sync.vocab.progress / sync.enrichment.progress / sync.finalising.progress events and the step discriminator in the reconnect snapshot"
    - "TopbarSyncIndicator shows a four-stage sub-label and spinner color per active shop"
    - "Implements CONTEXT decisions D-01 (the four named steps verbatim) and D-02 (per-step counters rendered from the new sync.vocab.progress / sync.finalising.progress events + step discriminator)"
  artifacts:
    - path: "frontend/src/widgets/review-management/ProgressModal.tsx"
      provides: "Four-step progress UI + new event handlers + extended SnapshotState"
      contains: "sync.vocab.progress"
    - path: "frontend/src/widgets/review-management/TopbarSyncIndicator.tsx"
      provides: "Four-stage sub-label + spinner color"
      contains: "finalising"
  key_links:
    - from: "ProgressModal.tsx ws.onmessage"
      to: "SnapshotState.step"
      via: "event-type switch updating step discriminator"
      pattern: "sync.finalising.progress"
---

<objective>
Extend the existing `ProgressModal` from two progress sections to the four named
steps (Fetching Reviews → Building Tag Vocabulary → AI Enrichment → Finalising)
and extend `TopbarSyncIndicator` to a four-stage sub-label/spinner, per the
approved 23-UI-SPEC contract. No new WebSocket consumer — the modal consumes the
two new event types (`sync.vocab.progress`, `sync.finalising.progress`) and the
extended snapshot schema.

Purpose: This is the only user-facing surface of the phase (SEED-01). It is
independent of the backend service files, so it runs in Wave 1 in parallel with
the settings/Redis foundation. The backend emits the new events in Plan 02/03;
the modal is forward-compatible (new fields are additive, pending steps render
from the start).
Output: Extended `ProgressModal.tsx` and `TopbarSyncIndicator.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-four-step-initial-sync-seeding-queue-split/23-CONTEXT.md
@.planning/phases/23-four-step-initial-sync-seeding-queue-split/23-UI-SPEC.md
@.planning/phases/23-four-step-initial-sync-seeding-queue-split/23-PATTERNS.md

<interfaces>
ProgressModal.tsx existing SnapshotState (lines ~5-17):
- status: "fetching"|"enriching"|"success"|"failed"; fetched; total_estimate; enriched;
  started_at; last_update_at; page_count; duration_seconds?; error_code?; error_message?
Existing ws.onmessage switch (lines ~58-126): handles sync.fetch.progress,
sync.enrichment.progress, sync.complete, sync.error, and the initial snapshot.
Existing fetch progress-bar JSX (lines ~254-293).

TopbarSyncIndicator.tsx existing SyncingShop (lines ~6-9): stage?: "fetching"|"enriching".
Existing onmessage stage handler (lines ~76-92); existing sub-label/color switch (lines ~187-193).

Event payload contract (23-UI-SPEC §WebSocket Event Payload Contract):
- sync.vocab.progress: {shop_id, enriched, total}
- sync.finalising.progress: {shop_id, processed, total}

Design tokens (23-UI-SPEC §Color): yellow (fetching), green (vocab + enrichment),
amber (finalising). Step labels verbatim per §Copywriting Contract.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend SnapshotState + four-step rendering + event handlers in ProgressModal.tsx</name>
  <read_first>
    - frontend/src/widgets/review-management/ProgressModal.tsx (SnapshotState ~5-17, ws.onmessage ~58-126, fetch JSX ~254-293)
    - .planning/phases/23-four-step-initial-sync-seeding-queue-split/23-UI-SPEC.md §"Component Inventory" + §"Copywriting Contract" + §"Accessibility Contract"
    - .planning/phases/23-four-step-initial-sync-seeding-queue-split/23-PATTERNS.md §"ProgressModal.tsx"
  </read_first>
  <files>frontend/src/widgets/review-management/ProgressModal.tsx</files>
  <action>
    Extend `SnapshotState` with `step: "fetching"|"vocab"|"enriching"|"finalising"|"success"|"failed"` plus optional
    `vocab_enriched?`, `vocab_total?`, `finalising_processed?`, `finalising_total?` (23-UI-SPEC §TypeScript Interface
    Extension). Add two `ws.onmessage` cases following the existing additive pattern: `sync.vocab.progress` sets
    `step:"vocab"`, `vocab_enriched`, `vocab_total`; `sync.finalising.progress` sets `step:"finalising"`,
    `finalising_processed`, `finalising_total`. Render all four steps in a `space-y-5` block so the full roadmap is
    visible from modal open. Step 1 (Fetching Reviews, `bg-yellow`) keeps existing logic. Step 2 (Building Tag
    Vocabulary, `bg-green`, counter "Building vocabulary {vocab_enriched}/{vocab_total}", hint "Seeding vocabulary…"
    when 0). Step 3 (AI Enrichment / "Analysing with Review Bee AI Engine", `bg-green`, counter "Enriching
    {enriched}/{fetched}"). Step 4 (Finalising, `bg-amber`, counter "Finalising {finalising_processed}/{finalising_total}",
    hint "Deduplicating tags…" when 0). Use the pending-step visual (`opacity-60`, counter "–", track-only bar) for
    steps not yet reached, derived from the `step` discriminator. Apply the §Accessibility Contract: each bar
    `role="progressbar"` with aria-valuenow/min/max (omit value attrs when indeterminate/pending), wrap the
    four-step block in `aria-live="polite"`. Use verbatim eyebrow labels per §Copywriting Contract (Steps 2 & 4 use
    the D-01 names; Steps 1 & 3 keep existing eyebrow wording — this divergence is intentional). Do NOT modify the
    Modal component, WebSocket lifecycle, footer buttons, or sync.complete/sync.error handling (23-UI-SPEC §What Does
    NOT Change).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'sync.vocab.progress\|sync.finalising.progress' frontend/src/widgets/review-management/ProgressModal.tsx` shows both handlers
    - SnapshotState includes `step` and the four optional counters
    - All four step labels present verbatim: "Building Tag Vocabulary" and "Finalising" appear in the file
    - Bars use bg-yellow (step1), bg-green (steps 2/3), bg-amber (step4)
    - `npx tsc --noEmit` passes with no new type errors
    - No edits to Modal.tsx, footer CTA, or sync.complete/sync.error branches
  </acceptance_criteria>
  <done>Four-step modal renders with new event handlers, extended SnapshotState, correct colors + a11y; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Four-stage sub-label + spinner color in TopbarSyncIndicator.tsx</name>
  <read_first>
    - frontend/src/widgets/review-management/TopbarSyncIndicator.tsx (SyncingShop ~6-9, onmessage ~76-92, sub-label/color switch ~187-193)
    - .planning/phases/23-four-step-initial-sync-seeding-queue-split/23-UI-SPEC.md §"TopbarSyncIndicator Extension"
    - .planning/phases/23-four-step-initial-sync-seeding-queue-split/23-PATTERNS.md §"TopbarSyncIndicator.tsx"
  </read_first>
  <files>frontend/src/widgets/review-management/TopbarSyncIndicator.tsx</files>
  <action>
    Extend `SyncingShop.stage` from `"fetching"|"enriching"` to `"fetching"|"vocab"|"enriching"|"finalising"`. Add
    `sync.vocab.progress` and `sync.finalising.progress` cases in `ws.onmessage` that set `stage:"vocab"` /
    `stage:"finalising"` for the matching shop, following the existing stage-update pattern. Replace the two-way
    sub-label/color ternary with the four-way maps from 23-UI-SPEC §TopbarSyncIndicator Extension: labels {fetching:
    "Fetching reviews from Google…", vocab: "Building tag vocabulary…", enriching: "Analysing reviews with AI…",
    finalising: "Finalising…"}; spinner colors {fetching: text-yellow, vocab: text-green, enriching: text-green,
    finalising: text-amber}; default to fetching when stage is undefined.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `SyncingShop.stage` union includes all four values
    - `grep -n 'Building tag vocabulary\|Finalising' frontend/src/widgets/review-management/TopbarSyncIndicator.tsx` shows the new sub-labels
    - vocab → text-green and finalising → text-amber spinner colors present
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Topbar indicator shows four-stage sub-label + spinner color; tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify four-step progress renders and advances correctly</name>
  <files>frontend/src/widgets/review-management/ProgressModal.tsx, frontend/src/widgets/review-management/TopbarSyncIndicator.tsx</files>
  <what-built>
    The ProgressModal now renders four named steps (Fetching Reviews → Building Tag Vocabulary → AI Enrichment →
    Finalising) with per-step counters and step-specific colors, and the topbar indicator shows a four-stage
    sub-label. This is the visual/UX behavior that 23-VALIDATION.md flags as manual-only (cannot be grepped).
  </what-built>
  <action>
    Manually verify the four-step progress UX (23-VALIDATION.md Manual-Only Verification — visual fidelity cannot be
    asserted by grep or tsc). Steps:
    1. Run the frontend build/dev server and the backend (Plans 01–03 merged) so events flow end to end.
    2. Trigger an initial sync for a store with more than 50 reviews (so seed + bulk + finalising all occur).
    3. Watch the ProgressModal: confirm all four steps are visible from open (pending steps dimmed at opacity-60),
       the active step shows live X/Y counts, bar colors are yellow → green → green → amber, and the steps complete
       in order Fetching → Building Tag Vocabulary → AI Enrichment → Finalising.
    4. Confirm the topbar indicator sub-label advances through "Fetching reviews from Google…", "Building tag
       vocabulary…", "Analysing reviews with AI…", "Finalising…".
    5. Reload mid-sync: confirm the modal repaints the correct current step from the reconnect snapshot.
  </action>
  <how-to-verify>
    Follow the five steps in the action block. Approve only when all four steps render, advance in order with the
    correct colors and counters, and the reconnect repaint shows the correct current step.
  </how-to-verify>
  <verify>
    <human-check>Four steps render from open; advance in order Fetching → Building Tag Vocabulary → AI Enrichment → Finalising with yellow/green/green/amber bars and live counters; topbar sub-label advances through all four; reconnect repaints the correct step.</human-check>
  </verify>
  <resume-signal>Type "approved" or describe any visual/step-ordering issues.</resume-signal>
  <done>User confirms the four-step progress renders, advances in order with correct colors/counters, and reconnect repaints correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebSocket message → React state | Untrusted/malformed event payloads must not crash the modal |
| Reconnect snapshot → render | Snapshot from server drives initial paint; cross-tenant access is the consumer's responsibility (unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-10 | Denial of Service | malformed WebSocket message | mitigate | New onmessage cases live inside the existing try/catch that ignores malformed JSON (23-UI-SPEC §ws.onmessage pattern) — additive cases do not change error handling |
| T-23-11 | Information Disclosure | cross-tenant data in snapshot | accept | SyncProgressConsumer auth + tenant scope unchanged (23-UI-SPEC §What Does NOT Change); the modal renders only its own shop's per-shop-keyed snapshot |
| T-23-SC | Tampering | npm/pip installs | accept | No new packages this phase (lucide-react already installed; RESEARCH.md audit N/A) |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.json` passes for both files
- Manual checkpoint: four-step progress renders and advances correctly (23-VALIDATION.md Manual-Only Verification)
</verification>

<success_criteria>
- Four named steps render in ProgressModal from open, advancing on the new events with correct colors + a11y
- TopbarSyncIndicator shows the four-stage sub-label/spinner
- No new WebSocket consumer; Modal component + lifecycle untouched
</success_criteria>

<output>
Create `.planning/phases/23-four-step-initial-sync-seeding-queue-split/23-04-SUMMARY.md` when done
</output>
